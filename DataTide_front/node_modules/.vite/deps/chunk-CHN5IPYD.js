import {
  require_react_dom
} from "./chunk-AUFLGUIX.js";
import {
  require_jsx_runtime
} from "./chunk-XO4JX7VM.js";
import {
  require_react
} from "./chunk-PSQR3SVX.js";
import {
  __commonJS,
  __toESM
} from "./chunk-5WRI5ZAA.js";

// node_modules/lodash/_freeGlobal.js
var require_freeGlobal = __commonJS({
  "node_modules/lodash/_freeGlobal.js"(exports, module) {
    var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
    module.exports = freeGlobal;
  }
});

// node_modules/lodash/_root.js
var require_root = __commonJS({
  "node_modules/lodash/_root.js"(exports, module) {
    var freeGlobal = require_freeGlobal();
    var freeSelf = typeof self == "object" && self && self.Object === Object && self;
    var root = freeGlobal || freeSelf || Function("return this")();
    module.exports = root;
  }
});

// node_modules/lodash/_Symbol.js
var require_Symbol = __commonJS({
  "node_modules/lodash/_Symbol.js"(exports, module) {
    var root = require_root();
    var Symbol3 = root.Symbol;
    module.exports = Symbol3;
  }
});

// node_modules/lodash/_getRawTag.js
var require_getRawTag = __commonJS({
  "node_modules/lodash/_getRawTag.js"(exports, module) {
    var Symbol3 = require_Symbol();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var nativeObjectToString = objectProto.toString;
    var symToStringTag = Symbol3 ? Symbol3.toStringTag : void 0;
    function getRawTag(value) {
      var isOwn = hasOwnProperty.call(value, symToStringTag), tag = value[symToStringTag];
      try {
        value[symToStringTag] = void 0;
        var unmasked = true;
      } catch (e11) {
      }
      var result = nativeObjectToString.call(value);
      if (unmasked) {
        if (isOwn) {
          value[symToStringTag] = tag;
        } else {
          delete value[symToStringTag];
        }
      }
      return result;
    }
    module.exports = getRawTag;
  }
});

// node_modules/lodash/_objectToString.js
var require_objectToString = __commonJS({
  "node_modules/lodash/_objectToString.js"(exports, module) {
    var objectProto = Object.prototype;
    var nativeObjectToString = objectProto.toString;
    function objectToString(value) {
      return nativeObjectToString.call(value);
    }
    module.exports = objectToString;
  }
});

// node_modules/lodash/_baseGetTag.js
var require_baseGetTag = __commonJS({
  "node_modules/lodash/_baseGetTag.js"(exports, module) {
    var Symbol3 = require_Symbol();
    var getRawTag = require_getRawTag();
    var objectToString = require_objectToString();
    var nullTag = "[object Null]";
    var undefinedTag = "[object Undefined]";
    var symToStringTag = Symbol3 ? Symbol3.toStringTag : void 0;
    function baseGetTag(value) {
      if (value == null) {
        return value === void 0 ? undefinedTag : nullTag;
      }
      return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
    }
    module.exports = baseGetTag;
  }
});

// node_modules/lodash/_overArg.js
var require_overArg = __commonJS({
  "node_modules/lodash/_overArg.js"(exports, module) {
    function overArg(func, transform) {
      return function(arg) {
        return func(transform(arg));
      };
    }
    module.exports = overArg;
  }
});

// node_modules/lodash/_getPrototype.js
var require_getPrototype = __commonJS({
  "node_modules/lodash/_getPrototype.js"(exports, module) {
    var overArg = require_overArg();
    var getPrototype = overArg(Object.getPrototypeOf, Object);
    module.exports = getPrototype;
  }
});

// node_modules/lodash/isObjectLike.js
var require_isObjectLike = __commonJS({
  "node_modules/lodash/isObjectLike.js"(exports, module) {
    function isObjectLike(value) {
      return value != null && typeof value == "object";
    }
    module.exports = isObjectLike;
  }
});

// node_modules/lodash/isPlainObject.js
var require_isPlainObject = __commonJS({
  "node_modules/lodash/isPlainObject.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var getPrototype = require_getPrototype();
    var isObjectLike = require_isObjectLike();
    var objectTag = "[object Object]";
    var funcProto = Function.prototype;
    var objectProto = Object.prototype;
    var funcToString = funcProto.toString;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var objectCtorString = funcToString.call(Object);
    function isPlainObject(value) {
      if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
        return false;
      }
      var proto = getPrototype(value);
      if (proto === null) {
        return true;
      }
      var Ctor = hasOwnProperty.call(proto, "constructor") && proto.constructor;
      return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
    }
    module.exports = isPlainObject;
  }
});

// node_modules/lodash/isArray.js
var require_isArray = __commonJS({
  "node_modules/lodash/isArray.js"(exports, module) {
    var isArray = Array.isArray;
    module.exports = isArray;
  }
});

// node_modules/lodash/isSymbol.js
var require_isSymbol = __commonJS({
  "node_modules/lodash/isSymbol.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isObjectLike = require_isObjectLike();
    var symbolTag = "[object Symbol]";
    function isSymbol(value) {
      return typeof value == "symbol" || isObjectLike(value) && baseGetTag(value) == symbolTag;
    }
    module.exports = isSymbol;
  }
});

// node_modules/lodash/_isKey.js
var require_isKey = __commonJS({
  "node_modules/lodash/_isKey.js"(exports, module) {
    var isArray = require_isArray();
    var isSymbol = require_isSymbol();
    var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/;
    var reIsPlainProp = /^\w*$/;
    function isKey(value, object) {
      if (isArray(value)) {
        return false;
      }
      var type = typeof value;
      if (type == "number" || type == "symbol" || type == "boolean" || value == null || isSymbol(value)) {
        return true;
      }
      return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object(object);
    }
    module.exports = isKey;
  }
});

// node_modules/lodash/isObject.js
var require_isObject = __commonJS({
  "node_modules/lodash/isObject.js"(exports, module) {
    function isObject(value) {
      var type = typeof value;
      return value != null && (type == "object" || type == "function");
    }
    module.exports = isObject;
  }
});

// node_modules/lodash/isFunction.js
var require_isFunction = __commonJS({
  "node_modules/lodash/isFunction.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isObject = require_isObject();
    var asyncTag = "[object AsyncFunction]";
    var funcTag = "[object Function]";
    var genTag = "[object GeneratorFunction]";
    var proxyTag = "[object Proxy]";
    function isFunction(value) {
      if (!isObject(value)) {
        return false;
      }
      var tag = baseGetTag(value);
      return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
    }
    module.exports = isFunction;
  }
});

// node_modules/lodash/_coreJsData.js
var require_coreJsData = __commonJS({
  "node_modules/lodash/_coreJsData.js"(exports, module) {
    var root = require_root();
    var coreJsData = root["__core-js_shared__"];
    module.exports = coreJsData;
  }
});

// node_modules/lodash/_isMasked.js
var require_isMasked = __commonJS({
  "node_modules/lodash/_isMasked.js"(exports, module) {
    var coreJsData = require_coreJsData();
    var maskSrcKey = (function() {
      var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
      return uid ? "Symbol(src)_1." + uid : "";
    })();
    function isMasked(func) {
      return !!maskSrcKey && maskSrcKey in func;
    }
    module.exports = isMasked;
  }
});

// node_modules/lodash/_toSource.js
var require_toSource = __commonJS({
  "node_modules/lodash/_toSource.js"(exports, module) {
    var funcProto = Function.prototype;
    var funcToString = funcProto.toString;
    function toSource(func) {
      if (func != null) {
        try {
          return funcToString.call(func);
        } catch (e11) {
        }
        try {
          return func + "";
        } catch (e11) {
        }
      }
      return "";
    }
    module.exports = toSource;
  }
});

// node_modules/lodash/_baseIsNative.js
var require_baseIsNative = __commonJS({
  "node_modules/lodash/_baseIsNative.js"(exports, module) {
    var isFunction = require_isFunction();
    var isMasked = require_isMasked();
    var isObject = require_isObject();
    var toSource = require_toSource();
    var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;
    var reIsHostCtor = /^\[object .+?Constructor\]$/;
    var funcProto = Function.prototype;
    var objectProto = Object.prototype;
    var funcToString = funcProto.toString;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var reIsNative = RegExp(
      "^" + funcToString.call(hasOwnProperty).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
    );
    function baseIsNative(value) {
      if (!isObject(value) || isMasked(value)) {
        return false;
      }
      var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
      return pattern.test(toSource(value));
    }
    module.exports = baseIsNative;
  }
});

// node_modules/lodash/_getValue.js
var require_getValue = __commonJS({
  "node_modules/lodash/_getValue.js"(exports, module) {
    function getValue(object, key) {
      return object == null ? void 0 : object[key];
    }
    module.exports = getValue;
  }
});

// node_modules/lodash/_getNative.js
var require_getNative = __commonJS({
  "node_modules/lodash/_getNative.js"(exports, module) {
    var baseIsNative = require_baseIsNative();
    var getValue = require_getValue();
    function getNative(object, key) {
      var value = getValue(object, key);
      return baseIsNative(value) ? value : void 0;
    }
    module.exports = getNative;
  }
});

// node_modules/lodash/_nativeCreate.js
var require_nativeCreate = __commonJS({
  "node_modules/lodash/_nativeCreate.js"(exports, module) {
    var getNative = require_getNative();
    var nativeCreate = getNative(Object, "create");
    module.exports = nativeCreate;
  }
});

// node_modules/lodash/_hashClear.js
var require_hashClear = __commonJS({
  "node_modules/lodash/_hashClear.js"(exports, module) {
    var nativeCreate = require_nativeCreate();
    function hashClear() {
      this.__data__ = nativeCreate ? nativeCreate(null) : {};
      this.size = 0;
    }
    module.exports = hashClear;
  }
});

// node_modules/lodash/_hashDelete.js
var require_hashDelete = __commonJS({
  "node_modules/lodash/_hashDelete.js"(exports, module) {
    function hashDelete(key) {
      var result = this.has(key) && delete this.__data__[key];
      this.size -= result ? 1 : 0;
      return result;
    }
    module.exports = hashDelete;
  }
});

// node_modules/lodash/_hashGet.js
var require_hashGet = __commonJS({
  "node_modules/lodash/_hashGet.js"(exports, module) {
    var nativeCreate = require_nativeCreate();
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function hashGet(key) {
      var data = this.__data__;
      if (nativeCreate) {
        var result = data[key];
        return result === HASH_UNDEFINED ? void 0 : result;
      }
      return hasOwnProperty.call(data, key) ? data[key] : void 0;
    }
    module.exports = hashGet;
  }
});

// node_modules/lodash/_hashHas.js
var require_hashHas = __commonJS({
  "node_modules/lodash/_hashHas.js"(exports, module) {
    var nativeCreate = require_nativeCreate();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function hashHas(key) {
      var data = this.__data__;
      return nativeCreate ? data[key] !== void 0 : hasOwnProperty.call(data, key);
    }
    module.exports = hashHas;
  }
});

// node_modules/lodash/_hashSet.js
var require_hashSet = __commonJS({
  "node_modules/lodash/_hashSet.js"(exports, module) {
    var nativeCreate = require_nativeCreate();
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    function hashSet(key, value) {
      var data = this.__data__;
      this.size += this.has(key) ? 0 : 1;
      data[key] = nativeCreate && value === void 0 ? HASH_UNDEFINED : value;
      return this;
    }
    module.exports = hashSet;
  }
});

// node_modules/lodash/_Hash.js
var require_Hash = __commonJS({
  "node_modules/lodash/_Hash.js"(exports, module) {
    var hashClear = require_hashClear();
    var hashDelete = require_hashDelete();
    var hashGet = require_hashGet();
    var hashHas = require_hashHas();
    var hashSet = require_hashSet();
    function Hash(entries) {
      var index2 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index2 < length) {
        var entry = entries[index2];
        this.set(entry[0], entry[1]);
      }
    }
    Hash.prototype.clear = hashClear;
    Hash.prototype["delete"] = hashDelete;
    Hash.prototype.get = hashGet;
    Hash.prototype.has = hashHas;
    Hash.prototype.set = hashSet;
    module.exports = Hash;
  }
});

// node_modules/lodash/_listCacheClear.js
var require_listCacheClear = __commonJS({
  "node_modules/lodash/_listCacheClear.js"(exports, module) {
    function listCacheClear() {
      this.__data__ = [];
      this.size = 0;
    }
    module.exports = listCacheClear;
  }
});

// node_modules/lodash/eq.js
var require_eq = __commonJS({
  "node_modules/lodash/eq.js"(exports, module) {
    function eq(value, other) {
      return value === other || value !== value && other !== other;
    }
    module.exports = eq;
  }
});

// node_modules/lodash/_assocIndexOf.js
var require_assocIndexOf = __commonJS({
  "node_modules/lodash/_assocIndexOf.js"(exports, module) {
    var eq = require_eq();
    function assocIndexOf(array2, key) {
      var length = array2.length;
      while (length--) {
        if (eq(array2[length][0], key)) {
          return length;
        }
      }
      return -1;
    }
    module.exports = assocIndexOf;
  }
});

// node_modules/lodash/_listCacheDelete.js
var require_listCacheDelete = __commonJS({
  "node_modules/lodash/_listCacheDelete.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf();
    var arrayProto = Array.prototype;
    var splice = arrayProto.splice;
    function listCacheDelete(key) {
      var data = this.__data__, index2 = assocIndexOf(data, key);
      if (index2 < 0) {
        return false;
      }
      var lastIndex = data.length - 1;
      if (index2 == lastIndex) {
        data.pop();
      } else {
        splice.call(data, index2, 1);
      }
      --this.size;
      return true;
    }
    module.exports = listCacheDelete;
  }
});

// node_modules/lodash/_listCacheGet.js
var require_listCacheGet = __commonJS({
  "node_modules/lodash/_listCacheGet.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf();
    function listCacheGet(key) {
      var data = this.__data__, index2 = assocIndexOf(data, key);
      return index2 < 0 ? void 0 : data[index2][1];
    }
    module.exports = listCacheGet;
  }
});

// node_modules/lodash/_listCacheHas.js
var require_listCacheHas = __commonJS({
  "node_modules/lodash/_listCacheHas.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf();
    function listCacheHas(key) {
      return assocIndexOf(this.__data__, key) > -1;
    }
    module.exports = listCacheHas;
  }
});

// node_modules/lodash/_listCacheSet.js
var require_listCacheSet = __commonJS({
  "node_modules/lodash/_listCacheSet.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf();
    function listCacheSet(key, value) {
      var data = this.__data__, index2 = assocIndexOf(data, key);
      if (index2 < 0) {
        ++this.size;
        data.push([key, value]);
      } else {
        data[index2][1] = value;
      }
      return this;
    }
    module.exports = listCacheSet;
  }
});

// node_modules/lodash/_ListCache.js
var require_ListCache = __commonJS({
  "node_modules/lodash/_ListCache.js"(exports, module) {
    var listCacheClear = require_listCacheClear();
    var listCacheDelete = require_listCacheDelete();
    var listCacheGet = require_listCacheGet();
    var listCacheHas = require_listCacheHas();
    var listCacheSet = require_listCacheSet();
    function ListCache(entries) {
      var index2 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index2 < length) {
        var entry = entries[index2];
        this.set(entry[0], entry[1]);
      }
    }
    ListCache.prototype.clear = listCacheClear;
    ListCache.prototype["delete"] = listCacheDelete;
    ListCache.prototype.get = listCacheGet;
    ListCache.prototype.has = listCacheHas;
    ListCache.prototype.set = listCacheSet;
    module.exports = ListCache;
  }
});

// node_modules/lodash/_Map.js
var require_Map = __commonJS({
  "node_modules/lodash/_Map.js"(exports, module) {
    var getNative = require_getNative();
    var root = require_root();
    var Map2 = getNative(root, "Map");
    module.exports = Map2;
  }
});

// node_modules/lodash/_mapCacheClear.js
var require_mapCacheClear = __commonJS({
  "node_modules/lodash/_mapCacheClear.js"(exports, module) {
    var Hash = require_Hash();
    var ListCache = require_ListCache();
    var Map2 = require_Map();
    function mapCacheClear() {
      this.size = 0;
      this.__data__ = {
        "hash": new Hash(),
        "map": new (Map2 || ListCache)(),
        "string": new Hash()
      };
    }
    module.exports = mapCacheClear;
  }
});

// node_modules/lodash/_isKeyable.js
var require_isKeyable = __commonJS({
  "node_modules/lodash/_isKeyable.js"(exports, module) {
    function isKeyable(value) {
      var type = typeof value;
      return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
    }
    module.exports = isKeyable;
  }
});

// node_modules/lodash/_getMapData.js
var require_getMapData = __commonJS({
  "node_modules/lodash/_getMapData.js"(exports, module) {
    var isKeyable = require_isKeyable();
    function getMapData(map4, key) {
      var data = map4.__data__;
      return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
    }
    module.exports = getMapData;
  }
});

// node_modules/lodash/_mapCacheDelete.js
var require_mapCacheDelete = __commonJS({
  "node_modules/lodash/_mapCacheDelete.js"(exports, module) {
    var getMapData = require_getMapData();
    function mapCacheDelete(key) {
      var result = getMapData(this, key)["delete"](key);
      this.size -= result ? 1 : 0;
      return result;
    }
    module.exports = mapCacheDelete;
  }
});

// node_modules/lodash/_mapCacheGet.js
var require_mapCacheGet = __commonJS({
  "node_modules/lodash/_mapCacheGet.js"(exports, module) {
    var getMapData = require_getMapData();
    function mapCacheGet(key) {
      return getMapData(this, key).get(key);
    }
    module.exports = mapCacheGet;
  }
});

// node_modules/lodash/_mapCacheHas.js
var require_mapCacheHas = __commonJS({
  "node_modules/lodash/_mapCacheHas.js"(exports, module) {
    var getMapData = require_getMapData();
    function mapCacheHas(key) {
      return getMapData(this, key).has(key);
    }
    module.exports = mapCacheHas;
  }
});

// node_modules/lodash/_mapCacheSet.js
var require_mapCacheSet = __commonJS({
  "node_modules/lodash/_mapCacheSet.js"(exports, module) {
    var getMapData = require_getMapData();
    function mapCacheSet(key, value) {
      var data = getMapData(this, key), size = data.size;
      data.set(key, value);
      this.size += data.size == size ? 0 : 1;
      return this;
    }
    module.exports = mapCacheSet;
  }
});

// node_modules/lodash/_MapCache.js
var require_MapCache = __commonJS({
  "node_modules/lodash/_MapCache.js"(exports, module) {
    var mapCacheClear = require_mapCacheClear();
    var mapCacheDelete = require_mapCacheDelete();
    var mapCacheGet = require_mapCacheGet();
    var mapCacheHas = require_mapCacheHas();
    var mapCacheSet = require_mapCacheSet();
    function MapCache(entries) {
      var index2 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index2 < length) {
        var entry = entries[index2];
        this.set(entry[0], entry[1]);
      }
    }
    MapCache.prototype.clear = mapCacheClear;
    MapCache.prototype["delete"] = mapCacheDelete;
    MapCache.prototype.get = mapCacheGet;
    MapCache.prototype.has = mapCacheHas;
    MapCache.prototype.set = mapCacheSet;
    module.exports = MapCache;
  }
});

// node_modules/lodash/memoize.js
var require_memoize = __commonJS({
  "node_modules/lodash/memoize.js"(exports, module) {
    var MapCache = require_MapCache();
    var FUNC_ERROR_TEXT = "Expected a function";
    function memoize(func, resolver) {
      if (typeof func != "function" || resolver != null && typeof resolver != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      var memoized = function() {
        var args = arguments, key = resolver ? resolver.apply(this, args) : args[0], cache = memoized.cache;
        if (cache.has(key)) {
          return cache.get(key);
        }
        var result = func.apply(this, args);
        memoized.cache = cache.set(key, result) || cache;
        return result;
      };
      memoized.cache = new (memoize.Cache || MapCache)();
      return memoized;
    }
    memoize.Cache = MapCache;
    module.exports = memoize;
  }
});

// node_modules/lodash/_memoizeCapped.js
var require_memoizeCapped = __commonJS({
  "node_modules/lodash/_memoizeCapped.js"(exports, module) {
    var memoize = require_memoize();
    var MAX_MEMOIZE_SIZE = 500;
    function memoizeCapped(func) {
      var result = memoize(func, function(key) {
        if (cache.size === MAX_MEMOIZE_SIZE) {
          cache.clear();
        }
        return key;
      });
      var cache = result.cache;
      return result;
    }
    module.exports = memoizeCapped;
  }
});

// node_modules/lodash/_stringToPath.js
var require_stringToPath = __commonJS({
  "node_modules/lodash/_stringToPath.js"(exports, module) {
    var memoizeCapped = require_memoizeCapped();
    var rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
    var reEscapeChar = /\\(\\)?/g;
    var stringToPath = memoizeCapped(function(string) {
      var result = [];
      if (string.charCodeAt(0) === 46) {
        result.push("");
      }
      string.replace(rePropName, function(match, number4, quote, subString) {
        result.push(quote ? subString.replace(reEscapeChar, "$1") : number4 || match);
      });
      return result;
    });
    module.exports = stringToPath;
  }
});

// node_modules/lodash/_arrayMap.js
var require_arrayMap = __commonJS({
  "node_modules/lodash/_arrayMap.js"(exports, module) {
    function arrayMap(array2, iteratee) {
      var index2 = -1, length = array2 == null ? 0 : array2.length, result = Array(length);
      while (++index2 < length) {
        result[index2] = iteratee(array2[index2], index2, array2);
      }
      return result;
    }
    module.exports = arrayMap;
  }
});

// node_modules/lodash/_baseToString.js
var require_baseToString = __commonJS({
  "node_modules/lodash/_baseToString.js"(exports, module) {
    var Symbol3 = require_Symbol();
    var arrayMap = require_arrayMap();
    var isArray = require_isArray();
    var isSymbol = require_isSymbol();
    var INFINITY = 1 / 0;
    var symbolProto = Symbol3 ? Symbol3.prototype : void 0;
    var symbolToString = symbolProto ? symbolProto.toString : void 0;
    function baseToString(value) {
      if (typeof value == "string") {
        return value;
      }
      if (isArray(value)) {
        return arrayMap(value, baseToString) + "";
      }
      if (isSymbol(value)) {
        return symbolToString ? symbolToString.call(value) : "";
      }
      var result = value + "";
      return result == "0" && 1 / value == -INFINITY ? "-0" : result;
    }
    module.exports = baseToString;
  }
});

// node_modules/lodash/toString.js
var require_toString = __commonJS({
  "node_modules/lodash/toString.js"(exports, module) {
    var baseToString = require_baseToString();
    function toString(value) {
      return value == null ? "" : baseToString(value);
    }
    module.exports = toString;
  }
});

// node_modules/lodash/_castPath.js
var require_castPath = __commonJS({
  "node_modules/lodash/_castPath.js"(exports, module) {
    var isArray = require_isArray();
    var isKey = require_isKey();
    var stringToPath = require_stringToPath();
    var toString = require_toString();
    function castPath(value, object) {
      if (isArray(value)) {
        return value;
      }
      return isKey(value, object) ? [value] : stringToPath(toString(value));
    }
    module.exports = castPath;
  }
});

// node_modules/lodash/_toKey.js
var require_toKey = __commonJS({
  "node_modules/lodash/_toKey.js"(exports, module) {
    var isSymbol = require_isSymbol();
    var INFINITY = 1 / 0;
    function toKey(value) {
      if (typeof value == "string" || isSymbol(value)) {
        return value;
      }
      var result = value + "";
      return result == "0" && 1 / value == -INFINITY ? "-0" : result;
    }
    module.exports = toKey;
  }
});

// node_modules/lodash/_baseGet.js
var require_baseGet = __commonJS({
  "node_modules/lodash/_baseGet.js"(exports, module) {
    var castPath = require_castPath();
    var toKey = require_toKey();
    function baseGet(object, path2) {
      path2 = castPath(path2, object);
      var index2 = 0, length = path2.length;
      while (object != null && index2 < length) {
        object = object[toKey(path2[index2++])];
      }
      return index2 && index2 == length ? object : void 0;
    }
    module.exports = baseGet;
  }
});

// node_modules/lodash/get.js
var require_get = __commonJS({
  "node_modules/lodash/get.js"(exports, module) {
    var baseGet = require_baseGet();
    function get(object, path2, defaultValue) {
      var result = object == null ? void 0 : baseGet(object, path2);
      return result === void 0 ? defaultValue : result;
    }
    module.exports = get;
  }
});

// node_modules/lodash/_stackClear.js
var require_stackClear = __commonJS({
  "node_modules/lodash/_stackClear.js"(exports, module) {
    var ListCache = require_ListCache();
    function stackClear() {
      this.__data__ = new ListCache();
      this.size = 0;
    }
    module.exports = stackClear;
  }
});

// node_modules/lodash/_stackDelete.js
var require_stackDelete = __commonJS({
  "node_modules/lodash/_stackDelete.js"(exports, module) {
    function stackDelete(key) {
      var data = this.__data__, result = data["delete"](key);
      this.size = data.size;
      return result;
    }
    module.exports = stackDelete;
  }
});

// node_modules/lodash/_stackGet.js
var require_stackGet = __commonJS({
  "node_modules/lodash/_stackGet.js"(exports, module) {
    function stackGet(key) {
      return this.__data__.get(key);
    }
    module.exports = stackGet;
  }
});

// node_modules/lodash/_stackHas.js
var require_stackHas = __commonJS({
  "node_modules/lodash/_stackHas.js"(exports, module) {
    function stackHas(key) {
      return this.__data__.has(key);
    }
    module.exports = stackHas;
  }
});

// node_modules/lodash/_stackSet.js
var require_stackSet = __commonJS({
  "node_modules/lodash/_stackSet.js"(exports, module) {
    var ListCache = require_ListCache();
    var Map2 = require_Map();
    var MapCache = require_MapCache();
    var LARGE_ARRAY_SIZE = 200;
    function stackSet(key, value) {
      var data = this.__data__;
      if (data instanceof ListCache) {
        var pairs2 = data.__data__;
        if (!Map2 || pairs2.length < LARGE_ARRAY_SIZE - 1) {
          pairs2.push([key, value]);
          this.size = ++data.size;
          return this;
        }
        data = this.__data__ = new MapCache(pairs2);
      }
      data.set(key, value);
      this.size = data.size;
      return this;
    }
    module.exports = stackSet;
  }
});

// node_modules/lodash/_Stack.js
var require_Stack = __commonJS({
  "node_modules/lodash/_Stack.js"(exports, module) {
    var ListCache = require_ListCache();
    var stackClear = require_stackClear();
    var stackDelete = require_stackDelete();
    var stackGet = require_stackGet();
    var stackHas = require_stackHas();
    var stackSet = require_stackSet();
    function Stack(entries) {
      var data = this.__data__ = new ListCache(entries);
      this.size = data.size;
    }
    Stack.prototype.clear = stackClear;
    Stack.prototype["delete"] = stackDelete;
    Stack.prototype.get = stackGet;
    Stack.prototype.has = stackHas;
    Stack.prototype.set = stackSet;
    module.exports = Stack;
  }
});

// node_modules/lodash/_defineProperty.js
var require_defineProperty = __commonJS({
  "node_modules/lodash/_defineProperty.js"(exports, module) {
    var getNative = require_getNative();
    var defineProperty = (function() {
      try {
        var func = getNative(Object, "defineProperty");
        func({}, "", {});
        return func;
      } catch (e11) {
      }
    })();
    module.exports = defineProperty;
  }
});

// node_modules/lodash/_baseAssignValue.js
var require_baseAssignValue = __commonJS({
  "node_modules/lodash/_baseAssignValue.js"(exports, module) {
    var defineProperty = require_defineProperty();
    function baseAssignValue(object, key, value) {
      if (key == "__proto__" && defineProperty) {
        defineProperty(object, key, {
          "configurable": true,
          "enumerable": true,
          "value": value,
          "writable": true
        });
      } else {
        object[key] = value;
      }
    }
    module.exports = baseAssignValue;
  }
});

// node_modules/lodash/_assignMergeValue.js
var require_assignMergeValue = __commonJS({
  "node_modules/lodash/_assignMergeValue.js"(exports, module) {
    var baseAssignValue = require_baseAssignValue();
    var eq = require_eq();
    function assignMergeValue(object, key, value) {
      if (value !== void 0 && !eq(object[key], value) || value === void 0 && !(key in object)) {
        baseAssignValue(object, key, value);
      }
    }
    module.exports = assignMergeValue;
  }
});

// node_modules/lodash/_createBaseFor.js
var require_createBaseFor = __commonJS({
  "node_modules/lodash/_createBaseFor.js"(exports, module) {
    function createBaseFor(fromRight) {
      return function(object, iteratee, keysFunc) {
        var index2 = -1, iterable = Object(object), props = keysFunc(object), length = props.length;
        while (length--) {
          var key = props[fromRight ? length : ++index2];
          if (iteratee(iterable[key], key, iterable) === false) {
            break;
          }
        }
        return object;
      };
    }
    module.exports = createBaseFor;
  }
});

// node_modules/lodash/_baseFor.js
var require_baseFor = __commonJS({
  "node_modules/lodash/_baseFor.js"(exports, module) {
    var createBaseFor = require_createBaseFor();
    var baseFor = createBaseFor();
    module.exports = baseFor;
  }
});

// node_modules/lodash/_cloneBuffer.js
var require_cloneBuffer = __commonJS({
  "node_modules/lodash/_cloneBuffer.js"(exports, module) {
    var root = require_root();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var Buffer = moduleExports ? root.Buffer : void 0;
    var allocUnsafe = Buffer ? Buffer.allocUnsafe : void 0;
    function cloneBuffer(buffer, isDeep) {
      if (isDeep) {
        return buffer.slice();
      }
      var length = buffer.length, result = allocUnsafe ? allocUnsafe(length) : new buffer.constructor(length);
      buffer.copy(result);
      return result;
    }
    module.exports = cloneBuffer;
  }
});

// node_modules/lodash/_Uint8Array.js
var require_Uint8Array = __commonJS({
  "node_modules/lodash/_Uint8Array.js"(exports, module) {
    var root = require_root();
    var Uint8Array = root.Uint8Array;
    module.exports = Uint8Array;
  }
});

// node_modules/lodash/_cloneArrayBuffer.js
var require_cloneArrayBuffer = __commonJS({
  "node_modules/lodash/_cloneArrayBuffer.js"(exports, module) {
    var Uint8Array = require_Uint8Array();
    function cloneArrayBuffer(arrayBuffer) {
      var result = new arrayBuffer.constructor(arrayBuffer.byteLength);
      new Uint8Array(result).set(new Uint8Array(arrayBuffer));
      return result;
    }
    module.exports = cloneArrayBuffer;
  }
});

// node_modules/lodash/_cloneTypedArray.js
var require_cloneTypedArray = __commonJS({
  "node_modules/lodash/_cloneTypedArray.js"(exports, module) {
    var cloneArrayBuffer = require_cloneArrayBuffer();
    function cloneTypedArray(typedArray, isDeep) {
      var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
      return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);
    }
    module.exports = cloneTypedArray;
  }
});

// node_modules/lodash/_copyArray.js
var require_copyArray = __commonJS({
  "node_modules/lodash/_copyArray.js"(exports, module) {
    function copyArray(source, array2) {
      var index2 = -1, length = source.length;
      array2 || (array2 = Array(length));
      while (++index2 < length) {
        array2[index2] = source[index2];
      }
      return array2;
    }
    module.exports = copyArray;
  }
});

// node_modules/lodash/_baseCreate.js
var require_baseCreate = __commonJS({
  "node_modules/lodash/_baseCreate.js"(exports, module) {
    var isObject = require_isObject();
    var objectCreate = Object.create;
    var baseCreate = /* @__PURE__ */ (function() {
      function object() {
      }
      return function(proto) {
        if (!isObject(proto)) {
          return {};
        }
        if (objectCreate) {
          return objectCreate(proto);
        }
        object.prototype = proto;
        var result = new object();
        object.prototype = void 0;
        return result;
      };
    })();
    module.exports = baseCreate;
  }
});

// node_modules/lodash/_isPrototype.js
var require_isPrototype = __commonJS({
  "node_modules/lodash/_isPrototype.js"(exports, module) {
    var objectProto = Object.prototype;
    function isPrototype(value) {
      var Ctor = value && value.constructor, proto = typeof Ctor == "function" && Ctor.prototype || objectProto;
      return value === proto;
    }
    module.exports = isPrototype;
  }
});

// node_modules/lodash/_initCloneObject.js
var require_initCloneObject = __commonJS({
  "node_modules/lodash/_initCloneObject.js"(exports, module) {
    var baseCreate = require_baseCreate();
    var getPrototype = require_getPrototype();
    var isPrototype = require_isPrototype();
    function initCloneObject(object) {
      return typeof object.constructor == "function" && !isPrototype(object) ? baseCreate(getPrototype(object)) : {};
    }
    module.exports = initCloneObject;
  }
});

// node_modules/lodash/_baseIsArguments.js
var require_baseIsArguments = __commonJS({
  "node_modules/lodash/_baseIsArguments.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isObjectLike = require_isObjectLike();
    var argsTag = "[object Arguments]";
    function baseIsArguments(value) {
      return isObjectLike(value) && baseGetTag(value) == argsTag;
    }
    module.exports = baseIsArguments;
  }
});

// node_modules/lodash/isArguments.js
var require_isArguments = __commonJS({
  "node_modules/lodash/isArguments.js"(exports, module) {
    var baseIsArguments = require_baseIsArguments();
    var isObjectLike = require_isObjectLike();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var propertyIsEnumerable = objectProto.propertyIsEnumerable;
    var isArguments = baseIsArguments(/* @__PURE__ */ (function() {
      return arguments;
    })()) ? baseIsArguments : function(value) {
      return isObjectLike(value) && hasOwnProperty.call(value, "callee") && !propertyIsEnumerable.call(value, "callee");
    };
    module.exports = isArguments;
  }
});

// node_modules/lodash/isLength.js
var require_isLength = __commonJS({
  "node_modules/lodash/isLength.js"(exports, module) {
    var MAX_SAFE_INTEGER = 9007199254740991;
    function isLength(value) {
      return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
    }
    module.exports = isLength;
  }
});

// node_modules/lodash/isArrayLike.js
var require_isArrayLike = __commonJS({
  "node_modules/lodash/isArrayLike.js"(exports, module) {
    var isFunction = require_isFunction();
    var isLength = require_isLength();
    function isArrayLike(value) {
      return value != null && isLength(value.length) && !isFunction(value);
    }
    module.exports = isArrayLike;
  }
});

// node_modules/lodash/isArrayLikeObject.js
var require_isArrayLikeObject = __commonJS({
  "node_modules/lodash/isArrayLikeObject.js"(exports, module) {
    var isArrayLike = require_isArrayLike();
    var isObjectLike = require_isObjectLike();
    function isArrayLikeObject(value) {
      return isObjectLike(value) && isArrayLike(value);
    }
    module.exports = isArrayLikeObject;
  }
});

// node_modules/lodash/stubFalse.js
var require_stubFalse = __commonJS({
  "node_modules/lodash/stubFalse.js"(exports, module) {
    function stubFalse() {
      return false;
    }
    module.exports = stubFalse;
  }
});

// node_modules/lodash/isBuffer.js
var require_isBuffer = __commonJS({
  "node_modules/lodash/isBuffer.js"(exports, module) {
    var root = require_root();
    var stubFalse = require_stubFalse();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var Buffer = moduleExports ? root.Buffer : void 0;
    var nativeIsBuffer = Buffer ? Buffer.isBuffer : void 0;
    var isBuffer = nativeIsBuffer || stubFalse;
    module.exports = isBuffer;
  }
});

// node_modules/lodash/_baseIsTypedArray.js
var require_baseIsTypedArray = __commonJS({
  "node_modules/lodash/_baseIsTypedArray.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isLength = require_isLength();
    var isObjectLike = require_isObjectLike();
    var argsTag = "[object Arguments]";
    var arrayTag = "[object Array]";
    var boolTag = "[object Boolean]";
    var dateTag = "[object Date]";
    var errorTag = "[object Error]";
    var funcTag = "[object Function]";
    var mapTag = "[object Map]";
    var numberTag = "[object Number]";
    var objectTag = "[object Object]";
    var regexpTag = "[object RegExp]";
    var setTag = "[object Set]";
    var stringTag = "[object String]";
    var weakMapTag = "[object WeakMap]";
    var arrayBufferTag = "[object ArrayBuffer]";
    var dataViewTag = "[object DataView]";
    var float32Tag = "[object Float32Array]";
    var float64Tag = "[object Float64Array]";
    var int8Tag = "[object Int8Array]";
    var int16Tag = "[object Int16Array]";
    var int32Tag = "[object Int32Array]";
    var uint8Tag = "[object Uint8Array]";
    var uint8ClampedTag = "[object Uint8ClampedArray]";
    var uint16Tag = "[object Uint16Array]";
    var uint32Tag = "[object Uint32Array]";
    var typedArrayTags = {};
    typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
    typedArrayTags[argsTag] = typedArrayTags[arrayTag] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag] = typedArrayTags[numberTag] = typedArrayTags[objectTag] = typedArrayTags[regexpTag] = typedArrayTags[setTag] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag] = false;
    function baseIsTypedArray(value) {
      return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
    }
    module.exports = baseIsTypedArray;
  }
});

// node_modules/lodash/_baseUnary.js
var require_baseUnary = __commonJS({
  "node_modules/lodash/_baseUnary.js"(exports, module) {
    function baseUnary(func) {
      return function(value) {
        return func(value);
      };
    }
    module.exports = baseUnary;
  }
});

// node_modules/lodash/_nodeUtil.js
var require_nodeUtil = __commonJS({
  "node_modules/lodash/_nodeUtil.js"(exports, module) {
    var freeGlobal = require_freeGlobal();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var freeProcess = moduleExports && freeGlobal.process;
    var nodeUtil = (function() {
      try {
        var types = freeModule && freeModule.require && freeModule.require("util").types;
        if (types) {
          return types;
        }
        return freeProcess && freeProcess.binding && freeProcess.binding("util");
      } catch (e11) {
      }
    })();
    module.exports = nodeUtil;
  }
});

// node_modules/lodash/isTypedArray.js
var require_isTypedArray = __commonJS({
  "node_modules/lodash/isTypedArray.js"(exports, module) {
    var baseIsTypedArray = require_baseIsTypedArray();
    var baseUnary = require_baseUnary();
    var nodeUtil = require_nodeUtil();
    var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
    var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
    module.exports = isTypedArray;
  }
});

// node_modules/lodash/_safeGet.js
var require_safeGet = __commonJS({
  "node_modules/lodash/_safeGet.js"(exports, module) {
    function safeGet(object, key) {
      if (key === "constructor" && typeof object[key] === "function") {
        return;
      }
      if (key == "__proto__") {
        return;
      }
      return object[key];
    }
    module.exports = safeGet;
  }
});

// node_modules/lodash/_assignValue.js
var require_assignValue = __commonJS({
  "node_modules/lodash/_assignValue.js"(exports, module) {
    var baseAssignValue = require_baseAssignValue();
    var eq = require_eq();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function assignValue(object, key, value) {
      var objValue = object[key];
      if (!(hasOwnProperty.call(object, key) && eq(objValue, value)) || value === void 0 && !(key in object)) {
        baseAssignValue(object, key, value);
      }
    }
    module.exports = assignValue;
  }
});

// node_modules/lodash/_copyObject.js
var require_copyObject = __commonJS({
  "node_modules/lodash/_copyObject.js"(exports, module) {
    var assignValue = require_assignValue();
    var baseAssignValue = require_baseAssignValue();
    function copyObject(source, props, object, customizer) {
      var isNew = !object;
      object || (object = {});
      var index2 = -1, length = props.length;
      while (++index2 < length) {
        var key = props[index2];
        var newValue = customizer ? customizer(object[key], source[key], key, object, source) : void 0;
        if (newValue === void 0) {
          newValue = source[key];
        }
        if (isNew) {
          baseAssignValue(object, key, newValue);
        } else {
          assignValue(object, key, newValue);
        }
      }
      return object;
    }
    module.exports = copyObject;
  }
});

// node_modules/lodash/_baseTimes.js
var require_baseTimes = __commonJS({
  "node_modules/lodash/_baseTimes.js"(exports, module) {
    function baseTimes(n9, iteratee) {
      var index2 = -1, result = Array(n9);
      while (++index2 < n9) {
        result[index2] = iteratee(index2);
      }
      return result;
    }
    module.exports = baseTimes;
  }
});

// node_modules/lodash/_isIndex.js
var require_isIndex = __commonJS({
  "node_modules/lodash/_isIndex.js"(exports, module) {
    var MAX_SAFE_INTEGER = 9007199254740991;
    var reIsUint = /^(?:0|[1-9]\d*)$/;
    function isIndex(value, length) {
      var type = typeof value;
      length = length == null ? MAX_SAFE_INTEGER : length;
      return !!length && (type == "number" || type != "symbol" && reIsUint.test(value)) && (value > -1 && value % 1 == 0 && value < length);
    }
    module.exports = isIndex;
  }
});

// node_modules/lodash/_arrayLikeKeys.js
var require_arrayLikeKeys = __commonJS({
  "node_modules/lodash/_arrayLikeKeys.js"(exports, module) {
    var baseTimes = require_baseTimes();
    var isArguments = require_isArguments();
    var isArray = require_isArray();
    var isBuffer = require_isBuffer();
    var isIndex = require_isIndex();
    var isTypedArray = require_isTypedArray();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function arrayLikeKeys(value, inherited) {
      var isArr = isArray(value), isArg = !isArr && isArguments(value), isBuff = !isArr && !isArg && isBuffer(value), isType = !isArr && !isArg && !isBuff && isTypedArray(value), skipIndexes = isArr || isArg || isBuff || isType, result = skipIndexes ? baseTimes(value.length, String) : [], length = result.length;
      for (var key in value) {
        if ((inherited || hasOwnProperty.call(value, key)) && !(skipIndexes && // Safari 9 has enumerable `arguments.length` in strict mode.
        (key == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
        isBuff && (key == "offset" || key == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
        isType && (key == "buffer" || key == "byteLength" || key == "byteOffset") || // Skip index properties.
        isIndex(key, length)))) {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = arrayLikeKeys;
  }
});

// node_modules/lodash/_nativeKeysIn.js
var require_nativeKeysIn = __commonJS({
  "node_modules/lodash/_nativeKeysIn.js"(exports, module) {
    function nativeKeysIn(object) {
      var result = [];
      if (object != null) {
        for (var key in Object(object)) {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = nativeKeysIn;
  }
});

// node_modules/lodash/_baseKeysIn.js
var require_baseKeysIn = __commonJS({
  "node_modules/lodash/_baseKeysIn.js"(exports, module) {
    var isObject = require_isObject();
    var isPrototype = require_isPrototype();
    var nativeKeysIn = require_nativeKeysIn();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function baseKeysIn(object) {
      if (!isObject(object)) {
        return nativeKeysIn(object);
      }
      var isProto = isPrototype(object), result = [];
      for (var key in object) {
        if (!(key == "constructor" && (isProto || !hasOwnProperty.call(object, key)))) {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = baseKeysIn;
  }
});

// node_modules/lodash/keysIn.js
var require_keysIn = __commonJS({
  "node_modules/lodash/keysIn.js"(exports, module) {
    var arrayLikeKeys = require_arrayLikeKeys();
    var baseKeysIn = require_baseKeysIn();
    var isArrayLike = require_isArrayLike();
    function keysIn(object) {
      return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
    }
    module.exports = keysIn;
  }
});

// node_modules/lodash/toPlainObject.js
var require_toPlainObject = __commonJS({
  "node_modules/lodash/toPlainObject.js"(exports, module) {
    var copyObject = require_copyObject();
    var keysIn = require_keysIn();
    function toPlainObject(value) {
      return copyObject(value, keysIn(value));
    }
    module.exports = toPlainObject;
  }
});

// node_modules/lodash/_baseMergeDeep.js
var require_baseMergeDeep = __commonJS({
  "node_modules/lodash/_baseMergeDeep.js"(exports, module) {
    var assignMergeValue = require_assignMergeValue();
    var cloneBuffer = require_cloneBuffer();
    var cloneTypedArray = require_cloneTypedArray();
    var copyArray = require_copyArray();
    var initCloneObject = require_initCloneObject();
    var isArguments = require_isArguments();
    var isArray = require_isArray();
    var isArrayLikeObject = require_isArrayLikeObject();
    var isBuffer = require_isBuffer();
    var isFunction = require_isFunction();
    var isObject = require_isObject();
    var isPlainObject = require_isPlainObject();
    var isTypedArray = require_isTypedArray();
    var safeGet = require_safeGet();
    var toPlainObject = require_toPlainObject();
    function baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {
      var objValue = safeGet(object, key), srcValue = safeGet(source, key), stacked = stack.get(srcValue);
      if (stacked) {
        assignMergeValue(object, key, stacked);
        return;
      }
      var newValue = customizer ? customizer(objValue, srcValue, key + "", object, source, stack) : void 0;
      var isCommon = newValue === void 0;
      if (isCommon) {
        var isArr = isArray(srcValue), isBuff = !isArr && isBuffer(srcValue), isTyped = !isArr && !isBuff && isTypedArray(srcValue);
        newValue = srcValue;
        if (isArr || isBuff || isTyped) {
          if (isArray(objValue)) {
            newValue = objValue;
          } else if (isArrayLikeObject(objValue)) {
            newValue = copyArray(objValue);
          } else if (isBuff) {
            isCommon = false;
            newValue = cloneBuffer(srcValue, true);
          } else if (isTyped) {
            isCommon = false;
            newValue = cloneTypedArray(srcValue, true);
          } else {
            newValue = [];
          }
        } else if (isPlainObject(srcValue) || isArguments(srcValue)) {
          newValue = objValue;
          if (isArguments(objValue)) {
            newValue = toPlainObject(objValue);
          } else if (!isObject(objValue) || isFunction(objValue)) {
            newValue = initCloneObject(srcValue);
          }
        } else {
          isCommon = false;
        }
      }
      if (isCommon) {
        stack.set(srcValue, newValue);
        mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
        stack["delete"](srcValue);
      }
      assignMergeValue(object, key, newValue);
    }
    module.exports = baseMergeDeep;
  }
});

// node_modules/lodash/_baseMerge.js
var require_baseMerge = __commonJS({
  "node_modules/lodash/_baseMerge.js"(exports, module) {
    var Stack = require_Stack();
    var assignMergeValue = require_assignMergeValue();
    var baseFor = require_baseFor();
    var baseMergeDeep = require_baseMergeDeep();
    var isObject = require_isObject();
    var keysIn = require_keysIn();
    var safeGet = require_safeGet();
    function baseMerge(object, source, srcIndex, customizer, stack) {
      if (object === source) {
        return;
      }
      baseFor(source, function(srcValue, key) {
        stack || (stack = new Stack());
        if (isObject(srcValue)) {
          baseMergeDeep(object, source, key, srcIndex, baseMerge, customizer, stack);
        } else {
          var newValue = customizer ? customizer(safeGet(object, key), srcValue, key + "", object, source, stack) : void 0;
          if (newValue === void 0) {
            newValue = srcValue;
          }
          assignMergeValue(object, key, newValue);
        }
      }, keysIn);
    }
    module.exports = baseMerge;
  }
});

// node_modules/lodash/identity.js
var require_identity = __commonJS({
  "node_modules/lodash/identity.js"(exports, module) {
    function identity5(value) {
      return value;
    }
    module.exports = identity5;
  }
});

// node_modules/lodash/_apply.js
var require_apply = __commonJS({
  "node_modules/lodash/_apply.js"(exports, module) {
    function apply(func, thisArg, args) {
      switch (args.length) {
        case 0:
          return func.call(thisArg);
        case 1:
          return func.call(thisArg, args[0]);
        case 2:
          return func.call(thisArg, args[0], args[1]);
        case 3:
          return func.call(thisArg, args[0], args[1], args[2]);
      }
      return func.apply(thisArg, args);
    }
    module.exports = apply;
  }
});

// node_modules/lodash/_overRest.js
var require_overRest = __commonJS({
  "node_modules/lodash/_overRest.js"(exports, module) {
    var apply = require_apply();
    var nativeMax = Math.max;
    function overRest(func, start2, transform) {
      start2 = nativeMax(start2 === void 0 ? func.length - 1 : start2, 0);
      return function() {
        var args = arguments, index2 = -1, length = nativeMax(args.length - start2, 0), array2 = Array(length);
        while (++index2 < length) {
          array2[index2] = args[start2 + index2];
        }
        index2 = -1;
        var otherArgs = Array(start2 + 1);
        while (++index2 < start2) {
          otherArgs[index2] = args[index2];
        }
        otherArgs[start2] = transform(array2);
        return apply(func, this, otherArgs);
      };
    }
    module.exports = overRest;
  }
});

// node_modules/lodash/constant.js
var require_constant = __commonJS({
  "node_modules/lodash/constant.js"(exports, module) {
    function constant2(value) {
      return function() {
        return value;
      };
    }
    module.exports = constant2;
  }
});

// node_modules/lodash/_baseSetToString.js
var require_baseSetToString = __commonJS({
  "node_modules/lodash/_baseSetToString.js"(exports, module) {
    var constant2 = require_constant();
    var defineProperty = require_defineProperty();
    var identity5 = require_identity();
    var baseSetToString = !defineProperty ? identity5 : function(func, string) {
      return defineProperty(func, "toString", {
        "configurable": true,
        "enumerable": false,
        "value": constant2(string),
        "writable": true
      });
    };
    module.exports = baseSetToString;
  }
});

// node_modules/lodash/_shortOut.js
var require_shortOut = __commonJS({
  "node_modules/lodash/_shortOut.js"(exports, module) {
    var HOT_COUNT = 800;
    var HOT_SPAN = 16;
    var nativeNow = Date.now;
    function shortOut(func) {
      var count2 = 0, lastCalled = 0;
      return function() {
        var stamp = nativeNow(), remaining = HOT_SPAN - (stamp - lastCalled);
        lastCalled = stamp;
        if (remaining > 0) {
          if (++count2 >= HOT_COUNT) {
            return arguments[0];
          }
        } else {
          count2 = 0;
        }
        return func.apply(void 0, arguments);
      };
    }
    module.exports = shortOut;
  }
});

// node_modules/lodash/_setToString.js
var require_setToString = __commonJS({
  "node_modules/lodash/_setToString.js"(exports, module) {
    var baseSetToString = require_baseSetToString();
    var shortOut = require_shortOut();
    var setToString = shortOut(baseSetToString);
    module.exports = setToString;
  }
});

// node_modules/lodash/_baseRest.js
var require_baseRest = __commonJS({
  "node_modules/lodash/_baseRest.js"(exports, module) {
    var identity5 = require_identity();
    var overRest = require_overRest();
    var setToString = require_setToString();
    function baseRest(func, start2) {
      return setToString(overRest(func, start2, identity5), func + "");
    }
    module.exports = baseRest;
  }
});

// node_modules/lodash/_isIterateeCall.js
var require_isIterateeCall = __commonJS({
  "node_modules/lodash/_isIterateeCall.js"(exports, module) {
    var eq = require_eq();
    var isArrayLike = require_isArrayLike();
    var isIndex = require_isIndex();
    var isObject = require_isObject();
    function isIterateeCall(value, index2, object) {
      if (!isObject(object)) {
        return false;
      }
      var type = typeof index2;
      if (type == "number" ? isArrayLike(object) && isIndex(index2, object.length) : type == "string" && index2 in object) {
        return eq(object[index2], value);
      }
      return false;
    }
    module.exports = isIterateeCall;
  }
});

// node_modules/lodash/_createAssigner.js
var require_createAssigner = __commonJS({
  "node_modules/lodash/_createAssigner.js"(exports, module) {
    var baseRest = require_baseRest();
    var isIterateeCall = require_isIterateeCall();
    function createAssigner(assigner) {
      return baseRest(function(object, sources) {
        var index2 = -1, length = sources.length, customizer = length > 1 ? sources[length - 1] : void 0, guard = length > 2 ? sources[2] : void 0;
        customizer = assigner.length > 3 && typeof customizer == "function" ? (length--, customizer) : void 0;
        if (guard && isIterateeCall(sources[0], sources[1], guard)) {
          customizer = length < 3 ? void 0 : customizer;
          length = 1;
        }
        object = Object(object);
        while (++index2 < length) {
          var source = sources[index2];
          if (source) {
            assigner(object, source, index2, customizer);
          }
        }
        return object;
      });
    }
    module.exports = createAssigner;
  }
});

// node_modules/lodash/merge.js
var require_merge = __commonJS({
  "node_modules/lodash/merge.js"(exports, module) {
    var baseMerge = require_baseMerge();
    var createAssigner = require_createAssigner();
    var merge2 = createAssigner(function(object, source, srcIndex) {
      baseMerge(object, source, srcIndex);
    });
    module.exports = merge2;
  }
});

// node_modules/lodash/_baseSet.js
var require_baseSet = __commonJS({
  "node_modules/lodash/_baseSet.js"(exports, module) {
    var assignValue = require_assignValue();
    var castPath = require_castPath();
    var isIndex = require_isIndex();
    var isObject = require_isObject();
    var toKey = require_toKey();
    function baseSet(object, path2, value, customizer) {
      if (!isObject(object)) {
        return object;
      }
      path2 = castPath(path2, object);
      var index2 = -1, length = path2.length, lastIndex = length - 1, nested = object;
      while (nested != null && ++index2 < length) {
        var key = toKey(path2[index2]), newValue = value;
        if (key === "__proto__" || key === "constructor" || key === "prototype") {
          return object;
        }
        if (index2 != lastIndex) {
          var objValue = nested[key];
          newValue = customizer ? customizer(objValue, key, nested) : void 0;
          if (newValue === void 0) {
            newValue = isObject(objValue) ? objValue : isIndex(path2[index2 + 1]) ? [] : {};
          }
        }
        assignValue(nested, key, newValue);
        nested = nested[key];
      }
      return object;
    }
    module.exports = baseSet;
  }
});

// node_modules/lodash/set.js
var require_set = __commonJS({
  "node_modules/lodash/set.js"(exports, module) {
    var baseSet = require_baseSet();
    function set(object, path2, value) {
      return object == null ? object : baseSet(object, path2, value);
    }
    module.exports = set;
  }
});

// node_modules/lodash/isString.js
var require_isString = __commonJS({
  "node_modules/lodash/isString.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isArray = require_isArray();
    var isObjectLike = require_isObjectLike();
    var stringTag = "[object String]";
    function isString(value) {
      return typeof value == "string" || !isArray(value) && isObjectLike(value) && baseGetTag(value) == stringTag;
    }
    module.exports = isString;
  }
});

// node_modules/lodash/_setCacheAdd.js
var require_setCacheAdd = __commonJS({
  "node_modules/lodash/_setCacheAdd.js"(exports, module) {
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    function setCacheAdd(value) {
      this.__data__.set(value, HASH_UNDEFINED);
      return this;
    }
    module.exports = setCacheAdd;
  }
});

// node_modules/lodash/_setCacheHas.js
var require_setCacheHas = __commonJS({
  "node_modules/lodash/_setCacheHas.js"(exports, module) {
    function setCacheHas(value) {
      return this.__data__.has(value);
    }
    module.exports = setCacheHas;
  }
});

// node_modules/lodash/_SetCache.js
var require_SetCache = __commonJS({
  "node_modules/lodash/_SetCache.js"(exports, module) {
    var MapCache = require_MapCache();
    var setCacheAdd = require_setCacheAdd();
    var setCacheHas = require_setCacheHas();
    function SetCache(values) {
      var index2 = -1, length = values == null ? 0 : values.length;
      this.__data__ = new MapCache();
      while (++index2 < length) {
        this.add(values[index2]);
      }
    }
    SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
    SetCache.prototype.has = setCacheHas;
    module.exports = SetCache;
  }
});

// node_modules/lodash/_baseFindIndex.js
var require_baseFindIndex = __commonJS({
  "node_modules/lodash/_baseFindIndex.js"(exports, module) {
    function baseFindIndex(array2, predicate, fromIndex, fromRight) {
      var length = array2.length, index2 = fromIndex + (fromRight ? 1 : -1);
      while (fromRight ? index2-- : ++index2 < length) {
        if (predicate(array2[index2], index2, array2)) {
          return index2;
        }
      }
      return -1;
    }
    module.exports = baseFindIndex;
  }
});

// node_modules/lodash/_baseIsNaN.js
var require_baseIsNaN = __commonJS({
  "node_modules/lodash/_baseIsNaN.js"(exports, module) {
    function baseIsNaN(value) {
      return value !== value;
    }
    module.exports = baseIsNaN;
  }
});

// node_modules/lodash/_strictIndexOf.js
var require_strictIndexOf = __commonJS({
  "node_modules/lodash/_strictIndexOf.js"(exports, module) {
    function strictIndexOf(array2, value, fromIndex) {
      var index2 = fromIndex - 1, length = array2.length;
      while (++index2 < length) {
        if (array2[index2] === value) {
          return index2;
        }
      }
      return -1;
    }
    module.exports = strictIndexOf;
  }
});

// node_modules/lodash/_baseIndexOf.js
var require_baseIndexOf = __commonJS({
  "node_modules/lodash/_baseIndexOf.js"(exports, module) {
    var baseFindIndex = require_baseFindIndex();
    var baseIsNaN = require_baseIsNaN();
    var strictIndexOf = require_strictIndexOf();
    function baseIndexOf(array2, value, fromIndex) {
      return value === value ? strictIndexOf(array2, value, fromIndex) : baseFindIndex(array2, baseIsNaN, fromIndex);
    }
    module.exports = baseIndexOf;
  }
});

// node_modules/lodash/_arrayIncludes.js
var require_arrayIncludes = __commonJS({
  "node_modules/lodash/_arrayIncludes.js"(exports, module) {
    var baseIndexOf = require_baseIndexOf();
    function arrayIncludes(array2, value) {
      var length = array2 == null ? 0 : array2.length;
      return !!length && baseIndexOf(array2, value, 0) > -1;
    }
    module.exports = arrayIncludes;
  }
});

// node_modules/lodash/_arrayIncludesWith.js
var require_arrayIncludesWith = __commonJS({
  "node_modules/lodash/_arrayIncludesWith.js"(exports, module) {
    function arrayIncludesWith(array2, value, comparator) {
      var index2 = -1, length = array2 == null ? 0 : array2.length;
      while (++index2 < length) {
        if (comparator(value, array2[index2])) {
          return true;
        }
      }
      return false;
    }
    module.exports = arrayIncludesWith;
  }
});

// node_modules/lodash/_cacheHas.js
var require_cacheHas = __commonJS({
  "node_modules/lodash/_cacheHas.js"(exports, module) {
    function cacheHas(cache, key) {
      return cache.has(key);
    }
    module.exports = cacheHas;
  }
});

// node_modules/lodash/_baseDifference.js
var require_baseDifference = __commonJS({
  "node_modules/lodash/_baseDifference.js"(exports, module) {
    var SetCache = require_SetCache();
    var arrayIncludes = require_arrayIncludes();
    var arrayIncludesWith = require_arrayIncludesWith();
    var arrayMap = require_arrayMap();
    var baseUnary = require_baseUnary();
    var cacheHas = require_cacheHas();
    var LARGE_ARRAY_SIZE = 200;
    function baseDifference(array2, values, iteratee, comparator) {
      var index2 = -1, includes = arrayIncludes, isCommon = true, length = array2.length, result = [], valuesLength = values.length;
      if (!length) {
        return result;
      }
      if (iteratee) {
        values = arrayMap(values, baseUnary(iteratee));
      }
      if (comparator) {
        includes = arrayIncludesWith;
        isCommon = false;
      } else if (values.length >= LARGE_ARRAY_SIZE) {
        includes = cacheHas;
        isCommon = false;
        values = new SetCache(values);
      }
      outer:
        while (++index2 < length) {
          var value = array2[index2], computed = iteratee == null ? value : iteratee(value);
          value = comparator || value !== 0 ? value : 0;
          if (isCommon && computed === computed) {
            var valuesIndex = valuesLength;
            while (valuesIndex--) {
              if (values[valuesIndex] === computed) {
                continue outer;
              }
            }
            result.push(value);
          } else if (!includes(values, computed, comparator)) {
            result.push(value);
          }
        }
      return result;
    }
    module.exports = baseDifference;
  }
});

// node_modules/lodash/without.js
var require_without = __commonJS({
  "node_modules/lodash/without.js"(exports, module) {
    var baseDifference = require_baseDifference();
    var baseRest = require_baseRest();
    var isArrayLikeObject = require_isArrayLikeObject();
    var without = baseRest(function(array2, values) {
      return isArrayLikeObject(array2) ? baseDifference(array2, values) : [];
    });
    module.exports = without;
  }
});

// node_modules/lodash/last.js
var require_last = __commonJS({
  "node_modules/lodash/last.js"(exports, module) {
    function last(array2) {
      var length = array2 == null ? 0 : array2.length;
      return length ? array2[length - 1] : void 0;
    }
    module.exports = last;
  }
});

// node_modules/lodash/_basePickBy.js
var require_basePickBy = __commonJS({
  "node_modules/lodash/_basePickBy.js"(exports, module) {
    var baseGet = require_baseGet();
    var baseSet = require_baseSet();
    var castPath = require_castPath();
    function basePickBy(object, paths, predicate) {
      var index2 = -1, length = paths.length, result = {};
      while (++index2 < length) {
        var path2 = paths[index2], value = baseGet(object, path2);
        if (predicate(value, path2)) {
          baseSet(result, castPath(path2, object), value);
        }
      }
      return result;
    }
    module.exports = basePickBy;
  }
});

// node_modules/lodash/_baseHasIn.js
var require_baseHasIn = __commonJS({
  "node_modules/lodash/_baseHasIn.js"(exports, module) {
    function baseHasIn(object, key) {
      return object != null && key in Object(object);
    }
    module.exports = baseHasIn;
  }
});

// node_modules/lodash/_hasPath.js
var require_hasPath = __commonJS({
  "node_modules/lodash/_hasPath.js"(exports, module) {
    var castPath = require_castPath();
    var isArguments = require_isArguments();
    var isArray = require_isArray();
    var isIndex = require_isIndex();
    var isLength = require_isLength();
    var toKey = require_toKey();
    function hasPath(object, path2, hasFunc) {
      path2 = castPath(path2, object);
      var index2 = -1, length = path2.length, result = false;
      while (++index2 < length) {
        var key = toKey(path2[index2]);
        if (!(result = object != null && hasFunc(object, key))) {
          break;
        }
        object = object[key];
      }
      if (result || ++index2 != length) {
        return result;
      }
      length = object == null ? 0 : object.length;
      return !!length && isLength(length) && isIndex(key, length) && (isArray(object) || isArguments(object));
    }
    module.exports = hasPath;
  }
});

// node_modules/lodash/hasIn.js
var require_hasIn = __commonJS({
  "node_modules/lodash/hasIn.js"(exports, module) {
    var baseHasIn = require_baseHasIn();
    var hasPath = require_hasPath();
    function hasIn(object, path2) {
      return object != null && hasPath(object, path2, baseHasIn);
    }
    module.exports = hasIn;
  }
});

// node_modules/lodash/_basePick.js
var require_basePick = __commonJS({
  "node_modules/lodash/_basePick.js"(exports, module) {
    var basePickBy = require_basePickBy();
    var hasIn = require_hasIn();
    function basePick(object, paths) {
      return basePickBy(object, paths, function(value, path2) {
        return hasIn(object, path2);
      });
    }
    module.exports = basePick;
  }
});

// node_modules/lodash/_arrayPush.js
var require_arrayPush = __commonJS({
  "node_modules/lodash/_arrayPush.js"(exports, module) {
    function arrayPush(array2, values) {
      var index2 = -1, length = values.length, offset = array2.length;
      while (++index2 < length) {
        array2[offset + index2] = values[index2];
      }
      return array2;
    }
    module.exports = arrayPush;
  }
});

// node_modules/lodash/_isFlattenable.js
var require_isFlattenable = __commonJS({
  "node_modules/lodash/_isFlattenable.js"(exports, module) {
    var Symbol3 = require_Symbol();
    var isArguments = require_isArguments();
    var isArray = require_isArray();
    var spreadableSymbol = Symbol3 ? Symbol3.isConcatSpreadable : void 0;
    function isFlattenable(value) {
      return isArray(value) || isArguments(value) || !!(spreadableSymbol && value && value[spreadableSymbol]);
    }
    module.exports = isFlattenable;
  }
});

// node_modules/lodash/_baseFlatten.js
var require_baseFlatten = __commonJS({
  "node_modules/lodash/_baseFlatten.js"(exports, module) {
    var arrayPush = require_arrayPush();
    var isFlattenable = require_isFlattenable();
    function baseFlatten(array2, depth, predicate, isStrict, result) {
      var index2 = -1, length = array2.length;
      predicate || (predicate = isFlattenable);
      result || (result = []);
      while (++index2 < length) {
        var value = array2[index2];
        if (depth > 0 && predicate(value)) {
          if (depth > 1) {
            baseFlatten(value, depth - 1, predicate, isStrict, result);
          } else {
            arrayPush(result, value);
          }
        } else if (!isStrict) {
          result[result.length] = value;
        }
      }
      return result;
    }
    module.exports = baseFlatten;
  }
});

// node_modules/lodash/flatten.js
var require_flatten = __commonJS({
  "node_modules/lodash/flatten.js"(exports, module) {
    var baseFlatten = require_baseFlatten();
    function flatten(array2) {
      var length = array2 == null ? 0 : array2.length;
      return length ? baseFlatten(array2, 1) : [];
    }
    module.exports = flatten;
  }
});

// node_modules/lodash/_flatRest.js
var require_flatRest = __commonJS({
  "node_modules/lodash/_flatRest.js"(exports, module) {
    var flatten = require_flatten();
    var overRest = require_overRest();
    var setToString = require_setToString();
    function flatRest(func) {
      return setToString(overRest(func, void 0, flatten), func + "");
    }
    module.exports = flatRest;
  }
});

// node_modules/lodash/pick.js
var require_pick = __commonJS({
  "node_modules/lodash/pick.js"(exports, module) {
    var basePick = require_basePick();
    var flatRest = require_flatRest();
    var pick = flatRest(function(object, paths) {
      return object == null ? {} : basePick(object, paths);
    });
    module.exports = pick;
  }
});

// node_modules/lodash/_arraySome.js
var require_arraySome = __commonJS({
  "node_modules/lodash/_arraySome.js"(exports, module) {
    function arraySome(array2, predicate) {
      var index2 = -1, length = array2 == null ? 0 : array2.length;
      while (++index2 < length) {
        if (predicate(array2[index2], index2, array2)) {
          return true;
        }
      }
      return false;
    }
    module.exports = arraySome;
  }
});

// node_modules/lodash/_equalArrays.js
var require_equalArrays = __commonJS({
  "node_modules/lodash/_equalArrays.js"(exports, module) {
    var SetCache = require_SetCache();
    var arraySome = require_arraySome();
    var cacheHas = require_cacheHas();
    var COMPARE_PARTIAL_FLAG = 1;
    var COMPARE_UNORDERED_FLAG = 2;
    function equalArrays(array2, other, bitmask, customizer, equalFunc, stack) {
      var isPartial = bitmask & COMPARE_PARTIAL_FLAG, arrLength = array2.length, othLength = other.length;
      if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
        return false;
      }
      var arrStacked = stack.get(array2);
      var othStacked = stack.get(other);
      if (arrStacked && othStacked) {
        return arrStacked == other && othStacked == array2;
      }
      var index2 = -1, result = true, seen = bitmask & COMPARE_UNORDERED_FLAG ? new SetCache() : void 0;
      stack.set(array2, other);
      stack.set(other, array2);
      while (++index2 < arrLength) {
        var arrValue = array2[index2], othValue = other[index2];
        if (customizer) {
          var compared = isPartial ? customizer(othValue, arrValue, index2, other, array2, stack) : customizer(arrValue, othValue, index2, array2, other, stack);
        }
        if (compared !== void 0) {
          if (compared) {
            continue;
          }
          result = false;
          break;
        }
        if (seen) {
          if (!arraySome(other, function(othValue2, othIndex) {
            if (!cacheHas(seen, othIndex) && (arrValue === othValue2 || equalFunc(arrValue, othValue2, bitmask, customizer, stack))) {
              return seen.push(othIndex);
            }
          })) {
            result = false;
            break;
          }
        } else if (!(arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
          result = false;
          break;
        }
      }
      stack["delete"](array2);
      stack["delete"](other);
      return result;
    }
    module.exports = equalArrays;
  }
});

// node_modules/lodash/_mapToArray.js
var require_mapToArray = __commonJS({
  "node_modules/lodash/_mapToArray.js"(exports, module) {
    function mapToArray(map4) {
      var index2 = -1, result = Array(map4.size);
      map4.forEach(function(value, key) {
        result[++index2] = [key, value];
      });
      return result;
    }
    module.exports = mapToArray;
  }
});

// node_modules/lodash/_setToArray.js
var require_setToArray = __commonJS({
  "node_modules/lodash/_setToArray.js"(exports, module) {
    function setToArray(set) {
      var index2 = -1, result = Array(set.size);
      set.forEach(function(value) {
        result[++index2] = value;
      });
      return result;
    }
    module.exports = setToArray;
  }
});

// node_modules/lodash/_equalByTag.js
var require_equalByTag = __commonJS({
  "node_modules/lodash/_equalByTag.js"(exports, module) {
    var Symbol3 = require_Symbol();
    var Uint8Array = require_Uint8Array();
    var eq = require_eq();
    var equalArrays = require_equalArrays();
    var mapToArray = require_mapToArray();
    var setToArray = require_setToArray();
    var COMPARE_PARTIAL_FLAG = 1;
    var COMPARE_UNORDERED_FLAG = 2;
    var boolTag = "[object Boolean]";
    var dateTag = "[object Date]";
    var errorTag = "[object Error]";
    var mapTag = "[object Map]";
    var numberTag = "[object Number]";
    var regexpTag = "[object RegExp]";
    var setTag = "[object Set]";
    var stringTag = "[object String]";
    var symbolTag = "[object Symbol]";
    var arrayBufferTag = "[object ArrayBuffer]";
    var dataViewTag = "[object DataView]";
    var symbolProto = Symbol3 ? Symbol3.prototype : void 0;
    var symbolValueOf = symbolProto ? symbolProto.valueOf : void 0;
    function equalByTag(object, other, tag, bitmask, customizer, equalFunc, stack) {
      switch (tag) {
        case dataViewTag:
          if (object.byteLength != other.byteLength || object.byteOffset != other.byteOffset) {
            return false;
          }
          object = object.buffer;
          other = other.buffer;
        case arrayBufferTag:
          if (object.byteLength != other.byteLength || !equalFunc(new Uint8Array(object), new Uint8Array(other))) {
            return false;
          }
          return true;
        case boolTag:
        case dateTag:
        case numberTag:
          return eq(+object, +other);
        case errorTag:
          return object.name == other.name && object.message == other.message;
        case regexpTag:
        case stringTag:
          return object == other + "";
        case mapTag:
          var convert = mapToArray;
        case setTag:
          var isPartial = bitmask & COMPARE_PARTIAL_FLAG;
          convert || (convert = setToArray);
          if (object.size != other.size && !isPartial) {
            return false;
          }
          var stacked = stack.get(object);
          if (stacked) {
            return stacked == other;
          }
          bitmask |= COMPARE_UNORDERED_FLAG;
          stack.set(object, other);
          var result = equalArrays(convert(object), convert(other), bitmask, customizer, equalFunc, stack);
          stack["delete"](object);
          return result;
        case symbolTag:
          if (symbolValueOf) {
            return symbolValueOf.call(object) == symbolValueOf.call(other);
          }
      }
      return false;
    }
    module.exports = equalByTag;
  }
});

// node_modules/lodash/_baseGetAllKeys.js
var require_baseGetAllKeys = __commonJS({
  "node_modules/lodash/_baseGetAllKeys.js"(exports, module) {
    var arrayPush = require_arrayPush();
    var isArray = require_isArray();
    function baseGetAllKeys(object, keysFunc, symbolsFunc) {
      var result = keysFunc(object);
      return isArray(object) ? result : arrayPush(result, symbolsFunc(object));
    }
    module.exports = baseGetAllKeys;
  }
});

// node_modules/lodash/_arrayFilter.js
var require_arrayFilter = __commonJS({
  "node_modules/lodash/_arrayFilter.js"(exports, module) {
    function arrayFilter(array2, predicate) {
      var index2 = -1, length = array2 == null ? 0 : array2.length, resIndex = 0, result = [];
      while (++index2 < length) {
        var value = array2[index2];
        if (predicate(value, index2, array2)) {
          result[resIndex++] = value;
        }
      }
      return result;
    }
    module.exports = arrayFilter;
  }
});

// node_modules/lodash/stubArray.js
var require_stubArray = __commonJS({
  "node_modules/lodash/stubArray.js"(exports, module) {
    function stubArray() {
      return [];
    }
    module.exports = stubArray;
  }
});

// node_modules/lodash/_getSymbols.js
var require_getSymbols = __commonJS({
  "node_modules/lodash/_getSymbols.js"(exports, module) {
    var arrayFilter = require_arrayFilter();
    var stubArray = require_stubArray();
    var objectProto = Object.prototype;
    var propertyIsEnumerable = objectProto.propertyIsEnumerable;
    var nativeGetSymbols = Object.getOwnPropertySymbols;
    var getSymbols = !nativeGetSymbols ? stubArray : function(object) {
      if (object == null) {
        return [];
      }
      object = Object(object);
      return arrayFilter(nativeGetSymbols(object), function(symbol) {
        return propertyIsEnumerable.call(object, symbol);
      });
    };
    module.exports = getSymbols;
  }
});

// node_modules/lodash/_nativeKeys.js
var require_nativeKeys = __commonJS({
  "node_modules/lodash/_nativeKeys.js"(exports, module) {
    var overArg = require_overArg();
    var nativeKeys = overArg(Object.keys, Object);
    module.exports = nativeKeys;
  }
});

// node_modules/lodash/_baseKeys.js
var require_baseKeys = __commonJS({
  "node_modules/lodash/_baseKeys.js"(exports, module) {
    var isPrototype = require_isPrototype();
    var nativeKeys = require_nativeKeys();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function baseKeys(object) {
      if (!isPrototype(object)) {
        return nativeKeys(object);
      }
      var result = [];
      for (var key in Object(object)) {
        if (hasOwnProperty.call(object, key) && key != "constructor") {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = baseKeys;
  }
});

// node_modules/lodash/keys.js
var require_keys = __commonJS({
  "node_modules/lodash/keys.js"(exports, module) {
    var arrayLikeKeys = require_arrayLikeKeys();
    var baseKeys = require_baseKeys();
    var isArrayLike = require_isArrayLike();
    function keys(object) {
      return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
    }
    module.exports = keys;
  }
});

// node_modules/lodash/_getAllKeys.js
var require_getAllKeys = __commonJS({
  "node_modules/lodash/_getAllKeys.js"(exports, module) {
    var baseGetAllKeys = require_baseGetAllKeys();
    var getSymbols = require_getSymbols();
    var keys = require_keys();
    function getAllKeys(object) {
      return baseGetAllKeys(object, keys, getSymbols);
    }
    module.exports = getAllKeys;
  }
});

// node_modules/lodash/_equalObjects.js
var require_equalObjects = __commonJS({
  "node_modules/lodash/_equalObjects.js"(exports, module) {
    var getAllKeys = require_getAllKeys();
    var COMPARE_PARTIAL_FLAG = 1;
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function equalObjects(object, other, bitmask, customizer, equalFunc, stack) {
      var isPartial = bitmask & COMPARE_PARTIAL_FLAG, objProps = getAllKeys(object), objLength = objProps.length, othProps = getAllKeys(other), othLength = othProps.length;
      if (objLength != othLength && !isPartial) {
        return false;
      }
      var index2 = objLength;
      while (index2--) {
        var key = objProps[index2];
        if (!(isPartial ? key in other : hasOwnProperty.call(other, key))) {
          return false;
        }
      }
      var objStacked = stack.get(object);
      var othStacked = stack.get(other);
      if (objStacked && othStacked) {
        return objStacked == other && othStacked == object;
      }
      var result = true;
      stack.set(object, other);
      stack.set(other, object);
      var skipCtor = isPartial;
      while (++index2 < objLength) {
        key = objProps[index2];
        var objValue = object[key], othValue = other[key];
        if (customizer) {
          var compared = isPartial ? customizer(othValue, objValue, key, other, object, stack) : customizer(objValue, othValue, key, object, other, stack);
        }
        if (!(compared === void 0 ? objValue === othValue || equalFunc(objValue, othValue, bitmask, customizer, stack) : compared)) {
          result = false;
          break;
        }
        skipCtor || (skipCtor = key == "constructor");
      }
      if (result && !skipCtor) {
        var objCtor = object.constructor, othCtor = other.constructor;
        if (objCtor != othCtor && ("constructor" in object && "constructor" in other) && !(typeof objCtor == "function" && objCtor instanceof objCtor && typeof othCtor == "function" && othCtor instanceof othCtor)) {
          result = false;
        }
      }
      stack["delete"](object);
      stack["delete"](other);
      return result;
    }
    module.exports = equalObjects;
  }
});

// node_modules/lodash/_DataView.js
var require_DataView = __commonJS({
  "node_modules/lodash/_DataView.js"(exports, module) {
    var getNative = require_getNative();
    var root = require_root();
    var DataView2 = getNative(root, "DataView");
    module.exports = DataView2;
  }
});

// node_modules/lodash/_Promise.js
var require_Promise = __commonJS({
  "node_modules/lodash/_Promise.js"(exports, module) {
    var getNative = require_getNative();
    var root = require_root();
    var Promise2 = getNative(root, "Promise");
    module.exports = Promise2;
  }
});

// node_modules/lodash/_Set.js
var require_Set = __commonJS({
  "node_modules/lodash/_Set.js"(exports, module) {
    var getNative = require_getNative();
    var root = require_root();
    var Set2 = getNative(root, "Set");
    module.exports = Set2;
  }
});

// node_modules/lodash/_WeakMap.js
var require_WeakMap = __commonJS({
  "node_modules/lodash/_WeakMap.js"(exports, module) {
    var getNative = require_getNative();
    var root = require_root();
    var WeakMap2 = getNative(root, "WeakMap");
    module.exports = WeakMap2;
  }
});

// node_modules/lodash/_getTag.js
var require_getTag = __commonJS({
  "node_modules/lodash/_getTag.js"(exports, module) {
    var DataView2 = require_DataView();
    var Map2 = require_Map();
    var Promise2 = require_Promise();
    var Set2 = require_Set();
    var WeakMap2 = require_WeakMap();
    var baseGetTag = require_baseGetTag();
    var toSource = require_toSource();
    var mapTag = "[object Map]";
    var objectTag = "[object Object]";
    var promiseTag = "[object Promise]";
    var setTag = "[object Set]";
    var weakMapTag = "[object WeakMap]";
    var dataViewTag = "[object DataView]";
    var dataViewCtorString = toSource(DataView2);
    var mapCtorString = toSource(Map2);
    var promiseCtorString = toSource(Promise2);
    var setCtorString = toSource(Set2);
    var weakMapCtorString = toSource(WeakMap2);
    var getTag = baseGetTag;
    if (DataView2 && getTag(new DataView2(new ArrayBuffer(1))) != dataViewTag || Map2 && getTag(new Map2()) != mapTag || Promise2 && getTag(Promise2.resolve()) != promiseTag || Set2 && getTag(new Set2()) != setTag || WeakMap2 && getTag(new WeakMap2()) != weakMapTag) {
      getTag = function(value) {
        var result = baseGetTag(value), Ctor = result == objectTag ? value.constructor : void 0, ctorString = Ctor ? toSource(Ctor) : "";
        if (ctorString) {
          switch (ctorString) {
            case dataViewCtorString:
              return dataViewTag;
            case mapCtorString:
              return mapTag;
            case promiseCtorString:
              return promiseTag;
            case setCtorString:
              return setTag;
            case weakMapCtorString:
              return weakMapTag;
          }
        }
        return result;
      };
    }
    module.exports = getTag;
  }
});

// node_modules/lodash/_baseIsEqualDeep.js
var require_baseIsEqualDeep = __commonJS({
  "node_modules/lodash/_baseIsEqualDeep.js"(exports, module) {
    var Stack = require_Stack();
    var equalArrays = require_equalArrays();
    var equalByTag = require_equalByTag();
    var equalObjects = require_equalObjects();
    var getTag = require_getTag();
    var isArray = require_isArray();
    var isBuffer = require_isBuffer();
    var isTypedArray = require_isTypedArray();
    var COMPARE_PARTIAL_FLAG = 1;
    var argsTag = "[object Arguments]";
    var arrayTag = "[object Array]";
    var objectTag = "[object Object]";
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function baseIsEqualDeep(object, other, bitmask, customizer, equalFunc, stack) {
      var objIsArr = isArray(object), othIsArr = isArray(other), objTag = objIsArr ? arrayTag : getTag(object), othTag = othIsArr ? arrayTag : getTag(other);
      objTag = objTag == argsTag ? objectTag : objTag;
      othTag = othTag == argsTag ? objectTag : othTag;
      var objIsObj = objTag == objectTag, othIsObj = othTag == objectTag, isSameTag = objTag == othTag;
      if (isSameTag && isBuffer(object)) {
        if (!isBuffer(other)) {
          return false;
        }
        objIsArr = true;
        objIsObj = false;
      }
      if (isSameTag && !objIsObj) {
        stack || (stack = new Stack());
        return objIsArr || isTypedArray(object) ? equalArrays(object, other, bitmask, customizer, equalFunc, stack) : equalByTag(object, other, objTag, bitmask, customizer, equalFunc, stack);
      }
      if (!(bitmask & COMPARE_PARTIAL_FLAG)) {
        var objIsWrapped = objIsObj && hasOwnProperty.call(object, "__wrapped__"), othIsWrapped = othIsObj && hasOwnProperty.call(other, "__wrapped__");
        if (objIsWrapped || othIsWrapped) {
          var objUnwrapped = objIsWrapped ? object.value() : object, othUnwrapped = othIsWrapped ? other.value() : other;
          stack || (stack = new Stack());
          return equalFunc(objUnwrapped, othUnwrapped, bitmask, customizer, stack);
        }
      }
      if (!isSameTag) {
        return false;
      }
      stack || (stack = new Stack());
      return equalObjects(object, other, bitmask, customizer, equalFunc, stack);
    }
    module.exports = baseIsEqualDeep;
  }
});

// node_modules/lodash/_baseIsEqual.js
var require_baseIsEqual = __commonJS({
  "node_modules/lodash/_baseIsEqual.js"(exports, module) {
    var baseIsEqualDeep = require_baseIsEqualDeep();
    var isObjectLike = require_isObjectLike();
    function baseIsEqual(value, other, bitmask, customizer, stack) {
      if (value === other) {
        return true;
      }
      if (value == null || other == null || !isObjectLike(value) && !isObjectLike(other)) {
        return value !== value && other !== other;
      }
      return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
    }
    module.exports = baseIsEqual;
  }
});

// node_modules/lodash/isEqual.js
var require_isEqual = __commonJS({
  "node_modules/lodash/isEqual.js"(exports, module) {
    var baseIsEqual = require_baseIsEqual();
    function isEqual2(value, other) {
      return baseIsEqual(value, other);
    }
    module.exports = isEqual2;
  }
});

// node_modules/lodash/noop.js
var require_noop = __commonJS({
  "node_modules/lodash/noop.js"(exports, module) {
    function noop2() {
    }
    module.exports = noop2;
  }
});

// node_modules/lodash/_createSet.js
var require_createSet = __commonJS({
  "node_modules/lodash/_createSet.js"(exports, module) {
    var Set2 = require_Set();
    var noop2 = require_noop();
    var setToArray = require_setToArray();
    var INFINITY = 1 / 0;
    var createSet = !(Set2 && 1 / setToArray(new Set2([, -0]))[1] == INFINITY) ? noop2 : function(values) {
      return new Set2(values);
    };
    module.exports = createSet;
  }
});

// node_modules/lodash/_baseUniq.js
var require_baseUniq = __commonJS({
  "node_modules/lodash/_baseUniq.js"(exports, module) {
    var SetCache = require_SetCache();
    var arrayIncludes = require_arrayIncludes();
    var arrayIncludesWith = require_arrayIncludesWith();
    var cacheHas = require_cacheHas();
    var createSet = require_createSet();
    var setToArray = require_setToArray();
    var LARGE_ARRAY_SIZE = 200;
    function baseUniq(array2, iteratee, comparator) {
      var index2 = -1, includes = arrayIncludes, length = array2.length, isCommon = true, result = [], seen = result;
      if (comparator) {
        isCommon = false;
        includes = arrayIncludesWith;
      } else if (length >= LARGE_ARRAY_SIZE) {
        var set = iteratee ? null : createSet(array2);
        if (set) {
          return setToArray(set);
        }
        isCommon = false;
        includes = cacheHas;
        seen = new SetCache();
      } else {
        seen = iteratee ? [] : result;
      }
      outer:
        while (++index2 < length) {
          var value = array2[index2], computed = iteratee ? iteratee(value) : value;
          value = comparator || value !== 0 ? value : 0;
          if (isCommon && computed === computed) {
            var seenIndex = seen.length;
            while (seenIndex--) {
              if (seen[seenIndex] === computed) {
                continue outer;
              }
            }
            if (iteratee) {
              seen.push(computed);
            }
            result.push(value);
          } else if (!includes(seen, computed, comparator)) {
            if (seen !== result) {
              seen.push(computed);
            }
            result.push(value);
          }
        }
      return result;
    }
    module.exports = baseUniq;
  }
});

// node_modules/lodash/uniq.js
var require_uniq = __commonJS({
  "node_modules/lodash/uniq.js"(exports, module) {
    var baseUniq = require_baseUniq();
    function uniq(array2) {
      return array2 && array2.length ? baseUniq(array2) : [];
    }
    module.exports = uniq;
  }
});

// node_modules/lodash/_baseIsMatch.js
var require_baseIsMatch = __commonJS({
  "node_modules/lodash/_baseIsMatch.js"(exports, module) {
    var Stack = require_Stack();
    var baseIsEqual = require_baseIsEqual();
    var COMPARE_PARTIAL_FLAG = 1;
    var COMPARE_UNORDERED_FLAG = 2;
    function baseIsMatch(object, source, matchData, customizer) {
      var index2 = matchData.length, length = index2, noCustomizer = !customizer;
      if (object == null) {
        return !length;
      }
      object = Object(object);
      while (index2--) {
        var data = matchData[index2];
        if (noCustomizer && data[2] ? data[1] !== object[data[0]] : !(data[0] in object)) {
          return false;
        }
      }
      while (++index2 < length) {
        data = matchData[index2];
        var key = data[0], objValue = object[key], srcValue = data[1];
        if (noCustomizer && data[2]) {
          if (objValue === void 0 && !(key in object)) {
            return false;
          }
        } else {
          var stack = new Stack();
          if (customizer) {
            var result = customizer(objValue, srcValue, key, object, source, stack);
          }
          if (!(result === void 0 ? baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG, customizer, stack) : result)) {
            return false;
          }
        }
      }
      return true;
    }
    module.exports = baseIsMatch;
  }
});

// node_modules/lodash/_isStrictComparable.js
var require_isStrictComparable = __commonJS({
  "node_modules/lodash/_isStrictComparable.js"(exports, module) {
    var isObject = require_isObject();
    function isStrictComparable(value) {
      return value === value && !isObject(value);
    }
    module.exports = isStrictComparable;
  }
});

// node_modules/lodash/_getMatchData.js
var require_getMatchData = __commonJS({
  "node_modules/lodash/_getMatchData.js"(exports, module) {
    var isStrictComparable = require_isStrictComparable();
    var keys = require_keys();
    function getMatchData(object) {
      var result = keys(object), length = result.length;
      while (length--) {
        var key = result[length], value = object[key];
        result[length] = [key, value, isStrictComparable(value)];
      }
      return result;
    }
    module.exports = getMatchData;
  }
});

// node_modules/lodash/_matchesStrictComparable.js
var require_matchesStrictComparable = __commonJS({
  "node_modules/lodash/_matchesStrictComparable.js"(exports, module) {
    function matchesStrictComparable(key, srcValue) {
      return function(object) {
        if (object == null) {
          return false;
        }
        return object[key] === srcValue && (srcValue !== void 0 || key in Object(object));
      };
    }
    module.exports = matchesStrictComparable;
  }
});

// node_modules/lodash/_baseMatches.js
var require_baseMatches = __commonJS({
  "node_modules/lodash/_baseMatches.js"(exports, module) {
    var baseIsMatch = require_baseIsMatch();
    var getMatchData = require_getMatchData();
    var matchesStrictComparable = require_matchesStrictComparable();
    function baseMatches(source) {
      var matchData = getMatchData(source);
      if (matchData.length == 1 && matchData[0][2]) {
        return matchesStrictComparable(matchData[0][0], matchData[0][1]);
      }
      return function(object) {
        return object === source || baseIsMatch(object, source, matchData);
      };
    }
    module.exports = baseMatches;
  }
});

// node_modules/lodash/_baseMatchesProperty.js
var require_baseMatchesProperty = __commonJS({
  "node_modules/lodash/_baseMatchesProperty.js"(exports, module) {
    var baseIsEqual = require_baseIsEqual();
    var get = require_get();
    var hasIn = require_hasIn();
    var isKey = require_isKey();
    var isStrictComparable = require_isStrictComparable();
    var matchesStrictComparable = require_matchesStrictComparable();
    var toKey = require_toKey();
    var COMPARE_PARTIAL_FLAG = 1;
    var COMPARE_UNORDERED_FLAG = 2;
    function baseMatchesProperty(path2, srcValue) {
      if (isKey(path2) && isStrictComparable(srcValue)) {
        return matchesStrictComparable(toKey(path2), srcValue);
      }
      return function(object) {
        var objValue = get(object, path2);
        return objValue === void 0 && objValue === srcValue ? hasIn(object, path2) : baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG);
      };
    }
    module.exports = baseMatchesProperty;
  }
});

// node_modules/lodash/_baseProperty.js
var require_baseProperty = __commonJS({
  "node_modules/lodash/_baseProperty.js"(exports, module) {
    function baseProperty(key) {
      return function(object) {
        return object == null ? void 0 : object[key];
      };
    }
    module.exports = baseProperty;
  }
});

// node_modules/lodash/_basePropertyDeep.js
var require_basePropertyDeep = __commonJS({
  "node_modules/lodash/_basePropertyDeep.js"(exports, module) {
    var baseGet = require_baseGet();
    function basePropertyDeep(path2) {
      return function(object) {
        return baseGet(object, path2);
      };
    }
    module.exports = basePropertyDeep;
  }
});

// node_modules/lodash/property.js
var require_property = __commonJS({
  "node_modules/lodash/property.js"(exports, module) {
    var baseProperty = require_baseProperty();
    var basePropertyDeep = require_basePropertyDeep();
    var isKey = require_isKey();
    var toKey = require_toKey();
    function property(path2) {
      return isKey(path2) ? baseProperty(toKey(path2)) : basePropertyDeep(path2);
    }
    module.exports = property;
  }
});

// node_modules/lodash/_baseIteratee.js
var require_baseIteratee = __commonJS({
  "node_modules/lodash/_baseIteratee.js"(exports, module) {
    var baseMatches = require_baseMatches();
    var baseMatchesProperty = require_baseMatchesProperty();
    var identity5 = require_identity();
    var isArray = require_isArray();
    var property = require_property();
    function baseIteratee(value) {
      if (typeof value == "function") {
        return value;
      }
      if (value == null) {
        return identity5;
      }
      if (typeof value == "object") {
        return isArray(value) ? baseMatchesProperty(value[0], value[1]) : baseMatches(value);
      }
      return property(value);
    }
    module.exports = baseIteratee;
  }
});

// node_modules/lodash/uniqBy.js
var require_uniqBy = __commonJS({
  "node_modules/lodash/uniqBy.js"(exports, module) {
    var baseIteratee = require_baseIteratee();
    var baseUniq = require_baseUniq();
    function uniqBy(array2, iteratee) {
      return array2 && array2.length ? baseUniq(array2, baseIteratee(iteratee, 2)) : [];
    }
    module.exports = uniqBy;
  }
});

// node_modules/lodash/_baseForOwn.js
var require_baseForOwn = __commonJS({
  "node_modules/lodash/_baseForOwn.js"(exports, module) {
    var baseFor = require_baseFor();
    var keys = require_keys();
    function baseForOwn(object, iteratee) {
      return object && baseFor(object, iteratee, keys);
    }
    module.exports = baseForOwn;
  }
});

// node_modules/lodash/_createBaseEach.js
var require_createBaseEach = __commonJS({
  "node_modules/lodash/_createBaseEach.js"(exports, module) {
    var isArrayLike = require_isArrayLike();
    function createBaseEach(eachFunc, fromRight) {
      return function(collection, iteratee) {
        if (collection == null) {
          return collection;
        }
        if (!isArrayLike(collection)) {
          return eachFunc(collection, iteratee);
        }
        var length = collection.length, index2 = fromRight ? length : -1, iterable = Object(collection);
        while (fromRight ? index2-- : ++index2 < length) {
          if (iteratee(iterable[index2], index2, iterable) === false) {
            break;
          }
        }
        return collection;
      };
    }
    module.exports = createBaseEach;
  }
});

// node_modules/lodash/_baseEach.js
var require_baseEach = __commonJS({
  "node_modules/lodash/_baseEach.js"(exports, module) {
    var baseForOwn = require_baseForOwn();
    var createBaseEach = require_createBaseEach();
    var baseEach = createBaseEach(baseForOwn);
    module.exports = baseEach;
  }
});

// node_modules/lodash/_baseMap.js
var require_baseMap = __commonJS({
  "node_modules/lodash/_baseMap.js"(exports, module) {
    var baseEach = require_baseEach();
    var isArrayLike = require_isArrayLike();
    function baseMap(collection, iteratee) {
      var index2 = -1, result = isArrayLike(collection) ? Array(collection.length) : [];
      baseEach(collection, function(value, key, collection2) {
        result[++index2] = iteratee(value, key, collection2);
      });
      return result;
    }
    module.exports = baseMap;
  }
});

// node_modules/lodash/_baseSortBy.js
var require_baseSortBy = __commonJS({
  "node_modules/lodash/_baseSortBy.js"(exports, module) {
    function baseSortBy(array2, comparer) {
      var length = array2.length;
      array2.sort(comparer);
      while (length--) {
        array2[length] = array2[length].value;
      }
      return array2;
    }
    module.exports = baseSortBy;
  }
});

// node_modules/lodash/_compareAscending.js
var require_compareAscending = __commonJS({
  "node_modules/lodash/_compareAscending.js"(exports, module) {
    var isSymbol = require_isSymbol();
    function compareAscending(value, other) {
      if (value !== other) {
        var valIsDefined = value !== void 0, valIsNull = value === null, valIsReflexive = value === value, valIsSymbol = isSymbol(value);
        var othIsDefined = other !== void 0, othIsNull = other === null, othIsReflexive = other === other, othIsSymbol = isSymbol(other);
        if (!othIsNull && !othIsSymbol && !valIsSymbol && value > other || valIsSymbol && othIsDefined && othIsReflexive && !othIsNull && !othIsSymbol || valIsNull && othIsDefined && othIsReflexive || !valIsDefined && othIsReflexive || !valIsReflexive) {
          return 1;
        }
        if (!valIsNull && !valIsSymbol && !othIsSymbol && value < other || othIsSymbol && valIsDefined && valIsReflexive && !valIsNull && !valIsSymbol || othIsNull && valIsDefined && valIsReflexive || !othIsDefined && valIsReflexive || !othIsReflexive) {
          return -1;
        }
      }
      return 0;
    }
    module.exports = compareAscending;
  }
});

// node_modules/lodash/_compareMultiple.js
var require_compareMultiple = __commonJS({
  "node_modules/lodash/_compareMultiple.js"(exports, module) {
    var compareAscending = require_compareAscending();
    function compareMultiple(object, other, orders) {
      var index2 = -1, objCriteria = object.criteria, othCriteria = other.criteria, length = objCriteria.length, ordersLength = orders.length;
      while (++index2 < length) {
        var result = compareAscending(objCriteria[index2], othCriteria[index2]);
        if (result) {
          if (index2 >= ordersLength) {
            return result;
          }
          var order = orders[index2];
          return result * (order == "desc" ? -1 : 1);
        }
      }
      return object.index - other.index;
    }
    module.exports = compareMultiple;
  }
});

// node_modules/lodash/_baseOrderBy.js
var require_baseOrderBy = __commonJS({
  "node_modules/lodash/_baseOrderBy.js"(exports, module) {
    var arrayMap = require_arrayMap();
    var baseGet = require_baseGet();
    var baseIteratee = require_baseIteratee();
    var baseMap = require_baseMap();
    var baseSortBy = require_baseSortBy();
    var baseUnary = require_baseUnary();
    var compareMultiple = require_compareMultiple();
    var identity5 = require_identity();
    var isArray = require_isArray();
    function baseOrderBy(collection, iteratees, orders) {
      if (iteratees.length) {
        iteratees = arrayMap(iteratees, function(iteratee) {
          if (isArray(iteratee)) {
            return function(value) {
              return baseGet(value, iteratee.length === 1 ? iteratee[0] : iteratee);
            };
          }
          return iteratee;
        });
      } else {
        iteratees = [identity5];
      }
      var index2 = -1;
      iteratees = arrayMap(iteratees, baseUnary(baseIteratee));
      var result = baseMap(collection, function(value, key, collection2) {
        var criteria = arrayMap(iteratees, function(iteratee) {
          return iteratee(value);
        });
        return { "criteria": criteria, "index": ++index2, "value": value };
      });
      return baseSortBy(result, function(object, other) {
        return compareMultiple(object, other, orders);
      });
    }
    module.exports = baseOrderBy;
  }
});

// node_modules/lodash/sortBy.js
var require_sortBy = __commonJS({
  "node_modules/lodash/sortBy.js"(exports, module) {
    var baseFlatten = require_baseFlatten();
    var baseOrderBy = require_baseOrderBy();
    var baseRest = require_baseRest();
    var isIterateeCall = require_isIterateeCall();
    var sortBy = baseRest(function(collection, iteratees) {
      if (collection == null) {
        return [];
      }
      var length = iteratees.length;
      if (length > 1 && isIterateeCall(collection, iteratees[0], iteratees[1])) {
        iteratees = [];
      } else if (length > 2 && isIterateeCall(iteratees[0], iteratees[1], iteratees[2])) {
        iteratees = [iteratees[0]];
      }
      return baseOrderBy(collection, baseFlatten(iteratees, 1), []);
    });
    module.exports = sortBy;
  }
});

// node_modules/lodash/_baseIsDate.js
var require_baseIsDate = __commonJS({
  "node_modules/lodash/_baseIsDate.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isObjectLike = require_isObjectLike();
    var dateTag = "[object Date]";
    function baseIsDate(value) {
      return isObjectLike(value) && baseGetTag(value) == dateTag;
    }
    module.exports = baseIsDate;
  }
});

// node_modules/lodash/isDate.js
var require_isDate = __commonJS({
  "node_modules/lodash/isDate.js"(exports, module) {
    var baseIsDate = require_baseIsDate();
    var baseUnary = require_baseUnary();
    var nodeUtil = require_nodeUtil();
    var nodeIsDate = nodeUtil && nodeUtil.isDate;
    var isDate = nodeIsDate ? baseUnary(nodeIsDate) : baseIsDate;
    module.exports = isDate;
  }
});

// node_modules/@react-spring/rafz/dist/react-spring_rafz.modern.mjs
var updateQueue = makeQueue();
var raf = (fn3) => schedule(fn3, updateQueue);
var writeQueue = makeQueue();
raf.write = (fn3) => schedule(fn3, writeQueue);
var onStartQueue = makeQueue();
raf.onStart = (fn3) => schedule(fn3, onStartQueue);
var onFrameQueue = makeQueue();
raf.onFrame = (fn3) => schedule(fn3, onFrameQueue);
var onFinishQueue = makeQueue();
raf.onFinish = (fn3) => schedule(fn3, onFinishQueue);
var timeouts = [];
raf.setTimeout = (handler, ms) => {
  const time2 = raf.now() + ms;
  const cancel = () => {
    const i8 = timeouts.findIndex((t9) => t9.cancel == cancel);
    if (~i8) timeouts.splice(i8, 1);
    pendingCount -= ~i8 ? 1 : 0;
  };
  const timeout = { time: time2, handler, cancel };
  timeouts.splice(findTimeout(time2), 0, timeout);
  pendingCount += 1;
  start();
  return timeout;
};
var findTimeout = (time2) => ~(~timeouts.findIndex((t9) => t9.time > time2) || ~timeouts.length);
raf.cancel = (fn3) => {
  onStartQueue.delete(fn3);
  onFrameQueue.delete(fn3);
  onFinishQueue.delete(fn3);
  updateQueue.delete(fn3);
  writeQueue.delete(fn3);
};
raf.sync = (fn3) => {
  sync = true;
  raf.batchedUpdates(fn3);
  sync = false;
};
raf.throttle = (fn3) => {
  let lastArgs;
  function queuedFn() {
    try {
      fn3(...lastArgs);
    } finally {
      lastArgs = null;
    }
  }
  function throttled(...args) {
    lastArgs = args;
    raf.onStart(queuedFn);
  }
  throttled.handler = fn3;
  throttled.cancel = () => {
    onStartQueue.delete(queuedFn);
    lastArgs = null;
  };
  return throttled;
};
var nativeRaf = typeof window != "undefined" ? window.requestAnimationFrame : (
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  (() => {
  })
);
raf.use = (impl) => nativeRaf = impl;
raf.now = typeof performance != "undefined" ? () => performance.now() : Date.now;
raf.batchedUpdates = (fn3) => fn3();
raf.catch = console.error;
raf.frameLoop = "always";
raf.advance = () => {
  if (raf.frameLoop !== "demand") {
    console.warn(
      "Cannot call the manual advancement of rafz whilst frameLoop is not set as demand"
    );
  } else {
    update();
  }
};
var ts = -1;
var pendingCount = 0;
var sync = false;
function schedule(fn3, queue) {
  if (sync) {
    queue.delete(fn3);
    fn3(0);
  } else {
    queue.add(fn3);
    start();
  }
}
function start() {
  if (ts < 0) {
    ts = 0;
    if (raf.frameLoop !== "demand") {
      nativeRaf(loop);
    }
  }
}
function stop() {
  ts = -1;
}
function loop() {
  if (~ts) {
    nativeRaf(loop);
    raf.batchedUpdates(update);
  }
}
function update() {
  const prevTs = ts;
  ts = raf.now();
  const count2 = findTimeout(ts);
  if (count2) {
    eachSafely(timeouts.splice(0, count2), (t9) => t9.handler());
    pendingCount -= count2;
  }
  if (!pendingCount) {
    stop();
    return;
  }
  onStartQueue.flush();
  updateQueue.flush(prevTs ? Math.min(64, ts - prevTs) : 16.667);
  onFrameQueue.flush();
  writeQueue.flush();
  onFinishQueue.flush();
}
function makeQueue() {
  let next = /* @__PURE__ */ new Set();
  let current = next;
  return {
    add(fn3) {
      pendingCount += current == next && !next.has(fn3) ? 1 : 0;
      next.add(fn3);
    },
    delete(fn3) {
      pendingCount -= current == next && next.has(fn3) ? 1 : 0;
      return next.delete(fn3);
    },
    flush(arg) {
      if (current.size) {
        next = /* @__PURE__ */ new Set();
        pendingCount -= current.size;
        eachSafely(current, (fn3) => fn3(arg) && next.add(fn3));
        pendingCount += next.size;
        current = next;
      }
    }
  };
}
function eachSafely(values, each2) {
  values.forEach((value) => {
    try {
      each2(value);
    } catch (e11) {
      raf.catch(e11);
    }
  });
}

// node_modules/@react-spring/shared/dist/react-spring_shared.modern.mjs
var import_react = __toESM(require_react(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_react3 = __toESM(require_react(), 1);
var import_react4 = __toESM(require_react(), 1);
var import_react5 = __toESM(require_react(), 1);
var import_react6 = __toESM(require_react(), 1);
var import_react7 = __toESM(require_react(), 1);
var import_react8 = __toESM(require_react(), 1);
var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var globals_exports = {};
__export(globals_exports, {
  assign: () => assign,
  colors: () => colors,
  createStringInterpolator: () => createStringInterpolator,
  skipAnimation: () => skipAnimation,
  to: () => to,
  willAdvance: () => willAdvance
});
function noop() {
}
var defineHidden = (obj, key, value) => Object.defineProperty(obj, key, { value, writable: true, configurable: true });
var is = {
  arr: Array.isArray,
  obj: (a5) => !!a5 && a5.constructor.name === "Object",
  fun: (a5) => typeof a5 === "function",
  str: (a5) => typeof a5 === "string",
  num: (a5) => typeof a5 === "number",
  und: (a5) => a5 === void 0
};
function isEqual(a5, b6) {
  if (is.arr(a5)) {
    if (!is.arr(b6) || a5.length !== b6.length) return false;
    for (let i8 = 0; i8 < a5.length; i8++) {
      if (a5[i8] !== b6[i8]) return false;
    }
    return true;
  }
  return a5 === b6;
}
var each = (obj, fn3) => obj.forEach(fn3);
function eachProp(obj, fn3, ctx) {
  if (is.arr(obj)) {
    for (let i8 = 0; i8 < obj.length; i8++) {
      fn3.call(ctx, obj[i8], `${i8}`);
    }
    return;
  }
  for (const key in obj) {
    if (obj.hasOwnProperty(key)) {
      fn3.call(ctx, obj[key], key);
    }
  }
}
var toArray = (a5) => is.und(a5) ? [] : is.arr(a5) ? a5 : [a5];
function flush(queue, iterator) {
  if (queue.size) {
    const items = Array.from(queue);
    queue.clear();
    each(items, iterator);
  }
}
var flushCalls = (queue, ...args) => flush(queue, (fn3) => fn3(...args));
var isSSR = () => typeof window === "undefined" || !window.navigator || /ServerSideRendering|^Deno\//.test(window.navigator.userAgent);
var createStringInterpolator;
var to;
var colors = null;
var skipAnimation = false;
var willAdvance = noop;
var assign = (globals) => {
  if (globals.to) to = globals.to;
  if (globals.now) raf.now = globals.now;
  if (globals.colors !== void 0) colors = globals.colors;
  if (globals.skipAnimation != null) skipAnimation = globals.skipAnimation;
  if (globals.createStringInterpolator)
    createStringInterpolator = globals.createStringInterpolator;
  if (globals.requestAnimationFrame) raf.use(globals.requestAnimationFrame);
  if (globals.batchedUpdates) raf.batchedUpdates = globals.batchedUpdates;
  if (globals.willAdvance) willAdvance = globals.willAdvance;
  if (globals.frameLoop) raf.frameLoop = globals.frameLoop;
};
var startQueue = /* @__PURE__ */ new Set();
var currentFrame = [];
var prevFrame = [];
var priority = 0;
var frameLoop = {
  get idle() {
    return !startQueue.size && !currentFrame.length;
  },
  /** Advance the given animation on every frame until idle. */
  start(animation) {
    if (priority > animation.priority) {
      startQueue.add(animation);
      raf.onStart(flushStartQueue);
    } else {
      startSafely(animation);
      raf(advance);
    }
  },
  /** Advance all animations by the given time. */
  advance,
  /** Call this when an animation's priority changes. */
  sort(animation) {
    if (priority) {
      raf.onFrame(() => frameLoop.sort(animation));
    } else {
      const prevIndex = currentFrame.indexOf(animation);
      if (~prevIndex) {
        currentFrame.splice(prevIndex, 1);
        startUnsafely(animation);
      }
    }
  },
  /**
   * Clear all animations. For testing purposes.
   *
   * ☠️ Never call this from within the frameloop.
   */
  clear() {
    currentFrame = [];
    startQueue.clear();
  }
};
function flushStartQueue() {
  startQueue.forEach(startSafely);
  startQueue.clear();
  raf(advance);
}
function startSafely(animation) {
  if (!currentFrame.includes(animation)) startUnsafely(animation);
}
function startUnsafely(animation) {
  currentFrame.splice(
    findIndex(currentFrame, (other) => other.priority > animation.priority),
    0,
    animation
  );
}
function advance(dt2) {
  const nextFrame = prevFrame;
  for (let i8 = 0; i8 < currentFrame.length; i8++) {
    const animation = currentFrame[i8];
    priority = animation.priority;
    if (!animation.idle) {
      willAdvance(animation);
      animation.advance(dt2);
      if (!animation.idle) {
        nextFrame.push(animation);
      }
    }
  }
  priority = 0;
  prevFrame = currentFrame;
  prevFrame.length = 0;
  currentFrame = nextFrame;
  return currentFrame.length > 0;
}
function findIndex(arr, test) {
  const index2 = arr.findIndex(test);
  return index2 < 0 ? arr.length : index2;
}
var clamp = (min3, max3, v3) => Math.min(Math.max(v3, min3), max3);
var colors2 = {
  transparent: 0,
  aliceblue: 4042850303,
  antiquewhite: 4209760255,
  aqua: 16777215,
  aquamarine: 2147472639,
  azure: 4043309055,
  beige: 4126530815,
  bisque: 4293182719,
  black: 255,
  blanchedalmond: 4293643775,
  blue: 65535,
  blueviolet: 2318131967,
  brown: 2771004159,
  burlywood: 3736635391,
  burntsienna: 3934150143,
  cadetblue: 1604231423,
  chartreuse: 2147418367,
  chocolate: 3530104575,
  coral: 4286533887,
  cornflowerblue: 1687547391,
  cornsilk: 4294499583,
  crimson: 3692313855,
  cyan: 16777215,
  darkblue: 35839,
  darkcyan: 9145343,
  darkgoldenrod: 3095792639,
  darkgray: 2846468607,
  darkgreen: 6553855,
  darkgrey: 2846468607,
  darkkhaki: 3182914559,
  darkmagenta: 2332068863,
  darkolivegreen: 1433087999,
  darkorange: 4287365375,
  darkorchid: 2570243327,
  darkred: 2332033279,
  darksalmon: 3918953215,
  darkseagreen: 2411499519,
  darkslateblue: 1211993087,
  darkslategray: 793726975,
  darkslategrey: 793726975,
  darkturquoise: 13554175,
  darkviolet: 2483082239,
  deeppink: 4279538687,
  deepskyblue: 12582911,
  dimgray: 1768516095,
  dimgrey: 1768516095,
  dodgerblue: 512819199,
  firebrick: 2988581631,
  floralwhite: 4294635775,
  forestgreen: 579543807,
  fuchsia: 4278255615,
  gainsboro: 3705462015,
  ghostwhite: 4177068031,
  gold: 4292280575,
  goldenrod: 3668254975,
  gray: 2155905279,
  green: 8388863,
  greenyellow: 2919182335,
  grey: 2155905279,
  honeydew: 4043305215,
  hotpink: 4285117695,
  indianred: 3445382399,
  indigo: 1258324735,
  ivory: 4294963455,
  khaki: 4041641215,
  lavender: 3873897215,
  lavenderblush: 4293981695,
  lawngreen: 2096890111,
  lemonchiffon: 4294626815,
  lightblue: 2916673279,
  lightcoral: 4034953471,
  lightcyan: 3774873599,
  lightgoldenrodyellow: 4210742015,
  lightgray: 3553874943,
  lightgreen: 2431553791,
  lightgrey: 3553874943,
  lightpink: 4290167295,
  lightsalmon: 4288707327,
  lightseagreen: 548580095,
  lightskyblue: 2278488831,
  lightslategray: 2005441023,
  lightslategrey: 2005441023,
  lightsteelblue: 2965692159,
  lightyellow: 4294959359,
  lime: 16711935,
  limegreen: 852308735,
  linen: 4210091775,
  magenta: 4278255615,
  maroon: 2147483903,
  mediumaquamarine: 1724754687,
  mediumblue: 52735,
  mediumorchid: 3126187007,
  mediumpurple: 2473647103,
  mediumseagreen: 1018393087,
  mediumslateblue: 2070474495,
  mediumspringgreen: 16423679,
  mediumturquoise: 1221709055,
  mediumvioletred: 3340076543,
  midnightblue: 421097727,
  mintcream: 4127193855,
  mistyrose: 4293190143,
  moccasin: 4293178879,
  navajowhite: 4292783615,
  navy: 33023,
  oldlace: 4260751103,
  olive: 2155872511,
  olivedrab: 1804477439,
  orange: 4289003775,
  orangered: 4282712319,
  orchid: 3664828159,
  palegoldenrod: 4008225535,
  palegreen: 2566625535,
  paleturquoise: 2951671551,
  palevioletred: 3681588223,
  papayawhip: 4293907967,
  peachpuff: 4292524543,
  peru: 3448061951,
  pink: 4290825215,
  plum: 3718307327,
  powderblue: 2967529215,
  purple: 2147516671,
  rebeccapurple: 1714657791,
  red: 4278190335,
  rosybrown: 3163525119,
  royalblue: 1097458175,
  saddlebrown: 2336560127,
  salmon: 4202722047,
  sandybrown: 4104413439,
  seagreen: 780883967,
  seashell: 4294307583,
  sienna: 2689740287,
  silver: 3233857791,
  skyblue: 2278484991,
  slateblue: 1784335871,
  slategray: 1887473919,
  slategrey: 1887473919,
  snow: 4294638335,
  springgreen: 16744447,
  steelblue: 1182971135,
  tan: 3535047935,
  teal: 8421631,
  thistle: 3636451583,
  tomato: 4284696575,
  turquoise: 1088475391,
  violet: 4001558271,
  wheat: 4125012991,
  white: 4294967295,
  whitesmoke: 4126537215,
  yellow: 4294902015,
  yellowgreen: 2597139199
};
var NUMBER = "[-+]?\\d*\\.?\\d+";
var PERCENTAGE = NUMBER + "%";
function call(...parts) {
  return "\\(\\s*(" + parts.join(")\\s*,\\s*(") + ")\\s*\\)";
}
var rgb = new RegExp("rgb" + call(NUMBER, NUMBER, NUMBER));
var rgba = new RegExp("rgba" + call(NUMBER, NUMBER, NUMBER, NUMBER));
var hsl = new RegExp("hsl" + call(NUMBER, PERCENTAGE, PERCENTAGE));
var hsla = new RegExp(
  "hsla" + call(NUMBER, PERCENTAGE, PERCENTAGE, NUMBER)
);
var hex3 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex4 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex6 = /^#([0-9a-fA-F]{6})$/;
var hex8 = /^#([0-9a-fA-F]{8})$/;
function normalizeColor(color2) {
  let match;
  if (typeof color2 === "number") {
    return color2 >>> 0 === color2 && color2 >= 0 && color2 <= 4294967295 ? color2 : null;
  }
  if (match = hex6.exec(color2))
    return parseInt(match[1] + "ff", 16) >>> 0;
  if (colors && colors[color2] !== void 0) {
    return colors[color2];
  }
  if (match = rgb.exec(color2)) {
    return (parse255(match[1]) << 24 | // r
    parse255(match[2]) << 16 | // g
    parse255(match[3]) << 8 | // b
    255) >>> // a
    0;
  }
  if (match = rgba.exec(color2)) {
    return (parse255(match[1]) << 24 | // r
    parse255(match[2]) << 16 | // g
    parse255(match[3]) << 8 | // b
    parse1(match[4])) >>> // a
    0;
  }
  if (match = hex3.exec(color2)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      "ff",
      // a
      16
    ) >>> 0;
  }
  if (match = hex8.exec(color2)) return parseInt(match[1], 16) >>> 0;
  if (match = hex4.exec(color2)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      match[4] + match[4],
      // a
      16
    ) >>> 0;
  }
  if (match = hsl.exec(color2)) {
    return (hslToRgb(
      parse360(match[1]),
      // h
      parsePercentage(match[2]),
      // s
      parsePercentage(match[3])
      // l
    ) | 255) >>> // a
    0;
  }
  if (match = hsla.exec(color2)) {
    return (hslToRgb(
      parse360(match[1]),
      // h
      parsePercentage(match[2]),
      // s
      parsePercentage(match[3])
      // l
    ) | parse1(match[4])) >>> // a
    0;
  }
  return null;
}
function hue2rgb(p3, q3, t9) {
  if (t9 < 0) t9 += 1;
  if (t9 > 1) t9 -= 1;
  if (t9 < 1 / 6) return p3 + (q3 - p3) * 6 * t9;
  if (t9 < 1 / 2) return q3;
  if (t9 < 2 / 3) return p3 + (q3 - p3) * (2 / 3 - t9) * 6;
  return p3;
}
function hslToRgb(h2, s5, l5) {
  const q3 = l5 < 0.5 ? l5 * (1 + s5) : l5 + s5 - l5 * s5;
  const p3 = 2 * l5 - q3;
  const r8 = hue2rgb(p3, q3, h2 + 1 / 3);
  const g2 = hue2rgb(p3, q3, h2);
  const b6 = hue2rgb(p3, q3, h2 - 1 / 3);
  return Math.round(r8 * 255) << 24 | Math.round(g2 * 255) << 16 | Math.round(b6 * 255) << 8;
}
function parse255(str) {
  const int = parseInt(str, 10);
  if (int < 0) return 0;
  if (int > 255) return 255;
  return int;
}
function parse360(str) {
  const int = parseFloat(str);
  return (int % 360 + 360) % 360 / 360;
}
function parse1(str) {
  const num = parseFloat(str);
  if (num < 0) return 0;
  if (num > 1) return 255;
  return Math.round(num * 255);
}
function parsePercentage(str) {
  const int = parseFloat(str);
  if (int < 0) return 0;
  if (int > 100) return 1;
  return int / 100;
}
function colorToRgba(input) {
  let int32Color = normalizeColor(input);
  if (int32Color === null) return input;
  int32Color = int32Color || 0;
  const r8 = (int32Color & 4278190080) >>> 24;
  const g2 = (int32Color & 16711680) >>> 16;
  const b6 = (int32Color & 65280) >>> 8;
  const a5 = (int32Color & 255) / 255;
  return `rgba(${r8}, ${g2}, ${b6}, ${a5})`;
}
var createInterpolator = (range2, output, extrapolate) => {
  if (is.fun(range2)) {
    return range2;
  }
  if (is.arr(range2)) {
    return createInterpolator({
      range: range2,
      output,
      extrapolate
    });
  }
  if (is.str(range2.output[0])) {
    return createStringInterpolator(range2);
  }
  const config2 = range2;
  const outputRange = config2.output;
  const inputRange = config2.range || [0, 1];
  const extrapolateLeft = config2.extrapolateLeft || config2.extrapolate || "extend";
  const extrapolateRight = config2.extrapolateRight || config2.extrapolate || "extend";
  const easing = config2.easing || ((t9) => t9);
  return (input) => {
    const range22 = findRange(input, inputRange);
    return interpolate(
      input,
      inputRange[range22],
      inputRange[range22 + 1],
      outputRange[range22],
      outputRange[range22 + 1],
      easing,
      extrapolateLeft,
      extrapolateRight,
      config2.map
    );
  };
};
function interpolate(input, inputMin, inputMax, outputMin, outputMax, easing, extrapolateLeft, extrapolateRight, map4) {
  let result = map4 ? map4(input) : input;
  if (result < inputMin) {
    if (extrapolateLeft === "identity") return result;
    else if (extrapolateLeft === "clamp") result = inputMin;
  }
  if (result > inputMax) {
    if (extrapolateRight === "identity") return result;
    else if (extrapolateRight === "clamp") result = inputMax;
  }
  if (outputMin === outputMax) return outputMin;
  if (inputMin === inputMax) return input <= inputMin ? outputMin : outputMax;
  if (inputMin === -Infinity) result = -result;
  else if (inputMax === Infinity) result = result - inputMin;
  else result = (result - inputMin) / (inputMax - inputMin);
  result = easing(result);
  if (outputMin === -Infinity) result = -result;
  else if (outputMax === Infinity) result = result + outputMin;
  else result = result * (outputMax - outputMin) + outputMin;
  return result;
}
function findRange(input, inputRange) {
  for (var i8 = 1; i8 < inputRange.length - 1; ++i8)
    if (inputRange[i8] >= input) break;
  return i8 - 1;
}
var steps = (steps2, direction = "end") => (progress2) => {
  progress2 = direction === "end" ? Math.min(progress2, 0.999) : Math.max(progress2, 1e-3);
  const expanded = progress2 * steps2;
  const rounded = direction === "end" ? Math.floor(expanded) : Math.ceil(expanded);
  return clamp(0, 1, rounded / steps2);
};
var c1 = 1.70158;
var c2 = c1 * 1.525;
var c3 = c1 + 1;
var c4 = 2 * Math.PI / 3;
var c5 = 2 * Math.PI / 4.5;
var bounceOut = (x4) => {
  const n1 = 7.5625;
  const d1 = 2.75;
  if (x4 < 1 / d1) {
    return n1 * x4 * x4;
  } else if (x4 < 2 / d1) {
    return n1 * (x4 -= 1.5 / d1) * x4 + 0.75;
  } else if (x4 < 2.5 / d1) {
    return n1 * (x4 -= 2.25 / d1) * x4 + 0.9375;
  } else {
    return n1 * (x4 -= 2.625 / d1) * x4 + 0.984375;
  }
};
var easings = {
  linear: (x4) => x4,
  easeInQuad: (x4) => x4 * x4,
  easeOutQuad: (x4) => 1 - (1 - x4) * (1 - x4),
  easeInOutQuad: (x4) => x4 < 0.5 ? 2 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 2) / 2,
  easeInCubic: (x4) => x4 * x4 * x4,
  easeOutCubic: (x4) => 1 - Math.pow(1 - x4, 3),
  easeInOutCubic: (x4) => x4 < 0.5 ? 4 * x4 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 3) / 2,
  easeInQuart: (x4) => x4 * x4 * x4 * x4,
  easeOutQuart: (x4) => 1 - Math.pow(1 - x4, 4),
  easeInOutQuart: (x4) => x4 < 0.5 ? 8 * x4 * x4 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 4) / 2,
  easeInQuint: (x4) => x4 * x4 * x4 * x4 * x4,
  easeOutQuint: (x4) => 1 - Math.pow(1 - x4, 5),
  easeInOutQuint: (x4) => x4 < 0.5 ? 16 * x4 * x4 * x4 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 5) / 2,
  easeInSine: (x4) => 1 - Math.cos(x4 * Math.PI / 2),
  easeOutSine: (x4) => Math.sin(x4 * Math.PI / 2),
  easeInOutSine: (x4) => -(Math.cos(Math.PI * x4) - 1) / 2,
  easeInExpo: (x4) => x4 === 0 ? 0 : Math.pow(2, 10 * x4 - 10),
  easeOutExpo: (x4) => x4 === 1 ? 1 : 1 - Math.pow(2, -10 * x4),
  easeInOutExpo: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : x4 < 0.5 ? Math.pow(2, 20 * x4 - 10) / 2 : (2 - Math.pow(2, -20 * x4 + 10)) / 2,
  easeInCirc: (x4) => 1 - Math.sqrt(1 - Math.pow(x4, 2)),
  easeOutCirc: (x4) => Math.sqrt(1 - Math.pow(x4 - 1, 2)),
  easeInOutCirc: (x4) => x4 < 0.5 ? (1 - Math.sqrt(1 - Math.pow(2 * x4, 2))) / 2 : (Math.sqrt(1 - Math.pow(-2 * x4 + 2, 2)) + 1) / 2,
  easeInBack: (x4) => c3 * x4 * x4 * x4 - c1 * x4 * x4,
  easeOutBack: (x4) => 1 + c3 * Math.pow(x4 - 1, 3) + c1 * Math.pow(x4 - 1, 2),
  easeInOutBack: (x4) => x4 < 0.5 ? Math.pow(2 * x4, 2) * ((c2 + 1) * 2 * x4 - c2) / 2 : (Math.pow(2 * x4 - 2, 2) * ((c2 + 1) * (x4 * 2 - 2) + c2) + 2) / 2,
  easeInElastic: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : -Math.pow(2, 10 * x4 - 10) * Math.sin((x4 * 10 - 10.75) * c4),
  easeOutElastic: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : Math.pow(2, -10 * x4) * Math.sin((x4 * 10 - 0.75) * c4) + 1,
  easeInOutElastic: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : x4 < 0.5 ? -(Math.pow(2, 20 * x4 - 10) * Math.sin((20 * x4 - 11.125) * c5)) / 2 : Math.pow(2, -20 * x4 + 10) * Math.sin((20 * x4 - 11.125) * c5) / 2 + 1,
  easeInBounce: (x4) => 1 - bounceOut(1 - x4),
  easeOutBounce: bounceOut,
  easeInOutBounce: (x4) => x4 < 0.5 ? (1 - bounceOut(1 - 2 * x4)) / 2 : (1 + bounceOut(2 * x4 - 1)) / 2,
  steps
};
var $get = Symbol.for("FluidValue.get");
var $observers = Symbol.for("FluidValue.observers");
var hasFluidValue = (arg) => Boolean(arg && arg[$get]);
var getFluidValue = (arg) => arg && arg[$get] ? arg[$get]() : arg;
var getFluidObservers = (target) => target[$observers] || null;
function callFluidObserver(observer2, event) {
  if (observer2.eventObserved) {
    observer2.eventObserved(event);
  } else {
    observer2(event);
  }
}
function callFluidObservers(target, event) {
  const observers = target[$observers];
  if (observers) {
    observers.forEach((observer2) => {
      callFluidObserver(observer2, event);
    });
  }
}
var FluidValue = class {
  constructor(get) {
    if (!get && !(get = this.get)) {
      throw Error("Unknown getter");
    }
    setFluidGetter(this, get);
  }
};
var setFluidGetter = (target, get) => setHidden(target, $get, get);
function addFluidObserver(target, observer2) {
  if (target[$get]) {
    let observers = target[$observers];
    if (!observers) {
      setHidden(target, $observers, observers = /* @__PURE__ */ new Set());
    }
    if (!observers.has(observer2)) {
      observers.add(observer2);
      if (target.observerAdded) {
        target.observerAdded(observers.size, observer2);
      }
    }
  }
  return observer2;
}
function removeFluidObserver(target, observer2) {
  const observers = target[$observers];
  if (observers && observers.has(observer2)) {
    const count2 = observers.size - 1;
    if (count2) {
      observers.delete(observer2);
    } else {
      target[$observers] = null;
    }
    if (target.observerRemoved) {
      target.observerRemoved(count2, observer2);
    }
  }
}
var setHidden = (target, key, value) => Object.defineProperty(target, key, {
  value,
  writable: true,
  configurable: true
});
var numberRegex = /[+\-]?(?:0|[1-9]\d*)(?:\.\d*)?(?:[eE][+\-]?\d+)?/g;
var colorRegex = /(#(?:[0-9a-f]{2}){2,4}|(#[0-9a-f]{3})|(rgb|hsl)a?\((-?\d+%?[,\s]+){2,3}\s*[\d\.]+%?\))/gi;
var unitRegex = new RegExp(`(${numberRegex.source})(%|[a-z]+)`, "i");
var rgbaRegex = /rgba\(([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+)\)/gi;
var cssVariableRegex = /var\((--[a-zA-Z0-9-_]+),? ?([a-zA-Z0-9 ()%#.,-]+)?\)/;
var variableToRgba = (input) => {
  const [token, fallback] = parseCSSVariable(input);
  if (!token || isSSR()) {
    return input;
  }
  const value = window.getComputedStyle(document.documentElement).getPropertyValue(token);
  if (value) {
    return value.trim();
  } else if (fallback && fallback.startsWith("--")) {
    const value2 = window.getComputedStyle(document.documentElement).getPropertyValue(fallback);
    if (value2) {
      return value2;
    } else {
      return input;
    }
  } else if (fallback && cssVariableRegex.test(fallback)) {
    return variableToRgba(fallback);
  } else if (fallback) {
    return fallback;
  }
  return input;
};
var parseCSSVariable = (current) => {
  const match = cssVariableRegex.exec(current);
  if (!match) return [,];
  const [, token, fallback] = match;
  return [token, fallback];
};
var namedColorRegex;
var rgbaRound = (_3, p1, p22, p3, p4) => `rgba(${Math.round(p1)}, ${Math.round(p22)}, ${Math.round(p3)}, ${p4})`;
var createStringInterpolator2 = (config2) => {
  if (!namedColorRegex)
    namedColorRegex = colors ? (
      // match color names, ignore partial matches
      new RegExp(`(${Object.keys(colors).join("|")})(?!\\w)`, "g")
    ) : (
      // never match
      /^\b$/
    );
  const output = config2.output.map((value) => {
    return getFluidValue(value).replace(cssVariableRegex, variableToRgba).replace(colorRegex, colorToRgba).replace(namedColorRegex, colorToRgba);
  });
  const keyframes = output.map((value) => value.match(numberRegex).map(Number));
  const outputRanges = keyframes[0].map(
    (_3, i8) => keyframes.map((values) => {
      if (!(i8 in values)) {
        throw Error('The arity of each "output" value must be equal');
      }
      return values[i8];
    })
  );
  const interpolators = outputRanges.map(
    (output2) => createInterpolator({ ...config2, output: output2 })
  );
  return (input) => {
    const missingUnit = !unitRegex.test(output[0]) && output.find((value) => unitRegex.test(value))?.replace(numberRegex, "");
    let i8 = 0;
    return output[0].replace(
      numberRegex,
      () => `${interpolators[i8++](input)}${missingUnit || ""}`
    ).replace(rgbaRegex, rgbaRound);
  };
};
var prefix = "react-spring: ";
var once = (fn3) => {
  const func = fn3;
  let called = false;
  if (typeof func != "function") {
    throw new TypeError(`${prefix}once requires a function parameter`);
  }
  return (...args) => {
    if (!called) {
      func(...args);
      called = true;
    }
  };
};
var warnInterpolate = once(console.warn);
function deprecateInterpolate() {
  warnInterpolate(
    `${prefix}The "interpolate" function is deprecated in v9 (use "to" instead)`
  );
}
var warnDirectCall = once(console.warn);
function deprecateDirectCall() {
  warnDirectCall(
    `${prefix}Directly calling start instead of using the api object is deprecated in v9 (use ".start" instead), this will be removed in later 0.X.0 versions`
  );
}
function isAnimatedString(value) {
  return is.str(value) && (value[0] == "#" || /\d/.test(value) || // Do not identify a CSS variable as an AnimatedString if its SSR
  !isSSR() && cssVariableRegex.test(value) || value in (colors || {}));
}
var useIsomorphicLayoutEffect = isSSR() ? import_react4.useEffect : import_react4.useLayoutEffect;
var useIsMounted = () => {
  const isMounted = (0, import_react3.useRef)(false);
  useIsomorphicLayoutEffect(() => {
    isMounted.current = true;
    return () => {
      isMounted.current = false;
    };
  }, []);
  return isMounted;
};
function useForceUpdate() {
  const update3 = (0, import_react2.useState)()[1];
  const isMounted = useIsMounted();
  return () => {
    if (isMounted.current) {
      update3(Math.random());
    }
  };
}
var useOnce = (effect) => (0, import_react6.useEffect)(effect, emptyDeps);
var emptyDeps = [];
function usePrev(value) {
  const prevRef = (0, import_react7.useRef)(void 0);
  (0, import_react7.useEffect)(() => {
    prevRef.current = value;
  });
  return prevRef.current;
}

// node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var import_react10 = __toESM(require_react(), 1);

// node_modules/@react-spring/animated/dist/react-spring_animated.modern.mjs
var React = __toESM(require_react(), 1);
var import_react9 = __toESM(require_react(), 1);
var $node = Symbol.for("Animated:node");
var isAnimated = (value) => !!value && value[$node] === value;
var getAnimated = (owner) => owner && owner[$node];
var setAnimated = (owner, node) => defineHidden(owner, $node, node);
var getPayload = (owner) => owner && owner[$node] && owner[$node].getPayload();
var Animated = class {
  constructor() {
    setAnimated(this, this);
  }
  /** Get every `AnimatedValue` used by this node. */
  getPayload() {
    return this.payload || [];
  }
};
var AnimatedValue = class _AnimatedValue extends Animated {
  constructor(_value) {
    super();
    this._value = _value;
    this.done = true;
    this.durationProgress = 0;
    if (is.num(this._value)) {
      this.lastPosition = this._value;
    }
  }
  /** @internal */
  static create(value) {
    return new _AnimatedValue(value);
  }
  getPayload() {
    return [this];
  }
  getValue() {
    return this._value;
  }
  setValue(value, step) {
    if (is.num(value)) {
      this.lastPosition = value;
      if (step) {
        value = Math.round(value / step) * step;
        if (this.done) {
          this.lastPosition = value;
        }
      }
    }
    if (this._value === value) {
      return false;
    }
    this._value = value;
    return true;
  }
  reset() {
    const { done } = this;
    this.done = false;
    if (is.num(this._value)) {
      this.elapsedTime = 0;
      this.durationProgress = 0;
      this.lastPosition = this._value;
      if (done) this.lastVelocity = null;
      this.v0 = null;
    }
  }
};
var AnimatedString = class _AnimatedString extends AnimatedValue {
  constructor(value) {
    super(0);
    this._string = null;
    this._toString = createInterpolator({
      output: [value, value]
    });
  }
  /** @internal */
  static create(value) {
    return new _AnimatedString(value);
  }
  getValue() {
    const value = this._string;
    return value == null ? this._string = this._toString(this._value) : value;
  }
  setValue(value) {
    if (is.str(value)) {
      if (value == this._string) {
        return false;
      }
      this._string = value;
      this._value = 1;
    } else if (super.setValue(value)) {
      this._string = null;
    } else {
      return false;
    }
    return true;
  }
  reset(goal) {
    if (goal) {
      this._toString = createInterpolator({
        output: [this.getValue(), goal]
      });
    }
    this._value = 0;
    super.reset();
  }
};
var TreeContext = { dependencies: null };
var AnimatedObject = class extends Animated {
  constructor(source) {
    super();
    this.source = source;
    this.setValue(source);
  }
  getValue(animated2) {
    const values = {};
    eachProp(this.source, (source, key) => {
      if (isAnimated(source)) {
        values[key] = source.getValue(animated2);
      } else if (hasFluidValue(source)) {
        values[key] = getFluidValue(source);
      } else if (!animated2) {
        values[key] = source;
      }
    });
    return values;
  }
  /** Replace the raw object data */
  setValue(source) {
    this.source = source;
    this.payload = this._makePayload(source);
  }
  reset() {
    if (this.payload) {
      each(this.payload, (node) => node.reset());
    }
  }
  /** Create a payload set. */
  _makePayload(source) {
    if (source) {
      const payload = /* @__PURE__ */ new Set();
      eachProp(source, this._addToPayload, payload);
      return Array.from(payload);
    }
  }
  /** Add to a payload set. */
  _addToPayload(source) {
    if (TreeContext.dependencies && hasFluidValue(source)) {
      TreeContext.dependencies.add(source);
    }
    const payload = getPayload(source);
    if (payload) {
      each(payload, (node) => this.add(node));
    }
  }
};
var AnimatedArray = class _AnimatedArray extends AnimatedObject {
  constructor(source) {
    super(source);
  }
  /** @internal */
  static create(source) {
    return new _AnimatedArray(source);
  }
  getValue() {
    return this.source.map((node) => node.getValue());
  }
  setValue(source) {
    const payload = this.getPayload();
    if (source.length == payload.length) {
      return payload.map((node, i8) => node.setValue(source[i8])).some(Boolean);
    }
    super.setValue(source.map(makeAnimated));
    return true;
  }
};
function makeAnimated(value) {
  const nodeType = isAnimatedString(value) ? AnimatedString : AnimatedValue;
  return nodeType.create(value);
}
function getAnimatedType(value) {
  const parentNode = getAnimated(value);
  return parentNode ? parentNode.constructor : is.arr(value) ? AnimatedArray : isAnimatedString(value) ? AnimatedString : AnimatedValue;
}
var withAnimated = (Component2, host2) => {
  const hasInstance = (
    // Function components must use "forwardRef" to avoid being
    // re-rendered on every animation frame.
    !is.fun(Component2) || Component2.prototype && Component2.prototype.isReactComponent
  );
  return (0, import_react9.forwardRef)((givenProps, givenRef) => {
    const instanceRef = (0, import_react9.useRef)(null);
    const ref = hasInstance && // eslint-disable-next-line react-hooks/rules-of-hooks
    (0, import_react9.useCallback)(
      (value) => {
        instanceRef.current = updateRef(givenRef, value);
      },
      [givenRef]
    );
    const [props, deps] = getAnimatedState(givenProps, host2);
    const forceUpdate = useForceUpdate();
    const callback = () => {
      const instance = instanceRef.current;
      if (hasInstance && !instance) {
        return;
      }
      const didUpdate = instance ? host2.applyAnimatedValues(instance, props.getValue(true)) : false;
      if (didUpdate === false) {
        forceUpdate();
      }
    };
    const observer = new PropsObserver(callback, deps);
    const observerRef = (0, import_react9.useRef)(void 0);
    useIsomorphicLayoutEffect(() => {
      observerRef.current = observer;
      each(deps, (dep) => addFluidObserver(dep, observer));
      return () => {
        if (observerRef.current) {
          each(
            observerRef.current.deps,
            (dep) => removeFluidObserver(dep, observerRef.current)
          );
          raf.cancel(observerRef.current.update);
        }
      };
    });
    (0, import_react9.useEffect)(callback, []);
    useOnce(() => () => {
      const observer2 = observerRef.current;
      each(observer2.deps, (dep) => removeFluidObserver(dep, observer2));
    });
    const usedProps = host2.getComponentProps(props.getValue());
    return React.createElement(Component2, { ...usedProps, ref });
  });
};
var PropsObserver = class {
  constructor(update3, deps) {
    this.update = update3;
    this.deps = deps;
  }
  eventObserved(event) {
    if (event.type == "change") {
      raf.write(this.update);
    }
  }
};
function getAnimatedState(props, host2) {
  const dependencies = /* @__PURE__ */ new Set();
  TreeContext.dependencies = dependencies;
  if (props.style)
    props = {
      ...props,
      style: host2.createAnimatedStyle(props.style)
    };
  props = new AnimatedObject(props);
  TreeContext.dependencies = null;
  return [props, dependencies];
}
function updateRef(ref, value) {
  if (ref) {
    if (is.fun(ref)) ref(value);
    else ref.current = value;
  }
  return value;
}
var cacheKey = Symbol.for("AnimatedComponent");
var createHost = (components, {
  applyAnimatedValues: applyAnimatedValues2 = () => false,
  createAnimatedStyle = (style) => new AnimatedObject(style),
  getComponentProps = (props) => props
} = {}) => {
  const hostConfig = {
    applyAnimatedValues: applyAnimatedValues2,
    createAnimatedStyle,
    getComponentProps
  };
  const animated2 = (Component2) => {
    const displayName = getDisplayName(Component2) || "Anonymous";
    if (is.str(Component2)) {
      Component2 = animated2[Component2] || (animated2[Component2] = withAnimated(Component2, hostConfig));
    } else {
      Component2 = Component2[cacheKey] || (Component2[cacheKey] = withAnimated(Component2, hostConfig));
    }
    Component2.displayName = `Animated(${displayName})`;
    return Component2;
  };
  eachProp(components, (Component2, key) => {
    if (is.arr(components)) {
      key = getDisplayName(Component2);
    }
    animated2[key] = animated2(Component2);
  });
  return {
    animated: animated2
  };
};
var getDisplayName = (arg) => is.str(arg) ? arg : arg && is.str(arg.displayName) ? arg.displayName : is.fun(arg) && arg.name || null;

// node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var React2 = __toESM(require_react(), 1);
var import_react11 = __toESM(require_react(), 1);
var import_react12 = __toESM(require_react(), 1);
var React22 = __toESM(require_react(), 1);
var import_react13 = __toESM(require_react(), 1);
var import_react14 = __toESM(require_react(), 1);
function callProp(value, ...args) {
  return is.fun(value) ? value(...args) : value;
}
var matchProp = (value, key) => value === true || !!(key && value && (is.fun(value) ? value(key) : toArray(value).includes(key)));
var resolveProp = (prop, key) => is.obj(prop) ? key && prop[key] : prop;
var getDefaultProp = (props, key) => props.default === true ? props[key] : props.default ? props.default[key] : void 0;
var noopTransform = (value) => value;
var getDefaultProps = (props, transform = noopTransform) => {
  let keys = DEFAULT_PROPS;
  if (props.default && props.default !== true) {
    props = props.default;
    keys = Object.keys(props);
  }
  const defaults2 = {};
  for (const key of keys) {
    const value = transform(props[key], key);
    if (!is.und(value)) {
      defaults2[key] = value;
    }
  }
  return defaults2;
};
var DEFAULT_PROPS = [
  "config",
  "onProps",
  "onStart",
  "onChange",
  "onPause",
  "onResume",
  "onRest"
];
var RESERVED_PROPS = {
  config: 1,
  from: 1,
  to: 1,
  ref: 1,
  loop: 1,
  reset: 1,
  pause: 1,
  cancel: 1,
  reverse: 1,
  immediate: 1,
  default: 1,
  delay: 1,
  onProps: 1,
  onStart: 1,
  onChange: 1,
  onPause: 1,
  onResume: 1,
  onRest: 1,
  onResolve: 1,
  // Transition props
  items: 1,
  trail: 1,
  sort: 1,
  expires: 1,
  initial: 1,
  enter: 1,
  update: 1,
  leave: 1,
  children: 1,
  onDestroyed: 1,
  // Internal props
  keys: 1,
  callId: 1,
  parentId: 1
};
function getForwardProps(props) {
  const forward = {};
  let count2 = 0;
  eachProp(props, (value, prop) => {
    if (!RESERVED_PROPS[prop]) {
      forward[prop] = value;
      count2++;
    }
  });
  if (count2) {
    return forward;
  }
}
function inferTo(props) {
  const to22 = getForwardProps(props);
  if (to22) {
    const out = { to: to22 };
    eachProp(props, (val, key) => key in to22 || (out[key] = val));
    return out;
  }
  return { ...props };
}
function computeGoal(value) {
  value = getFluidValue(value);
  return is.arr(value) ? value.map(computeGoal) : isAnimatedString(value) ? globals_exports.createStringInterpolator({
    range: [0, 1],
    output: [value, value]
  })(1) : value;
}
function hasProps(props) {
  for (const _3 in props) return true;
  return false;
}
function isAsyncTo(to22) {
  return is.fun(to22) || is.arr(to22) && is.obj(to22[0]);
}
function detachRefs(ctrl, ref) {
  ctrl.ref?.delete(ctrl);
  ref?.delete(ctrl);
}
function replaceRef(ctrl, ref) {
  if (ref && ctrl.ref !== ref) {
    ctrl.ref?.delete(ctrl);
    ref.add(ctrl);
    ctrl.ref = ref;
  }
}
var config = {
  default: { tension: 170, friction: 26 },
  gentle: { tension: 120, friction: 14 },
  wobbly: { tension: 180, friction: 12 },
  stiff: { tension: 210, friction: 20 },
  slow: { tension: 280, friction: 60 },
  molasses: { tension: 280, friction: 120 }
};
var defaults = {
  ...config.default,
  mass: 1,
  damping: 1,
  easing: easings.linear,
  clamp: false
};
var AnimationConfig = class {
  constructor() {
    this.velocity = 0;
    Object.assign(this, defaults);
  }
};
function mergeConfig(config2, newConfig, defaultConfig) {
  if (defaultConfig) {
    defaultConfig = { ...defaultConfig };
    sanitizeConfig(defaultConfig, newConfig);
    newConfig = { ...defaultConfig, ...newConfig };
  }
  sanitizeConfig(config2, newConfig);
  Object.assign(config2, newConfig);
  for (const key in defaults) {
    if (config2[key] == null) {
      config2[key] = defaults[key];
    }
  }
  let { frequency, damping } = config2;
  const { mass } = config2;
  if (!is.und(frequency)) {
    if (frequency < 0.01) frequency = 0.01;
    if (damping < 0) damping = 0;
    config2.tension = Math.pow(2 * Math.PI / frequency, 2) * mass;
    config2.friction = 4 * Math.PI * damping * mass / frequency;
  }
  return config2;
}
function sanitizeConfig(config2, props) {
  if (!is.und(props.decay)) {
    config2.duration = void 0;
  } else {
    const isTensionConfig = !is.und(props.tension) || !is.und(props.friction);
    if (isTensionConfig || !is.und(props.frequency) || !is.und(props.damping) || !is.und(props.mass)) {
      config2.duration = void 0;
      config2.decay = void 0;
    }
    if (isTensionConfig) {
      config2.frequency = void 0;
    }
  }
}
var emptyArray = [];
var Animation = class {
  constructor() {
    this.changed = false;
    this.values = emptyArray;
    this.toValues = null;
    this.fromValues = emptyArray;
    this.config = new AnimationConfig();
    this.immediate = false;
  }
};
function scheduleProps(callId, { key, props, defaultProps, state, actions }) {
  return new Promise((resolve, reject) => {
    let delay;
    let timeout;
    let cancel = matchProp(props.cancel ?? defaultProps?.cancel, key);
    if (cancel) {
      onStart();
    } else {
      if (!is.und(props.pause)) {
        state.paused = matchProp(props.pause, key);
      }
      let pause = defaultProps?.pause;
      if (pause !== true) {
        pause = state.paused || matchProp(pause, key);
      }
      delay = callProp(props.delay || 0, key);
      if (pause) {
        state.resumeQueue.add(onResume);
        actions.pause();
      } else {
        actions.resume();
        onResume();
      }
    }
    function onPause() {
      state.resumeQueue.add(onResume);
      state.timeouts.delete(timeout);
      timeout.cancel();
      delay = timeout.time - raf.now();
    }
    function onResume() {
      if (delay > 0 && !globals_exports.skipAnimation) {
        state.delayed = true;
        timeout = raf.setTimeout(onStart, delay);
        state.pauseQueue.add(onPause);
        state.timeouts.add(timeout);
      } else {
        onStart();
      }
    }
    function onStart() {
      if (state.delayed) {
        state.delayed = false;
      }
      state.pauseQueue.delete(onPause);
      state.timeouts.delete(timeout);
      if (callId <= (state.cancelId || 0)) {
        cancel = true;
      }
      try {
        actions.start({ ...props, callId, cancel }, resolve);
      } catch (err) {
        reject(err);
      }
    }
  });
}
var getCombinedResult = (target, results) => results.length == 1 ? results[0] : results.some((result) => result.cancelled) ? getCancelledResult(target.get()) : results.every((result) => result.noop) ? getNoopResult(target.get()) : getFinishedResult(
  target.get(),
  results.every((result) => result.finished)
);
var getNoopResult = (value) => ({
  value,
  noop: true,
  finished: true,
  cancelled: false
});
var getFinishedResult = (value, finished, cancelled = false) => ({
  value,
  finished,
  cancelled
});
var getCancelledResult = (value) => ({
  value,
  cancelled: true,
  finished: false
});
function runAsync(to22, props, state, target) {
  const { callId, parentId, onRest } = props;
  const { asyncTo: prevTo, promise: prevPromise } = state;
  if (!parentId && to22 === prevTo && !props.reset) {
    return prevPromise;
  }
  return state.promise = (async () => {
    state.asyncId = callId;
    state.asyncTo = to22;
    const defaultProps = getDefaultProps(
      props,
      (value, key) => (
        // The `onRest` prop is only called when the `runAsync` promise is resolved.
        key === "onRest" ? void 0 : value
      )
    );
    let preventBail;
    let bail;
    const bailPromise = new Promise(
      (resolve, reject) => (preventBail = resolve, bail = reject)
    );
    const bailIfEnded = (bailSignal) => {
      const bailResult = (
        // The `cancel` prop or `stop` method was used.
        callId <= (state.cancelId || 0) && getCancelledResult(target) || // The async `to` prop was replaced.
        callId !== state.asyncId && getFinishedResult(target, false)
      );
      if (bailResult) {
        bailSignal.result = bailResult;
        bail(bailSignal);
        throw bailSignal;
      }
    };
    const animate = (arg1, arg2) => {
      const bailSignal = new BailSignal();
      const skipAnimationSignal = new SkipAnimationSignal();
      return (async () => {
        if (globals_exports.skipAnimation) {
          stopAsync(state);
          skipAnimationSignal.result = getFinishedResult(target, false);
          bail(skipAnimationSignal);
          throw skipAnimationSignal;
        }
        bailIfEnded(bailSignal);
        const props2 = is.obj(arg1) ? { ...arg1 } : { ...arg2, to: arg1 };
        props2.parentId = callId;
        eachProp(defaultProps, (value, key) => {
          if (is.und(props2[key])) {
            props2[key] = value;
          }
        });
        const result2 = await target.start(props2);
        bailIfEnded(bailSignal);
        if (state.paused) {
          await new Promise((resume) => {
            state.resumeQueue.add(resume);
          });
        }
        return result2;
      })();
    };
    let result;
    if (globals_exports.skipAnimation) {
      stopAsync(state);
      return getFinishedResult(target, false);
    }
    try {
      let animating;
      if (is.arr(to22)) {
        animating = (async (queue) => {
          for (const props2 of queue) {
            await animate(props2);
          }
        })(to22);
      } else {
        animating = Promise.resolve(to22(animate, target.stop.bind(target)));
      }
      await Promise.all([animating.then(preventBail), bailPromise]);
      result = getFinishedResult(target.get(), true, false);
    } catch (err) {
      if (err instanceof BailSignal) {
        result = err.result;
      } else if (err instanceof SkipAnimationSignal) {
        result = err.result;
      } else {
        throw err;
      }
    } finally {
      if (callId == state.asyncId) {
        state.asyncId = parentId;
        state.asyncTo = parentId ? prevTo : void 0;
        state.promise = parentId ? prevPromise : void 0;
      }
    }
    if (is.fun(onRest)) {
      raf.batchedUpdates(() => {
        onRest(result, target, target.item);
      });
    }
    return result;
  })();
}
function stopAsync(state, cancelId) {
  flush(state.timeouts, (t9) => t9.cancel());
  state.pauseQueue.clear();
  state.resumeQueue.clear();
  state.asyncId = state.asyncTo = state.promise = void 0;
  if (cancelId) state.cancelId = cancelId;
}
var BailSignal = class extends Error {
  constructor() {
    super(
      "An async animation has been interrupted. You see this error because you forgot to use `await` or `.catch(...)` on its returned promise."
    );
  }
};
var SkipAnimationSignal = class extends Error {
  constructor() {
    super("SkipAnimationSignal");
  }
};
var isFrameValue = (value) => value instanceof FrameValue;
var nextId = 1;
var FrameValue = class extends FluidValue {
  constructor() {
    super(...arguments);
    this.id = nextId++;
    this._priority = 0;
  }
  get priority() {
    return this._priority;
  }
  set priority(priority2) {
    if (this._priority != priority2) {
      this._priority = priority2;
      this._onPriorityChange(priority2);
    }
  }
  /** Get the current value */
  get() {
    const node = getAnimated(this);
    return node && node.getValue();
  }
  /** Create a spring that maps our value to another value */
  to(...args) {
    return globals_exports.to(this, args);
  }
  /** @deprecated Use the `to` method instead. */
  interpolate(...args) {
    deprecateInterpolate();
    return globals_exports.to(this, args);
  }
  toJSON() {
    return this.get();
  }
  observerAdded(count2) {
    if (count2 == 1) this._attach();
  }
  observerRemoved(count2) {
    if (count2 == 0) this._detach();
  }
  /** Called when the first child is added. */
  _attach() {
  }
  /** Called when the last child is removed. */
  _detach() {
  }
  /** Tell our children about our new value */
  _onChange(value, idle = false) {
    callFluidObservers(this, {
      type: "change",
      parent: this,
      value,
      idle
    });
  }
  /** Tell our children about our new priority */
  _onPriorityChange(priority2) {
    if (!this.idle) {
      frameLoop.sort(this);
    }
    callFluidObservers(this, {
      type: "priority",
      parent: this,
      priority: priority2
    });
  }
};
var $P = Symbol.for("SpringPhase");
var HAS_ANIMATED = 1;
var IS_ANIMATING = 2;
var IS_PAUSED = 4;
var hasAnimated = (target) => (target[$P] & HAS_ANIMATED) > 0;
var isAnimating = (target) => (target[$P] & IS_ANIMATING) > 0;
var isPaused = (target) => (target[$P] & IS_PAUSED) > 0;
var setActiveBit = (target, active) => active ? target[$P] |= IS_ANIMATING | HAS_ANIMATED : target[$P] &= ~IS_ANIMATING;
var setPausedBit = (target, paused) => paused ? target[$P] |= IS_PAUSED : target[$P] &= ~IS_PAUSED;
var SpringValue = class extends FrameValue {
  constructor(arg1, arg2) {
    super();
    this.animation = new Animation();
    this.defaultProps = {};
    this._state = {
      paused: false,
      delayed: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._pendingCalls = /* @__PURE__ */ new Set();
    this._lastCallId = 0;
    this._lastToId = 0;
    this._memoizedDuration = 0;
    if (!is.und(arg1) || !is.und(arg2)) {
      const props = is.obj(arg1) ? { ...arg1 } : { ...arg2, from: arg1 };
      if (is.und(props.default)) {
        props.default = true;
      }
      this.start(props);
    }
  }
  /** Equals true when not advancing on each frame. */
  get idle() {
    return !(isAnimating(this) || this._state.asyncTo) || isPaused(this);
  }
  get goal() {
    return getFluidValue(this.animation.to);
  }
  get velocity() {
    const node = getAnimated(this);
    return node instanceof AnimatedValue ? node.lastVelocity || 0 : node.getPayload().map((node2) => node2.lastVelocity || 0);
  }
  /**
   * When true, this value has been animated at least once.
   */
  get hasAnimated() {
    return hasAnimated(this);
  }
  /**
   * When true, this value has an unfinished animation,
   * which is either active or paused.
   */
  get isAnimating() {
    return isAnimating(this);
  }
  /**
   * When true, all current and future animations are paused.
   */
  get isPaused() {
    return isPaused(this);
  }
  /**
   *
   *
   */
  get isDelayed() {
    return this._state.delayed;
  }
  /** Advance the current animation by a number of milliseconds */
  advance(dt2) {
    let idle = true;
    let changed = false;
    const anim = this.animation;
    let { toValues } = anim;
    const { config: config2 } = anim;
    const payload = getPayload(anim.to);
    if (!payload && hasFluidValue(anim.to)) {
      toValues = toArray(getFluidValue(anim.to));
    }
    anim.values.forEach((node2, i8) => {
      if (node2.done) return;
      const to22 = (
        // Animated strings always go from 0 to 1.
        node2.constructor == AnimatedString ? 1 : payload ? payload[i8].lastPosition : toValues[i8]
      );
      let finished = anim.immediate;
      let position = to22;
      if (!finished) {
        position = node2.lastPosition;
        if (config2.tension <= 0) {
          node2.done = true;
          return;
        }
        let elapsed = node2.elapsedTime += dt2;
        const from = anim.fromValues[i8];
        const v0 = node2.v0 != null ? node2.v0 : node2.v0 = is.arr(config2.velocity) ? config2.velocity[i8] : config2.velocity;
        let velocity;
        const precision = config2.precision || (from == to22 ? 5e-3 : Math.min(1, Math.abs(to22 - from) * 1e-3));
        if (!is.und(config2.duration)) {
          let p3 = 1;
          if (config2.duration > 0) {
            if (this._memoizedDuration !== config2.duration) {
              this._memoizedDuration = config2.duration;
              if (node2.durationProgress > 0) {
                node2.elapsedTime = config2.duration * node2.durationProgress;
                elapsed = node2.elapsedTime += dt2;
              }
            }
            p3 = (config2.progress || 0) + elapsed / this._memoizedDuration;
            p3 = p3 > 1 ? 1 : p3 < 0 ? 0 : p3;
            node2.durationProgress = p3;
          }
          position = from + config2.easing(p3) * (to22 - from);
          velocity = (position - node2.lastPosition) / dt2;
          finished = p3 == 1;
        } else if (config2.decay) {
          const decay = config2.decay === true ? 0.998 : config2.decay;
          const e11 = Math.exp(-(1 - decay) * elapsed);
          position = from + v0 / (1 - decay) * (1 - e11);
          finished = Math.abs(node2.lastPosition - position) <= precision;
          velocity = v0 * e11;
        } else {
          velocity = node2.lastVelocity == null ? v0 : node2.lastVelocity;
          const restVelocity = config2.restVelocity || precision / 10;
          const bounceFactor = config2.clamp ? 0 : config2.bounce;
          const canBounce = !is.und(bounceFactor);
          const isGrowing = from == to22 ? node2.v0 > 0 : from < to22;
          let isMoving;
          let isBouncing = false;
          const step = 1;
          const numSteps = Math.ceil(dt2 / step);
          for (let n9 = 0; n9 < numSteps; ++n9) {
            isMoving = Math.abs(velocity) > restVelocity;
            if (!isMoving) {
              finished = Math.abs(to22 - position) <= precision;
              if (finished) {
                break;
              }
            }
            if (canBounce) {
              isBouncing = position == to22 || position > to22 == isGrowing;
              if (isBouncing) {
                velocity = -velocity * bounceFactor;
                position = to22;
              }
            }
            const springForce = -config2.tension * 1e-6 * (position - to22);
            const dampingForce = -config2.friction * 1e-3 * velocity;
            const acceleration = (springForce + dampingForce) / config2.mass;
            velocity = velocity + acceleration * step;
            position = position + velocity * step;
          }
        }
        node2.lastVelocity = velocity;
        if (Number.isNaN(position)) {
          console.warn(`Got NaN while animating:`, this);
          finished = true;
        }
      }
      if (payload && !payload[i8].done) {
        finished = false;
      }
      if (finished) {
        node2.done = true;
      } else {
        idle = false;
      }
      if (node2.setValue(position, config2.round)) {
        changed = true;
      }
    });
    const node = getAnimated(this);
    const currVal = node.getValue();
    if (idle) {
      const finalVal = getFluidValue(anim.to);
      if ((currVal !== finalVal || changed) && !config2.decay) {
        node.setValue(finalVal);
        this._onChange(finalVal);
      } else if (changed && config2.decay) {
        this._onChange(currVal);
      }
      this._stop();
    } else if (changed) {
      this._onChange(currVal);
    }
  }
  /** Set the current value, while stopping the current animation */
  set(value) {
    raf.batchedUpdates(() => {
      this._stop();
      this._focus(value);
      this._set(value);
    });
    return this;
  }
  /**
   * Freeze the active animation in time, as well as any updates merged
   * before `resume` is called.
   */
  pause() {
    this._update({ pause: true });
  }
  /** Resume the animation if paused. */
  resume() {
    this._update({ pause: false });
  }
  /** Skip to the end of the current animation. */
  finish() {
    if (isAnimating(this)) {
      const { to: to22, config: config2 } = this.animation;
      raf.batchedUpdates(() => {
        this._onStart();
        if (!config2.decay) {
          this._set(to22, false);
        }
        this._stop();
      });
    }
    return this;
  }
  /** Push props into the pending queue. */
  update(props) {
    const queue = this.queue || (this.queue = []);
    queue.push(props);
    return this;
  }
  start(to22, arg2) {
    let queue;
    if (!is.und(to22)) {
      queue = [is.obj(to22) ? to22 : { ...arg2, to: to22 }];
    } else {
      queue = this.queue || [];
      this.queue = [];
    }
    return Promise.all(
      queue.map((props) => {
        const up = this._update(props);
        return up;
      })
    ).then((results) => getCombinedResult(this, results));
  }
  /**
   * Stop the current animation, and cancel any delayed updates.
   *
   * Pass `true` to call `onRest` with `cancelled: true`.
   */
  stop(cancel) {
    const { to: to22 } = this.animation;
    this._focus(this.get());
    stopAsync(this._state, cancel && this._lastCallId);
    raf.batchedUpdates(() => this._stop(to22, cancel));
    return this;
  }
  /** Restart the animation. */
  reset() {
    this._update({ reset: true });
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._start();
    } else if (event.type == "priority") {
      this.priority = event.priority + 1;
    }
  }
  /**
   * Parse the `to` and `from` range from the given `props` object.
   *
   * This also ensures the initial value is available to animated components
   * during the render phase.
   */
  _prepareNode(props) {
    const key = this.key || "";
    let { to: to22, from } = props;
    to22 = is.obj(to22) ? to22[key] : to22;
    if (to22 == null || isAsyncTo(to22)) {
      to22 = void 0;
    }
    from = is.obj(from) ? from[key] : from;
    if (from == null) {
      from = void 0;
    }
    const range2 = { to: to22, from };
    if (!hasAnimated(this)) {
      if (props.reverse) [to22, from] = [from, to22];
      from = getFluidValue(from);
      if (!is.und(from)) {
        this._set(from);
      } else if (!getAnimated(this)) {
        this._set(to22);
      }
    }
    return range2;
  }
  /** Every update is processed by this method before merging. */
  _update({ ...props }, isLoop) {
    const { key, defaultProps } = this;
    if (props.default)
      Object.assign(
        defaultProps,
        getDefaultProps(
          props,
          (value, prop) => /^on/.test(prop) ? resolveProp(value, key) : value
        )
      );
    mergeActiveFn(this, props, "onProps");
    sendEvent(this, "onProps", props, this);
    const range2 = this._prepareNode(props);
    if (Object.isFrozen(this)) {
      throw Error(
        "Cannot animate a `SpringValue` object that is frozen. Did you forget to pass your component to `animated(...)` before animating its props?"
      );
    }
    const state = this._state;
    return scheduleProps(++this._lastCallId, {
      key,
      props,
      defaultProps,
      state,
      actions: {
        pause: () => {
          if (!isPaused(this)) {
            setPausedBit(this, true);
            flushCalls(state.pauseQueue);
            sendEvent(
              this,
              "onPause",
              getFinishedResult(this, checkFinished(this, this.animation.to)),
              this
            );
          }
        },
        resume: () => {
          if (isPaused(this)) {
            setPausedBit(this, false);
            if (isAnimating(this)) {
              this._resume();
            }
            flushCalls(state.resumeQueue);
            sendEvent(
              this,
              "onResume",
              getFinishedResult(this, checkFinished(this, this.animation.to)),
              this
            );
          }
        },
        start: this._merge.bind(this, range2)
      }
    }).then((result) => {
      if (props.loop && result.finished && !(isLoop && result.noop)) {
        const nextProps = createLoopUpdate(props);
        if (nextProps) {
          return this._update(nextProps, true);
        }
      }
      return result;
    });
  }
  /** Merge props into the current animation */
  _merge(range2, props, resolve) {
    if (props.cancel) {
      this.stop(true);
      return resolve(getCancelledResult(this));
    }
    const hasToProp = !is.und(range2.to);
    const hasFromProp = !is.und(range2.from);
    if (hasToProp || hasFromProp) {
      if (props.callId > this._lastToId) {
        this._lastToId = props.callId;
      } else {
        return resolve(getCancelledResult(this));
      }
    }
    const { key, defaultProps, animation: anim } = this;
    const { to: prevTo, from: prevFrom } = anim;
    let { to: to22 = prevTo, from = prevFrom } = range2;
    if (hasFromProp && !hasToProp && (!props.default || is.und(to22))) {
      to22 = from;
    }
    if (props.reverse) [to22, from] = [from, to22];
    const hasFromChanged = !isEqual(from, prevFrom);
    if (hasFromChanged) {
      anim.from = from;
    }
    from = getFluidValue(from);
    const hasToChanged = !isEqual(to22, prevTo);
    if (hasToChanged) {
      this._focus(to22);
    }
    const hasAsyncTo = isAsyncTo(props.to);
    const { config: config2 } = anim;
    const { decay, velocity } = config2;
    if (hasToProp || hasFromProp) {
      config2.velocity = 0;
    }
    if (props.config && !hasAsyncTo) {
      mergeConfig(
        config2,
        callProp(props.config, key),
        // Avoid calling the same "config" prop twice.
        props.config !== defaultProps.config ? callProp(defaultProps.config, key) : void 0
      );
    }
    let node = getAnimated(this);
    if (!node || is.und(to22)) {
      return resolve(getFinishedResult(this, true));
    }
    const reset = (
      // When `reset` is undefined, the `from` prop implies `reset: true`,
      // except for declarative updates. When `reset` is defined, there
      // must exist a value to animate from.
      is.und(props.reset) ? hasFromProp && !props.default : !is.und(from) && matchProp(props.reset, key)
    );
    const value = reset ? from : this.get();
    const goal = computeGoal(to22);
    const isAnimatable = is.num(goal) || is.arr(goal) || isAnimatedString(goal);
    const immediate = !hasAsyncTo && (!isAnimatable || matchProp(defaultProps.immediate || props.immediate, key));
    if (hasToChanged) {
      const nodeType = getAnimatedType(to22);
      if (nodeType !== node.constructor) {
        if (immediate) {
          node = this._set(goal);
        } else
          throw Error(
            `Cannot animate between ${node.constructor.name} and ${nodeType.name}, as the "to" prop suggests`
          );
      }
    }
    const goalType = node.constructor;
    let started = hasFluidValue(to22);
    let finished = false;
    if (!started) {
      const hasValueChanged = reset || !hasAnimated(this) && hasFromChanged;
      if (hasToChanged || hasValueChanged) {
        finished = isEqual(computeGoal(value), goal);
        started = !finished;
      }
      if (!isEqual(anim.immediate, immediate) && !immediate || !isEqual(config2.decay, decay) || !isEqual(config2.velocity, velocity)) {
        started = true;
      }
    }
    if (finished && isAnimating(this)) {
      if (anim.changed && !reset) {
        started = true;
      } else if (!started) {
        this._stop(prevTo);
      }
    }
    if (!hasAsyncTo) {
      if (started || hasFluidValue(prevTo)) {
        anim.values = node.getPayload();
        anim.toValues = hasFluidValue(to22) ? null : goalType == AnimatedString ? [1] : toArray(goal);
      }
      if (anim.immediate != immediate) {
        anim.immediate = immediate;
        if (!immediate && !reset) {
          this._set(prevTo);
        }
      }
      if (started) {
        const { onRest } = anim;
        each(ACTIVE_EVENTS, (type) => mergeActiveFn(this, props, type));
        const result = getFinishedResult(this, checkFinished(this, prevTo));
        flushCalls(this._pendingCalls, result);
        this._pendingCalls.add(resolve);
        if (anim.changed)
          raf.batchedUpdates(() => {
            anim.changed = !reset;
            onRest?.(result, this);
            if (reset) {
              callProp(defaultProps.onRest, result);
            } else {
              anim.onStart?.(result, this);
            }
          });
      }
    }
    if (reset) {
      this._set(value);
    }
    if (hasAsyncTo) {
      resolve(runAsync(props.to, props, this._state, this));
    } else if (started) {
      this._start();
    } else if (isAnimating(this) && !hasToChanged) {
      this._pendingCalls.add(resolve);
    } else {
      resolve(getNoopResult(value));
    }
  }
  /** Update the `animation.to` value, which might be a `FluidValue` */
  _focus(value) {
    const anim = this.animation;
    if (value !== anim.to) {
      if (getFluidObservers(this)) {
        this._detach();
      }
      anim.to = value;
      if (getFluidObservers(this)) {
        this._attach();
      }
    }
  }
  _attach() {
    let priority2 = 0;
    const { to: to22 } = this.animation;
    if (hasFluidValue(to22)) {
      addFluidObserver(to22, this);
      if (isFrameValue(to22)) {
        priority2 = to22.priority + 1;
      }
    }
    this.priority = priority2;
  }
  _detach() {
    const { to: to22 } = this.animation;
    if (hasFluidValue(to22)) {
      removeFluidObserver(to22, this);
    }
  }
  /**
   * Update the current value from outside the frameloop,
   * and return the `Animated` node.
   */
  _set(arg, idle = true) {
    const value = getFluidValue(arg);
    if (!is.und(value)) {
      const oldNode = getAnimated(this);
      if (!oldNode || !isEqual(value, oldNode.getValue())) {
        const nodeType = getAnimatedType(value);
        if (!oldNode || oldNode.constructor != nodeType) {
          setAnimated(this, nodeType.create(value));
        } else {
          oldNode.setValue(value);
        }
        if (oldNode) {
          raf.batchedUpdates(() => {
            this._onChange(value, idle);
          });
        }
      }
    }
    return getAnimated(this);
  }
  _onStart() {
    const anim = this.animation;
    if (!anim.changed) {
      anim.changed = true;
      sendEvent(
        this,
        "onStart",
        getFinishedResult(this, checkFinished(this, anim.to)),
        this
      );
    }
  }
  _onChange(value, idle) {
    if (!idle) {
      this._onStart();
      callProp(this.animation.onChange, value, this);
    }
    callProp(this.defaultProps.onChange, value, this);
    super._onChange(value, idle);
  }
  // This method resets the animation state (even if already animating) to
  // ensure the latest from/to range is used, and it also ensures this spring
  // is added to the frameloop.
  _start() {
    const anim = this.animation;
    getAnimated(this).reset(getFluidValue(anim.to));
    if (!anim.immediate) {
      anim.fromValues = anim.values.map((node) => node.lastPosition);
    }
    if (!isAnimating(this)) {
      setActiveBit(this, true);
      if (!isPaused(this)) {
        this._resume();
      }
    }
  }
  _resume() {
    if (globals_exports.skipAnimation) {
      this.finish();
    } else {
      frameLoop.start(this);
    }
  }
  /**
   * Exit the frameloop and notify `onRest` listeners.
   *
   * Always wrap `_stop` calls with `batchedUpdates`.
   */
  _stop(goal, cancel) {
    if (isAnimating(this)) {
      setActiveBit(this, false);
      const anim = this.animation;
      each(anim.values, (node) => {
        node.done = true;
      });
      if (anim.toValues) {
        anim.onChange = anim.onPause = anim.onResume = void 0;
      }
      callFluidObservers(this, {
        type: "idle",
        parent: this
      });
      const result = cancel ? getCancelledResult(this.get()) : getFinishedResult(this.get(), checkFinished(this, goal ?? anim.to));
      flushCalls(this._pendingCalls, result);
      if (anim.changed) {
        anim.changed = false;
        sendEvent(this, "onRest", result, this);
      }
    }
  }
};
function checkFinished(target, to22) {
  const goal = computeGoal(to22);
  const value = computeGoal(target.get());
  return isEqual(value, goal);
}
function createLoopUpdate(props, loop2 = props.loop, to22 = props.to) {
  const loopRet = callProp(loop2);
  if (loopRet) {
    const overrides = loopRet !== true && inferTo(loopRet);
    const reverse2 = (overrides || props).reverse;
    const reset = !overrides || overrides.reset;
    return createUpdate({
      ...props,
      loop: loop2,
      // Avoid updating default props when looping.
      default: false,
      // Never loop the `pause` prop.
      pause: void 0,
      // For the "reverse" prop to loop as expected, the "to" prop
      // must be undefined. The "reverse" prop is ignored when the
      // "to" prop is an array or function.
      to: !reverse2 || isAsyncTo(to22) ? to22 : void 0,
      // Ignore the "from" prop except on reset.
      from: reset ? props.from : void 0,
      reset,
      // The "loop" prop can return a "useSpring" props object to
      // override any of the original props.
      ...overrides
    });
  }
}
function createUpdate(props) {
  const { to: to22, from } = props = inferTo(props);
  const keys = /* @__PURE__ */ new Set();
  if (is.obj(to22)) findDefined(to22, keys);
  if (is.obj(from)) findDefined(from, keys);
  props.keys = keys.size ? Array.from(keys) : null;
  return props;
}
function declareUpdate(props) {
  const update22 = createUpdate(props);
  if (is.und(update22.default)) {
    update22.default = getDefaultProps(update22);
  }
  return update22;
}
function findDefined(values, keys) {
  eachProp(values, (value, key) => value != null && keys.add(key));
}
var ACTIVE_EVENTS = [
  "onStart",
  "onRest",
  "onChange",
  "onPause",
  "onResume"
];
function mergeActiveFn(target, props, type) {
  target.animation[type] = props[type] !== getDefaultProp(props, type) ? resolveProp(props[type], target.key) : void 0;
}
function sendEvent(target, type, ...args) {
  target.animation[type]?.(...args);
  target.defaultProps[type]?.(...args);
}
var BATCHED_EVENTS = ["onStart", "onChange", "onRest"];
var nextId2 = 1;
var Controller = class {
  constructor(props, flush3) {
    this.id = nextId2++;
    this.springs = {};
    this.queue = [];
    this._lastAsyncId = 0;
    this._active = /* @__PURE__ */ new Set();
    this._changed = /* @__PURE__ */ new Set();
    this._started = false;
    this._state = {
      paused: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._events = {
      onStart: /* @__PURE__ */ new Map(),
      onChange: /* @__PURE__ */ new Map(),
      onRest: /* @__PURE__ */ new Map()
    };
    this._onFrame = this._onFrame.bind(this);
    if (flush3) {
      this._flush = flush3;
    }
    if (props) {
      this.start({ default: true, ...props });
    }
  }
  /**
   * Equals `true` when no spring values are in the frameloop, and
   * no async animation is currently active.
   */
  get idle() {
    return !this._state.asyncTo && Object.values(this.springs).every((spring) => {
      return spring.idle && !spring.isDelayed && !spring.isPaused;
    });
  }
  get item() {
    return this._item;
  }
  set item(item) {
    this._item = item;
  }
  /** Get the current values of our springs */
  get() {
    const values = {};
    this.each((spring, key) => values[key] = spring.get());
    return values;
  }
  /** Set the current values without animating. */
  set(values) {
    for (const key in values) {
      const value = values[key];
      if (!is.und(value)) {
        this.springs[key].set(value);
      }
    }
  }
  /** Push an update onto the queue of each value. */
  update(props) {
    if (props) {
      this.queue.push(createUpdate(props));
    }
    return this;
  }
  /**
   * Start the queued animations for every spring, and resolve the returned
   * promise once all queued animations have finished or been cancelled.
   *
   * When you pass a queue (instead of nothing), that queue is used instead of
   * the queued animations added with the `update` method, which are left alone.
   */
  start(props) {
    let { queue } = this;
    if (props) {
      queue = toArray(props).map(createUpdate);
    } else {
      this.queue = [];
    }
    if (this._flush) {
      return this._flush(this, queue);
    }
    prepareKeys(this, queue);
    return flushUpdateQueue(this, queue);
  }
  /** @internal */
  stop(arg, keys) {
    if (arg !== !!arg) {
      keys = arg;
    }
    if (keys) {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].stop(!!arg));
    } else {
      stopAsync(this._state, this._lastAsyncId);
      this.each((spring) => spring.stop(!!arg));
    }
    return this;
  }
  /** Freeze the active animation in time */
  pause(keys) {
    if (is.und(keys)) {
      this.start({ pause: true });
    } else {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].pause());
    }
    return this;
  }
  /** Resume the animation if paused. */
  resume(keys) {
    if (is.und(keys)) {
      this.start({ pause: false });
    } else {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].resume());
    }
    return this;
  }
  /** Call a function once per spring value */
  each(iterator) {
    eachProp(this.springs, iterator);
  }
  /** @internal Called at the end of every animation frame */
  _onFrame() {
    const { onStart, onChange, onRest } = this._events;
    const active = this._active.size > 0;
    const changed = this._changed.size > 0;
    if (active && !this._started || changed && !this._started) {
      this._started = true;
      flush(onStart, ([onStart2, result]) => {
        result.value = this.get();
        onStart2(result, this, this._item);
      });
    }
    const idle = !active && this._started;
    const values = changed || idle && onRest.size ? this.get() : null;
    if (changed && onChange.size) {
      flush(onChange, ([onChange2, result]) => {
        result.value = values;
        onChange2(result, this, this._item);
      });
    }
    if (idle) {
      this._started = false;
      flush(onRest, ([onRest2, result]) => {
        result.value = values;
        onRest2(result, this, this._item);
      });
    }
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._changed.add(event.parent);
      if (!event.idle) {
        this._active.add(event.parent);
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else return;
    raf.onFrame(this._onFrame);
  }
};
function flushUpdateQueue(ctrl, queue) {
  return Promise.all(queue.map((props) => flushUpdate(ctrl, props))).then(
    (results) => getCombinedResult(ctrl, results)
  );
}
async function flushUpdate(ctrl, props, isLoop) {
  const { keys, to: to22, from, loop: loop2, onRest, onResolve } = props;
  const defaults2 = is.obj(props.default) && props.default;
  if (loop2) {
    props.loop = false;
  }
  if (to22 === false) props.to = null;
  if (from === false) props.from = null;
  const asyncTo = is.arr(to22) || is.fun(to22) ? to22 : void 0;
  if (asyncTo) {
    props.to = void 0;
    props.onRest = void 0;
    if (defaults2) {
      defaults2.onRest = void 0;
    }
  } else {
    each(BATCHED_EVENTS, (key) => {
      const handler = props[key];
      if (is.fun(handler)) {
        const queue = ctrl["_events"][key];
        props[key] = ({ finished, cancelled }) => {
          const result2 = queue.get(handler);
          if (result2) {
            if (!finished) result2.finished = false;
            if (cancelled) result2.cancelled = true;
          } else {
            queue.set(handler, {
              value: null,
              finished: finished || false,
              cancelled: cancelled || false
            });
          }
        };
        if (defaults2) {
          defaults2[key] = props[key];
        }
      }
    });
  }
  const state = ctrl["_state"];
  if (props.pause === !state.paused) {
    state.paused = props.pause;
    flushCalls(props.pause ? state.pauseQueue : state.resumeQueue);
  } else if (state.paused) {
    props.pause = true;
  }
  const promises = (keys || Object.keys(ctrl.springs)).map(
    (key) => ctrl.springs[key].start(props)
  );
  const cancel = props.cancel === true || getDefaultProp(props, "cancel") === true;
  if (asyncTo || cancel && state.asyncId) {
    promises.push(
      scheduleProps(++ctrl["_lastAsyncId"], {
        props,
        state,
        actions: {
          pause: noop,
          resume: noop,
          start(props2, resolve) {
            if (cancel) {
              stopAsync(state, ctrl["_lastAsyncId"]);
              resolve(getCancelledResult(ctrl));
            } else {
              props2.onRest = onRest;
              resolve(
                runAsync(
                  asyncTo,
                  props2,
                  state,
                  ctrl
                )
              );
            }
          }
        }
      })
    );
  }
  if (state.paused) {
    await new Promise((resume) => {
      state.resumeQueue.add(resume);
    });
  }
  const result = getCombinedResult(ctrl, await Promise.all(promises));
  if (loop2 && result.finished && !(isLoop && result.noop)) {
    const nextProps = createLoopUpdate(props, loop2, to22);
    if (nextProps) {
      prepareKeys(ctrl, [nextProps]);
      return flushUpdate(ctrl, nextProps, true);
    }
  }
  if (onResolve) {
    raf.batchedUpdates(() => onResolve(result, ctrl, ctrl.item));
  }
  return result;
}
function getSprings(ctrl, props) {
  const springs = { ...ctrl.springs };
  if (props) {
    each(toArray(props), (props2) => {
      if (is.und(props2.keys)) {
        props2 = createUpdate(props2);
      }
      if (!is.obj(props2.to)) {
        props2 = { ...props2, to: void 0 };
      }
      prepareSprings(springs, props2, (key) => {
        return createSpring(key);
      });
    });
  }
  setSprings(ctrl, springs);
  return springs;
}
function setSprings(ctrl, springs) {
  eachProp(springs, (spring, key) => {
    if (!ctrl.springs[key]) {
      ctrl.springs[key] = spring;
      addFluidObserver(spring, ctrl);
    }
  });
}
function createSpring(key, observer) {
  const spring = new SpringValue();
  spring.key = key;
  if (observer) {
    addFluidObserver(spring, observer);
  }
  return spring;
}
function prepareSprings(springs, props, create) {
  if (props.keys) {
    each(props.keys, (key) => {
      const spring = springs[key] || (springs[key] = create(key));
      spring["_prepareNode"](props);
    });
  }
}
function prepareKeys(ctrl, queue) {
  each(queue, (props) => {
    prepareSprings(ctrl.springs, props, (key) => {
      return createSpring(key, ctrl);
    });
  });
}
var SpringContext = React2.createContext({
  pause: false,
  immediate: false
});
var SpringRef = () => {
  const current = [];
  const SpringRef2 = function(props) {
    deprecateDirectCall();
    const results = [];
    each(current, (ctrl, i8) => {
      if (is.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = _getProps(props, ctrl, i8);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef2.current = current;
  SpringRef2.add = function(ctrl) {
    if (!current.includes(ctrl)) {
      current.push(ctrl);
    }
  };
  SpringRef2.delete = function(ctrl) {
    const i8 = current.indexOf(ctrl);
    if (~i8) current.splice(i8, 1);
  };
  SpringRef2.pause = function() {
    each(current, (ctrl) => ctrl.pause(...arguments));
    return this;
  };
  SpringRef2.resume = function() {
    each(current, (ctrl) => ctrl.resume(...arguments));
    return this;
  };
  SpringRef2.set = function(values) {
    each(current, (ctrl, i8) => {
      const update22 = is.fun(values) ? values(i8, ctrl) : values;
      if (update22) {
        ctrl.set(update22);
      }
    });
  };
  SpringRef2.start = function(props) {
    const results = [];
    each(current, (ctrl, i8) => {
      if (is.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = this._getProps(props, ctrl, i8);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef2.stop = function() {
    each(current, (ctrl) => ctrl.stop(...arguments));
    return this;
  };
  SpringRef2.update = function(props) {
    each(current, (ctrl, i8) => ctrl.update(this._getProps(props, ctrl, i8)));
    return this;
  };
  const _getProps = function(arg, ctrl, index2) {
    return is.fun(arg) ? arg(index2, ctrl) : arg;
  };
  SpringRef2._getProps = _getProps;
  return SpringRef2;
};
function useSprings(length, props, deps) {
  const propsFn = is.fun(props) && props;
  if (propsFn && !deps) deps = [];
  const ref = (0, import_react10.useMemo)(
    () => propsFn || arguments.length == 3 ? SpringRef() : void 0,
    []
  );
  const layoutId = (0, import_react10.useRef)(0);
  const forceUpdate = useForceUpdate();
  const state = (0, import_react10.useMemo)(
    () => ({
      ctrls: [],
      queue: [],
      flush(ctrl, updates2) {
        const springs2 = getSprings(ctrl, updates2);
        const canFlushSync = layoutId.current > 0 && !state.queue.length && !Object.keys(springs2).some((key) => !ctrl.springs[key]);
        return canFlushSync ? flushUpdateQueue(ctrl, updates2) : new Promise((resolve) => {
          setSprings(ctrl, springs2);
          state.queue.push(() => {
            resolve(flushUpdateQueue(ctrl, updates2));
          });
          forceUpdate();
        });
      }
    }),
    []
  );
  const ctrls = (0, import_react10.useRef)([...state.ctrls]);
  const updates = (0, import_react10.useRef)([]);
  const prevLength = usePrev(length) || 0;
  (0, import_react10.useMemo)(() => {
    each(ctrls.current.slice(length, prevLength), (ctrl) => {
      detachRefs(ctrl, ref);
      ctrl.stop(true);
    });
    ctrls.current.length = length;
    declareUpdates(prevLength, length);
  }, [length]);
  (0, import_react10.useMemo)(() => {
    declareUpdates(0, Math.min(prevLength, length));
  }, deps);
  function declareUpdates(startIndex, endIndex) {
    for (let i8 = startIndex; i8 < endIndex; i8++) {
      const ctrl = ctrls.current[i8] || (ctrls.current[i8] = new Controller(null, state.flush));
      const update22 = propsFn ? propsFn(i8, ctrl) : props[i8];
      if (update22) {
        updates.current[i8] = declareUpdate(update22);
      }
    }
  }
  const springs = ctrls.current.map(
    (ctrl, i8) => getSprings(ctrl, updates.current[i8])
  );
  const context = (0, import_react10.useContext)(SpringContext);
  const prevContext = usePrev(context);
  const hasContext = context !== prevContext && hasProps(context);
  useIsomorphicLayoutEffect(() => {
    layoutId.current++;
    state.ctrls = ctrls.current;
    const { queue } = state;
    if (queue.length) {
      state.queue = [];
      each(queue, (cb) => cb());
    }
    each(ctrls.current, (ctrl, i8) => {
      ref?.add(ctrl);
      if (hasContext) {
        ctrl.start({ default: context });
      }
      const update22 = updates.current[i8];
      if (update22) {
        replaceRef(ctrl, update22.ref);
        if (ctrl.ref) {
          ctrl.queue.push(update22);
        } else {
          ctrl.start(update22);
        }
      }
    });
  });
  useOnce(() => () => {
    each(state.ctrls, (ctrl) => ctrl.stop(true));
  });
  const values = springs.map((x4) => ({ ...x4 }));
  return ref ? [values, ref] : values;
}
function useSpring(props, deps) {
  const isFn = is.fun(props);
  const [[values], ref] = useSprings(
    1,
    isFn ? props : [props],
    isFn ? deps || [] : deps
  );
  return isFn || arguments.length == 2 ? [values, ref] : values;
}
function useTransition(data, props, deps) {
  const propsFn = is.fun(props) && props;
  const {
    reset,
    sort: sort2,
    trail = 0,
    expires = true,
    exitBeforeEnter = false,
    onDestroyed,
    ref: propsRef,
    config: propsConfig
  } = propsFn ? propsFn() : props;
  const ref = (0, import_react13.useMemo)(
    () => propsFn || arguments.length == 3 ? SpringRef() : void 0,
    []
  );
  const items = toArray(data);
  const transitions = [];
  const usedTransitions = (0, import_react13.useRef)(null);
  const prevTransitions = reset ? null : usedTransitions.current;
  useIsomorphicLayoutEffect(() => {
    usedTransitions.current = transitions;
  });
  useOnce(() => {
    each(transitions, (t9) => {
      ref?.add(t9.ctrl);
      t9.ctrl.ref = ref;
    });
    return () => {
      each(usedTransitions.current, (t9) => {
        if (t9.expired) {
          clearTimeout(t9.expirationId);
        }
        detachRefs(t9.ctrl, ref);
        t9.ctrl.stop(true);
      });
    };
  });
  const keys = getKeys(items, propsFn ? propsFn() : props, prevTransitions);
  const expired = reset && usedTransitions.current || [];
  useIsomorphicLayoutEffect(
    () => each(expired, ({ ctrl, item, key }) => {
      detachRefs(ctrl, ref);
      callProp(onDestroyed, item, key);
    })
  );
  const reused = [];
  if (prevTransitions)
    each(prevTransitions, (t9, i8) => {
      if (t9.expired) {
        clearTimeout(t9.expirationId);
        expired.push(t9);
      } else {
        i8 = reused[i8] = keys.indexOf(t9.key);
        if (~i8) transitions[i8] = t9;
      }
    });
  each(items, (item, i8) => {
    if (!transitions[i8]) {
      transitions[i8] = {
        key: keys[i8],
        item,
        phase: "mount",
        ctrl: new Controller()
      };
      transitions[i8].ctrl.item = item;
    }
  });
  if (reused.length) {
    let i8 = -1;
    const { leave } = propsFn ? propsFn() : props;
    each(reused, (keyIndex, prevIndex) => {
      const t9 = prevTransitions[prevIndex];
      if (~keyIndex) {
        i8 = transitions.indexOf(t9);
        transitions[i8] = { ...t9, item: items[keyIndex] };
      } else if (leave) {
        transitions.splice(++i8, 0, t9);
      }
    });
  }
  if (is.fun(sort2)) {
    transitions.sort((a5, b6) => sort2(a5.item, b6.item));
  }
  let delay = -trail;
  const forceUpdate = useForceUpdate();
  const defaultProps = getDefaultProps(props);
  const changes = /* @__PURE__ */ new Map();
  const exitingTransitions = (0, import_react13.useRef)(/* @__PURE__ */ new Map());
  const forceChange = (0, import_react13.useRef)(false);
  each(transitions, (t9, i8) => {
    const key = t9.key;
    const prevPhase = t9.phase;
    const p3 = propsFn ? propsFn() : props;
    let to22;
    let phase;
    const propsDelay = callProp(p3.delay || 0, key);
    if (prevPhase == "mount") {
      to22 = p3.enter;
      phase = "enter";
    } else {
      const isLeave = keys.indexOf(key) < 0;
      if (prevPhase != "leave") {
        if (isLeave) {
          to22 = p3.leave;
          phase = "leave";
        } else if (to22 = p3.update) {
          phase = "update";
        } else return;
      } else if (!isLeave) {
        to22 = p3.enter;
        phase = "enter";
      } else return;
    }
    to22 = callProp(to22, t9.item, i8);
    to22 = is.obj(to22) ? inferTo(to22) : { to: to22 };
    if (!to22.config) {
      const config2 = propsConfig || defaultProps.config;
      to22.config = callProp(config2, t9.item, i8, phase);
    }
    delay += trail;
    const payload = {
      ...defaultProps,
      // we need to add our props.delay value you here.
      delay: propsDelay + delay,
      ref: propsRef,
      immediate: p3.immediate,
      // This prevents implied resets.
      reset: false,
      // Merge any phase-specific props.
      ...to22
    };
    if (phase == "enter" && is.und(payload.from)) {
      const p22 = propsFn ? propsFn() : props;
      const from = is.und(p22.initial) || prevTransitions ? p22.from : p22.initial;
      payload.from = callProp(from, t9.item, i8);
    }
    const { onResolve } = payload;
    payload.onResolve = (result) => {
      callProp(onResolve, result);
      const transitions2 = usedTransitions.current;
      const t23 = transitions2.find((t32) => t32.key === key);
      if (!t23) return;
      if (result.cancelled && t23.phase != "update") {
        return;
      }
      if (t23.ctrl.idle) {
        const idle = transitions2.every((t32) => t32.ctrl.idle);
        if (t23.phase == "leave") {
          const expiry = callProp(expires, t23.item);
          if (expiry !== false) {
            const expiryMs = expiry === true ? 0 : expiry;
            t23.expired = true;
            if (!idle && expiryMs > 0) {
              if (expiryMs <= 2147483647)
                t23.expirationId = setTimeout(forceUpdate, expiryMs);
              return;
            }
          }
        }
        if (idle && transitions2.some((t32) => t32.expired)) {
          exitingTransitions.current.delete(t23);
          if (exitBeforeEnter) {
            forceChange.current = true;
          }
          forceUpdate();
        }
      }
    };
    const springs = getSprings(t9.ctrl, payload);
    if (phase === "leave" && exitBeforeEnter) {
      exitingTransitions.current.set(t9, { phase, springs, payload });
    } else {
      changes.set(t9, { phase, springs, payload });
    }
  });
  const context = (0, import_react13.useContext)(SpringContext);
  const prevContext = usePrev(context);
  const hasContext = context !== prevContext && hasProps(context);
  useIsomorphicLayoutEffect(() => {
    if (hasContext) {
      each(transitions, (t9) => {
        t9.ctrl.start({ default: context });
      });
    }
  }, [context]);
  each(changes, (_3, t9) => {
    if (exitingTransitions.current.size) {
      const ind = transitions.findIndex((state) => state.key === t9.key);
      transitions.splice(ind, 1);
    }
  });
  useIsomorphicLayoutEffect(
    () => {
      each(
        exitingTransitions.current.size ? exitingTransitions.current : changes,
        ({ phase, payload }, t9) => {
          const { ctrl } = t9;
          t9.phase = phase;
          ref?.add(ctrl);
          if (hasContext && phase == "enter") {
            ctrl.start({ default: context });
          }
          if (payload) {
            replaceRef(ctrl, payload.ref);
            if ((ctrl.ref || ref) && !forceChange.current) {
              ctrl.update(payload);
            } else {
              ctrl.start(payload);
              if (forceChange.current) {
                forceChange.current = false;
              }
            }
          }
        }
      );
    },
    reset ? void 0 : deps
  );
  const renderTransitions = (render) => React22.createElement(React22.Fragment, null, transitions.map((t9, i8) => {
    const { springs } = changes.get(t9) || t9.ctrl;
    const elem = render({ ...springs }, t9.item, t9, i8);
    const key = is.str(t9.key) || is.num(t9.key) ? t9.key : t9.ctrl.id;
    const isLegacyReact = React22.version < "19.0.0";
    const props2 = elem?.props ?? {};
    if (isLegacyReact) {
      props2.ref = elem.ref;
    }
    return elem && elem.type ? React22.createElement(elem.type, { key, ...props2 }) : elem;
  }));
  return ref ? [renderTransitions, ref] : renderTransitions;
}
var nextKey = 1;
function getKeys(items, { key, keys = key }, prevTransitions) {
  if (keys === null) {
    const reused = /* @__PURE__ */ new Set();
    return items.map((item) => {
      const t9 = prevTransitions && prevTransitions.find(
        (t23) => t23.item === item && t23.phase !== "leave" && !reused.has(t23)
      );
      if (t9) {
        reused.add(t9);
        return t9.key;
      }
      return nextKey++;
    });
  }
  return is.und(keys) ? items : is.fun(keys) ? items.map(keys) : toArray(keys);
}
var Interpolation = class extends FrameValue {
  constructor(source, args) {
    super();
    this.source = source;
    this.idle = true;
    this._active = /* @__PURE__ */ new Set();
    this.calc = createInterpolator(...args);
    const value = this._get();
    const nodeType = getAnimatedType(value);
    setAnimated(this, nodeType.create(value));
  }
  advance(_dt) {
    const value = this._get();
    const oldValue = this.get();
    if (!isEqual(value, oldValue)) {
      getAnimated(this).setValue(value);
      this._onChange(value, this.idle);
    }
    if (!this.idle && checkIdle(this._active)) {
      becomeIdle(this);
    }
  }
  _get() {
    const inputs = is.arr(this.source) ? this.source.map(getFluidValue) : toArray(getFluidValue(this.source));
    return this.calc(...inputs);
  }
  _start() {
    if (this.idle && !checkIdle(this._active)) {
      this.idle = false;
      each(getPayload(this), (node) => {
        node.done = false;
      });
      if (globals_exports.skipAnimation) {
        raf.batchedUpdates(() => this.advance());
        becomeIdle(this);
      } else {
        frameLoop.start(this);
      }
    }
  }
  // Observe our sources only when we're observed.
  _attach() {
    let priority2 = 1;
    each(toArray(this.source), (source) => {
      if (hasFluidValue(source)) {
        addFluidObserver(source, this);
      }
      if (isFrameValue(source)) {
        if (!source.idle) {
          this._active.add(source);
        }
        priority2 = Math.max(priority2, source.priority + 1);
      }
    });
    this.priority = priority2;
    this._start();
  }
  // Stop observing our sources once we have no observers.
  _detach() {
    each(toArray(this.source), (source) => {
      if (hasFluidValue(source)) {
        removeFluidObserver(source, this);
      }
    });
    this._active.clear();
    becomeIdle(this);
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      if (event.idle) {
        this.advance();
      } else {
        this._active.add(event.parent);
        this._start();
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else if (event.type == "priority") {
      this.priority = toArray(this.source).reduce(
        (highest, parent) => Math.max(highest, (isFrameValue(parent) ? parent.priority : 0) + 1),
        0
      );
    }
  }
};
function isIdle(source) {
  return source.idle !== false;
}
function checkIdle(active) {
  return !active.size || Array.from(active).every(isIdle);
}
function becomeIdle(self2) {
  if (!self2.idle) {
    self2.idle = true;
    each(getPayload(self2), (node) => {
      node.done = true;
    });
    callFluidObservers(self2, {
      type: "idle",
      parent: self2
    });
  }
}
var to2 = (source, ...args) => new Interpolation(source, args);
globals_exports.assign({
  createStringInterpolator: createStringInterpolator2,
  to: (source, args) => new Interpolation(source, args)
});
var update2 = frameLoop.advance;

// node_modules/@react-spring/web/dist/react-spring_web.modern.mjs
var import_react_dom = __toESM(require_react_dom(), 1);
var isCustomPropRE = /^--/;
function dangerousStyleValue(name, value) {
  if (value == null || typeof value === "boolean" || value === "") return "";
  if (typeof value === "number" && value !== 0 && !isCustomPropRE.test(name) && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name]))
    return value + "px";
  return ("" + value).trim();
}
var attributeCache = {};
function applyAnimatedValues(instance, props) {
  if (!instance.nodeType || !instance.setAttribute) {
    return false;
  }
  const isFilterElement = instance.nodeName === "filter" || instance.parentNode && instance.parentNode.nodeName === "filter";
  const {
    className,
    style,
    children,
    scrollTop,
    scrollLeft,
    viewBox,
    ...attributes
  } = props;
  const values = Object.values(attributes);
  const names = Object.keys(attributes).map(
    (name) => isFilterElement || instance.hasAttribute(name) ? name : attributeCache[name] || (attributeCache[name] = name.replace(
      /([A-Z])/g,
      // Attributes are written in dash case
      (n9) => "-" + n9.toLowerCase()
    ))
  );
  if (children !== void 0) {
    instance.textContent = children;
  }
  for (const name in style) {
    if (style.hasOwnProperty(name)) {
      const value = dangerousStyleValue(name, style[name]);
      if (isCustomPropRE.test(name)) {
        instance.style.setProperty(name, value);
      } else {
        instance.style[name] = value;
      }
    }
  }
  names.forEach((name, i8) => {
    instance.setAttribute(name, values[i8]);
  });
  if (className !== void 0) {
    instance.className = className;
  }
  if (scrollTop !== void 0) {
    instance.scrollTop = scrollTop;
  }
  if (scrollLeft !== void 0) {
    instance.scrollLeft = scrollLeft;
  }
  if (viewBox !== void 0) {
    instance.setAttribute("viewBox", viewBox);
  }
}
var isUnitlessNumber = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  columns: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridRow: true,
  gridRowEnd: true,
  gridRowSpan: true,
  gridRowStart: true,
  gridColumn: true,
  gridColumnEnd: true,
  gridColumnSpan: true,
  gridColumnStart: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,
  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};
var prefixKey = (prefix2, key) => prefix2 + key.charAt(0).toUpperCase() + key.substring(1);
var prefixes = ["Webkit", "Ms", "Moz", "O"];
isUnitlessNumber = Object.keys(isUnitlessNumber).reduce((acc, prop) => {
  prefixes.forEach((prefix2) => acc[prefixKey(prefix2, prop)] = acc[prop]);
  return acc;
}, isUnitlessNumber);
var domTransforms = /^(matrix|translate|scale|rotate|skew)/;
var pxTransforms = /^(translate)/;
var degTransforms = /^(rotate|skew)/;
var addUnit = (value, unit2) => is.num(value) && value !== 0 ? value + unit2 : value;
var isValueIdentity = (value, id) => is.arr(value) ? value.every((v3) => isValueIdentity(v3, id)) : is.num(value) ? value === id : parseFloat(value) === id;
var AnimatedStyle = class extends AnimatedObject {
  constructor({ x: x4, y: y5, z: z5, ...style }) {
    const inputs = [];
    const transforms = [];
    if (x4 || y5 || z5) {
      inputs.push([x4 || 0, y5 || 0, z5 || 0]);
      transforms.push((xyz) => [
        `translate3d(${xyz.map((v3) => addUnit(v3, "px")).join(",")})`,
        // prettier-ignore
        isValueIdentity(xyz, 0)
      ]);
    }
    eachProp(style, (value, key) => {
      if (key === "transform") {
        inputs.push([value || ""]);
        transforms.push((transform) => [transform, transform === ""]);
      } else if (domTransforms.test(key)) {
        delete style[key];
        if (is.und(value)) return;
        const unit2 = pxTransforms.test(key) ? "px" : degTransforms.test(key) ? "deg" : "";
        inputs.push(toArray(value));
        transforms.push(
          key === "rotate3d" ? ([x22, y22, z22, deg]) => [
            `rotate3d(${x22},${y22},${z22},${addUnit(deg, unit2)})`,
            isValueIdentity(deg, 0)
          ] : (input) => [
            `${key}(${input.map((v3) => addUnit(v3, unit2)).join(",")})`,
            isValueIdentity(input, key.startsWith("scale") ? 1 : 0)
          ]
        );
      }
    });
    if (inputs.length) {
      style.transform = new FluidTransform(inputs, transforms);
    }
    super(style);
  }
};
var FluidTransform = class extends FluidValue {
  constructor(inputs, transforms) {
    super();
    this.inputs = inputs;
    this.transforms = transforms;
    this._value = null;
  }
  get() {
    return this._value || (this._value = this._get());
  }
  _get() {
    let transform = "";
    let identity5 = true;
    each(this.inputs, (input, i8) => {
      const arg1 = getFluidValue(input[0]);
      const [t9, id] = this.transforms[i8](
        is.arr(arg1) ? arg1 : input.map(getFluidValue)
      );
      transform += " " + t9;
      identity5 = identity5 && id;
    });
    return identity5 ? "none" : transform;
  }
  // Start observing our inputs once we have an observer.
  observerAdded(count2) {
    if (count2 == 1)
      each(
        this.inputs,
        (input) => each(
          input,
          (value) => hasFluidValue(value) && addFluidObserver(value, this)
        )
      );
  }
  // Stop observing our inputs once we have no observers.
  observerRemoved(count2) {
    if (count2 == 0)
      each(
        this.inputs,
        (input) => each(
          input,
          (value) => hasFluidValue(value) && removeFluidObserver(value, this)
        )
      );
  }
  eventObserved(event) {
    if (event.type == "change") {
      this._value = null;
    }
    callFluidObservers(this, event);
  }
};
var primitives = [
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "big",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hgroup",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "keygen",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "menu",
  "menuitem",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "param",
  "picture",
  "pre",
  "progress",
  "q",
  "rp",
  "rt",
  "ruby",
  "s",
  "samp",
  "script",
  "section",
  "select",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "table",
  "tbody",
  "td",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "track",
  "u",
  "ul",
  "var",
  "video",
  "wbr",
  // SVG
  "circle",
  "clipPath",
  "defs",
  "ellipse",
  "foreignObject",
  "g",
  "image",
  "line",
  "linearGradient",
  "mask",
  "path",
  "pattern",
  "polygon",
  "polyline",
  "radialGradient",
  "rect",
  "stop",
  "svg",
  "text",
  "tspan"
];
globals_exports.assign({
  batchedUpdates: import_react_dom.unstable_batchedUpdates,
  createStringInterpolator: createStringInterpolator2,
  colors: colors2
});
var host = createHost(primitives, {
  applyAnimatedValues,
  createAnimatedStyle: (style) => new AnimatedStyle(style),
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  getComponentProps: ({ scrollTop, scrollLeft, ...props }) => props
});
var animated = host.animated;

// node_modules/@nivo/theming/dist/nivo-theming.mjs
var import_react15 = __toESM(require_react(), 1);
var import_merge = __toESM(require_merge(), 1);
var import_get = __toESM(require_get(), 1);
var import_set = __toESM(require_set(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
function u() {
  return u = Object.assign ? Object.assign.bind() : function(t9) {
    for (var e11 = 1; e11 < arguments.length; e11++) {
      var o6 = arguments[e11];
      for (var i8 in o6) ({}).hasOwnProperty.call(o6, i8) && (t9[i8] = o6[i8]);
    }
    return t9;
  }, u.apply(null, arguments);
}
function d(t9, e11) {
  if (null == t9) return {};
  var o6 = {};
  for (var i8 in t9) if ({}.hasOwnProperty.call(t9, i8)) {
    if (-1 !== e11.indexOf(i8)) continue;
    o6[i8] = t9[i8];
  }
  return o6;
}
var s = ["outlineWidth", "outlineColor", "outlineOpacity"];
var b = function(t9) {
  return t9.outlineWidth, t9.outlineColor, t9.outlineOpacity, d(t9, s);
};
var y = ["axis.ticks.text", "axis.legend.text", "legends.title.text", "legends.text", "legends.ticks.text", "legends.title.text", "labels.text", "dots.text", "markers.text", "annotations.text"];
var W = function(t9, e11) {
  return u({}, e11, t9);
};
var O = function(t9, e11) {
  var o6 = (0, import_merge.default)({}, t9, e11);
  return y.forEach((function(t10) {
    (0, import_set.default)(o6, t10, W((0, import_get.default)(o6, t10), o6.text));
  })), o6;
};
var R = function(t9, e11) {
  return e11 ? (0, import_merge.default)({}, t9, e11) : t9;
};
var C = { background: "transparent", text: { fontFamily: "sans-serif", fontSize: 11, fill: "#333333", outlineWidth: 0, outlineColor: "#ffffff", outlineOpacity: 1 }, axis: { domain: { line: { stroke: "transparent", strokeWidth: 1 } }, ticks: { line: { stroke: "#777777", strokeWidth: 1 }, text: {} }, legend: { text: { fontSize: 12 } } }, grid: { line: { stroke: "#dddddd", strokeWidth: 1 } }, legends: { hidden: { symbol: { fill: "#333333", opacity: 0.6 }, text: { fill: "#333333", opacity: 0.6 } }, text: {}, ticks: { line: { stroke: "#777777", strokeWidth: 1 }, text: { fontSize: 10 } }, title: { text: {} } }, labels: { text: {} }, markers: { lineColor: "#000000", lineStrokeWidth: 1, text: {} }, dots: { text: {} }, tooltip: { container: { background: "white", color: "inherit", fontSize: "inherit", borderRadius: "2px", boxShadow: "0 1px 2px rgba(0, 0, 0, 0.25)", padding: "5px 9px" }, basic: { whiteSpace: "pre", display: "flex", alignItems: "center" }, chip: { marginRight: 7 }, table: {}, tableCell: { padding: "3px 5px" }, tableCellValue: { fontWeight: "bold" } }, crosshair: { line: { stroke: "#000000", strokeWidth: 1, strokeOpacity: 0.75, strokeDasharray: "6 6" } }, annotations: { text: { fontSize: 13, outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 }, link: { stroke: "#000000", strokeWidth: 1, outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 }, outline: { fill: "none", stroke: "#000000", strokeWidth: 2, outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 }, symbol: { fill: "#000000", outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 } } };
var L = function(e11) {
  return (0, import_react15.useMemo)((function() {
    return O(C, e11);
  }), [e11]);
};
var w = function(e11, o6) {
  return (0, import_react15.useMemo)((function() {
    return R(e11, o6);
  }), [e11, o6]);
};
var S = (0, import_react15.createContext)(null);
var j = {};
var z = function(t9) {
  var e11 = t9.theme, o6 = void 0 === e11 ? j : e11, i8 = t9.children, n9 = L(o6);
  return (0, import_jsx_runtime.jsx)(S.Provider, { value: n9, children: i8 });
};
var M = function() {
  var t9 = (0, import_react15.useContext)(S);
  if (null === t9) throw new Error("Unable to find the theme, did you forget to wrap your component with ThemeProvider?");
  return t9;
};

// node_modules/@nivo/tooltip/dist/nivo-tooltip.mjs
var import_react19 = __toESM(require_react(), 1);

// node_modules/@nivo/core/dist/nivo-core.mjs
var import_react18 = __toESM(require_react(), 1);
var import_isString = __toESM(require_isString(), 1);
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-virtualized-auto-sizer/dist/react-virtualized-auto-sizer.esm.js
var import_react16 = __toESM(require_react());
var windowObject;
if (typeof window !== "undefined") {
  windowObject = window;
} else if (typeof self !== "undefined") {
  windowObject = self;
} else {
  windowObject = global;
}
var cancelFrame = null;
var requestFrame = null;
var TIMEOUT_DURATION = 20;
var clearTimeoutFn = windowObject.clearTimeout;
var setTimeoutFn = windowObject.setTimeout;
var cancelAnimationFrameFn = windowObject.cancelAnimationFrame || windowObject.mozCancelAnimationFrame || windowObject.webkitCancelAnimationFrame;
var requestAnimationFrameFn = windowObject.requestAnimationFrame || windowObject.mozRequestAnimationFrame || windowObject.webkitRequestAnimationFrame;
if (cancelAnimationFrameFn == null || requestAnimationFrameFn == null) {
  cancelFrame = clearTimeoutFn;
  requestFrame = function requestAnimationFrameViaSetTimeout(callback) {
    return setTimeoutFn(callback, TIMEOUT_DURATION);
  };
} else {
  cancelFrame = function cancelFrame2([animationFrameID, timeoutID]) {
    cancelAnimationFrameFn(animationFrameID);
    clearTimeoutFn(timeoutID);
  };
  requestFrame = function requestAnimationFrameWithSetTimeoutFallback(callback) {
    const animationFrameID = requestAnimationFrameFn(function animationFrameCallback() {
      clearTimeoutFn(timeoutID);
      callback();
    });
    const timeoutID = setTimeoutFn(function timeoutCallback() {
      cancelAnimationFrameFn(animationFrameID);
      callback();
    }, TIMEOUT_DURATION);
    return [animationFrameID, timeoutID];
  };
}
function createDetectElementResize(nonce) {
  let animationKeyframes;
  let animationName;
  let animationStartEvent;
  let animationStyle;
  let checkTriggers;
  let resetTriggers;
  let scrollListener;
  const attachEvent = typeof document !== "undefined" && document.attachEvent;
  if (!attachEvent) {
    resetTriggers = function(element) {
      const triggers = element.__resizeTriggers__, expand = triggers.firstElementChild, contract = triggers.lastElementChild, expandChild = expand.firstElementChild;
      contract.scrollLeft = contract.scrollWidth;
      contract.scrollTop = contract.scrollHeight;
      expandChild.style.width = expand.offsetWidth + 1 + "px";
      expandChild.style.height = expand.offsetHeight + 1 + "px";
      expand.scrollLeft = expand.scrollWidth;
      expand.scrollTop = expand.scrollHeight;
    };
    checkTriggers = function(element) {
      return element.offsetWidth !== element.__resizeLast__.width || element.offsetHeight !== element.__resizeLast__.height;
    };
    scrollListener = function(e11) {
      if (e11.target.className && typeof e11.target.className.indexOf === "function" && e11.target.className.indexOf("contract-trigger") < 0 && e11.target.className.indexOf("expand-trigger") < 0) {
        return;
      }
      const element = this;
      resetTriggers(this);
      if (this.__resizeRAF__) {
        cancelFrame(this.__resizeRAF__);
      }
      this.__resizeRAF__ = requestFrame(function animationFrame() {
        if (checkTriggers(element)) {
          element.__resizeLast__.width = element.offsetWidth;
          element.__resizeLast__.height = element.offsetHeight;
          element.__resizeListeners__.forEach(function forEachResizeListener(fn3) {
            fn3.call(element, e11);
          });
        }
      });
    };
    let animation = false;
    let keyframeprefix = "";
    animationStartEvent = "animationstart";
    const domPrefixes = "Webkit Moz O ms".split(" ");
    let startEvents = "webkitAnimationStart animationstart oAnimationStart MSAnimationStart".split(" ");
    let pfx = "";
    {
      const elm = document.createElement("fakeelement");
      if (elm.style.animationName !== void 0) {
        animation = true;
      }
      if (animation === false) {
        for (let i8 = 0; i8 < domPrefixes.length; i8++) {
          if (elm.style[domPrefixes[i8] + "AnimationName"] !== void 0) {
            pfx = domPrefixes[i8];
            keyframeprefix = "-" + pfx.toLowerCase() + "-";
            animationStartEvent = startEvents[i8];
            animation = true;
            break;
          }
        }
      }
    }
    animationName = "resizeanim";
    animationKeyframes = "@" + keyframeprefix + "keyframes " + animationName + " { from { opacity: 0; } to { opacity: 0; } } ";
    animationStyle = keyframeprefix + "animation: 1ms " + animationName + "; ";
  }
  const createStyles = function(doc) {
    if (!doc.getElementById("detectElementResize")) {
      const css = (animationKeyframes ? animationKeyframes : "") + ".resize-triggers { " + (animationStyle ? animationStyle : "") + 'visibility: hidden; opacity: 0; } .resize-triggers, .resize-triggers > div, .contract-trigger:before { content: " "; display: block; position: absolute; top: 0; left: 0; height: 100%; width: 100%; overflow: hidden; z-index: -1; } .resize-triggers > div { background: #eee; overflow: auto; } .contract-trigger:before { width: 200%; height: 200%; }', head = doc.head || doc.getElementsByTagName("head")[0], style = doc.createElement("style");
      style.id = "detectElementResize";
      style.type = "text/css";
      if (nonce != null) {
        style.setAttribute("nonce", nonce);
      }
      if (style.styleSheet) {
        style.styleSheet.cssText = css;
      } else {
        style.appendChild(doc.createTextNode(css));
      }
      head.appendChild(style);
    }
  };
  const addResizeListener = function(element, fn3) {
    if (attachEvent) {
      element.attachEvent("onresize", fn3);
    } else {
      if (!element.__resizeTriggers__) {
        const doc = element.ownerDocument;
        const elementStyle = windowObject.getComputedStyle(element);
        if (elementStyle && elementStyle.position === "static") {
          element.style.position = "relative";
        }
        createStyles(doc);
        element.__resizeLast__ = {};
        element.__resizeListeners__ = [];
        (element.__resizeTriggers__ = doc.createElement("div")).className = "resize-triggers";
        const expandTrigger = doc.createElement("div");
        expandTrigger.className = "expand-trigger";
        expandTrigger.appendChild(doc.createElement("div"));
        const contractTrigger = doc.createElement("div");
        contractTrigger.className = "contract-trigger";
        element.__resizeTriggers__.appendChild(expandTrigger);
        element.__resizeTriggers__.appendChild(contractTrigger);
        element.appendChild(element.__resizeTriggers__);
        resetTriggers(element);
        element.addEventListener("scroll", scrollListener, true);
        if (animationStartEvent) {
          element.__resizeTriggers__.__animationListener__ = function animationListener(e11) {
            if (e11.animationName === animationName) {
              resetTriggers(element);
            }
          };
          element.__resizeTriggers__.addEventListener(animationStartEvent, element.__resizeTriggers__.__animationListener__);
        }
      }
      element.__resizeListeners__.push(fn3);
    }
  };
  const removeResizeListener = function(element, fn3) {
    if (attachEvent) {
      element.detachEvent("onresize", fn3);
    } else {
      element.__resizeListeners__.splice(element.__resizeListeners__.indexOf(fn3), 1);
      if (!element.__resizeListeners__.length) {
        element.removeEventListener("scroll", scrollListener, true);
        if (element.__resizeTriggers__.__animationListener__) {
          element.__resizeTriggers__.removeEventListener(animationStartEvent, element.__resizeTriggers__.__animationListener__);
          element.__resizeTriggers__.__animationListener__ = null;
        }
        try {
          element.__resizeTriggers__ = !element.removeChild(element.__resizeTriggers__);
        } catch (e11) {
        }
      }
    }
  };
  return {
    addResizeListener,
    removeResizeListener
  };
}
var AutoSizer = class extends import_react16.Component {
  constructor(...args) {
    super(...args);
    this.state = {
      height: this.props.defaultHeight || 0,
      width: this.props.defaultWidth || 0
    };
    this._autoSizer = null;
    this._detectElementResize = null;
    this._didLogDeprecationWarning = false;
    this._parentNode = null;
    this._resizeObserver = null;
    this._timeoutId = null;
    this._onResize = () => {
      this._timeoutId = null;
      const {
        disableHeight,
        disableWidth,
        onResize: onResize2
      } = this.props;
      if (this._parentNode) {
        const style = window.getComputedStyle(this._parentNode) || {};
        const paddingLeft = parseFloat(style.paddingLeft || "0");
        const paddingRight = parseFloat(style.paddingRight || "0");
        const paddingTop = parseFloat(style.paddingTop || "0");
        const paddingBottom = parseFloat(style.paddingBottom || "0");
        const rect = this._parentNode.getBoundingClientRect();
        const height = rect.height - paddingTop - paddingBottom;
        const width = rect.width - paddingLeft - paddingRight;
        if (!disableHeight && this.state.height !== height || !disableWidth && this.state.width !== width) {
          this.setState({
            height,
            width
          });
          const maybeLogDeprecationWarning = () => {
            if (!this._didLogDeprecationWarning) {
              this._didLogDeprecationWarning = true;
              console.warn("scaledWidth and scaledHeight parameters have been deprecated; use width and height instead");
            }
          };
          if (typeof onResize2 === "function") {
            onResize2({
              height,
              width,
              // TODO Remove these params in the next major release
              get scaledHeight() {
                maybeLogDeprecationWarning();
                return height;
              },
              get scaledWidth() {
                maybeLogDeprecationWarning();
                return width;
              }
            });
          }
        }
      }
    };
    this._setRef = (autoSizer) => {
      this._autoSizer = autoSizer;
    };
  }
  componentDidMount() {
    const {
      nonce
    } = this.props;
    const parentNode = this._autoSizer ? this._autoSizer.parentNode : null;
    if (parentNode != null && parentNode.ownerDocument && parentNode.ownerDocument.defaultView && parentNode instanceof parentNode.ownerDocument.defaultView.HTMLElement) {
      this._parentNode = parentNode;
      const ResizeObserverInstance = parentNode.ownerDocument.defaultView.ResizeObserver;
      if (ResizeObserverInstance != null) {
        this._resizeObserver = new ResizeObserverInstance(() => {
          this._timeoutId = setTimeout(this._onResize, 0);
        });
        this._resizeObserver.observe(parentNode);
      } else {
        this._detectElementResize = createDetectElementResize(nonce);
        this._detectElementResize.addResizeListener(parentNode, this._onResize);
      }
      this._onResize();
    }
  }
  componentWillUnmount() {
    if (this._parentNode) {
      if (this._detectElementResize) {
        this._detectElementResize.removeResizeListener(this._parentNode, this._onResize);
      }
      if (this._timeoutId !== null) {
        clearTimeout(this._timeoutId);
      }
      if (this._resizeObserver) {
        this._resizeObserver.disconnect();
      }
    }
  }
  render() {
    const {
      children,
      defaultHeight,
      defaultWidth,
      disableHeight = false,
      disableWidth = false,
      doNotBailOutOnEmptyChildren = false,
      nonce,
      onResize: onResize2,
      style = {},
      tagName = "div",
      ...rest
    } = this.props;
    const {
      height,
      width
    } = this.state;
    const outerStyle = {
      overflow: "visible"
    };
    const childParams = {};
    let bailoutOnChildren = false;
    if (!disableHeight) {
      if (height === 0) {
        bailoutOnChildren = true;
      }
      outerStyle.height = 0;
      childParams.height = height;
      childParams.scaledHeight = height;
    }
    if (!disableWidth) {
      if (width === 0) {
        bailoutOnChildren = true;
      }
      outerStyle.width = 0;
      childParams.width = width;
      childParams.scaledWidth = width;
    }
    if (doNotBailOutOnEmptyChildren) {
      bailoutOnChildren = false;
    }
    return (0, import_react16.createElement)(tagName, {
      ref: this._setRef,
      style: {
        ...outerStyle,
        ...style
      },
      ...rest
    }, !bailoutOnChildren && children(childParams));
  }
};

// node_modules/use-debounce/dist/index.module.js
var import_react17 = __toESM(require_react());
function c(e11, u8, c9, i8) {
  var a5 = this, o6 = (0, import_react17.useRef)(null), f2 = (0, import_react17.useRef)(0), l5 = (0, import_react17.useRef)(0), v3 = (0, import_react17.useRef)(null), m4 = (0, import_react17.useRef)([]), d4 = (0, import_react17.useRef)(), g2 = (0, import_react17.useRef)(), p3 = (0, import_react17.useRef)(e11), w5 = (0, import_react17.useRef)(true);
  p3.current = e11;
  var s5 = "undefined" != typeof window, x4 = !u8 && 0 !== u8 && s5;
  if ("function" != typeof e11) throw new TypeError("Expected a function");
  u8 = +u8 || 0;
  var h2 = !!(c9 = c9 || {}).leading, y5 = !("trailing" in c9) || !!c9.trailing, F3 = "maxWait" in c9, A5 = "debounceOnServer" in c9 && !!c9.debounceOnServer, D5 = F3 ? Math.max(+c9.maxWait || 0, u8) : null;
  (0, import_react17.useEffect)(function() {
    return w5.current = true, function() {
      w5.current = false;
    };
  }, []);
  var T4 = (0, import_react17.useMemo)(function() {
    var r8 = function(r9) {
      var n10 = m4.current, t10 = d4.current;
      return m4.current = d4.current = null, f2.current = r9, l5.current = l5.current || r9, g2.current = p3.current.apply(t10, n10);
    }, n9 = function(r9, n10) {
      x4 && cancelAnimationFrame(v3.current), v3.current = x4 ? requestAnimationFrame(r9) : setTimeout(r9, n10);
    }, t9 = function(r9) {
      if (!w5.current) return false;
      var n10 = r9 - o6.current;
      return !o6.current || n10 >= u8 || n10 < 0 || F3 && r9 - f2.current >= D5;
    }, e12 = function(n10) {
      return v3.current = null, y5 && m4.current ? r8(n10) : (m4.current = d4.current = null, g2.current);
    }, c10 = function r9() {
      var c11 = Date.now();
      if (h2 && l5.current === f2.current && T5(), t9(c11)) return e12(c11);
      if (w5.current) {
        var i9 = u8 - (c11 - o6.current), a6 = F3 ? Math.min(i9, D5 - (c11 - f2.current)) : i9;
        n9(r9, a6);
      }
    }, T5 = function() {
      i8 && i8({});
    }, W6 = function() {
      if (s5 || A5) {
        var e13 = Date.now(), i9 = t9(e13);
        if (m4.current = [].slice.call(arguments), d4.current = a5, o6.current = e13, i9) {
          if (!v3.current && w5.current) return f2.current = o6.current, n9(c10, u8), h2 ? r8(o6.current) : g2.current;
          if (F3) return n9(c10, u8), r8(o6.current);
        }
        return v3.current || n9(c10, u8), g2.current;
      }
    };
    return W6.cancel = function() {
      var r9 = v3.current;
      r9 && (x4 ? cancelAnimationFrame(v3.current) : clearTimeout(v3.current)), f2.current = 0, m4.current = o6.current = d4.current = v3.current = null, r9 && i8 && i8({});
    }, W6.isPending = function() {
      return !!v3.current;
    }, W6.flush = function() {
      return v3.current ? e12(Date.now()) : g2.current;
    }, W6;
  }, [h2, F3, u8, D5, y5, x4, s5, A5, i8]);
  return T4;
}
function i2(r8, n9) {
  return r8 === n9;
}
function a(n9, t9, a5) {
  var o6 = a5 && a5.equalityFn || i2, f2 = (0, import_react17.useRef)(n9), l5 = (0, import_react17.useState)({})[1], v3 = c((0, import_react17.useCallback)(function(r8) {
    f2.current = r8, l5({});
  }, [l5]), t9, a5, l5), m4 = (0, import_react17.useRef)(n9);
  return o6(m4.current, n9) || (v3(n9), m4.current = n9), [f2.current, v3];
}

// node_modules/d3-color/src/define.js
function define_default(constructor, factory, prototype) {
  constructor.prototype = factory.prototype = prototype;
  prototype.constructor = constructor;
}
function extend(parent, definition) {
  var prototype = Object.create(parent.prototype);
  for (var key in definition) prototype[key] = definition[key];
  return prototype;
}

// node_modules/d3-color/src/color.js
function Color() {
}
var darker = 0.7;
var brighter = 1 / darker;
var reI = "\\s*([+-]?\\d+)\\s*";
var reN = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*";
var reP = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*";
var reHex = /^#([0-9a-f]{3,8})$/;
var reRgbInteger = new RegExp(`^rgb\\(${reI},${reI},${reI}\\)$`);
var reRgbPercent = new RegExp(`^rgb\\(${reP},${reP},${reP}\\)$`);
var reRgbaInteger = new RegExp(`^rgba\\(${reI},${reI},${reI},${reN}\\)$`);
var reRgbaPercent = new RegExp(`^rgba\\(${reP},${reP},${reP},${reN}\\)$`);
var reHslPercent = new RegExp(`^hsl\\(${reN},${reP},${reP}\\)$`);
var reHslaPercent = new RegExp(`^hsla\\(${reN},${reP},${reP},${reN}\\)$`);
var named = {
  aliceblue: 15792383,
  antiquewhite: 16444375,
  aqua: 65535,
  aquamarine: 8388564,
  azure: 15794175,
  beige: 16119260,
  bisque: 16770244,
  black: 0,
  blanchedalmond: 16772045,
  blue: 255,
  blueviolet: 9055202,
  brown: 10824234,
  burlywood: 14596231,
  cadetblue: 6266528,
  chartreuse: 8388352,
  chocolate: 13789470,
  coral: 16744272,
  cornflowerblue: 6591981,
  cornsilk: 16775388,
  crimson: 14423100,
  cyan: 65535,
  darkblue: 139,
  darkcyan: 35723,
  darkgoldenrod: 12092939,
  darkgray: 11119017,
  darkgreen: 25600,
  darkgrey: 11119017,
  darkkhaki: 12433259,
  darkmagenta: 9109643,
  darkolivegreen: 5597999,
  darkorange: 16747520,
  darkorchid: 10040012,
  darkred: 9109504,
  darksalmon: 15308410,
  darkseagreen: 9419919,
  darkslateblue: 4734347,
  darkslategray: 3100495,
  darkslategrey: 3100495,
  darkturquoise: 52945,
  darkviolet: 9699539,
  deeppink: 16716947,
  deepskyblue: 49151,
  dimgray: 6908265,
  dimgrey: 6908265,
  dodgerblue: 2003199,
  firebrick: 11674146,
  floralwhite: 16775920,
  forestgreen: 2263842,
  fuchsia: 16711935,
  gainsboro: 14474460,
  ghostwhite: 16316671,
  gold: 16766720,
  goldenrod: 14329120,
  gray: 8421504,
  green: 32768,
  greenyellow: 11403055,
  grey: 8421504,
  honeydew: 15794160,
  hotpink: 16738740,
  indianred: 13458524,
  indigo: 4915330,
  ivory: 16777200,
  khaki: 15787660,
  lavender: 15132410,
  lavenderblush: 16773365,
  lawngreen: 8190976,
  lemonchiffon: 16775885,
  lightblue: 11393254,
  lightcoral: 15761536,
  lightcyan: 14745599,
  lightgoldenrodyellow: 16448210,
  lightgray: 13882323,
  lightgreen: 9498256,
  lightgrey: 13882323,
  lightpink: 16758465,
  lightsalmon: 16752762,
  lightseagreen: 2142890,
  lightskyblue: 8900346,
  lightslategray: 7833753,
  lightslategrey: 7833753,
  lightsteelblue: 11584734,
  lightyellow: 16777184,
  lime: 65280,
  limegreen: 3329330,
  linen: 16445670,
  magenta: 16711935,
  maroon: 8388608,
  mediumaquamarine: 6737322,
  mediumblue: 205,
  mediumorchid: 12211667,
  mediumpurple: 9662683,
  mediumseagreen: 3978097,
  mediumslateblue: 8087790,
  mediumspringgreen: 64154,
  mediumturquoise: 4772300,
  mediumvioletred: 13047173,
  midnightblue: 1644912,
  mintcream: 16121850,
  mistyrose: 16770273,
  moccasin: 16770229,
  navajowhite: 16768685,
  navy: 128,
  oldlace: 16643558,
  olive: 8421376,
  olivedrab: 7048739,
  orange: 16753920,
  orangered: 16729344,
  orchid: 14315734,
  palegoldenrod: 15657130,
  palegreen: 10025880,
  paleturquoise: 11529966,
  palevioletred: 14381203,
  papayawhip: 16773077,
  peachpuff: 16767673,
  peru: 13468991,
  pink: 16761035,
  plum: 14524637,
  powderblue: 11591910,
  purple: 8388736,
  rebeccapurple: 6697881,
  red: 16711680,
  rosybrown: 12357519,
  royalblue: 4286945,
  saddlebrown: 9127187,
  salmon: 16416882,
  sandybrown: 16032864,
  seagreen: 3050327,
  seashell: 16774638,
  sienna: 10506797,
  silver: 12632256,
  skyblue: 8900331,
  slateblue: 6970061,
  slategray: 7372944,
  slategrey: 7372944,
  snow: 16775930,
  springgreen: 65407,
  steelblue: 4620980,
  tan: 13808780,
  teal: 32896,
  thistle: 14204888,
  tomato: 16737095,
  turquoise: 4251856,
  violet: 15631086,
  wheat: 16113331,
  white: 16777215,
  whitesmoke: 16119285,
  yellow: 16776960,
  yellowgreen: 10145074
};
define_default(Color, color, {
  copy(channels) {
    return Object.assign(new this.constructor(), this, channels);
  },
  displayable() {
    return this.rgb().displayable();
  },
  hex: color_formatHex,
  // Deprecated! Use color.formatHex.
  formatHex: color_formatHex,
  formatHex8: color_formatHex8,
  formatHsl: color_formatHsl,
  formatRgb: color_formatRgb,
  toString: color_formatRgb
});
function color_formatHex() {
  return this.rgb().formatHex();
}
function color_formatHex8() {
  return this.rgb().formatHex8();
}
function color_formatHsl() {
  return hslConvert(this).formatHsl();
}
function color_formatRgb() {
  return this.rgb().formatRgb();
}
function color(format2) {
  var m4, l5;
  format2 = (format2 + "").trim().toLowerCase();
  return (m4 = reHex.exec(format2)) ? (l5 = m4[1].length, m4 = parseInt(m4[1], 16), l5 === 6 ? rgbn(m4) : l5 === 3 ? new Rgb(m4 >> 8 & 15 | m4 >> 4 & 240, m4 >> 4 & 15 | m4 & 240, (m4 & 15) << 4 | m4 & 15, 1) : l5 === 8 ? rgba2(m4 >> 24 & 255, m4 >> 16 & 255, m4 >> 8 & 255, (m4 & 255) / 255) : l5 === 4 ? rgba2(m4 >> 12 & 15 | m4 >> 8 & 240, m4 >> 8 & 15 | m4 >> 4 & 240, m4 >> 4 & 15 | m4 & 240, ((m4 & 15) << 4 | m4 & 15) / 255) : null) : (m4 = reRgbInteger.exec(format2)) ? new Rgb(m4[1], m4[2], m4[3], 1) : (m4 = reRgbPercent.exec(format2)) ? new Rgb(m4[1] * 255 / 100, m4[2] * 255 / 100, m4[3] * 255 / 100, 1) : (m4 = reRgbaInteger.exec(format2)) ? rgba2(m4[1], m4[2], m4[3], m4[4]) : (m4 = reRgbaPercent.exec(format2)) ? rgba2(m4[1] * 255 / 100, m4[2] * 255 / 100, m4[3] * 255 / 100, m4[4]) : (m4 = reHslPercent.exec(format2)) ? hsla2(m4[1], m4[2] / 100, m4[3] / 100, 1) : (m4 = reHslaPercent.exec(format2)) ? hsla2(m4[1], m4[2] / 100, m4[3] / 100, m4[4]) : named.hasOwnProperty(format2) ? rgbn(named[format2]) : format2 === "transparent" ? new Rgb(NaN, NaN, NaN, 0) : null;
}
function rgbn(n9) {
  return new Rgb(n9 >> 16 & 255, n9 >> 8 & 255, n9 & 255, 1);
}
function rgba2(r8, g2, b6, a5) {
  if (a5 <= 0) r8 = g2 = b6 = NaN;
  return new Rgb(r8, g2, b6, a5);
}
function rgbConvert(o6) {
  if (!(o6 instanceof Color)) o6 = color(o6);
  if (!o6) return new Rgb();
  o6 = o6.rgb();
  return new Rgb(o6.r, o6.g, o6.b, o6.opacity);
}
function rgb2(r8, g2, b6, opacity) {
  return arguments.length === 1 ? rgbConvert(r8) : new Rgb(r8, g2, b6, opacity == null ? 1 : opacity);
}
function Rgb(r8, g2, b6, opacity) {
  this.r = +r8;
  this.g = +g2;
  this.b = +b6;
  this.opacity = +opacity;
}
define_default(Rgb, rgb2, extend(Color, {
  brighter(k4) {
    k4 = k4 == null ? brighter : Math.pow(brighter, k4);
    return new Rgb(this.r * k4, this.g * k4, this.b * k4, this.opacity);
  },
  darker(k4) {
    k4 = k4 == null ? darker : Math.pow(darker, k4);
    return new Rgb(this.r * k4, this.g * k4, this.b * k4, this.opacity);
  },
  rgb() {
    return this;
  },
  clamp() {
    return new Rgb(clampi(this.r), clampi(this.g), clampi(this.b), clampa(this.opacity));
  },
  displayable() {
    return -0.5 <= this.r && this.r < 255.5 && (-0.5 <= this.g && this.g < 255.5) && (-0.5 <= this.b && this.b < 255.5) && (0 <= this.opacity && this.opacity <= 1);
  },
  hex: rgb_formatHex,
  // Deprecated! Use color.formatHex.
  formatHex: rgb_formatHex,
  formatHex8: rgb_formatHex8,
  formatRgb: rgb_formatRgb,
  toString: rgb_formatRgb
}));
function rgb_formatHex() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}`;
}
function rgb_formatHex8() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}${hex((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
}
function rgb_formatRgb() {
  const a5 = clampa(this.opacity);
  return `${a5 === 1 ? "rgb(" : "rgba("}${clampi(this.r)}, ${clampi(this.g)}, ${clampi(this.b)}${a5 === 1 ? ")" : `, ${a5})`}`;
}
function clampa(opacity) {
  return isNaN(opacity) ? 1 : Math.max(0, Math.min(1, opacity));
}
function clampi(value) {
  return Math.max(0, Math.min(255, Math.round(value) || 0));
}
function hex(value) {
  value = clampi(value);
  return (value < 16 ? "0" : "") + value.toString(16);
}
function hsla2(h2, s5, l5, a5) {
  if (a5 <= 0) h2 = s5 = l5 = NaN;
  else if (l5 <= 0 || l5 >= 1) h2 = s5 = NaN;
  else if (s5 <= 0) h2 = NaN;
  return new Hsl(h2, s5, l5, a5);
}
function hslConvert(o6) {
  if (o6 instanceof Hsl) return new Hsl(o6.h, o6.s, o6.l, o6.opacity);
  if (!(o6 instanceof Color)) o6 = color(o6);
  if (!o6) return new Hsl();
  if (o6 instanceof Hsl) return o6;
  o6 = o6.rgb();
  var r8 = o6.r / 255, g2 = o6.g / 255, b6 = o6.b / 255, min3 = Math.min(r8, g2, b6), max3 = Math.max(r8, g2, b6), h2 = NaN, s5 = max3 - min3, l5 = (max3 + min3) / 2;
  if (s5) {
    if (r8 === max3) h2 = (g2 - b6) / s5 + (g2 < b6) * 6;
    else if (g2 === max3) h2 = (b6 - r8) / s5 + 2;
    else h2 = (r8 - g2) / s5 + 4;
    s5 /= l5 < 0.5 ? max3 + min3 : 2 - max3 - min3;
    h2 *= 60;
  } else {
    s5 = l5 > 0 && l5 < 1 ? 0 : h2;
  }
  return new Hsl(h2, s5, l5, o6.opacity);
}
function hsl2(h2, s5, l5, opacity) {
  return arguments.length === 1 ? hslConvert(h2) : new Hsl(h2, s5, l5, opacity == null ? 1 : opacity);
}
function Hsl(h2, s5, l5, opacity) {
  this.h = +h2;
  this.s = +s5;
  this.l = +l5;
  this.opacity = +opacity;
}
define_default(Hsl, hsl2, extend(Color, {
  brighter(k4) {
    k4 = k4 == null ? brighter : Math.pow(brighter, k4);
    return new Hsl(this.h, this.s, this.l * k4, this.opacity);
  },
  darker(k4) {
    k4 = k4 == null ? darker : Math.pow(darker, k4);
    return new Hsl(this.h, this.s, this.l * k4, this.opacity);
  },
  rgb() {
    var h2 = this.h % 360 + (this.h < 0) * 360, s5 = isNaN(h2) || isNaN(this.s) ? 0 : this.s, l5 = this.l, m22 = l5 + (l5 < 0.5 ? l5 : 1 - l5) * s5, m1 = 2 * l5 - m22;
    return new Rgb(
      hsl2rgb(h2 >= 240 ? h2 - 240 : h2 + 120, m1, m22),
      hsl2rgb(h2, m1, m22),
      hsl2rgb(h2 < 120 ? h2 + 240 : h2 - 120, m1, m22),
      this.opacity
    );
  },
  clamp() {
    return new Hsl(clamph(this.h), clampt(this.s), clampt(this.l), clampa(this.opacity));
  },
  displayable() {
    return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && (0 <= this.l && this.l <= 1) && (0 <= this.opacity && this.opacity <= 1);
  },
  formatHsl() {
    const a5 = clampa(this.opacity);
    return `${a5 === 1 ? "hsl(" : "hsla("}${clamph(this.h)}, ${clampt(this.s) * 100}%, ${clampt(this.l) * 100}%${a5 === 1 ? ")" : `, ${a5})`}`;
  }
}));
function clamph(value) {
  value = (value || 0) % 360;
  return value < 0 ? value + 360 : value;
}
function clampt(value) {
  return Math.max(0, Math.min(1, value || 0));
}
function hsl2rgb(h2, m1, m22) {
  return (h2 < 60 ? m1 + (m22 - m1) * h2 / 60 : h2 < 180 ? m22 : h2 < 240 ? m1 + (m22 - m1) * (240 - h2) / 60 : m1) * 255;
}

// node_modules/d3-color/src/math.js
var radians = Math.PI / 180;
var degrees = 180 / Math.PI;

// node_modules/d3-color/src/lab.js
var K = 18;
var Xn = 0.96422;
var Yn = 1;
var Zn = 0.82521;
var t0 = 4 / 29;
var t1 = 6 / 29;
var t22 = 3 * t1 * t1;
var t3 = t1 * t1 * t1;
function labConvert(o6) {
  if (o6 instanceof Lab) return new Lab(o6.l, o6.a, o6.b, o6.opacity);
  if (o6 instanceof Hcl) return hcl2lab(o6);
  if (!(o6 instanceof Rgb)) o6 = rgbConvert(o6);
  var r8 = rgb2lrgb(o6.r), g2 = rgb2lrgb(o6.g), b6 = rgb2lrgb(o6.b), y5 = xyz2lab((0.2225045 * r8 + 0.7168786 * g2 + 0.0606169 * b6) / Yn), x4, z5;
  if (r8 === g2 && g2 === b6) x4 = z5 = y5;
  else {
    x4 = xyz2lab((0.4360747 * r8 + 0.3850649 * g2 + 0.1430804 * b6) / Xn);
    z5 = xyz2lab((0.0139322 * r8 + 0.0971045 * g2 + 0.7141733 * b6) / Zn);
  }
  return new Lab(116 * y5 - 16, 500 * (x4 - y5), 200 * (y5 - z5), o6.opacity);
}
function lab(l5, a5, b6, opacity) {
  return arguments.length === 1 ? labConvert(l5) : new Lab(l5, a5, b6, opacity == null ? 1 : opacity);
}
function Lab(l5, a5, b6, opacity) {
  this.l = +l5;
  this.a = +a5;
  this.b = +b6;
  this.opacity = +opacity;
}
define_default(Lab, lab, extend(Color, {
  brighter(k4) {
    return new Lab(this.l + K * (k4 == null ? 1 : k4), this.a, this.b, this.opacity);
  },
  darker(k4) {
    return new Lab(this.l - K * (k4 == null ? 1 : k4), this.a, this.b, this.opacity);
  },
  rgb() {
    var y5 = (this.l + 16) / 116, x4 = isNaN(this.a) ? y5 : y5 + this.a / 500, z5 = isNaN(this.b) ? y5 : y5 - this.b / 200;
    x4 = Xn * lab2xyz(x4);
    y5 = Yn * lab2xyz(y5);
    z5 = Zn * lab2xyz(z5);
    return new Rgb(
      lrgb2rgb(3.1338561 * x4 - 1.6168667 * y5 - 0.4906146 * z5),
      lrgb2rgb(-0.9787684 * x4 + 1.9161415 * y5 + 0.033454 * z5),
      lrgb2rgb(0.0719453 * x4 - 0.2289914 * y5 + 1.4052427 * z5),
      this.opacity
    );
  }
}));
function xyz2lab(t9) {
  return t9 > t3 ? Math.pow(t9, 1 / 3) : t9 / t22 + t0;
}
function lab2xyz(t9) {
  return t9 > t1 ? t9 * t9 * t9 : t22 * (t9 - t0);
}
function lrgb2rgb(x4) {
  return 255 * (x4 <= 31308e-7 ? 12.92 * x4 : 1.055 * Math.pow(x4, 1 / 2.4) - 0.055);
}
function rgb2lrgb(x4) {
  return (x4 /= 255) <= 0.04045 ? x4 / 12.92 : Math.pow((x4 + 0.055) / 1.055, 2.4);
}
function hclConvert(o6) {
  if (o6 instanceof Hcl) return new Hcl(o6.h, o6.c, o6.l, o6.opacity);
  if (!(o6 instanceof Lab)) o6 = labConvert(o6);
  if (o6.a === 0 && o6.b === 0) return new Hcl(NaN, 0 < o6.l && o6.l < 100 ? 0 : NaN, o6.l, o6.opacity);
  var h2 = Math.atan2(o6.b, o6.a) * degrees;
  return new Hcl(h2 < 0 ? h2 + 360 : h2, Math.sqrt(o6.a * o6.a + o6.b * o6.b), o6.l, o6.opacity);
}
function hcl(h2, c9, l5, opacity) {
  return arguments.length === 1 ? hclConvert(h2) : new Hcl(h2, c9, l5, opacity == null ? 1 : opacity);
}
function Hcl(h2, c9, l5, opacity) {
  this.h = +h2;
  this.c = +c9;
  this.l = +l5;
  this.opacity = +opacity;
}
function hcl2lab(o6) {
  if (isNaN(o6.h)) return new Lab(o6.l, 0, 0, o6.opacity);
  var h2 = o6.h * radians;
  return new Lab(o6.l, Math.cos(h2) * o6.c, Math.sin(h2) * o6.c, o6.opacity);
}
define_default(Hcl, hcl, extend(Color, {
  brighter(k4) {
    return new Hcl(this.h, this.c, this.l + K * (k4 == null ? 1 : k4), this.opacity);
  },
  darker(k4) {
    return new Hcl(this.h, this.c, this.l - K * (k4 == null ? 1 : k4), this.opacity);
  },
  rgb() {
    return hcl2lab(this).rgb();
  }
}));

// node_modules/d3-color/src/cubehelix.js
var A = -0.14861;
var B = 1.78277;
var C2 = -0.29227;
var D = -0.90649;
var E = 1.97294;
var ED = E * D;
var EB = E * B;
var BC_DA = B * C2 - D * A;
function cubehelixConvert(o6) {
  if (o6 instanceof Cubehelix) return new Cubehelix(o6.h, o6.s, o6.l, o6.opacity);
  if (!(o6 instanceof Rgb)) o6 = rgbConvert(o6);
  var r8 = o6.r / 255, g2 = o6.g / 255, b6 = o6.b / 255, l5 = (BC_DA * b6 + ED * r8 - EB * g2) / (BC_DA + ED - EB), bl = b6 - l5, k4 = (E * (g2 - l5) - C2 * bl) / D, s5 = Math.sqrt(k4 * k4 + bl * bl) / (E * l5 * (1 - l5)), h2 = s5 ? Math.atan2(k4, bl) * degrees - 120 : NaN;
  return new Cubehelix(h2 < 0 ? h2 + 360 : h2, s5, l5, o6.opacity);
}
function cubehelix(h2, s5, l5, opacity) {
  return arguments.length === 1 ? cubehelixConvert(h2) : new Cubehelix(h2, s5, l5, opacity == null ? 1 : opacity);
}
function Cubehelix(h2, s5, l5, opacity) {
  this.h = +h2;
  this.s = +s5;
  this.l = +l5;
  this.opacity = +opacity;
}
define_default(Cubehelix, cubehelix, extend(Color, {
  brighter(k4) {
    k4 = k4 == null ? brighter : Math.pow(brighter, k4);
    return new Cubehelix(this.h, this.s, this.l * k4, this.opacity);
  },
  darker(k4) {
    k4 = k4 == null ? darker : Math.pow(darker, k4);
    return new Cubehelix(this.h, this.s, this.l * k4, this.opacity);
  },
  rgb() {
    var h2 = isNaN(this.h) ? 0 : (this.h + 120) * radians, l5 = +this.l, a5 = isNaN(this.s) ? 0 : this.s * l5 * (1 - l5), cosh2 = Math.cos(h2), sinh2 = Math.sin(h2);
    return new Rgb(
      255 * (l5 + a5 * (A * cosh2 + B * sinh2)),
      255 * (l5 + a5 * (C2 * cosh2 + D * sinh2)),
      255 * (l5 + a5 * (E * cosh2)),
      this.opacity
    );
  }
}));

// node_modules/d3-interpolate/src/basis.js
function basis(t14, v0, v1, v22, v3) {
  var t23 = t14 * t14, t32 = t23 * t14;
  return ((1 - 3 * t14 + 3 * t23 - t32) * v0 + (4 - 6 * t23 + 3 * t32) * v1 + (1 + 3 * t14 + 3 * t23 - 3 * t32) * v22 + t32 * v3) / 6;
}
function basis_default(values) {
  var n9 = values.length - 1;
  return function(t9) {
    var i8 = t9 <= 0 ? t9 = 0 : t9 >= 1 ? (t9 = 1, n9 - 1) : Math.floor(t9 * n9), v1 = values[i8], v22 = values[i8 + 1], v0 = i8 > 0 ? values[i8 - 1] : 2 * v1 - v22, v3 = i8 < n9 - 1 ? values[i8 + 2] : 2 * v22 - v1;
    return basis((t9 - i8 / n9) * n9, v0, v1, v22, v3);
  };
}

// node_modules/d3-interpolate/src/basisClosed.js
function basisClosed_default(values) {
  var n9 = values.length;
  return function(t9) {
    var i8 = Math.floor(((t9 %= 1) < 0 ? ++t9 : t9) * n9), v0 = values[(i8 + n9 - 1) % n9], v1 = values[i8 % n9], v22 = values[(i8 + 1) % n9], v3 = values[(i8 + 2) % n9];
    return basis((t9 - i8 / n9) * n9, v0, v1, v22, v3);
  };
}

// node_modules/d3-interpolate/src/constant.js
var constant_default = (x4) => () => x4;

// node_modules/d3-interpolate/src/color.js
function linear(a5, d4) {
  return function(t9) {
    return a5 + t9 * d4;
  };
}
function exponential(a5, b6, y5) {
  return a5 = Math.pow(a5, y5), b6 = Math.pow(b6, y5) - a5, y5 = 1 / y5, function(t9) {
    return Math.pow(a5 + t9 * b6, y5);
  };
}
function hue(a5, b6) {
  var d4 = b6 - a5;
  return d4 ? linear(a5, d4 > 180 || d4 < -180 ? d4 - 360 * Math.round(d4 / 360) : d4) : constant_default(isNaN(a5) ? b6 : a5);
}
function gamma(y5) {
  return (y5 = +y5) === 1 ? nogamma : function(a5, b6) {
    return b6 - a5 ? exponential(a5, b6, y5) : constant_default(isNaN(a5) ? b6 : a5);
  };
}
function nogamma(a5, b6) {
  var d4 = b6 - a5;
  return d4 ? linear(a5, d4) : constant_default(isNaN(a5) ? b6 : a5);
}

// node_modules/d3-interpolate/src/rgb.js
var rgb_default = (function rgbGamma(y5) {
  var color2 = gamma(y5);
  function rgb3(start2, end) {
    var r8 = color2((start2 = rgb2(start2)).r, (end = rgb2(end)).r), g2 = color2(start2.g, end.g), b6 = color2(start2.b, end.b), opacity = nogamma(start2.opacity, end.opacity);
    return function(t9) {
      start2.r = r8(t9);
      start2.g = g2(t9);
      start2.b = b6(t9);
      start2.opacity = opacity(t9);
      return start2 + "";
    };
  }
  rgb3.gamma = rgbGamma;
  return rgb3;
})(1);
function rgbSpline(spline) {
  return function(colors3) {
    var n9 = colors3.length, r8 = new Array(n9), g2 = new Array(n9), b6 = new Array(n9), i8, color2;
    for (i8 = 0; i8 < n9; ++i8) {
      color2 = rgb2(colors3[i8]);
      r8[i8] = color2.r || 0;
      g2[i8] = color2.g || 0;
      b6[i8] = color2.b || 0;
    }
    r8 = spline(r8);
    g2 = spline(g2);
    b6 = spline(b6);
    color2.opacity = 1;
    return function(t9) {
      color2.r = r8(t9);
      color2.g = g2(t9);
      color2.b = b6(t9);
      return color2 + "";
    };
  };
}
var rgbBasis = rgbSpline(basis_default);
var rgbBasisClosed = rgbSpline(basisClosed_default);

// node_modules/d3-interpolate/src/numberArray.js
function numberArray_default(a5, b6) {
  if (!b6) b6 = [];
  var n9 = a5 ? Math.min(b6.length, a5.length) : 0, c9 = b6.slice(), i8;
  return function(t9) {
    for (i8 = 0; i8 < n9; ++i8) c9[i8] = a5[i8] * (1 - t9) + b6[i8] * t9;
    return c9;
  };
}
function isNumberArray(x4) {
  return ArrayBuffer.isView(x4) && !(x4 instanceof DataView);
}

// node_modules/d3-interpolate/src/array.js
function genericArray(a5, b6) {
  var nb = b6 ? b6.length : 0, na = a5 ? Math.min(nb, a5.length) : 0, x4 = new Array(na), c9 = new Array(nb), i8;
  for (i8 = 0; i8 < na; ++i8) x4[i8] = value_default(a5[i8], b6[i8]);
  for (; i8 < nb; ++i8) c9[i8] = b6[i8];
  return function(t9) {
    for (i8 = 0; i8 < na; ++i8) c9[i8] = x4[i8](t9);
    return c9;
  };
}

// node_modules/d3-interpolate/src/date.js
function date_default(a5, b6) {
  var d4 = /* @__PURE__ */ new Date();
  return a5 = +a5, b6 = +b6, function(t9) {
    return d4.setTime(a5 * (1 - t9) + b6 * t9), d4;
  };
}

// node_modules/d3-interpolate/src/number.js
function number_default(a5, b6) {
  return a5 = +a5, b6 = +b6, function(t9) {
    return a5 * (1 - t9) + b6 * t9;
  };
}

// node_modules/d3-interpolate/src/object.js
function object_default(a5, b6) {
  var i8 = {}, c9 = {}, k4;
  if (a5 === null || typeof a5 !== "object") a5 = {};
  if (b6 === null || typeof b6 !== "object") b6 = {};
  for (k4 in b6) {
    if (k4 in a5) {
      i8[k4] = value_default(a5[k4], b6[k4]);
    } else {
      c9[k4] = b6[k4];
    }
  }
  return function(t9) {
    for (k4 in i8) c9[k4] = i8[k4](t9);
    return c9;
  };
}

// node_modules/d3-interpolate/src/string.js
var reA = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g;
var reB = new RegExp(reA.source, "g");
function zero(b6) {
  return function() {
    return b6;
  };
}
function one(b6) {
  return function(t9) {
    return b6(t9) + "";
  };
}
function string_default(a5, b6) {
  var bi = reA.lastIndex = reB.lastIndex = 0, am, bm, bs, i8 = -1, s5 = [], q3 = [];
  a5 = a5 + "", b6 = b6 + "";
  while ((am = reA.exec(a5)) && (bm = reB.exec(b6))) {
    if ((bs = bm.index) > bi) {
      bs = b6.slice(bi, bs);
      if (s5[i8]) s5[i8] += bs;
      else s5[++i8] = bs;
    }
    if ((am = am[0]) === (bm = bm[0])) {
      if (s5[i8]) s5[i8] += bm;
      else s5[++i8] = bm;
    } else {
      s5[++i8] = null;
      q3.push({ i: i8, x: number_default(am, bm) });
    }
    bi = reB.lastIndex;
  }
  if (bi < b6.length) {
    bs = b6.slice(bi);
    if (s5[i8]) s5[i8] += bs;
    else s5[++i8] = bs;
  }
  return s5.length < 2 ? q3[0] ? one(q3[0].x) : zero(b6) : (b6 = q3.length, function(t9) {
    for (var i9 = 0, o6; i9 < b6; ++i9) s5[(o6 = q3[i9]).i] = o6.x(t9);
    return s5.join("");
  });
}

// node_modules/d3-interpolate/src/value.js
function value_default(a5, b6) {
  var t9 = typeof b6, c9;
  return b6 == null || t9 === "boolean" ? constant_default(b6) : (t9 === "number" ? number_default : t9 === "string" ? (c9 = color(b6)) ? (b6 = c9, rgb_default) : string_default : b6 instanceof color ? rgb_default : b6 instanceof Date ? date_default : isNumberArray(b6) ? numberArray_default : Array.isArray(b6) ? genericArray : typeof b6.valueOf !== "function" && typeof b6.toString !== "function" || isNaN(b6) ? object_default : number_default)(a5, b6);
}

// node_modules/d3-interpolate/src/round.js
function round_default(a5, b6) {
  return a5 = +a5, b6 = +b6, function(t9) {
    return Math.round(a5 * (1 - t9) + b6 * t9);
  };
}

// node_modules/d3-interpolate/src/transform/decompose.js
var degrees2 = 180 / Math.PI;
var identity = {
  translateX: 0,
  translateY: 0,
  rotate: 0,
  skewX: 0,
  scaleX: 1,
  scaleY: 1
};
function decompose_default(a5, b6, c9, d4, e11, f2) {
  var scaleX, scaleY, skewX;
  if (scaleX = Math.sqrt(a5 * a5 + b6 * b6)) a5 /= scaleX, b6 /= scaleX;
  if (skewX = a5 * c9 + b6 * d4) c9 -= a5 * skewX, d4 -= b6 * skewX;
  if (scaleY = Math.sqrt(c9 * c9 + d4 * d4)) c9 /= scaleY, d4 /= scaleY, skewX /= scaleY;
  if (a5 * d4 < b6 * c9) a5 = -a5, b6 = -b6, skewX = -skewX, scaleX = -scaleX;
  return {
    translateX: e11,
    translateY: f2,
    rotate: Math.atan2(b6, a5) * degrees2,
    skewX: Math.atan(skewX) * degrees2,
    scaleX,
    scaleY
  };
}

// node_modules/d3-interpolate/src/transform/parse.js
var svgNode;
function parseCss(value) {
  const m4 = new (typeof DOMMatrix === "function" ? DOMMatrix : WebKitCSSMatrix)(value + "");
  return m4.isIdentity ? identity : decompose_default(m4.a, m4.b, m4.c, m4.d, m4.e, m4.f);
}
function parseSvg(value) {
  if (value == null) return identity;
  if (!svgNode) svgNode = document.createElementNS("http://www.w3.org/2000/svg", "g");
  svgNode.setAttribute("transform", value);
  if (!(value = svgNode.transform.baseVal.consolidate())) return identity;
  value = value.matrix;
  return decompose_default(value.a, value.b, value.c, value.d, value.e, value.f);
}

// node_modules/d3-interpolate/src/transform/index.js
function interpolateTransform(parse, pxComma, pxParen, degParen) {
  function pop(s5) {
    return s5.length ? s5.pop() + " " : "";
  }
  function translate(xa, ya, xb, yb, s5, q3) {
    if (xa !== xb || ya !== yb) {
      var i8 = s5.push("translate(", null, pxComma, null, pxParen);
      q3.push({ i: i8 - 4, x: number_default(xa, xb) }, { i: i8 - 2, x: number_default(ya, yb) });
    } else if (xb || yb) {
      s5.push("translate(" + xb + pxComma + yb + pxParen);
    }
  }
  function rotate(a5, b6, s5, q3) {
    if (a5 !== b6) {
      if (a5 - b6 > 180) b6 += 360;
      else if (b6 - a5 > 180) a5 += 360;
      q3.push({ i: s5.push(pop(s5) + "rotate(", null, degParen) - 2, x: number_default(a5, b6) });
    } else if (b6) {
      s5.push(pop(s5) + "rotate(" + b6 + degParen);
    }
  }
  function skewX(a5, b6, s5, q3) {
    if (a5 !== b6) {
      q3.push({ i: s5.push(pop(s5) + "skewX(", null, degParen) - 2, x: number_default(a5, b6) });
    } else if (b6) {
      s5.push(pop(s5) + "skewX(" + b6 + degParen);
    }
  }
  function scale2(xa, ya, xb, yb, s5, q3) {
    if (xa !== xb || ya !== yb) {
      var i8 = s5.push(pop(s5) + "scale(", null, ",", null, ")");
      q3.push({ i: i8 - 4, x: number_default(xa, xb) }, { i: i8 - 2, x: number_default(ya, yb) });
    } else if (xb !== 1 || yb !== 1) {
      s5.push(pop(s5) + "scale(" + xb + "," + yb + ")");
    }
  }
  return function(a5, b6) {
    var s5 = [], q3 = [];
    a5 = parse(a5), b6 = parse(b6);
    translate(a5.translateX, a5.translateY, b6.translateX, b6.translateY, s5, q3);
    rotate(a5.rotate, b6.rotate, s5, q3);
    skewX(a5.skewX, b6.skewX, s5, q3);
    scale2(a5.scaleX, a5.scaleY, b6.scaleX, b6.scaleY, s5, q3);
    a5 = b6 = null;
    return function(t9) {
      var i8 = -1, n9 = q3.length, o6;
      while (++i8 < n9) s5[(o6 = q3[i8]).i] = o6.x(t9);
      return s5.join("");
    };
  };
}
var interpolateTransformCss = interpolateTransform(parseCss, "px, ", "px)", "deg)");
var interpolateTransformSvg = interpolateTransform(parseSvg, ", ", ")", ")");

// node_modules/d3-interpolate/src/zoom.js
var epsilon2 = 1e-12;
function cosh(x4) {
  return ((x4 = Math.exp(x4)) + 1 / x4) / 2;
}
function sinh(x4) {
  return ((x4 = Math.exp(x4)) - 1 / x4) / 2;
}
function tanh(x4) {
  return ((x4 = Math.exp(2 * x4)) - 1) / (x4 + 1);
}
var zoom_default = (function zoomRho(rho, rho2, rho4) {
  function zoom(p0, p1) {
    var ux0 = p0[0], uy0 = p0[1], w0 = p0[2], ux1 = p1[0], uy1 = p1[1], w1 = p1[2], dx = ux1 - ux0, dy = uy1 - uy0, d22 = dx * dx + dy * dy, i8, S4;
    if (d22 < epsilon2) {
      S4 = Math.log(w1 / w0) / rho;
      i8 = function(t9) {
        return [
          ux0 + t9 * dx,
          uy0 + t9 * dy,
          w0 * Math.exp(rho * t9 * S4)
        ];
      };
    } else {
      var d1 = Math.sqrt(d22), b0 = (w1 * w1 - w0 * w0 + rho4 * d22) / (2 * w0 * rho2 * d1), b1 = (w1 * w1 - w0 * w0 - rho4 * d22) / (2 * w1 * rho2 * d1), r0 = Math.log(Math.sqrt(b0 * b0 + 1) - b0), r1 = Math.log(Math.sqrt(b1 * b1 + 1) - b1);
      S4 = (r1 - r0) / rho;
      i8 = function(t9) {
        var s5 = t9 * S4, coshr0 = cosh(r0), u8 = w0 / (rho2 * d1) * (coshr0 * tanh(rho * s5 + r0) - sinh(r0));
        return [
          ux0 + u8 * dx,
          uy0 + u8 * dy,
          w0 * coshr0 / cosh(rho * s5 + r0)
        ];
      };
    }
    i8.duration = S4 * 1e3 * rho / Math.SQRT2;
    return i8;
  }
  zoom.rho = function(_3) {
    var _1 = Math.max(1e-3, +_3), _22 = _1 * _1, _4 = _22 * _22;
    return zoomRho(_1, _22, _4);
  };
  return zoom;
})(Math.SQRT2, 2, 4);

// node_modules/d3-interpolate/src/hsl.js
function hsl3(hue2) {
  return function(start2, end) {
    var h2 = hue2((start2 = hsl2(start2)).h, (end = hsl2(end)).h), s5 = nogamma(start2.s, end.s), l5 = nogamma(start2.l, end.l), opacity = nogamma(start2.opacity, end.opacity);
    return function(t9) {
      start2.h = h2(t9);
      start2.s = s5(t9);
      start2.l = l5(t9);
      start2.opacity = opacity(t9);
      return start2 + "";
    };
  };
}
var hsl_default = hsl3(hue);
var hslLong = hsl3(nogamma);

// node_modules/d3-interpolate/src/hcl.js
function hcl2(hue2) {
  return function(start2, end) {
    var h2 = hue2((start2 = hcl(start2)).h, (end = hcl(end)).h), c9 = nogamma(start2.c, end.c), l5 = nogamma(start2.l, end.l), opacity = nogamma(start2.opacity, end.opacity);
    return function(t9) {
      start2.h = h2(t9);
      start2.c = c9(t9);
      start2.l = l5(t9);
      start2.opacity = opacity(t9);
      return start2 + "";
    };
  };
}
var hcl_default = hcl2(hue);
var hclLong = hcl2(nogamma);

// node_modules/d3-interpolate/src/cubehelix.js
function cubehelix2(hue2) {
  return (function cubehelixGamma(y5) {
    y5 = +y5;
    function cubehelix3(start2, end) {
      var h2 = hue2((start2 = cubehelix(start2)).h, (end = cubehelix(end)).h), s5 = nogamma(start2.s, end.s), l5 = nogamma(start2.l, end.l), opacity = nogamma(start2.opacity, end.opacity);
      return function(t9) {
        start2.h = h2(t9);
        start2.s = s5(t9);
        start2.l = l5(Math.pow(t9, y5));
        start2.opacity = opacity(t9);
        return start2 + "";
      };
    }
    cubehelix3.gamma = cubehelixGamma;
    return cubehelix3;
  })(1);
}
var cubehelix_default = cubehelix2(hue);
var cubehelixLong = cubehelix2(nogamma);

// node_modules/@nivo/core/dist/nivo-core.mjs
var import_without = __toESM(require_without(), 1);

// node_modules/@nivo/core/node_modules/d3-shape/src/math.js
var cos = Math.cos;
var sin = Math.sin;
var sqrt = Math.sqrt;
var epsilon = 1e-12;
var pi = Math.PI;
var halfPi = pi / 2;
var tau = 2 * pi;

// node_modules/@nivo/core/node_modules/d3-path/src/path.js
var pi2 = Math.PI;
var tau2 = 2 * pi2;
var epsilon3 = 1e-6;
var tauEpsilon = tau2 - epsilon3;
function append(strings) {
  this._ += strings[0];
  for (let i8 = 1, n9 = strings.length; i8 < n9; ++i8) {
    this._ += arguments[i8] + strings[i8];
  }
}
function appendRound(digits) {
  let d4 = Math.floor(digits);
  if (!(d4 >= 0)) throw new Error(`invalid digits: ${digits}`);
  if (d4 > 15) return append;
  const k4 = 10 ** d4;
  return function(strings) {
    this._ += strings[0];
    for (let i8 = 1, n9 = strings.length; i8 < n9; ++i8) {
      this._ += Math.round(arguments[i8] * k4) / k4 + strings[i8];
    }
  };
}
var Path = class {
  constructor(digits) {
    this._x0 = this._y0 = // start of current subpath
    this._x1 = this._y1 = null;
    this._ = "";
    this._append = digits == null ? append : appendRound(digits);
  }
  moveTo(x4, y5) {
    this._append`M${this._x0 = this._x1 = +x4},${this._y0 = this._y1 = +y5}`;
  }
  closePath() {
    if (this._x1 !== null) {
      this._x1 = this._x0, this._y1 = this._y0;
      this._append`Z`;
    }
  }
  lineTo(x4, y5) {
    this._append`L${this._x1 = +x4},${this._y1 = +y5}`;
  }
  quadraticCurveTo(x1, y1, x4, y5) {
    this._append`Q${+x1},${+y1},${this._x1 = +x4},${this._y1 = +y5}`;
  }
  bezierCurveTo(x1, y1, x22, y22, x4, y5) {
    this._append`C${+x1},${+y1},${+x22},${+y22},${this._x1 = +x4},${this._y1 = +y5}`;
  }
  arcTo(x1, y1, x22, y22, r8) {
    x1 = +x1, y1 = +y1, x22 = +x22, y22 = +y22, r8 = +r8;
    if (r8 < 0) throw new Error(`negative radius: ${r8}`);
    let x0 = this._x1, y0 = this._y1, x21 = x22 - x1, y21 = y22 - y1, x01 = x0 - x1, y01 = y0 - y1, l01_2 = x01 * x01 + y01 * y01;
    if (this._x1 === null) {
      this._append`M${this._x1 = x1},${this._y1 = y1}`;
    } else if (!(l01_2 > epsilon3)) ;
    else if (!(Math.abs(y01 * x21 - y21 * x01) > epsilon3) || !r8) {
      this._append`L${this._x1 = x1},${this._y1 = y1}`;
    } else {
      let x20 = x22 - x0, y20 = y22 - y0, l21_2 = x21 * x21 + y21 * y21, l20_2 = x20 * x20 + y20 * y20, l21 = Math.sqrt(l21_2), l01 = Math.sqrt(l01_2), l5 = r8 * Math.tan((pi2 - Math.acos((l21_2 + l01_2 - l20_2) / (2 * l21 * l01))) / 2), t01 = l5 / l01, t21 = l5 / l21;
      if (Math.abs(t01 - 1) > epsilon3) {
        this._append`L${x1 + t01 * x01},${y1 + t01 * y01}`;
      }
      this._append`A${r8},${r8},0,0,${+(y01 * x20 > x01 * y20)},${this._x1 = x1 + t21 * x21},${this._y1 = y1 + t21 * y21}`;
    }
  }
  arc(x4, y5, r8, a0, a1, ccw) {
    x4 = +x4, y5 = +y5, r8 = +r8, ccw = !!ccw;
    if (r8 < 0) throw new Error(`negative radius: ${r8}`);
    let dx = r8 * Math.cos(a0), dy = r8 * Math.sin(a0), x0 = x4 + dx, y0 = y5 + dy, cw = 1 ^ ccw, da2 = ccw ? a0 - a1 : a1 - a0;
    if (this._x1 === null) {
      this._append`M${x0},${y0}`;
    } else if (Math.abs(this._x1 - x0) > epsilon3 || Math.abs(this._y1 - y0) > epsilon3) {
      this._append`L${x0},${y0}`;
    }
    if (!r8) return;
    if (da2 < 0) da2 = da2 % tau2 + tau2;
    if (da2 > tauEpsilon) {
      this._append`A${r8},${r8},0,1,${cw},${x4 - dx},${y5 - dy}A${r8},${r8},0,1,${cw},${this._x1 = x0},${this._y1 = y0}`;
    } else if (da2 > epsilon3) {
      this._append`A${r8},${r8},0,${+(da2 >= pi2)},${cw},${this._x1 = x4 + r8 * Math.cos(a1)},${this._y1 = y5 + r8 * Math.sin(a1)}`;
    }
  }
  rect(x4, y5, w5, h2) {
    this._append`M${this._x0 = this._x1 = +x4},${this._y0 = this._y1 = +y5}h${w5 = +w5}v${+h2}h${-w5}Z`;
  }
  toString() {
    return this._;
  }
};
function path() {
  return new Path();
}
path.prototype = Path.prototype;

// node_modules/@nivo/core/node_modules/d3-shape/src/array.js
var slice = Array.prototype.slice;

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/linear.js
function Linear(context) {
  this._context = context;
}
Linear.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y5) : this._context.moveTo(x4, y5);
        break;
      case 1:
        this._point = 2;
      // falls through
      default:
        this._context.lineTo(x4, y5);
        break;
    }
  }
};
function linear_default(context) {
  return new Linear(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/radial.js
var curveRadialLinear = curveRadial(linear_default);
function Radial(curve) {
  this._curve = curve;
}
Radial.prototype = {
  areaStart: function() {
    this._curve.areaStart();
  },
  areaEnd: function() {
    this._curve.areaEnd();
  },
  lineStart: function() {
    this._curve.lineStart();
  },
  lineEnd: function() {
    this._curve.lineEnd();
  },
  point: function(a5, r8) {
    this._curve.point(r8 * Math.sin(a5), r8 * -Math.cos(a5));
  }
};
function curveRadial(curve) {
  function radial2(context) {
    return new Radial(curve(context));
  }
  radial2._curve = curve;
  return radial2;
}

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/asterisk.js
var sqrt3 = sqrt(3);

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/diamond.js
var tan30 = sqrt(1 / 3);
var tan30_2 = tan30 * 2;

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/star.js
var kr = sin(pi / 10) / sin(7 * pi / 10);
var kx = sin(tau / 10) * kr;
var ky = -cos(tau / 10) * kr;

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/triangle.js
var sqrt32 = sqrt(3);

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/triangle2.js
var sqrt33 = sqrt(3);

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/wye.js
var s2 = sqrt(3) / 2;
var k = 1 / sqrt(12);
var a2 = (k / 2 + 1) * 3;

// node_modules/@nivo/core/node_modules/d3-shape/src/noop.js
function noop_default() {
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/basis.js
function point(that, x4, y5) {
  that._context.bezierCurveTo(
    (2 * that._x0 + that._x1) / 3,
    (2 * that._y0 + that._y1) / 3,
    (that._x0 + 2 * that._x1) / 3,
    (that._y0 + 2 * that._y1) / 3,
    (that._x0 + 4 * that._x1 + x4) / 6,
    (that._y0 + 4 * that._y1 + y5) / 6
  );
}
function Basis(context) {
  this._context = context;
}
Basis.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 3:
        point(this, this._x1, this._y1);
      // falls through
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y5) : this._context.moveTo(x4, y5);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._context.lineTo((5 * this._x0 + this._x1) / 6, (5 * this._y0 + this._y1) / 6);
      // falls through
      default:
        point(this, x4, y5);
        break;
    }
    this._x0 = this._x1, this._x1 = x4;
    this._y0 = this._y1, this._y1 = y5;
  }
};
function basis_default2(context) {
  return new Basis(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/basisClosed.js
function BasisClosed(context) {
  this._context = context;
}
BasisClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x2, this._y2);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.moveTo((this._x2 + 2 * this._x3) / 3, (this._y2 + 2 * this._y3) / 3);
        this._context.lineTo((this._x3 + 2 * this._x2) / 3, (this._y3 + 2 * this._y2) / 3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x2, this._y2);
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        break;
      }
    }
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x2 = x4, this._y2 = y5;
        break;
      case 1:
        this._point = 2;
        this._x3 = x4, this._y3 = y5;
        break;
      case 2:
        this._point = 3;
        this._x4 = x4, this._y4 = y5;
        this._context.moveTo((this._x0 + 4 * this._x1 + x4) / 6, (this._y0 + 4 * this._y1 + y5) / 6);
        break;
      default:
        point(this, x4, y5);
        break;
    }
    this._x0 = this._x1, this._x1 = x4;
    this._y0 = this._y1, this._y1 = y5;
  }
};
function basisClosed_default2(context) {
  return new BasisClosed(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/basisOpen.js
function BasisOpen(context) {
  this._context = context;
}
BasisOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        var x0 = (this._x0 + 4 * this._x1 + x4) / 6, y0 = (this._y0 + 4 * this._y1 + y5) / 6;
        this._line ? this._context.lineTo(x0, y0) : this._context.moveTo(x0, y0);
        break;
      case 3:
        this._point = 4;
      // falls through
      default:
        point(this, x4, y5);
        break;
    }
    this._x0 = this._x1, this._x1 = x4;
    this._y0 = this._y1, this._y1 = y5;
  }
};
function basisOpen_default(context) {
  return new BasisOpen(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/bundle.js
function Bundle(context, beta) {
  this._basis = new Basis(context);
  this._beta = beta;
}
Bundle.prototype = {
  lineStart: function() {
    this._x = [];
    this._y = [];
    this._basis.lineStart();
  },
  lineEnd: function() {
    var x4 = this._x, y5 = this._y, j5 = x4.length - 1;
    if (j5 > 0) {
      var x0 = x4[0], y0 = y5[0], dx = x4[j5] - x0, dy = y5[j5] - y0, i8 = -1, t9;
      while (++i8 <= j5) {
        t9 = i8 / j5;
        this._basis.point(
          this._beta * x4[i8] + (1 - this._beta) * (x0 + t9 * dx),
          this._beta * y5[i8] + (1 - this._beta) * (y0 + t9 * dy)
        );
      }
    }
    this._x = this._y = null;
    this._basis.lineEnd();
  },
  point: function(x4, y5) {
    this._x.push(+x4);
    this._y.push(+y5);
  }
};
var bundle_default = (function custom(beta) {
  function bundle(context) {
    return beta === 1 ? new Basis(context) : new Bundle(context, beta);
  }
  bundle.beta = function(beta2) {
    return custom(+beta2);
  };
  return bundle;
})(0.85);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/cardinal.js
function point2(that, x4, y5) {
  that._context.bezierCurveTo(
    that._x1 + that._k * (that._x2 - that._x0),
    that._y1 + that._k * (that._y2 - that._y0),
    that._x2 + that._k * (that._x1 - x4),
    that._y2 + that._k * (that._y1 - y5),
    that._x2,
    that._y2
  );
}
function Cardinal(context, tension) {
  this._context = context;
  this._k = (1 - tension) / 6;
}
Cardinal.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x2, this._y2);
        break;
      case 3:
        point2(this, this._x1, this._y1);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y5) : this._context.moveTo(x4, y5);
        break;
      case 1:
        this._point = 2;
        this._x1 = x4, this._y1 = y5;
        break;
      case 2:
        this._point = 3;
      // falls through
      default:
        point2(this, x4, y5);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y5;
  }
};
var cardinal_default = (function custom2(tension) {
  function cardinal(context) {
    return new Cardinal(context, tension);
  }
  cardinal.tension = function(tension2) {
    return custom2(+tension2);
  };
  return cardinal;
})(0);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/cardinalClosed.js
function CardinalClosed(context, tension) {
  this._context = context;
  this._k = (1 - tension) / 6;
}
CardinalClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.lineTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        this.point(this._x5, this._y5);
        break;
      }
    }
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x3 = x4, this._y3 = y5;
        break;
      case 1:
        this._point = 2;
        this._context.moveTo(this._x4 = x4, this._y4 = y5);
        break;
      case 2:
        this._point = 3;
        this._x5 = x4, this._y5 = y5;
        break;
      default:
        point2(this, x4, y5);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y5;
  }
};
var cardinalClosed_default = (function custom3(tension) {
  function cardinal(context) {
    return new CardinalClosed(context, tension);
  }
  cardinal.tension = function(tension2) {
    return custom3(+tension2);
  };
  return cardinal;
})(0);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/cardinalOpen.js
function CardinalOpen(context, tension) {
  this._context = context;
  this._k = (1 - tension) / 6;
}
CardinalOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2);
        break;
      case 3:
        this._point = 4;
      // falls through
      default:
        point2(this, x4, y5);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y5;
  }
};
var cardinalOpen_default = (function custom4(tension) {
  function cardinal(context) {
    return new CardinalOpen(context, tension);
  }
  cardinal.tension = function(tension2) {
    return custom4(+tension2);
  };
  return cardinal;
})(0);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/catmullRom.js
function point3(that, x4, y5) {
  var x1 = that._x1, y1 = that._y1, x22 = that._x2, y22 = that._y2;
  if (that._l01_a > epsilon) {
    var a5 = 2 * that._l01_2a + 3 * that._l01_a * that._l12_a + that._l12_2a, n9 = 3 * that._l01_a * (that._l01_a + that._l12_a);
    x1 = (x1 * a5 - that._x0 * that._l12_2a + that._x2 * that._l01_2a) / n9;
    y1 = (y1 * a5 - that._y0 * that._l12_2a + that._y2 * that._l01_2a) / n9;
  }
  if (that._l23_a > epsilon) {
    var b6 = 2 * that._l23_2a + 3 * that._l23_a * that._l12_a + that._l12_2a, m4 = 3 * that._l23_a * (that._l23_a + that._l12_a);
    x22 = (x22 * b6 + that._x1 * that._l23_2a - x4 * that._l12_2a) / m4;
    y22 = (y22 * b6 + that._y1 * that._l23_2a - y5 * that._l12_2a) / m4;
  }
  that._context.bezierCurveTo(x1, y1, x22, y22, that._x2, that._y2);
}
function CatmullRom(context, alpha) {
  this._context = context;
  this._alpha = alpha;
}
CatmullRom.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x2, this._y2);
        break;
      case 3:
        this.point(this._x2, this._y2);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    if (this._point) {
      var x23 = this._x2 - x4, y23 = this._y2 - y5;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y5) : this._context.moveTo(x4, y5);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
      // falls through
      default:
        point3(this, x4, y5);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y5;
  }
};
var catmullRom_default = (function custom5(alpha) {
  function catmullRom(context) {
    return alpha ? new CatmullRom(context, alpha) : new Cardinal(context, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom5(+alpha2);
  };
  return catmullRom;
})(0.5);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/catmullRomClosed.js
function CatmullRomClosed(context, alpha) {
  this._context = context;
  this._alpha = alpha;
}
CatmullRomClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.lineTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        this.point(this._x5, this._y5);
        break;
      }
    }
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    if (this._point) {
      var x23 = this._x2 - x4, y23 = this._y2 - y5;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x3 = x4, this._y3 = y5;
        break;
      case 1:
        this._point = 2;
        this._context.moveTo(this._x4 = x4, this._y4 = y5);
        break;
      case 2:
        this._point = 3;
        this._x5 = x4, this._y5 = y5;
        break;
      default:
        point3(this, x4, y5);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y5;
  }
};
var catmullRomClosed_default = (function custom6(alpha) {
  function catmullRom(context) {
    return alpha ? new CatmullRomClosed(context, alpha) : new CardinalClosed(context, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom6(+alpha2);
  };
  return catmullRom;
})(0.5);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/catmullRomOpen.js
function CatmullRomOpen(context, alpha) {
  this._context = context;
  this._alpha = alpha;
}
CatmullRomOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    if (this._point) {
      var x23 = this._x2 - x4, y23 = this._y2 - y5;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2);
        break;
      case 3:
        this._point = 4;
      // falls through
      default:
        point3(this, x4, y5);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y5;
  }
};
var catmullRomOpen_default = (function custom7(alpha) {
  function catmullRom(context) {
    return alpha ? new CatmullRomOpen(context, alpha) : new CardinalOpen(context, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom7(+alpha2);
  };
  return catmullRom;
})(0.5);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/linearClosed.js
function LinearClosed(context) {
  this._context = context;
}
LinearClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._point) this._context.closePath();
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    if (this._point) this._context.lineTo(x4, y5);
    else this._point = 1, this._context.moveTo(x4, y5);
  }
};
function linearClosed_default(context) {
  return new LinearClosed(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/monotone.js
function sign(x4) {
  return x4 < 0 ? -1 : 1;
}
function slope3(that, x22, y22) {
  var h0 = that._x1 - that._x0, h1 = x22 - that._x1, s0 = (that._y1 - that._y0) / (h0 || h1 < 0 && -0), s1 = (y22 - that._y1) / (h1 || h0 < 0 && -0), p3 = (s0 * h1 + s1 * h0) / (h0 + h1);
  return (sign(s0) + sign(s1)) * Math.min(Math.abs(s0), Math.abs(s1), 0.5 * Math.abs(p3)) || 0;
}
function slope2(that, t9) {
  var h2 = that._x1 - that._x0;
  return h2 ? (3 * (that._y1 - that._y0) / h2 - t9) / 2 : t9;
}
function point4(that, t04, t14) {
  var x0 = that._x0, y0 = that._y0, x1 = that._x1, y1 = that._y1, dx = (x1 - x0) / 3;
  that._context.bezierCurveTo(x0 + dx, y0 + dx * t04, x1 - dx, y1 - dx * t14, x1, y1);
}
function MonotoneX(context) {
  this._context = context;
}
MonotoneX.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = this._t0 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
      case 3:
        point4(this, this._t0, slope2(this, this._t0));
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y5) {
    var t14 = NaN;
    x4 = +x4, y5 = +y5;
    if (x4 === this._x1 && y5 === this._y1) return;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y5) : this._context.moveTo(x4, y5);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        point4(this, slope2(this, t14 = slope3(this, x4, y5)), t14);
        break;
      default:
        point4(this, this._t0, t14 = slope3(this, x4, y5));
        break;
    }
    this._x0 = this._x1, this._x1 = x4;
    this._y0 = this._y1, this._y1 = y5;
    this._t0 = t14;
  }
};
function MonotoneY(context) {
  this._context = new ReflectContext(context);
}
(MonotoneY.prototype = Object.create(MonotoneX.prototype)).point = function(x4, y5) {
  MonotoneX.prototype.point.call(this, y5, x4);
};
function ReflectContext(context) {
  this._context = context;
}
ReflectContext.prototype = {
  moveTo: function(x4, y5) {
    this._context.moveTo(y5, x4);
  },
  closePath: function() {
    this._context.closePath();
  },
  lineTo: function(x4, y5) {
    this._context.lineTo(y5, x4);
  },
  bezierCurveTo: function(x1, y1, x22, y22, x4, y5) {
    this._context.bezierCurveTo(y1, x1, y22, x22, y5, x4);
  }
};
function monotoneX(context) {
  return new MonotoneX(context);
}
function monotoneY(context) {
  return new MonotoneY(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/natural.js
function Natural(context) {
  this._context = context;
}
Natural.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = [];
    this._y = [];
  },
  lineEnd: function() {
    var x4 = this._x, y5 = this._y, n9 = x4.length;
    if (n9) {
      this._line ? this._context.lineTo(x4[0], y5[0]) : this._context.moveTo(x4[0], y5[0]);
      if (n9 === 2) {
        this._context.lineTo(x4[1], y5[1]);
      } else {
        var px = controlPoints(x4), py = controlPoints(y5);
        for (var i0 = 0, i1 = 1; i1 < n9; ++i0, ++i1) {
          this._context.bezierCurveTo(px[0][i0], py[0][i0], px[1][i0], py[1][i0], x4[i1], y5[i1]);
        }
      }
    }
    if (this._line || this._line !== 0 && n9 === 1) this._context.closePath();
    this._line = 1 - this._line;
    this._x = this._y = null;
  },
  point: function(x4, y5) {
    this._x.push(+x4);
    this._y.push(+y5);
  }
};
function controlPoints(x4) {
  var i8, n9 = x4.length - 1, m4, a5 = new Array(n9), b6 = new Array(n9), r8 = new Array(n9);
  a5[0] = 0, b6[0] = 2, r8[0] = x4[0] + 2 * x4[1];
  for (i8 = 1; i8 < n9 - 1; ++i8) a5[i8] = 1, b6[i8] = 4, r8[i8] = 4 * x4[i8] + 2 * x4[i8 + 1];
  a5[n9 - 1] = 2, b6[n9 - 1] = 7, r8[n9 - 1] = 8 * x4[n9 - 1] + x4[n9];
  for (i8 = 1; i8 < n9; ++i8) m4 = a5[i8] / b6[i8 - 1], b6[i8] -= m4, r8[i8] -= m4 * r8[i8 - 1];
  a5[n9 - 1] = r8[n9 - 1] / b6[n9 - 1];
  for (i8 = n9 - 2; i8 >= 0; --i8) a5[i8] = (r8[i8] - a5[i8 + 1]) / b6[i8];
  b6[n9 - 1] = (x4[n9] + a5[n9 - 1]) / 2;
  for (i8 = 0; i8 < n9 - 1; ++i8) b6[i8] = 2 * x4[i8 + 1] - a5[i8 + 1];
  return [a5, b6];
}
function natural_default(context) {
  return new Natural(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/step.js
function Step(context, t9) {
  this._context = context;
  this._t = t9;
}
Step.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = this._y = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (0 < this._t && this._t < 1 && this._point === 2) this._context.lineTo(this._x, this._y);
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    if (this._line >= 0) this._t = 1 - this._t, this._line = 1 - this._line;
  },
  point: function(x4, y5) {
    x4 = +x4, y5 = +y5;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y5) : this._context.moveTo(x4, y5);
        break;
      case 1:
        this._point = 2;
      // falls through
      default: {
        if (this._t <= 0) {
          this._context.lineTo(this._x, y5);
          this._context.lineTo(x4, y5);
        } else {
          var x1 = this._x * (1 - this._t) + x4 * this._t;
          this._context.lineTo(x1, this._y);
          this._context.lineTo(x1, y5);
        }
        break;
      }
    }
    this._x = x4, this._y = y5;
  }
};
function step_default(context) {
  return new Step(context, 0.5);
}
function stepBefore(context) {
  return new Step(context, 0);
}
function stepAfter(context) {
  return new Step(context, 1);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/offset/none.js
function none_default(series, order) {
  if (!((n9 = series.length) > 1)) return;
  for (var i8 = 1, j5, s0, s1 = series[order[0]], n9, m4 = s1.length; i8 < n9; ++i8) {
    s0 = s1, s1 = series[order[i8]];
    for (j5 = 0; j5 < m4; ++j5) {
      s1[j5][1] += s1[j5][0] = isNaN(s0[j5][1]) ? s0[j5][0] : s0[j5][1];
    }
  }
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/none.js
function none_default2(series) {
  var n9 = series.length, o6 = new Array(n9);
  while (--n9 >= 0) o6[n9] = n9;
  return o6;
}

// node_modules/@nivo/core/node_modules/d3-shape/src/offset/expand.js
function expand_default(series, order) {
  if (!((n9 = series.length) > 0)) return;
  for (var i8, n9, j5 = 0, m4 = series[0].length, y5; j5 < m4; ++j5) {
    for (y5 = i8 = 0; i8 < n9; ++i8) y5 += series[i8][j5][1] || 0;
    if (y5) for (i8 = 0; i8 < n9; ++i8) series[i8][j5][1] /= y5;
  }
  none_default(series, order);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/offset/diverging.js
function diverging_default(series, order) {
  if (!((n9 = series.length) > 0)) return;
  for (var i8, j5 = 0, d4, dy, yp, yn3, n9, m4 = series[order[0]].length; j5 < m4; ++j5) {
    for (yp = yn3 = 0, i8 = 0; i8 < n9; ++i8) {
      if ((dy = (d4 = series[order[i8]][j5])[1] - d4[0]) > 0) {
        d4[0] = yp, d4[1] = yp += dy;
      } else if (dy < 0) {
        d4[1] = yn3, d4[0] = yn3 += dy;
      } else {
        d4[0] = 0, d4[1] = dy;
      }
    }
  }
}

// node_modules/@nivo/core/node_modules/d3-shape/src/offset/silhouette.js
function silhouette_default(series, order) {
  if (!((n9 = series.length) > 0)) return;
  for (var j5 = 0, s0 = series[order[0]], n9, m4 = s0.length; j5 < m4; ++j5) {
    for (var i8 = 0, y5 = 0; i8 < n9; ++i8) y5 += series[i8][j5][1] || 0;
    s0[j5][1] += s0[j5][0] = -y5 / 2;
  }
  none_default(series, order);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/offset/wiggle.js
function wiggle_default(series, order) {
  if (!((n9 = series.length) > 0) || !((m4 = (s0 = series[order[0]]).length) > 0)) return;
  for (var y5 = 0, j5 = 1, s0, m4, n9; j5 < m4; ++j5) {
    for (var i8 = 0, s1 = 0, s22 = 0; i8 < n9; ++i8) {
      var si = series[order[i8]], sij0 = si[j5][1] || 0, sij1 = si[j5 - 1][1] || 0, s32 = (sij0 - sij1) / 2;
      for (var k4 = 0; k4 < i8; ++k4) {
        var sk = series[order[k4]], skj0 = sk[j5][1] || 0, skj1 = sk[j5 - 1][1] || 0;
        s32 += skj0 - skj1;
      }
      s1 += sij0, s22 += s32 * sij0;
    }
    s0[j5 - 1][1] += s0[j5 - 1][0] = y5;
    if (s1) y5 -= s22 / s1;
  }
  s0[j5 - 1][1] += s0[j5 - 1][0] = y5;
  none_default(series, order);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/appearance.js
function appearance_default(series) {
  var peaks = series.map(peak);
  return none_default2(series).sort(function(a5, b6) {
    return peaks[a5] - peaks[b6];
  });
}
function peak(series) {
  var i8 = -1, j5 = 0, n9 = series.length, vi, vj = -Infinity;
  while (++i8 < n9) if ((vi = +series[i8][1]) > vj) vj = vi, j5 = i8;
  return j5;
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/ascending.js
function ascending_default(series) {
  var sums = series.map(sum);
  return none_default2(series).sort(function(a5, b6) {
    return sums[a5] - sums[b6];
  });
}
function sum(series) {
  var s5 = 0, i8 = -1, n9 = series.length, v3;
  while (++i8 < n9) if (v3 = +series[i8][1]) s5 += v3;
  return s5;
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/descending.js
function descending_default2(series) {
  return ascending_default(series).reverse();
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/insideOut.js
function insideOut_default(series) {
  var n9 = series.length, i8, j5, sums = series.map(sum), order = appearance_default(series), top = 0, bottom = 0, tops = [], bottoms = [];
  for (i8 = 0; i8 < n9; ++i8) {
    j5 = order[i8];
    if (top < bottom) {
      top += sums[j5];
      tops.push(j5);
    } else {
      bottom += sums[j5];
      bottoms.push(j5);
    }
  }
  return bottoms.reverse().concat(tops);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/reverse.js
function reverse_default(series) {
  return none_default2(series).reverse();
}

// node_modules/d3-scale/node_modules/d3-array/src/ascending.js
function ascending(a5, b6) {
  return a5 == null || b6 == null ? NaN : a5 < b6 ? -1 : a5 > b6 ? 1 : a5 >= b6 ? 0 : NaN;
}

// node_modules/d3-scale/node_modules/d3-array/src/descending.js
function descending(a5, b6) {
  return a5 == null || b6 == null ? NaN : b6 < a5 ? -1 : b6 > a5 ? 1 : b6 >= a5 ? 0 : NaN;
}

// node_modules/d3-scale/node_modules/d3-array/src/bisector.js
function bisector(f2) {
  let compare1, compare2, delta;
  if (f2.length !== 2) {
    compare1 = ascending;
    compare2 = (d4, x4) => ascending(f2(d4), x4);
    delta = (d4, x4) => f2(d4) - x4;
  } else {
    compare1 = f2 === ascending || f2 === descending ? f2 : zero2;
    compare2 = f2;
    delta = f2;
  }
  function left(a5, x4, lo = 0, hi = a5.length) {
    if (lo < hi) {
      if (compare1(x4, x4) !== 0) return hi;
      do {
        const mid = lo + hi >>> 1;
        if (compare2(a5[mid], x4) < 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }
  function right(a5, x4, lo = 0, hi = a5.length) {
    if (lo < hi) {
      if (compare1(x4, x4) !== 0) return hi;
      do {
        const mid = lo + hi >>> 1;
        if (compare2(a5[mid], x4) <= 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }
  function center(a5, x4, lo = 0, hi = a5.length) {
    const i8 = left(a5, x4, lo, hi - 1);
    return i8 > lo && delta(a5[i8 - 1], x4) > -delta(a5[i8], x4) ? i8 - 1 : i8;
  }
  return { left, center, right };
}
function zero2() {
  return 0;
}

// node_modules/d3-scale/node_modules/d3-array/src/number.js
function number(x4) {
  return x4 === null ? NaN : +x4;
}

// node_modules/d3-scale/node_modules/d3-array/src/bisect.js
var ascendingBisect = bisector(ascending);
var bisectRight = ascendingBisect.right;
var bisectLeft = ascendingBisect.left;
var bisectCenter = bisector(number).center;
var bisect_default = bisectRight;

// node_modules/d3-scale/node_modules/d3-array/src/blur.js
var blur2 = Blur2(blurf);
var blurImage = Blur2(blurfImage);
function Blur2(blur3) {
  return function(data, rx, ry = rx) {
    if (!((rx = +rx) >= 0)) throw new RangeError("invalid rx");
    if (!((ry = +ry) >= 0)) throw new RangeError("invalid ry");
    let { data: values, width, height } = data;
    if (!((width = Math.floor(width)) >= 0)) throw new RangeError("invalid width");
    if (!((height = Math.floor(height !== void 0 ? height : values.length / width)) >= 0)) throw new RangeError("invalid height");
    if (!width || !height || !rx && !ry) return data;
    const blurx = rx && blur3(rx);
    const blury = ry && blur3(ry);
    const temp = values.slice();
    if (blurx && blury) {
      blurh(blurx, temp, values, width, height);
      blurh(blurx, values, temp, width, height);
      blurh(blurx, temp, values, width, height);
      blurv(blury, values, temp, width, height);
      blurv(blury, temp, values, width, height);
      blurv(blury, values, temp, width, height);
    } else if (blurx) {
      blurh(blurx, values, temp, width, height);
      blurh(blurx, temp, values, width, height);
      blurh(blurx, values, temp, width, height);
    } else if (blury) {
      blurv(blury, values, temp, width, height);
      blurv(blury, temp, values, width, height);
      blurv(blury, values, temp, width, height);
    }
    return data;
  };
}
function blurh(blur3, T4, S4, w5, h2) {
  for (let y5 = 0, n9 = w5 * h2; y5 < n9; ) {
    blur3(T4, S4, y5, y5 += w5, 1);
  }
}
function blurv(blur3, T4, S4, w5, h2) {
  for (let x4 = 0, n9 = w5 * h2; x4 < w5; ++x4) {
    blur3(T4, S4, x4, x4 + n9, w5);
  }
}
function blurfImage(radius) {
  const blur3 = blurf(radius);
  return (T4, S4, start2, stop2, step) => {
    start2 <<= 2, stop2 <<= 2, step <<= 2;
    blur3(T4, S4, start2 + 0, stop2 + 0, step);
    blur3(T4, S4, start2 + 1, stop2 + 1, step);
    blur3(T4, S4, start2 + 2, stop2 + 2, step);
    blur3(T4, S4, start2 + 3, stop2 + 3, step);
  };
}
function blurf(radius) {
  const radius0 = Math.floor(radius);
  if (radius0 === radius) return bluri(radius);
  const t9 = radius - radius0;
  const w5 = 2 * radius + 1;
  return (T4, S4, start2, stop2, step) => {
    if (!((stop2 -= step) >= start2)) return;
    let sum4 = radius0 * S4[start2];
    const s0 = step * radius0;
    const s1 = s0 + step;
    for (let i8 = start2, j5 = start2 + s0; i8 < j5; i8 += step) {
      sum4 += S4[Math.min(stop2, i8)];
    }
    for (let i8 = start2, j5 = stop2; i8 <= j5; i8 += step) {
      sum4 += S4[Math.min(stop2, i8 + s0)];
      T4[i8] = (sum4 + t9 * (S4[Math.max(start2, i8 - s1)] + S4[Math.min(stop2, i8 + s1)])) / w5;
      sum4 -= S4[Math.max(start2, i8 - s0)];
    }
  };
}
function bluri(radius) {
  const w5 = 2 * radius + 1;
  return (T4, S4, start2, stop2, step) => {
    if (!((stop2 -= step) >= start2)) return;
    let sum4 = radius * S4[start2];
    const s5 = step * radius;
    for (let i8 = start2, j5 = start2 + s5; i8 < j5; i8 += step) {
      sum4 += S4[Math.min(stop2, i8)];
    }
    for (let i8 = start2, j5 = stop2; i8 <= j5; i8 += step) {
      sum4 += S4[Math.min(stop2, i8 + s5)];
      T4[i8] = sum4 / w5;
      sum4 -= S4[Math.max(start2, i8 - s5)];
    }
  };
}

// node_modules/internmap/src/index.js
var InternMap = class extends Map {
  constructor(entries, key = keyof) {
    super();
    Object.defineProperties(this, { _intern: { value: /* @__PURE__ */ new Map() }, _key: { value: key } });
    if (entries != null) for (const [key2, value] of entries) this.set(key2, value);
  }
  get(key) {
    return super.get(intern_get(this, key));
  }
  has(key) {
    return super.has(intern_get(this, key));
  }
  set(key, value) {
    return super.set(intern_set(this, key), value);
  }
  delete(key) {
    return super.delete(intern_delete(this, key));
  }
};
function intern_get({ _intern, _key }, value) {
  const key = _key(value);
  return _intern.has(key) ? _intern.get(key) : value;
}
function intern_set({ _intern, _key }, value) {
  const key = _key(value);
  if (_intern.has(key)) return _intern.get(key);
  _intern.set(key, value);
  return value;
}
function intern_delete({ _intern, _key }, value) {
  const key = _key(value);
  if (_intern.has(key)) {
    value = _intern.get(key);
    _intern.delete(key);
  }
  return value;
}
function keyof(value) {
  return value !== null && typeof value === "object" ? value.valueOf() : value;
}

// node_modules/d3-scale/node_modules/d3-array/src/array.js
var array = Array.prototype;
var slice2 = array.slice;
var map = array.map;

// node_modules/d3-scale/node_modules/d3-array/src/ticks.js
var e10 = Math.sqrt(50);
var e5 = Math.sqrt(10);
var e22 = Math.sqrt(2);
function tickSpec(start2, stop2, count2) {
  const step = (stop2 - start2) / Math.max(0, count2), power = Math.floor(Math.log10(step)), error = step / Math.pow(10, power), factor = error >= e10 ? 10 : error >= e5 ? 5 : error >= e22 ? 2 : 1;
  let i1, i22, inc;
  if (power < 0) {
    inc = Math.pow(10, -power) / factor;
    i1 = Math.round(start2 * inc);
    i22 = Math.round(stop2 * inc);
    if (i1 / inc < start2) ++i1;
    if (i22 / inc > stop2) --i22;
    inc = -inc;
  } else {
    inc = Math.pow(10, power) * factor;
    i1 = Math.round(start2 / inc);
    i22 = Math.round(stop2 / inc);
    if (i1 * inc < start2) ++i1;
    if (i22 * inc > stop2) --i22;
  }
  if (i22 < i1 && 0.5 <= count2 && count2 < 2) return tickSpec(start2, stop2, count2 * 2);
  return [i1, i22, inc];
}
function ticks(start2, stop2, count2) {
  stop2 = +stop2, start2 = +start2, count2 = +count2;
  if (!(count2 > 0)) return [];
  if (start2 === stop2) return [start2];
  const reverse2 = stop2 < start2, [i1, i22, inc] = reverse2 ? tickSpec(stop2, start2, count2) : tickSpec(start2, stop2, count2);
  if (!(i22 >= i1)) return [];
  const n9 = i22 - i1 + 1, ticks2 = new Array(n9);
  if (reverse2) {
    if (inc < 0) for (let i8 = 0; i8 < n9; ++i8) ticks2[i8] = (i22 - i8) / -inc;
    else for (let i8 = 0; i8 < n9; ++i8) ticks2[i8] = (i22 - i8) * inc;
  } else {
    if (inc < 0) for (let i8 = 0; i8 < n9; ++i8) ticks2[i8] = (i1 + i8) / -inc;
    else for (let i8 = 0; i8 < n9; ++i8) ticks2[i8] = (i1 + i8) * inc;
  }
  return ticks2;
}
function tickIncrement(start2, stop2, count2) {
  stop2 = +stop2, start2 = +start2, count2 = +count2;
  return tickSpec(start2, stop2, count2)[2];
}
function tickStep(start2, stop2, count2) {
  stop2 = +stop2, start2 = +start2, count2 = +count2;
  const reverse2 = stop2 < start2, inc = reverse2 ? tickIncrement(stop2, start2, count2) : tickIncrement(start2, stop2, count2);
  return (reverse2 ? -1 : 1) * (inc < 0 ? 1 / -inc : inc);
}

// node_modules/d3-scale/node_modules/d3-array/src/range.js
function range(start2, stop2, step) {
  start2 = +start2, stop2 = +stop2, step = (n9 = arguments.length) < 2 ? (stop2 = start2, start2 = 0, 1) : n9 < 3 ? 1 : +step;
  var i8 = -1, n9 = Math.max(0, Math.ceil((stop2 - start2) / step)) | 0, range2 = new Array(n9);
  while (++i8 < n9) {
    range2[i8] = start2 + i8 * step;
  }
  return range2;
}

// node_modules/d3-scale/node_modules/d3-array/src/shuffle.js
var shuffle_default = shuffler(Math.random);
function shuffler(random) {
  return function shuffle(array2, i0 = 0, i1 = array2.length) {
    let m4 = i1 - (i0 = +i0);
    while (m4) {
      const i8 = random() * m4-- | 0, t9 = array2[m4 + i0];
      array2[m4 + i0] = array2[i8 + i0];
      array2[i8 + i0] = t9;
    }
    return array2;
  };
}

// node_modules/d3-scale/src/init.js
function initRange(domain, range2) {
  switch (arguments.length) {
    case 0:
      break;
    case 1:
      this.range(domain);
      break;
    default:
      this.range(range2).domain(domain);
      break;
  }
  return this;
}

// node_modules/d3-scale/src/ordinal.js
var implicit = Symbol("implicit");
function ordinal() {
  var index2 = new InternMap(), domain = [], range2 = [], unknown = implicit;
  function scale2(d4) {
    let i8 = index2.get(d4);
    if (i8 === void 0) {
      if (unknown !== implicit) return unknown;
      index2.set(d4, i8 = domain.push(d4) - 1);
    }
    return range2[i8 % range2.length];
  }
  scale2.domain = function(_3) {
    if (!arguments.length) return domain.slice();
    domain = [], index2 = new InternMap();
    for (const value of _3) {
      if (index2.has(value)) continue;
      index2.set(value, domain.push(value) - 1);
    }
    return scale2;
  };
  scale2.range = function(_3) {
    return arguments.length ? (range2 = Array.from(_3), scale2) : range2.slice();
  };
  scale2.unknown = function(_3) {
    return arguments.length ? (unknown = _3, scale2) : unknown;
  };
  scale2.copy = function() {
    return ordinal(domain, range2).unknown(unknown);
  };
  initRange.apply(scale2, arguments);
  return scale2;
}

// node_modules/d3-scale/src/band.js
function band() {
  var scale2 = ordinal().unknown(void 0), domain = scale2.domain, ordinalRange = scale2.range, r0 = 0, r1 = 1, step, bandwidth, round = false, paddingInner = 0, paddingOuter = 0, align = 0.5;
  delete scale2.unknown;
  function rescale() {
    var n9 = domain().length, reverse2 = r1 < r0, start2 = reverse2 ? r1 : r0, stop2 = reverse2 ? r0 : r1;
    step = (stop2 - start2) / Math.max(1, n9 - paddingInner + paddingOuter * 2);
    if (round) step = Math.floor(step);
    start2 += (stop2 - start2 - step * (n9 - paddingInner)) * align;
    bandwidth = step * (1 - paddingInner);
    if (round) start2 = Math.round(start2), bandwidth = Math.round(bandwidth);
    var values = range(n9).map(function(i8) {
      return start2 + step * i8;
    });
    return ordinalRange(reverse2 ? values.reverse() : values);
  }
  scale2.domain = function(_3) {
    return arguments.length ? (domain(_3), rescale()) : domain();
  };
  scale2.range = function(_3) {
    return arguments.length ? ([r0, r1] = _3, r0 = +r0, r1 = +r1, rescale()) : [r0, r1];
  };
  scale2.rangeRound = function(_3) {
    return [r0, r1] = _3, r0 = +r0, r1 = +r1, round = true, rescale();
  };
  scale2.bandwidth = function() {
    return bandwidth;
  };
  scale2.step = function() {
    return step;
  };
  scale2.round = function(_3) {
    return arguments.length ? (round = !!_3, rescale()) : round;
  };
  scale2.padding = function(_3) {
    return arguments.length ? (paddingInner = Math.min(1, paddingOuter = +_3), rescale()) : paddingInner;
  };
  scale2.paddingInner = function(_3) {
    return arguments.length ? (paddingInner = Math.min(1, _3), rescale()) : paddingInner;
  };
  scale2.paddingOuter = function(_3) {
    return arguments.length ? (paddingOuter = +_3, rescale()) : paddingOuter;
  };
  scale2.align = function(_3) {
    return arguments.length ? (align = Math.max(0, Math.min(1, _3)), rescale()) : align;
  };
  scale2.copy = function() {
    return band(domain(), [r0, r1]).round(round).paddingInner(paddingInner).paddingOuter(paddingOuter).align(align);
  };
  return initRange.apply(rescale(), arguments);
}
function pointish(scale2) {
  var copy3 = scale2.copy;
  scale2.padding = scale2.paddingOuter;
  delete scale2.paddingInner;
  delete scale2.paddingOuter;
  scale2.copy = function() {
    return pointish(copy3());
  };
  return scale2;
}
function point5() {
  return pointish(band.apply(null, arguments).paddingInner(1));
}

// node_modules/d3-scale/src/constant.js
function constants(x4) {
  return function() {
    return x4;
  };
}

// node_modules/d3-scale/src/number.js
function number2(x4) {
  return +x4;
}

// node_modules/d3-scale/src/continuous.js
var unit = [0, 1];
function identity3(x4) {
  return x4;
}
function normalize(a5, b6) {
  return (b6 -= a5 = +a5) ? function(x4) {
    return (x4 - a5) / b6;
  } : constants(isNaN(b6) ? NaN : 0.5);
}
function clamper(a5, b6) {
  var t9;
  if (a5 > b6) t9 = a5, a5 = b6, b6 = t9;
  return function(x4) {
    return Math.max(a5, Math.min(b6, x4));
  };
}
function bimap(domain, range2, interpolate2) {
  var d0 = domain[0], d1 = domain[1], r0 = range2[0], r1 = range2[1];
  if (d1 < d0) d0 = normalize(d1, d0), r0 = interpolate2(r1, r0);
  else d0 = normalize(d0, d1), r0 = interpolate2(r0, r1);
  return function(x4) {
    return r0(d0(x4));
  };
}
function polymap(domain, range2, interpolate2) {
  var j5 = Math.min(domain.length, range2.length) - 1, d4 = new Array(j5), r8 = new Array(j5), i8 = -1;
  if (domain[j5] < domain[0]) {
    domain = domain.slice().reverse();
    range2 = range2.slice().reverse();
  }
  while (++i8 < j5) {
    d4[i8] = normalize(domain[i8], domain[i8 + 1]);
    r8[i8] = interpolate2(range2[i8], range2[i8 + 1]);
  }
  return function(x4) {
    var i9 = bisect_default(domain, x4, 1, j5) - 1;
    return r8[i9](d4[i9](x4));
  };
}
function copy(source, target) {
  return target.domain(source.domain()).range(source.range()).interpolate(source.interpolate()).clamp(source.clamp()).unknown(source.unknown());
}
function transformer() {
  var domain = unit, range2 = unit, interpolate2 = value_default, transform, untransform, unknown, clamp2 = identity3, piecewise2, output, input;
  function rescale() {
    var n9 = Math.min(domain.length, range2.length);
    if (clamp2 !== identity3) clamp2 = clamper(domain[0], domain[n9 - 1]);
    piecewise2 = n9 > 2 ? polymap : bimap;
    output = input = null;
    return scale2;
  }
  function scale2(x4) {
    return x4 == null || isNaN(x4 = +x4) ? unknown : (output || (output = piecewise2(domain.map(transform), range2, interpolate2)))(transform(clamp2(x4)));
  }
  scale2.invert = function(y5) {
    return clamp2(untransform((input || (input = piecewise2(range2, domain.map(transform), number_default)))(y5)));
  };
  scale2.domain = function(_3) {
    return arguments.length ? (domain = Array.from(_3, number2), rescale()) : domain.slice();
  };
  scale2.range = function(_3) {
    return arguments.length ? (range2 = Array.from(_3), rescale()) : range2.slice();
  };
  scale2.rangeRound = function(_3) {
    return range2 = Array.from(_3), interpolate2 = round_default, rescale();
  };
  scale2.clamp = function(_3) {
    return arguments.length ? (clamp2 = _3 ? true : identity3, rescale()) : clamp2 !== identity3;
  };
  scale2.interpolate = function(_3) {
    return arguments.length ? (interpolate2 = _3, rescale()) : interpolate2;
  };
  scale2.unknown = function(_3) {
    return arguments.length ? (unknown = _3, scale2) : unknown;
  };
  return function(t9, u8) {
    transform = t9, untransform = u8;
    return rescale();
  };
}
function continuous() {
  return transformer()(identity3, identity3);
}

// node_modules/d3-format/src/formatDecimal.js
function formatDecimal_default(x4) {
  return Math.abs(x4 = Math.round(x4)) >= 1e21 ? x4.toLocaleString("en").replace(/,/g, "") : x4.toString(10);
}
function formatDecimalParts(x4, p3) {
  if ((i8 = (x4 = p3 ? x4.toExponential(p3 - 1) : x4.toExponential()).indexOf("e")) < 0) return null;
  var i8, coefficient = x4.slice(0, i8);
  return [
    coefficient.length > 1 ? coefficient[0] + coefficient.slice(2) : coefficient,
    +x4.slice(i8 + 1)
  ];
}

// node_modules/d3-format/src/exponent.js
function exponent_default(x4) {
  return x4 = formatDecimalParts(Math.abs(x4)), x4 ? x4[1] : NaN;
}

// node_modules/d3-format/src/formatGroup.js
function formatGroup_default(grouping, thousands) {
  return function(value, width) {
    var i8 = value.length, t9 = [], j5 = 0, g2 = grouping[0], length = 0;
    while (i8 > 0 && g2 > 0) {
      if (length + g2 + 1 > width) g2 = Math.max(1, width - length);
      t9.push(value.substring(i8 -= g2, i8 + g2));
      if ((length += g2 + 1) > width) break;
      g2 = grouping[j5 = (j5 + 1) % grouping.length];
    }
    return t9.reverse().join(thousands);
  };
}

// node_modules/d3-format/src/formatNumerals.js
function formatNumerals_default(numerals) {
  return function(value) {
    return value.replace(/[0-9]/g, function(i8) {
      return numerals[+i8];
    });
  };
}

// node_modules/d3-format/src/formatSpecifier.js
var re = /^(?:(.)?([<>=^]))?([+\-( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?(~)?([a-z%])?$/i;
function formatSpecifier(specifier) {
  if (!(match = re.exec(specifier))) throw new Error("invalid format: " + specifier);
  var match;
  return new FormatSpecifier({
    fill: match[1],
    align: match[2],
    sign: match[3],
    symbol: match[4],
    zero: match[5],
    width: match[6],
    comma: match[7],
    precision: match[8] && match[8].slice(1),
    trim: match[9],
    type: match[10]
  });
}
formatSpecifier.prototype = FormatSpecifier.prototype;
function FormatSpecifier(specifier) {
  this.fill = specifier.fill === void 0 ? " " : specifier.fill + "";
  this.align = specifier.align === void 0 ? ">" : specifier.align + "";
  this.sign = specifier.sign === void 0 ? "-" : specifier.sign + "";
  this.symbol = specifier.symbol === void 0 ? "" : specifier.symbol + "";
  this.zero = !!specifier.zero;
  this.width = specifier.width === void 0 ? void 0 : +specifier.width;
  this.comma = !!specifier.comma;
  this.precision = specifier.precision === void 0 ? void 0 : +specifier.precision;
  this.trim = !!specifier.trim;
  this.type = specifier.type === void 0 ? "" : specifier.type + "";
}
FormatSpecifier.prototype.toString = function() {
  return this.fill + this.align + this.sign + this.symbol + (this.zero ? "0" : "") + (this.width === void 0 ? "" : Math.max(1, this.width | 0)) + (this.comma ? "," : "") + (this.precision === void 0 ? "" : "." + Math.max(0, this.precision | 0)) + (this.trim ? "~" : "") + this.type;
};

// node_modules/d3-format/src/formatTrim.js
function formatTrim_default(s5) {
  out: for (var n9 = s5.length, i8 = 1, i0 = -1, i1; i8 < n9; ++i8) {
    switch (s5[i8]) {
      case ".":
        i0 = i1 = i8;
        break;
      case "0":
        if (i0 === 0) i0 = i8;
        i1 = i8;
        break;
      default:
        if (!+s5[i8]) break out;
        if (i0 > 0) i0 = 0;
        break;
    }
  }
  return i0 > 0 ? s5.slice(0, i0) + s5.slice(i1 + 1) : s5;
}

// node_modules/d3-format/src/formatPrefixAuto.js
var prefixExponent;
function formatPrefixAuto_default(x4, p3) {
  var d4 = formatDecimalParts(x4, p3);
  if (!d4) return x4 + "";
  var coefficient = d4[0], exponent = d4[1], i8 = exponent - (prefixExponent = Math.max(-8, Math.min(8, Math.floor(exponent / 3))) * 3) + 1, n9 = coefficient.length;
  return i8 === n9 ? coefficient : i8 > n9 ? coefficient + new Array(i8 - n9 + 1).join("0") : i8 > 0 ? coefficient.slice(0, i8) + "." + coefficient.slice(i8) : "0." + new Array(1 - i8).join("0") + formatDecimalParts(x4, Math.max(0, p3 + i8 - 1))[0];
}

// node_modules/d3-format/src/formatRounded.js
function formatRounded_default(x4, p3) {
  var d4 = formatDecimalParts(x4, p3);
  if (!d4) return x4 + "";
  var coefficient = d4[0], exponent = d4[1];
  return exponent < 0 ? "0." + new Array(-exponent).join("0") + coefficient : coefficient.length > exponent + 1 ? coefficient.slice(0, exponent + 1) + "." + coefficient.slice(exponent + 1) : coefficient + new Array(exponent - coefficient.length + 2).join("0");
}

// node_modules/d3-format/src/formatTypes.js
var formatTypes_default = {
  "%": function(x4, p3) {
    return (x4 * 100).toFixed(p3);
  },
  "b": function(x4) {
    return Math.round(x4).toString(2);
  },
  "c": function(x4) {
    return x4 + "";
  },
  "d": formatDecimal_default,
  "e": function(x4, p3) {
    return x4.toExponential(p3);
  },
  "f": function(x4, p3) {
    return x4.toFixed(p3);
  },
  "g": function(x4, p3) {
    return x4.toPrecision(p3);
  },
  "o": function(x4) {
    return Math.round(x4).toString(8);
  },
  "p": function(x4, p3) {
    return formatRounded_default(x4 * 100, p3);
  },
  "r": formatRounded_default,
  "s": formatPrefixAuto_default,
  "X": function(x4) {
    return Math.round(x4).toString(16).toUpperCase();
  },
  "x": function(x4) {
    return Math.round(x4).toString(16);
  }
};

// node_modules/d3-format/src/identity.js
function identity_default2(x4) {
  return x4;
}

// node_modules/d3-format/src/locale.js
var map3 = Array.prototype.map;
var prefixes2 = ["y", "z", "a", "f", "p", "n", "µ", "m", "", "k", "M", "G", "T", "P", "E", "Z", "Y"];
function locale_default(locale6) {
  var group2 = locale6.grouping === void 0 || locale6.thousands === void 0 ? identity_default2 : formatGroup_default(map3.call(locale6.grouping, Number), locale6.thousands + ""), currencyPrefix = locale6.currency === void 0 ? "" : locale6.currency[0] + "", currencySuffix = locale6.currency === void 0 ? "" : locale6.currency[1] + "", decimal = locale6.decimal === void 0 ? "." : locale6.decimal + "", numerals = locale6.numerals === void 0 ? identity_default2 : formatNumerals_default(map3.call(locale6.numerals, String)), percent = locale6.percent === void 0 ? "%" : locale6.percent + "", minus = locale6.minus === void 0 ? "-" : locale6.minus + "", nan = locale6.nan === void 0 ? "NaN" : locale6.nan + "";
  function newFormat(specifier) {
    specifier = formatSpecifier(specifier);
    var fill = specifier.fill, align = specifier.align, sign2 = specifier.sign, symbol = specifier.symbol, zero3 = specifier.zero, width = specifier.width, comma = specifier.comma, precision = specifier.precision, trim = specifier.trim, type = specifier.type;
    if (type === "n") comma = true, type = "g";
    else if (!formatTypes_default[type]) precision === void 0 && (precision = 12), trim = true, type = "g";
    if (zero3 || fill === "0" && align === "=") zero3 = true, fill = "0", align = "=";
    var prefix2 = symbol === "$" ? currencyPrefix : symbol === "#" && /[boxX]/.test(type) ? "0" + type.toLowerCase() : "", suffix = symbol === "$" ? currencySuffix : /[%p]/.test(type) ? percent : "";
    var formatType = formatTypes_default[type], maybeSuffix = /[defgprs%]/.test(type);
    precision = precision === void 0 ? 6 : /[gprs]/.test(type) ? Math.max(1, Math.min(21, precision)) : Math.max(0, Math.min(20, precision));
    function format2(value) {
      var valuePrefix = prefix2, valueSuffix = suffix, i8, n9, c9;
      if (type === "c") {
        valueSuffix = formatType(value) + valueSuffix;
        value = "";
      } else {
        value = +value;
        var valueNegative = value < 0 || 1 / value < 0;
        value = isNaN(value) ? nan : formatType(Math.abs(value), precision);
        if (trim) value = formatTrim_default(value);
        if (valueNegative && +value === 0 && sign2 !== "+") valueNegative = false;
        valuePrefix = (valueNegative ? sign2 === "(" ? sign2 : minus : sign2 === "-" || sign2 === "(" ? "" : sign2) + valuePrefix;
        valueSuffix = (type === "s" ? prefixes2[8 + prefixExponent / 3] : "") + valueSuffix + (valueNegative && sign2 === "(" ? ")" : "");
        if (maybeSuffix) {
          i8 = -1, n9 = value.length;
          while (++i8 < n9) {
            if (c9 = value.charCodeAt(i8), 48 > c9 || c9 > 57) {
              valueSuffix = (c9 === 46 ? decimal + value.slice(i8 + 1) : value.slice(i8)) + valueSuffix;
              value = value.slice(0, i8);
              break;
            }
          }
        }
      }
      if (comma && !zero3) value = group2(value, Infinity);
      var length = valuePrefix.length + value.length + valueSuffix.length, padding = length < width ? new Array(width - length + 1).join(fill) : "";
      if (comma && zero3) value = group2(padding + value, padding.length ? width - valueSuffix.length : Infinity), padding = "";
      switch (align) {
        case "<":
          value = valuePrefix + value + valueSuffix + padding;
          break;
        case "=":
          value = valuePrefix + padding + value + valueSuffix;
          break;
        case "^":
          value = padding.slice(0, length = padding.length >> 1) + valuePrefix + value + valueSuffix + padding.slice(length);
          break;
        default:
          value = padding + valuePrefix + value + valueSuffix;
          break;
      }
      return numerals(value);
    }
    format2.toString = function() {
      return specifier + "";
    };
    return format2;
  }
  function formatPrefix2(specifier, value) {
    var f2 = newFormat((specifier = formatSpecifier(specifier), specifier.type = "f", specifier)), e11 = Math.max(-8, Math.min(8, Math.floor(exponent_default(value) / 3))) * 3, k4 = Math.pow(10, -e11), prefix2 = prefixes2[8 + e11 / 3];
    return function(value2) {
      return f2(k4 * value2) + prefix2;
    };
  }
  return {
    format: newFormat,
    formatPrefix: formatPrefix2
  };
}

// node_modules/d3-format/src/defaultLocale.js
var locale;
var format;
var formatPrefix;
defaultLocale({
  decimal: ".",
  thousands: ",",
  grouping: [3],
  currency: ["$", ""],
  minus: "-"
});
function defaultLocale(definition) {
  locale = locale_default(definition);
  format = locale.format;
  formatPrefix = locale.formatPrefix;
  return locale;
}

// node_modules/d3-format/src/precisionFixed.js
function precisionFixed_default(step) {
  return Math.max(0, -exponent_default(Math.abs(step)));
}

// node_modules/d3-format/src/precisionPrefix.js
function precisionPrefix_default(step, value) {
  return Math.max(0, Math.max(-8, Math.min(8, Math.floor(exponent_default(value) / 3))) * 3 - exponent_default(Math.abs(step)));
}

// node_modules/d3-format/src/precisionRound.js
function precisionRound_default(step, max3) {
  step = Math.abs(step), max3 = Math.abs(max3) - step;
  return Math.max(0, exponent_default(max3) - exponent_default(step)) + 1;
}

// node_modules/d3-scale/src/tickFormat.js
function tickFormat(start2, stop2, count2, specifier) {
  var step = tickStep(start2, stop2, count2), precision;
  specifier = formatSpecifier(specifier == null ? ",f" : specifier);
  switch (specifier.type) {
    case "s": {
      var value = Math.max(Math.abs(start2), Math.abs(stop2));
      if (specifier.precision == null && !isNaN(precision = precisionPrefix_default(step, value))) specifier.precision = precision;
      return formatPrefix(specifier, value);
    }
    case "":
    case "e":
    case "g":
    case "p":
    case "r": {
      if (specifier.precision == null && !isNaN(precision = precisionRound_default(step, Math.max(Math.abs(start2), Math.abs(stop2))))) specifier.precision = precision - (specifier.type === "e");
      break;
    }
    case "f":
    case "%": {
      if (specifier.precision == null && !isNaN(precision = precisionFixed_default(step))) specifier.precision = precision - (specifier.type === "%") * 2;
      break;
    }
  }
  return format(specifier);
}

// node_modules/d3-scale/src/linear.js
function linearish(scale2) {
  var domain = scale2.domain;
  scale2.ticks = function(count2) {
    var d4 = domain();
    return ticks(d4[0], d4[d4.length - 1], count2 == null ? 10 : count2);
  };
  scale2.tickFormat = function(count2, specifier) {
    var d4 = domain();
    return tickFormat(d4[0], d4[d4.length - 1], count2 == null ? 10 : count2, specifier);
  };
  scale2.nice = function(count2) {
    if (count2 == null) count2 = 10;
    var d4 = domain();
    var i0 = 0;
    var i1 = d4.length - 1;
    var start2 = d4[i0];
    var stop2 = d4[i1];
    var prestep;
    var step;
    var maxIter = 10;
    if (stop2 < start2) {
      step = start2, start2 = stop2, stop2 = step;
      step = i0, i0 = i1, i1 = step;
    }
    while (maxIter-- > 0) {
      step = tickIncrement(start2, stop2, count2);
      if (step === prestep) {
        d4[i0] = start2;
        d4[i1] = stop2;
        return domain(d4);
      } else if (step > 0) {
        start2 = Math.floor(start2 / step) * step;
        stop2 = Math.ceil(stop2 / step) * step;
      } else if (step < 0) {
        start2 = Math.ceil(start2 * step) / step;
        stop2 = Math.floor(stop2 * step) / step;
      } else {
        break;
      }
      prestep = step;
    }
    return scale2;
  };
  return scale2;
}
function linear2() {
  var scale2 = continuous();
  scale2.copy = function() {
    return copy(scale2, linear2());
  };
  initRange.apply(scale2, arguments);
  return linearish(scale2);
}

// node_modules/d3-scale/src/nice.js
function nice2(domain, interval) {
  domain = domain.slice();
  var i0 = 0, i1 = domain.length - 1, x0 = domain[i0], x1 = domain[i1], t9;
  if (x1 < x0) {
    t9 = i0, i0 = i1, i1 = t9;
    t9 = x0, x0 = x1, x1 = t9;
  }
  domain[i0] = interval.floor(x0);
  domain[i1] = interval.ceil(x1);
  return domain;
}

// node_modules/d3-scale/src/log.js
function transformLog(x4) {
  return Math.log(x4);
}
function transformExp(x4) {
  return Math.exp(x4);
}
function transformLogn(x4) {
  return -Math.log(-x4);
}
function transformExpn(x4) {
  return -Math.exp(-x4);
}
function pow10(x4) {
  return isFinite(x4) ? +("1e" + x4) : x4 < 0 ? 0 : x4;
}
function powp(base) {
  return base === 10 ? pow10 : base === Math.E ? Math.exp : (x4) => Math.pow(base, x4);
}
function logp(base) {
  return base === Math.E ? Math.log : base === 10 && Math.log10 || base === 2 && Math.log2 || (base = Math.log(base), (x4) => Math.log(x4) / base);
}
function reflect(f2) {
  return (x4, k4) => -f2(-x4, k4);
}
function loggish(transform) {
  const scale2 = transform(transformLog, transformExp);
  const domain = scale2.domain;
  let base = 10;
  let logs;
  let pows;
  function rescale() {
    logs = logp(base), pows = powp(base);
    if (domain()[0] < 0) {
      logs = reflect(logs), pows = reflect(pows);
      transform(transformLogn, transformExpn);
    } else {
      transform(transformLog, transformExp);
    }
    return scale2;
  }
  scale2.base = function(_3) {
    return arguments.length ? (base = +_3, rescale()) : base;
  };
  scale2.domain = function(_3) {
    return arguments.length ? (domain(_3), rescale()) : domain();
  };
  scale2.ticks = (count2) => {
    const d4 = domain();
    let u8 = d4[0];
    let v3 = d4[d4.length - 1];
    const r8 = v3 < u8;
    if (r8) [u8, v3] = [v3, u8];
    let i8 = logs(u8);
    let j5 = logs(v3);
    let k4;
    let t9;
    const n9 = count2 == null ? 10 : +count2;
    let z5 = [];
    if (!(base % 1) && j5 - i8 < n9) {
      i8 = Math.floor(i8), j5 = Math.ceil(j5);
      if (u8 > 0) for (; i8 <= j5; ++i8) {
        for (k4 = 1; k4 < base; ++k4) {
          t9 = i8 < 0 ? k4 / pows(-i8) : k4 * pows(i8);
          if (t9 < u8) continue;
          if (t9 > v3) break;
          z5.push(t9);
        }
      }
      else for (; i8 <= j5; ++i8) {
        for (k4 = base - 1; k4 >= 1; --k4) {
          t9 = i8 > 0 ? k4 / pows(-i8) : k4 * pows(i8);
          if (t9 < u8) continue;
          if (t9 > v3) break;
          z5.push(t9);
        }
      }
      if (z5.length * 2 < n9) z5 = ticks(u8, v3, n9);
    } else {
      z5 = ticks(i8, j5, Math.min(j5 - i8, n9)).map(pows);
    }
    return r8 ? z5.reverse() : z5;
  };
  scale2.tickFormat = (count2, specifier) => {
    if (count2 == null) count2 = 10;
    if (specifier == null) specifier = base === 10 ? "s" : ",";
    if (typeof specifier !== "function") {
      if (!(base % 1) && (specifier = formatSpecifier(specifier)).precision == null) specifier.trim = true;
      specifier = format(specifier);
    }
    if (count2 === Infinity) return specifier;
    const k4 = Math.max(1, base * count2 / scale2.ticks().length);
    return (d4) => {
      let i8 = d4 / pows(Math.round(logs(d4)));
      if (i8 * base < base - 0.5) i8 *= base;
      return i8 <= k4 ? specifier(d4) : "";
    };
  };
  scale2.nice = () => {
    return domain(nice2(domain(), {
      floor: (x4) => pows(Math.floor(logs(x4))),
      ceil: (x4) => pows(Math.ceil(logs(x4)))
    }));
  };
  return scale2;
}
function log() {
  const scale2 = loggish(transformer()).domain([1, 10]);
  scale2.copy = () => copy(scale2, log()).base(scale2.base());
  initRange.apply(scale2, arguments);
  return scale2;
}

// node_modules/d3-scale/src/symlog.js
function transformSymlog(c9) {
  return function(x4) {
    return Math.sign(x4) * Math.log1p(Math.abs(x4 / c9));
  };
}
function transformSymexp(c9) {
  return function(x4) {
    return Math.sign(x4) * Math.expm1(Math.abs(x4)) * c9;
  };
}
function symlogish(transform) {
  var c9 = 1, scale2 = transform(transformSymlog(c9), transformSymexp(c9));
  scale2.constant = function(_3) {
    return arguments.length ? transform(transformSymlog(c9 = +_3), transformSymexp(c9)) : c9;
  };
  return linearish(scale2);
}
function symlog() {
  var scale2 = symlogish(transformer());
  scale2.copy = function() {
    return copy(scale2, symlog()).constant(scale2.constant());
  };
  return initRange.apply(scale2, arguments);
}

// node_modules/d3-scale/node_modules/d3-time/src/interval.js
var t02 = /* @__PURE__ */ new Date();
var t12 = /* @__PURE__ */ new Date();
function timeInterval(floori, offseti, count2, field) {
  function interval(date2) {
    return floori(date2 = arguments.length === 0 ? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date(+date2)), date2;
  }
  interval.floor = (date2) => {
    return floori(date2 = /* @__PURE__ */ new Date(+date2)), date2;
  };
  interval.ceil = (date2) => {
    return floori(date2 = new Date(date2 - 1)), offseti(date2, 1), floori(date2), date2;
  };
  interval.round = (date2) => {
    const d0 = interval(date2), d1 = interval.ceil(date2);
    return date2 - d0 < d1 - date2 ? d0 : d1;
  };
  interval.offset = (date2, step) => {
    return offseti(date2 = /* @__PURE__ */ new Date(+date2), step == null ? 1 : Math.floor(step)), date2;
  };
  interval.range = (start2, stop2, step) => {
    const range2 = [];
    start2 = interval.ceil(start2);
    step = step == null ? 1 : Math.floor(step);
    if (!(start2 < stop2) || !(step > 0)) return range2;
    let previous;
    do
      range2.push(previous = /* @__PURE__ */ new Date(+start2)), offseti(start2, step), floori(start2);
    while (previous < start2 && start2 < stop2);
    return range2;
  };
  interval.filter = (test) => {
    return timeInterval((date2) => {
      if (date2 >= date2) while (floori(date2), !test(date2)) date2.setTime(date2 - 1);
    }, (date2, step) => {
      if (date2 >= date2) {
        if (step < 0) while (++step <= 0) {
          while (offseti(date2, -1), !test(date2)) {
          }
        }
        else while (--step >= 0) {
          while (offseti(date2, 1), !test(date2)) {
          }
        }
      }
    });
  };
  if (count2) {
    interval.count = (start2, end) => {
      t02.setTime(+start2), t12.setTime(+end);
      floori(t02), floori(t12);
      return Math.floor(count2(t02, t12));
    };
    interval.every = (step) => {
      step = Math.floor(step);
      return !isFinite(step) || !(step > 0) ? null : !(step > 1) ? interval : interval.filter(field ? (d4) => field(d4) % step === 0 : (d4) => interval.count(0, d4) % step === 0);
    };
  }
  return interval;
}

// node_modules/d3-scale/node_modules/d3-time/src/millisecond.js
var millisecond = timeInterval(() => {
}, (date2, step) => {
  date2.setTime(+date2 + step);
}, (start2, end) => {
  return end - start2;
});
millisecond.every = (k4) => {
  k4 = Math.floor(k4);
  if (!isFinite(k4) || !(k4 > 0)) return null;
  if (!(k4 > 1)) return millisecond;
  return timeInterval((date2) => {
    date2.setTime(Math.floor(date2 / k4) * k4);
  }, (date2, step) => {
    date2.setTime(+date2 + step * k4);
  }, (start2, end) => {
    return (end - start2) / k4;
  });
};
var milliseconds = millisecond.range;

// node_modules/d3-scale/node_modules/d3-time/src/duration.js
var durationSecond = 1e3;
var durationMinute = durationSecond * 60;
var durationHour = durationMinute * 60;
var durationDay = durationHour * 24;
var durationWeek = durationDay * 7;
var durationMonth = durationDay * 30;
var durationYear = durationDay * 365;

// node_modules/d3-scale/node_modules/d3-time/src/second.js
var second = timeInterval((date2) => {
  date2.setTime(date2 - date2.getMilliseconds());
}, (date2, step) => {
  date2.setTime(+date2 + step * durationSecond);
}, (start2, end) => {
  return (end - start2) / durationSecond;
}, (date2) => {
  return date2.getUTCSeconds();
});
var seconds = second.range;

// node_modules/d3-scale/node_modules/d3-time/src/minute.js
var timeMinute = timeInterval((date2) => {
  date2.setTime(date2 - date2.getMilliseconds() - date2.getSeconds() * durationSecond);
}, (date2, step) => {
  date2.setTime(+date2 + step * durationMinute);
}, (start2, end) => {
  return (end - start2) / durationMinute;
}, (date2) => {
  return date2.getMinutes();
});
var timeMinutes = timeMinute.range;
var utcMinute = timeInterval((date2) => {
  date2.setUTCSeconds(0, 0);
}, (date2, step) => {
  date2.setTime(+date2 + step * durationMinute);
}, (start2, end) => {
  return (end - start2) / durationMinute;
}, (date2) => {
  return date2.getUTCMinutes();
});
var utcMinutes = utcMinute.range;

// node_modules/d3-scale/node_modules/d3-time/src/hour.js
var timeHour = timeInterval((date2) => {
  date2.setTime(date2 - date2.getMilliseconds() - date2.getSeconds() * durationSecond - date2.getMinutes() * durationMinute);
}, (date2, step) => {
  date2.setTime(+date2 + step * durationHour);
}, (start2, end) => {
  return (end - start2) / durationHour;
}, (date2) => {
  return date2.getHours();
});
var timeHours = timeHour.range;
var utcHour = timeInterval((date2) => {
  date2.setUTCMinutes(0, 0, 0);
}, (date2, step) => {
  date2.setTime(+date2 + step * durationHour);
}, (start2, end) => {
  return (end - start2) / durationHour;
}, (date2) => {
  return date2.getUTCHours();
});
var utcHours = utcHour.range;

// node_modules/d3-scale/node_modules/d3-time/src/day.js
var timeDay = timeInterval(
  (date2) => date2.setHours(0, 0, 0, 0),
  (date2, step) => date2.setDate(date2.getDate() + step),
  (start2, end) => (end - start2 - (end.getTimezoneOffset() - start2.getTimezoneOffset()) * durationMinute) / durationDay,
  (date2) => date2.getDate() - 1
);
var timeDays = timeDay.range;
var utcDay = timeInterval((date2) => {
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setUTCDate(date2.getUTCDate() + step);
}, (start2, end) => {
  return (end - start2) / durationDay;
}, (date2) => {
  return date2.getUTCDate() - 1;
});
var utcDays = utcDay.range;
var unixDay = timeInterval((date2) => {
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setUTCDate(date2.getUTCDate() + step);
}, (start2, end) => {
  return (end - start2) / durationDay;
}, (date2) => {
  return Math.floor(date2 / durationDay);
});
var unixDays = unixDay.range;

// node_modules/d3-scale/node_modules/d3-time/src/week.js
function timeWeekday(i8) {
  return timeInterval((date2) => {
    date2.setDate(date2.getDate() - (date2.getDay() + 7 - i8) % 7);
    date2.setHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setDate(date2.getDate() + step * 7);
  }, (start2, end) => {
    return (end - start2 - (end.getTimezoneOffset() - start2.getTimezoneOffset()) * durationMinute) / durationWeek;
  });
}
var timeSunday = timeWeekday(0);
var timeMonday = timeWeekday(1);
var timeTuesday = timeWeekday(2);
var timeWednesday = timeWeekday(3);
var timeThursday = timeWeekday(4);
var timeFriday = timeWeekday(5);
var timeSaturday = timeWeekday(6);
var timeSundays = timeSunday.range;
var timeMondays = timeMonday.range;
var timeTuesdays = timeTuesday.range;
var timeWednesdays = timeWednesday.range;
var timeThursdays = timeThursday.range;
var timeFridays = timeFriday.range;
var timeSaturdays = timeSaturday.range;
function utcWeekday(i8) {
  return timeInterval((date2) => {
    date2.setUTCDate(date2.getUTCDate() - (date2.getUTCDay() + 7 - i8) % 7);
    date2.setUTCHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setUTCDate(date2.getUTCDate() + step * 7);
  }, (start2, end) => {
    return (end - start2) / durationWeek;
  });
}
var utcSunday = utcWeekday(0);
var utcMonday = utcWeekday(1);
var utcTuesday = utcWeekday(2);
var utcWednesday = utcWeekday(3);
var utcThursday = utcWeekday(4);
var utcFriday = utcWeekday(5);
var utcSaturday = utcWeekday(6);
var utcSundays = utcSunday.range;
var utcMondays = utcMonday.range;
var utcTuesdays = utcTuesday.range;
var utcWednesdays = utcWednesday.range;
var utcThursdays = utcThursday.range;
var utcFridays = utcFriday.range;
var utcSaturdays = utcSaturday.range;

// node_modules/d3-scale/node_modules/d3-time/src/month.js
var timeMonth = timeInterval((date2) => {
  date2.setDate(1);
  date2.setHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setMonth(date2.getMonth() + step);
}, (start2, end) => {
  return end.getMonth() - start2.getMonth() + (end.getFullYear() - start2.getFullYear()) * 12;
}, (date2) => {
  return date2.getMonth();
});
var timeMonths = timeMonth.range;
var utcMonth = timeInterval((date2) => {
  date2.setUTCDate(1);
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setUTCMonth(date2.getUTCMonth() + step);
}, (start2, end) => {
  return end.getUTCMonth() - start2.getUTCMonth() + (end.getUTCFullYear() - start2.getUTCFullYear()) * 12;
}, (date2) => {
  return date2.getUTCMonth();
});
var utcMonths = utcMonth.range;

// node_modules/d3-scale/node_modules/d3-time/src/year.js
var timeYear = timeInterval((date2) => {
  date2.setMonth(0, 1);
  date2.setHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setFullYear(date2.getFullYear() + step);
}, (start2, end) => {
  return end.getFullYear() - start2.getFullYear();
}, (date2) => {
  return date2.getFullYear();
});
timeYear.every = (k4) => {
  return !isFinite(k4 = Math.floor(k4)) || !(k4 > 0) ? null : timeInterval((date2) => {
    date2.setFullYear(Math.floor(date2.getFullYear() / k4) * k4);
    date2.setMonth(0, 1);
    date2.setHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setFullYear(date2.getFullYear() + step * k4);
  });
};
var timeYears = timeYear.range;
var utcYear = timeInterval((date2) => {
  date2.setUTCMonth(0, 1);
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setUTCFullYear(date2.getUTCFullYear() + step);
}, (start2, end) => {
  return end.getUTCFullYear() - start2.getUTCFullYear();
}, (date2) => {
  return date2.getUTCFullYear();
});
utcYear.every = (k4) => {
  return !isFinite(k4 = Math.floor(k4)) || !(k4 > 0) ? null : timeInterval((date2) => {
    date2.setUTCFullYear(Math.floor(date2.getUTCFullYear() / k4) * k4);
    date2.setUTCMonth(0, 1);
    date2.setUTCHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setUTCFullYear(date2.getUTCFullYear() + step * k4);
  });
};
var utcYears = utcYear.range;

// node_modules/d3-scale/node_modules/d3-time/src/ticks.js
function ticker(year2, month2, week, day2, hour2, minute2) {
  const tickIntervals = [
    [second, 1, durationSecond],
    [second, 5, 5 * durationSecond],
    [second, 15, 15 * durationSecond],
    [second, 30, 30 * durationSecond],
    [minute2, 1, durationMinute],
    [minute2, 5, 5 * durationMinute],
    [minute2, 15, 15 * durationMinute],
    [minute2, 30, 30 * durationMinute],
    [hour2, 1, durationHour],
    [hour2, 3, 3 * durationHour],
    [hour2, 6, 6 * durationHour],
    [hour2, 12, 12 * durationHour],
    [day2, 1, durationDay],
    [day2, 2, 2 * durationDay],
    [week, 1, durationWeek],
    [month2, 1, durationMonth],
    [month2, 3, 3 * durationMonth],
    [year2, 1, durationYear]
  ];
  function ticks2(start2, stop2, count2) {
    const reverse2 = stop2 < start2;
    if (reverse2) [start2, stop2] = [stop2, start2];
    const interval = count2 && typeof count2.range === "function" ? count2 : tickInterval(start2, stop2, count2);
    const ticks3 = interval ? interval.range(start2, +stop2 + 1) : [];
    return reverse2 ? ticks3.reverse() : ticks3;
  }
  function tickInterval(start2, stop2, count2) {
    const target = Math.abs(stop2 - start2) / count2;
    const i8 = bisector(([, , step2]) => step2).right(tickIntervals, target);
    if (i8 === tickIntervals.length) return year2.every(tickStep(start2 / durationYear, stop2 / durationYear, count2));
    if (i8 === 0) return millisecond.every(Math.max(tickStep(start2, stop2, count2), 1));
    const [t9, step] = tickIntervals[target / tickIntervals[i8 - 1][2] < tickIntervals[i8][2] / target ? i8 - 1 : i8];
    return t9.every(step);
  }
  return [ticks2, tickInterval];
}
var [utcTicks, utcTickInterval] = ticker(utcYear, utcMonth, utcSunday, unixDay, utcHour, utcMinute);
var [timeTicks, timeTickInterval] = ticker(timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute);

// node_modules/d3-time/src/interval.js
var t03 = /* @__PURE__ */ new Date();
var t13 = /* @__PURE__ */ new Date();
function newInterval(floori, offseti, count2, field) {
  function interval(date2) {
    return floori(date2 = arguments.length === 0 ? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date(+date2)), date2;
  }
  interval.floor = function(date2) {
    return floori(date2 = /* @__PURE__ */ new Date(+date2)), date2;
  };
  interval.ceil = function(date2) {
    return floori(date2 = new Date(date2 - 1)), offseti(date2, 1), floori(date2), date2;
  };
  interval.round = function(date2) {
    var d0 = interval(date2), d1 = interval.ceil(date2);
    return date2 - d0 < d1 - date2 ? d0 : d1;
  };
  interval.offset = function(date2, step) {
    return offseti(date2 = /* @__PURE__ */ new Date(+date2), step == null ? 1 : Math.floor(step)), date2;
  };
  interval.range = function(start2, stop2, step) {
    var range2 = [], previous;
    start2 = interval.ceil(start2);
    step = step == null ? 1 : Math.floor(step);
    if (!(start2 < stop2) || !(step > 0)) return range2;
    do
      range2.push(previous = /* @__PURE__ */ new Date(+start2)), offseti(start2, step), floori(start2);
    while (previous < start2 && start2 < stop2);
    return range2;
  };
  interval.filter = function(test) {
    return newInterval(function(date2) {
      if (date2 >= date2) while (floori(date2), !test(date2)) date2.setTime(date2 - 1);
    }, function(date2, step) {
      if (date2 >= date2) {
        if (step < 0) while (++step <= 0) {
          while (offseti(date2, -1), !test(date2)) {
          }
        }
        else while (--step >= 0) {
          while (offseti(date2, 1), !test(date2)) {
          }
        }
      }
    });
  };
  if (count2) {
    interval.count = function(start2, end) {
      t03.setTime(+start2), t13.setTime(+end);
      floori(t03), floori(t13);
      return Math.floor(count2(t03, t13));
    };
    interval.every = function(step) {
      step = Math.floor(step);
      return !isFinite(step) || !(step > 0) ? null : !(step > 1) ? interval : interval.filter(field ? function(d4) {
        return field(d4) % step === 0;
      } : function(d4) {
        return interval.count(0, d4) % step === 0;
      });
    };
  }
  return interval;
}

// node_modules/d3-time/src/millisecond.js
var millisecond2 = newInterval(function() {
}, function(date2, step) {
  date2.setTime(+date2 + step);
}, function(start2, end) {
  return end - start2;
});
millisecond2.every = function(k4) {
  k4 = Math.floor(k4);
  if (!isFinite(k4) || !(k4 > 0)) return null;
  if (!(k4 > 1)) return millisecond2;
  return newInterval(function(date2) {
    date2.setTime(Math.floor(date2 / k4) * k4);
  }, function(date2, step) {
    date2.setTime(+date2 + step * k4);
  }, function(start2, end) {
    return (end - start2) / k4;
  });
};
var millisecond_default = millisecond2;
var milliseconds2 = millisecond2.range;

// node_modules/d3-time/src/duration.js
var durationSecond2 = 1e3;
var durationMinute2 = 6e4;
var durationHour2 = 36e5;
var durationDay2 = 864e5;
var durationWeek2 = 6048e5;

// node_modules/d3-time/src/second.js
var second2 = newInterval(function(date2) {
  date2.setTime(date2 - date2.getMilliseconds());
}, function(date2, step) {
  date2.setTime(+date2 + step * durationSecond2);
}, function(start2, end) {
  return (end - start2) / durationSecond2;
}, function(date2) {
  return date2.getUTCSeconds();
});
var second_default = second2;
var seconds2 = second2.range;

// node_modules/d3-time/src/minute.js
var minute = newInterval(function(date2) {
  date2.setTime(date2 - date2.getMilliseconds() - date2.getSeconds() * durationSecond2);
}, function(date2, step) {
  date2.setTime(+date2 + step * durationMinute2);
}, function(start2, end) {
  return (end - start2) / durationMinute2;
}, function(date2) {
  return date2.getMinutes();
});
var minute_default = minute;
var minutes = minute.range;

// node_modules/d3-time/src/hour.js
var hour = newInterval(function(date2) {
  date2.setTime(date2 - date2.getMilliseconds() - date2.getSeconds() * durationSecond2 - date2.getMinutes() * durationMinute2);
}, function(date2, step) {
  date2.setTime(+date2 + step * durationHour2);
}, function(start2, end) {
  return (end - start2) / durationHour2;
}, function(date2) {
  return date2.getHours();
});
var hour_default = hour;
var hours = hour.range;

// node_modules/d3-time/src/day.js
var day = newInterval(function(date2) {
  date2.setHours(0, 0, 0, 0);
}, function(date2, step) {
  date2.setDate(date2.getDate() + step);
}, function(start2, end) {
  return (end - start2 - (end.getTimezoneOffset() - start2.getTimezoneOffset()) * durationMinute2) / durationDay2;
}, function(date2) {
  return date2.getDate() - 1;
});
var day_default = day;
var days = day.range;

// node_modules/d3-time/src/week.js
function weekday(i8) {
  return newInterval(function(date2) {
    date2.setDate(date2.getDate() - (date2.getDay() + 7 - i8) % 7);
    date2.setHours(0, 0, 0, 0);
  }, function(date2, step) {
    date2.setDate(date2.getDate() + step * 7);
  }, function(start2, end) {
    return (end - start2 - (end.getTimezoneOffset() - start2.getTimezoneOffset()) * durationMinute2) / durationWeek2;
  });
}
var sunday = weekday(0);
var monday = weekday(1);
var tuesday = weekday(2);
var wednesday = weekday(3);
var thursday = weekday(4);
var friday = weekday(5);
var saturday = weekday(6);
var sundays = sunday.range;
var mondays = monday.range;
var tuesdays = tuesday.range;
var wednesdays = wednesday.range;
var thursdays = thursday.range;
var fridays = friday.range;
var saturdays = saturday.range;

// node_modules/d3-time/src/month.js
var month = newInterval(function(date2) {
  date2.setDate(1);
  date2.setHours(0, 0, 0, 0);
}, function(date2, step) {
  date2.setMonth(date2.getMonth() + step);
}, function(start2, end) {
  return end.getMonth() - start2.getMonth() + (end.getFullYear() - start2.getFullYear()) * 12;
}, function(date2) {
  return date2.getMonth();
});
var month_default = month;
var months = month.range;

// node_modules/d3-time/src/year.js
var year = newInterval(function(date2) {
  date2.setMonth(0, 1);
  date2.setHours(0, 0, 0, 0);
}, function(date2, step) {
  date2.setFullYear(date2.getFullYear() + step);
}, function(start2, end) {
  return end.getFullYear() - start2.getFullYear();
}, function(date2) {
  return date2.getFullYear();
});
year.every = function(k4) {
  return !isFinite(k4 = Math.floor(k4)) || !(k4 > 0) ? null : newInterval(function(date2) {
    date2.setFullYear(Math.floor(date2.getFullYear() / k4) * k4);
    date2.setMonth(0, 1);
    date2.setHours(0, 0, 0, 0);
  }, function(date2, step) {
    date2.setFullYear(date2.getFullYear() + step * k4);
  });
};
var year_default = year;
var years = year.range;

// node_modules/d3-time/src/utcMinute.js
var utcMinute2 = newInterval(function(date2) {
  date2.setUTCSeconds(0, 0);
}, function(date2, step) {
  date2.setTime(+date2 + step * durationMinute2);
}, function(start2, end) {
  return (end - start2) / durationMinute2;
}, function(date2) {
  return date2.getUTCMinutes();
});
var utcMinute_default = utcMinute2;
var utcMinutes2 = utcMinute2.range;

// node_modules/d3-time/src/utcHour.js
var utcHour2 = newInterval(function(date2) {
  date2.setUTCMinutes(0, 0, 0);
}, function(date2, step) {
  date2.setTime(+date2 + step * durationHour2);
}, function(start2, end) {
  return (end - start2) / durationHour2;
}, function(date2) {
  return date2.getUTCHours();
});
var utcHour_default = utcHour2;
var utcHours2 = utcHour2.range;

// node_modules/d3-time/src/utcDay.js
var utcDay2 = newInterval(function(date2) {
  date2.setUTCHours(0, 0, 0, 0);
}, function(date2, step) {
  date2.setUTCDate(date2.getUTCDate() + step);
}, function(start2, end) {
  return (end - start2) / durationDay2;
}, function(date2) {
  return date2.getUTCDate() - 1;
});
var utcDay_default = utcDay2;
var utcDays2 = utcDay2.range;

// node_modules/d3-time/src/utcWeek.js
function utcWeekday2(i8) {
  return newInterval(function(date2) {
    date2.setUTCDate(date2.getUTCDate() - (date2.getUTCDay() + 7 - i8) % 7);
    date2.setUTCHours(0, 0, 0, 0);
  }, function(date2, step) {
    date2.setUTCDate(date2.getUTCDate() + step * 7);
  }, function(start2, end) {
    return (end - start2) / durationWeek2;
  });
}
var utcSunday2 = utcWeekday2(0);
var utcMonday2 = utcWeekday2(1);
var utcTuesday2 = utcWeekday2(2);
var utcWednesday2 = utcWeekday2(3);
var utcThursday2 = utcWeekday2(4);
var utcFriday2 = utcWeekday2(5);
var utcSaturday2 = utcWeekday2(6);
var utcSundays2 = utcSunday2.range;
var utcMondays2 = utcMonday2.range;
var utcTuesdays2 = utcTuesday2.range;
var utcWednesdays2 = utcWednesday2.range;
var utcThursdays2 = utcThursday2.range;
var utcFridays2 = utcFriday2.range;
var utcSaturdays2 = utcSaturday2.range;

// node_modules/d3-time/src/utcMonth.js
var utcMonth2 = newInterval(function(date2) {
  date2.setUTCDate(1);
  date2.setUTCHours(0, 0, 0, 0);
}, function(date2, step) {
  date2.setUTCMonth(date2.getUTCMonth() + step);
}, function(start2, end) {
  return end.getUTCMonth() - start2.getUTCMonth() + (end.getUTCFullYear() - start2.getUTCFullYear()) * 12;
}, function(date2) {
  return date2.getUTCMonth();
});
var utcMonth_default = utcMonth2;
var utcMonths2 = utcMonth2.range;

// node_modules/d3-time/src/utcYear.js
var utcYear2 = newInterval(function(date2) {
  date2.setUTCMonth(0, 1);
  date2.setUTCHours(0, 0, 0, 0);
}, function(date2, step) {
  date2.setUTCFullYear(date2.getUTCFullYear() + step);
}, function(start2, end) {
  return end.getUTCFullYear() - start2.getUTCFullYear();
}, function(date2) {
  return date2.getUTCFullYear();
});
utcYear2.every = function(k4) {
  return !isFinite(k4 = Math.floor(k4)) || !(k4 > 0) ? null : newInterval(function(date2) {
    date2.setUTCFullYear(Math.floor(date2.getUTCFullYear() / k4) * k4);
    date2.setUTCMonth(0, 1);
    date2.setUTCHours(0, 0, 0, 0);
  }, function(date2, step) {
    date2.setUTCFullYear(date2.getUTCFullYear() + step * k4);
  });
};
var utcYear_default = utcYear2;
var utcYears2 = utcYear2.range;

// node_modules/d3-time-format/src/locale.js
function localDate(d4) {
  if (0 <= d4.y && d4.y < 100) {
    var date2 = new Date(-1, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L);
    date2.setFullYear(d4.y);
    return date2;
  }
  return new Date(d4.y, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L);
}
function utcDate(d4) {
  if (0 <= d4.y && d4.y < 100) {
    var date2 = new Date(Date.UTC(-1, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L));
    date2.setUTCFullYear(d4.y);
    return date2;
  }
  return new Date(Date.UTC(d4.y, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L));
}
function newDate(y5, m4, d4) {
  return { y: y5, m: m4, d: d4, H: 0, M: 0, S: 0, L: 0 };
}
function formatLocale(locale6) {
  var locale_dateTime = locale6.dateTime, locale_date = locale6.date, locale_time = locale6.time, locale_periods = locale6.periods, locale_weekdays = locale6.days, locale_shortWeekdays = locale6.shortDays, locale_months = locale6.months, locale_shortMonths = locale6.shortMonths;
  var periodRe = formatRe(locale_periods), periodLookup = formatLookup(locale_periods), weekdayRe = formatRe(locale_weekdays), weekdayLookup = formatLookup(locale_weekdays), shortWeekdayRe = formatRe(locale_shortWeekdays), shortWeekdayLookup = formatLookup(locale_shortWeekdays), monthRe = formatRe(locale_months), monthLookup = formatLookup(locale_months), shortMonthRe = formatRe(locale_shortMonths), shortMonthLookup = formatLookup(locale_shortMonths);
  var formats = {
    "a": formatShortWeekday,
    "A": formatWeekday,
    "b": formatShortMonth,
    "B": formatMonth,
    "c": null,
    "d": formatDayOfMonth,
    "e": formatDayOfMonth,
    "f": formatMicroseconds,
    "g": formatYearISO,
    "G": formatFullYearISO,
    "H": formatHour24,
    "I": formatHour12,
    "j": formatDayOfYear,
    "L": formatMilliseconds,
    "m": formatMonthNumber,
    "M": formatMinutes,
    "p": formatPeriod,
    "q": formatQuarter,
    "Q": formatUnixTimestamp,
    "s": formatUnixTimestampSeconds,
    "S": formatSeconds,
    "u": formatWeekdayNumberMonday,
    "U": formatWeekNumberSunday,
    "V": formatWeekNumberISO,
    "w": formatWeekdayNumberSunday,
    "W": formatWeekNumberMonday,
    "x": null,
    "X": null,
    "y": formatYear,
    "Y": formatFullYear,
    "Z": formatZone,
    "%": formatLiteralPercent
  };
  var utcFormats = {
    "a": formatUTCShortWeekday,
    "A": formatUTCWeekday,
    "b": formatUTCShortMonth,
    "B": formatUTCMonth,
    "c": null,
    "d": formatUTCDayOfMonth,
    "e": formatUTCDayOfMonth,
    "f": formatUTCMicroseconds,
    "g": formatUTCYearISO,
    "G": formatUTCFullYearISO,
    "H": formatUTCHour24,
    "I": formatUTCHour12,
    "j": formatUTCDayOfYear,
    "L": formatUTCMilliseconds,
    "m": formatUTCMonthNumber,
    "M": formatUTCMinutes,
    "p": formatUTCPeriod,
    "q": formatUTCQuarter,
    "Q": formatUnixTimestamp,
    "s": formatUnixTimestampSeconds,
    "S": formatUTCSeconds,
    "u": formatUTCWeekdayNumberMonday,
    "U": formatUTCWeekNumberSunday,
    "V": formatUTCWeekNumberISO,
    "w": formatUTCWeekdayNumberSunday,
    "W": formatUTCWeekNumberMonday,
    "x": null,
    "X": null,
    "y": formatUTCYear,
    "Y": formatUTCFullYear,
    "Z": formatUTCZone,
    "%": formatLiteralPercent
  };
  var parses = {
    "a": parseShortWeekday,
    "A": parseWeekday,
    "b": parseShortMonth,
    "B": parseMonth,
    "c": parseLocaleDateTime,
    "d": parseDayOfMonth,
    "e": parseDayOfMonth,
    "f": parseMicroseconds,
    "g": parseYear,
    "G": parseFullYear,
    "H": parseHour24,
    "I": parseHour24,
    "j": parseDayOfYear,
    "L": parseMilliseconds,
    "m": parseMonthNumber,
    "M": parseMinutes,
    "p": parsePeriod,
    "q": parseQuarter,
    "Q": parseUnixTimestamp,
    "s": parseUnixTimestampSeconds,
    "S": parseSeconds,
    "u": parseWeekdayNumberMonday,
    "U": parseWeekNumberSunday,
    "V": parseWeekNumberISO,
    "w": parseWeekdayNumberSunday,
    "W": parseWeekNumberMonday,
    "x": parseLocaleDate,
    "X": parseLocaleTime,
    "y": parseYear,
    "Y": parseFullYear,
    "Z": parseZone,
    "%": parseLiteralPercent
  };
  formats.x = newFormat(locale_date, formats);
  formats.X = newFormat(locale_time, formats);
  formats.c = newFormat(locale_dateTime, formats);
  utcFormats.x = newFormat(locale_date, utcFormats);
  utcFormats.X = newFormat(locale_time, utcFormats);
  utcFormats.c = newFormat(locale_dateTime, utcFormats);
  function newFormat(specifier, formats2) {
    return function(date2) {
      var string = [], i8 = -1, j5 = 0, n9 = specifier.length, c9, pad5, format2;
      if (!(date2 instanceof Date)) date2 = /* @__PURE__ */ new Date(+date2);
      while (++i8 < n9) {
        if (specifier.charCodeAt(i8) === 37) {
          string.push(specifier.slice(j5, i8));
          if ((pad5 = pads[c9 = specifier.charAt(++i8)]) != null) c9 = specifier.charAt(++i8);
          else pad5 = c9 === "e" ? " " : "0";
          if (format2 = formats2[c9]) c9 = format2(date2, pad5);
          string.push(c9);
          j5 = i8 + 1;
        }
      }
      string.push(specifier.slice(j5, i8));
      return string.join("");
    };
  }
  function newParse(specifier, Z2) {
    return function(string) {
      var d4 = newDate(1900, void 0, 1), i8 = parseSpecifier(d4, specifier, string += "", 0), week, day2;
      if (i8 != string.length) return null;
      if ("Q" in d4) return new Date(d4.Q);
      if ("s" in d4) return new Date(d4.s * 1e3 + ("L" in d4 ? d4.L : 0));
      if (Z2 && !("Z" in d4)) d4.Z = 0;
      if ("p" in d4) d4.H = d4.H % 12 + d4.p * 12;
      if (d4.m === void 0) d4.m = "q" in d4 ? d4.q : 0;
      if ("V" in d4) {
        if (d4.V < 1 || d4.V > 53) return null;
        if (!("w" in d4)) d4.w = 1;
        if ("Z" in d4) {
          week = utcDate(newDate(d4.y, 0, 1)), day2 = week.getUTCDay();
          week = day2 > 4 || day2 === 0 ? utcMonday2.ceil(week) : utcMonday2(week);
          week = utcDay_default.offset(week, (d4.V - 1) * 7);
          d4.y = week.getUTCFullYear();
          d4.m = week.getUTCMonth();
          d4.d = week.getUTCDate() + (d4.w + 6) % 7;
        } else {
          week = localDate(newDate(d4.y, 0, 1)), day2 = week.getDay();
          week = day2 > 4 || day2 === 0 ? monday.ceil(week) : monday(week);
          week = day_default.offset(week, (d4.V - 1) * 7);
          d4.y = week.getFullYear();
          d4.m = week.getMonth();
          d4.d = week.getDate() + (d4.w + 6) % 7;
        }
      } else if ("W" in d4 || "U" in d4) {
        if (!("w" in d4)) d4.w = "u" in d4 ? d4.u % 7 : "W" in d4 ? 1 : 0;
        day2 = "Z" in d4 ? utcDate(newDate(d4.y, 0, 1)).getUTCDay() : localDate(newDate(d4.y, 0, 1)).getDay();
        d4.m = 0;
        d4.d = "W" in d4 ? (d4.w + 6) % 7 + d4.W * 7 - (day2 + 5) % 7 : d4.w + d4.U * 7 - (day2 + 6) % 7;
      }
      if ("Z" in d4) {
        d4.H += d4.Z / 100 | 0;
        d4.M += d4.Z % 100;
        return utcDate(d4);
      }
      return localDate(d4);
    };
  }
  function parseSpecifier(d4, specifier, string, j5) {
    var i8 = 0, n9 = specifier.length, m4 = string.length, c9, parse;
    while (i8 < n9) {
      if (j5 >= m4) return -1;
      c9 = specifier.charCodeAt(i8++);
      if (c9 === 37) {
        c9 = specifier.charAt(i8++);
        parse = parses[c9 in pads ? specifier.charAt(i8++) : c9];
        if (!parse || (j5 = parse(d4, string, j5)) < 0) return -1;
      } else if (c9 != string.charCodeAt(j5++)) {
        return -1;
      }
    }
    return j5;
  }
  function parsePeriod(d4, string, i8) {
    var n9 = periodRe.exec(string.slice(i8));
    return n9 ? (d4.p = periodLookup[n9[0].toLowerCase()], i8 + n9[0].length) : -1;
  }
  function parseShortWeekday(d4, string, i8) {
    var n9 = shortWeekdayRe.exec(string.slice(i8));
    return n9 ? (d4.w = shortWeekdayLookup[n9[0].toLowerCase()], i8 + n9[0].length) : -1;
  }
  function parseWeekday(d4, string, i8) {
    var n9 = weekdayRe.exec(string.slice(i8));
    return n9 ? (d4.w = weekdayLookup[n9[0].toLowerCase()], i8 + n9[0].length) : -1;
  }
  function parseShortMonth(d4, string, i8) {
    var n9 = shortMonthRe.exec(string.slice(i8));
    return n9 ? (d4.m = shortMonthLookup[n9[0].toLowerCase()], i8 + n9[0].length) : -1;
  }
  function parseMonth(d4, string, i8) {
    var n9 = monthRe.exec(string.slice(i8));
    return n9 ? (d4.m = monthLookup[n9[0].toLowerCase()], i8 + n9[0].length) : -1;
  }
  function parseLocaleDateTime(d4, string, i8) {
    return parseSpecifier(d4, locale_dateTime, string, i8);
  }
  function parseLocaleDate(d4, string, i8) {
    return parseSpecifier(d4, locale_date, string, i8);
  }
  function parseLocaleTime(d4, string, i8) {
    return parseSpecifier(d4, locale_time, string, i8);
  }
  function formatShortWeekday(d4) {
    return locale_shortWeekdays[d4.getDay()];
  }
  function formatWeekday(d4) {
    return locale_weekdays[d4.getDay()];
  }
  function formatShortMonth(d4) {
    return locale_shortMonths[d4.getMonth()];
  }
  function formatMonth(d4) {
    return locale_months[d4.getMonth()];
  }
  function formatPeriod(d4) {
    return locale_periods[+(d4.getHours() >= 12)];
  }
  function formatQuarter(d4) {
    return 1 + ~~(d4.getMonth() / 3);
  }
  function formatUTCShortWeekday(d4) {
    return locale_shortWeekdays[d4.getUTCDay()];
  }
  function formatUTCWeekday(d4) {
    return locale_weekdays[d4.getUTCDay()];
  }
  function formatUTCShortMonth(d4) {
    return locale_shortMonths[d4.getUTCMonth()];
  }
  function formatUTCMonth(d4) {
    return locale_months[d4.getUTCMonth()];
  }
  function formatUTCPeriod(d4) {
    return locale_periods[+(d4.getUTCHours() >= 12)];
  }
  function formatUTCQuarter(d4) {
    return 1 + ~~(d4.getUTCMonth() / 3);
  }
  return {
    format: function(specifier) {
      var f2 = newFormat(specifier += "", formats);
      f2.toString = function() {
        return specifier;
      };
      return f2;
    },
    parse: function(specifier) {
      var p3 = newParse(specifier += "", false);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    },
    utcFormat: function(specifier) {
      var f2 = newFormat(specifier += "", utcFormats);
      f2.toString = function() {
        return specifier;
      };
      return f2;
    },
    utcParse: function(specifier) {
      var p3 = newParse(specifier += "", true);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    }
  };
}
var pads = { "-": "", "_": " ", "0": "0" };
var numberRe = /^\s*\d+/;
var percentRe = /^%/;
var requoteRe = /[\\^$*+?|[\]().{}]/g;
function pad(value, fill, width) {
  var sign2 = value < 0 ? "-" : "", string = (sign2 ? -value : value) + "", length = string.length;
  return sign2 + (length < width ? new Array(width - length + 1).join(fill) + string : string);
}
function requote(s5) {
  return s5.replace(requoteRe, "\\$&");
}
function formatRe(names) {
  return new RegExp("^(?:" + names.map(requote).join("|") + ")", "i");
}
function formatLookup(names) {
  var map4 = {}, i8 = -1, n9 = names.length;
  while (++i8 < n9) map4[names[i8].toLowerCase()] = i8;
  return map4;
}
function parseWeekdayNumberSunday(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.w = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekdayNumberMonday(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.u = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberSunday(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.U = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberISO(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.V = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberMonday(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.W = +n9[0], i8 + n9[0].length) : -1;
}
function parseFullYear(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 4));
  return n9 ? (d4.y = +n9[0], i8 + n9[0].length) : -1;
}
function parseYear(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.y = +n9[0] + (+n9[0] > 68 ? 1900 : 2e3), i8 + n9[0].length) : -1;
}
function parseZone(d4, string, i8) {
  var n9 = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i8, i8 + 6));
  return n9 ? (d4.Z = n9[1] ? 0 : -(n9[2] + (n9[3] || "00")), i8 + n9[0].length) : -1;
}
function parseQuarter(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.q = n9[0] * 3 - 3, i8 + n9[0].length) : -1;
}
function parseMonthNumber(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.m = n9[0] - 1, i8 + n9[0].length) : -1;
}
function parseDayOfMonth(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.d = +n9[0], i8 + n9[0].length) : -1;
}
function parseDayOfYear(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 3));
  return n9 ? (d4.m = 0, d4.d = +n9[0], i8 + n9[0].length) : -1;
}
function parseHour24(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.H = +n9[0], i8 + n9[0].length) : -1;
}
function parseMinutes(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.M = +n9[0], i8 + n9[0].length) : -1;
}
function parseSeconds(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.S = +n9[0], i8 + n9[0].length) : -1;
}
function parseMilliseconds(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 3));
  return n9 ? (d4.L = +n9[0], i8 + n9[0].length) : -1;
}
function parseMicroseconds(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8, i8 + 6));
  return n9 ? (d4.L = Math.floor(n9[0] / 1e3), i8 + n9[0].length) : -1;
}
function parseLiteralPercent(d4, string, i8) {
  var n9 = percentRe.exec(string.slice(i8, i8 + 1));
  return n9 ? i8 + n9[0].length : -1;
}
function parseUnixTimestamp(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8));
  return n9 ? (d4.Q = +n9[0], i8 + n9[0].length) : -1;
}
function parseUnixTimestampSeconds(d4, string, i8) {
  var n9 = numberRe.exec(string.slice(i8));
  return n9 ? (d4.s = +n9[0], i8 + n9[0].length) : -1;
}
function formatDayOfMonth(d4, p3) {
  return pad(d4.getDate(), p3, 2);
}
function formatHour24(d4, p3) {
  return pad(d4.getHours(), p3, 2);
}
function formatHour12(d4, p3) {
  return pad(d4.getHours() % 12 || 12, p3, 2);
}
function formatDayOfYear(d4, p3) {
  return pad(1 + day_default.count(year_default(d4), d4), p3, 3);
}
function formatMilliseconds(d4, p3) {
  return pad(d4.getMilliseconds(), p3, 3);
}
function formatMicroseconds(d4, p3) {
  return formatMilliseconds(d4, p3) + "000";
}
function formatMonthNumber(d4, p3) {
  return pad(d4.getMonth() + 1, p3, 2);
}
function formatMinutes(d4, p3) {
  return pad(d4.getMinutes(), p3, 2);
}
function formatSeconds(d4, p3) {
  return pad(d4.getSeconds(), p3, 2);
}
function formatWeekdayNumberMonday(d4) {
  var day2 = d4.getDay();
  return day2 === 0 ? 7 : day2;
}
function formatWeekNumberSunday(d4, p3) {
  return pad(sunday.count(year_default(d4) - 1, d4), p3, 2);
}
function dISO(d4) {
  var day2 = d4.getDay();
  return day2 >= 4 || day2 === 0 ? thursday(d4) : thursday.ceil(d4);
}
function formatWeekNumberISO(d4, p3) {
  d4 = dISO(d4);
  return pad(thursday.count(year_default(d4), d4) + (year_default(d4).getDay() === 4), p3, 2);
}
function formatWeekdayNumberSunday(d4) {
  return d4.getDay();
}
function formatWeekNumberMonday(d4, p3) {
  return pad(monday.count(year_default(d4) - 1, d4), p3, 2);
}
function formatYear(d4, p3) {
  return pad(d4.getFullYear() % 100, p3, 2);
}
function formatYearISO(d4, p3) {
  d4 = dISO(d4);
  return pad(d4.getFullYear() % 100, p3, 2);
}
function formatFullYear(d4, p3) {
  return pad(d4.getFullYear() % 1e4, p3, 4);
}
function formatFullYearISO(d4, p3) {
  var day2 = d4.getDay();
  d4 = day2 >= 4 || day2 === 0 ? thursday(d4) : thursday.ceil(d4);
  return pad(d4.getFullYear() % 1e4, p3, 4);
}
function formatZone(d4) {
  var z5 = d4.getTimezoneOffset();
  return (z5 > 0 ? "-" : (z5 *= -1, "+")) + pad(z5 / 60 | 0, "0", 2) + pad(z5 % 60, "0", 2);
}
function formatUTCDayOfMonth(d4, p3) {
  return pad(d4.getUTCDate(), p3, 2);
}
function formatUTCHour24(d4, p3) {
  return pad(d4.getUTCHours(), p3, 2);
}
function formatUTCHour12(d4, p3) {
  return pad(d4.getUTCHours() % 12 || 12, p3, 2);
}
function formatUTCDayOfYear(d4, p3) {
  return pad(1 + utcDay_default.count(utcYear_default(d4), d4), p3, 3);
}
function formatUTCMilliseconds(d4, p3) {
  return pad(d4.getUTCMilliseconds(), p3, 3);
}
function formatUTCMicroseconds(d4, p3) {
  return formatUTCMilliseconds(d4, p3) + "000";
}
function formatUTCMonthNumber(d4, p3) {
  return pad(d4.getUTCMonth() + 1, p3, 2);
}
function formatUTCMinutes(d4, p3) {
  return pad(d4.getUTCMinutes(), p3, 2);
}
function formatUTCSeconds(d4, p3) {
  return pad(d4.getUTCSeconds(), p3, 2);
}
function formatUTCWeekdayNumberMonday(d4) {
  var dow = d4.getUTCDay();
  return dow === 0 ? 7 : dow;
}
function formatUTCWeekNumberSunday(d4, p3) {
  return pad(utcSunday2.count(utcYear_default(d4) - 1, d4), p3, 2);
}
function UTCdISO(d4) {
  var day2 = d4.getUTCDay();
  return day2 >= 4 || day2 === 0 ? utcThursday2(d4) : utcThursday2.ceil(d4);
}
function formatUTCWeekNumberISO(d4, p3) {
  d4 = UTCdISO(d4);
  return pad(utcThursday2.count(utcYear_default(d4), d4) + (utcYear_default(d4).getUTCDay() === 4), p3, 2);
}
function formatUTCWeekdayNumberSunday(d4) {
  return d4.getUTCDay();
}
function formatUTCWeekNumberMonday(d4, p3) {
  return pad(utcMonday2.count(utcYear_default(d4) - 1, d4), p3, 2);
}
function formatUTCYear(d4, p3) {
  return pad(d4.getUTCFullYear() % 100, p3, 2);
}
function formatUTCYearISO(d4, p3) {
  d4 = UTCdISO(d4);
  return pad(d4.getUTCFullYear() % 100, p3, 2);
}
function formatUTCFullYear(d4, p3) {
  return pad(d4.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCFullYearISO(d4, p3) {
  var day2 = d4.getUTCDay();
  d4 = day2 >= 4 || day2 === 0 ? utcThursday2(d4) : utcThursday2.ceil(d4);
  return pad(d4.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCZone() {
  return "+0000";
}
function formatLiteralPercent() {
  return "%";
}
function formatUnixTimestamp(d4) {
  return +d4;
}
function formatUnixTimestampSeconds(d4) {
  return Math.floor(+d4 / 1e3);
}

// node_modules/d3-time-format/src/defaultLocale.js
var locale2;
var timeFormat;
var timeParse;
var utcFormat;
var utcParse;
defaultLocale2({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});
function defaultLocale2(definition) {
  locale2 = formatLocale(definition);
  timeFormat = locale2.format;
  timeParse = locale2.parse;
  utcFormat = locale2.utcFormat;
  utcParse = locale2.utcParse;
  return locale2;
}

// node_modules/d3-time-format/src/isoFormat.js
var isoSpecifier = "%Y-%m-%dT%H:%M:%S.%LZ";
function formatIsoNative(date2) {
  return date2.toISOString();
}
var formatIso = Date.prototype.toISOString ? formatIsoNative : utcFormat(isoSpecifier);

// node_modules/d3-time-format/src/isoParse.js
function parseIsoNative(string) {
  var date2 = new Date(string);
  return isNaN(date2) ? null : date2;
}
var parseIso = +/* @__PURE__ */ new Date("2000-01-01T00:00:00.000Z") ? parseIsoNative : utcParse(isoSpecifier);

// node_modules/d3-scale/src/time.js
function date(t9) {
  return new Date(t9);
}
function number3(t9) {
  return t9 instanceof Date ? +t9 : +/* @__PURE__ */ new Date(+t9);
}
function calendar(ticks2, tickInterval, year2, month2, week, day2, hour2, minute2, second3, format2) {
  var scale2 = continuous(), invert = scale2.invert, domain = scale2.domain;
  var formatMillisecond = format2(".%L"), formatSecond = format2(":%S"), formatMinute = format2("%I:%M"), formatHour = format2("%I %p"), formatDay = format2("%a %d"), formatWeek = format2("%b %d"), formatMonth = format2("%B"), formatYear5 = format2("%Y");
  function tickFormat2(date2) {
    return (second3(date2) < date2 ? formatMillisecond : minute2(date2) < date2 ? formatSecond : hour2(date2) < date2 ? formatMinute : day2(date2) < date2 ? formatHour : month2(date2) < date2 ? week(date2) < date2 ? formatDay : formatWeek : year2(date2) < date2 ? formatMonth : formatYear5)(date2);
  }
  scale2.invert = function(y5) {
    return new Date(invert(y5));
  };
  scale2.domain = function(_3) {
    return arguments.length ? domain(Array.from(_3, number3)) : domain().map(date);
  };
  scale2.ticks = function(interval) {
    var d4 = domain();
    return ticks2(d4[0], d4[d4.length - 1], interval == null ? 10 : interval);
  };
  scale2.tickFormat = function(count2, specifier) {
    return specifier == null ? tickFormat2 : format2(specifier);
  };
  scale2.nice = function(interval) {
    var d4 = domain();
    if (!interval || typeof interval.range !== "function") interval = tickInterval(d4[0], d4[d4.length - 1], interval == null ? 10 : interval);
    return interval ? domain(nice2(d4, interval)) : scale2;
  };
  scale2.copy = function() {
    return copy(scale2, calendar(ticks2, tickInterval, year2, month2, week, day2, hour2, minute2, second3, format2));
  };
  return scale2;
}
function time() {
  return initRange.apply(calendar(timeTicks, timeTickInterval, timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute, second, timeFormat).domain([new Date(2e3, 0, 1), new Date(2e3, 0, 2)]), arguments);
}

// node_modules/d3-scale/src/utcTime.js
function utcTime() {
  return initRange.apply(calendar(utcTicks, utcTickInterval, utcYear, utcMonth, utcSunday, utcDay, utcHour, utcMinute, second, utcFormat).domain([Date.UTC(2e3, 0, 1), Date.UTC(2e3, 0, 2)]), arguments);
}

// node_modules/d3-scale-chromatic/src/colors.js
function colors_default(specifier) {
  var n9 = specifier.length / 6 | 0, colors3 = new Array(n9), i8 = 0;
  while (i8 < n9) colors3[i8] = "#" + specifier.slice(i8 * 6, ++i8 * 6);
  return colors3;
}

// node_modules/d3-scale-chromatic/src/categorical/category10.js
var category10_default = colors_default("1f77b4ff7f0e2ca02cd627289467bd8c564be377c27f7f7fbcbd2217becf");

// node_modules/d3-scale-chromatic/src/categorical/Accent.js
var Accent_default = colors_default("7fc97fbeaed4fdc086ffff99386cb0f0027fbf5b17666666");

// node_modules/d3-scale-chromatic/src/categorical/Dark2.js
var Dark2_default = colors_default("1b9e77d95f027570b3e7298a66a61ee6ab02a6761d666666");

// node_modules/d3-scale-chromatic/src/categorical/observable10.js
var observable10_default = colors_default("4269d0efb118ff725c6cc5b03ca951ff8ab7a463f297bbf59c6b4e9498a0");

// node_modules/d3-scale-chromatic/src/categorical/Paired.js
var Paired_default = colors_default("a6cee31f78b4b2df8a33a02cfb9a99e31a1cfdbf6fff7f00cab2d66a3d9affff99b15928");

// node_modules/d3-scale-chromatic/src/categorical/Pastel1.js
var Pastel1_default = colors_default("fbb4aeb3cde3ccebc5decbe4fed9a6ffffcce5d8bdfddaecf2f2f2");

// node_modules/d3-scale-chromatic/src/categorical/Pastel2.js
var Pastel2_default = colors_default("b3e2cdfdcdaccbd5e8f4cae4e6f5c9fff2aef1e2cccccccc");

// node_modules/d3-scale-chromatic/src/categorical/Set1.js
var Set1_default = colors_default("e41a1c377eb84daf4a984ea3ff7f00ffff33a65628f781bf999999");

// node_modules/d3-scale-chromatic/src/categorical/Set2.js
var Set2_default = colors_default("66c2a5fc8d628da0cbe78ac3a6d854ffd92fe5c494b3b3b3");

// node_modules/d3-scale-chromatic/src/categorical/Set3.js
var Set3_default = colors_default("8dd3c7ffffb3bebadafb807280b1d3fdb462b3de69fccde5d9d9d9bc80bdccebc5ffed6f");

// node_modules/d3-scale-chromatic/src/categorical/Tableau10.js
var Tableau10_default = colors_default("4e79a7f28e2ce1575976b7b259a14fedc949af7aa1ff9da79c755fbab0ab");

// node_modules/d3-scale-chromatic/src/ramp.js
var ramp_default = (scheme28) => rgbBasis(scheme28[scheme28.length - 1]);

// node_modules/d3-scale-chromatic/src/diverging/BrBG.js
var scheme = new Array(3).concat(
  "d8b365f5f5f55ab4ac",
  "a6611adfc27d80cdc1018571",
  "a6611adfc27df5f5f580cdc1018571",
  "8c510ad8b365f6e8c3c7eae55ab4ac01665e",
  "8c510ad8b365f6e8c3f5f5f5c7eae55ab4ac01665e",
  "8c510abf812ddfc27df6e8c3c7eae580cdc135978f01665e",
  "8c510abf812ddfc27df6e8c3f5f5f5c7eae580cdc135978f01665e",
  "5430058c510abf812ddfc27df6e8c3c7eae580cdc135978f01665e003c30",
  "5430058c510abf812ddfc27df6e8c3f5f5f5c7eae580cdc135978f01665e003c30"
).map(colors_default);
var BrBG_default = ramp_default(scheme);

// node_modules/d3-scale-chromatic/src/diverging/PRGn.js
var scheme2 = new Array(3).concat(
  "af8dc3f7f7f77fbf7b",
  "7b3294c2a5cfa6dba0008837",
  "7b3294c2a5cff7f7f7a6dba0008837",
  "762a83af8dc3e7d4e8d9f0d37fbf7b1b7837",
  "762a83af8dc3e7d4e8f7f7f7d9f0d37fbf7b1b7837",
  "762a839970abc2a5cfe7d4e8d9f0d3a6dba05aae611b7837",
  "762a839970abc2a5cfe7d4e8f7f7f7d9f0d3a6dba05aae611b7837",
  "40004b762a839970abc2a5cfe7d4e8d9f0d3a6dba05aae611b783700441b",
  "40004b762a839970abc2a5cfe7d4e8f7f7f7d9f0d3a6dba05aae611b783700441b"
).map(colors_default);
var PRGn_default = ramp_default(scheme2);

// node_modules/d3-scale-chromatic/src/diverging/PiYG.js
var scheme3 = new Array(3).concat(
  "e9a3c9f7f7f7a1d76a",
  "d01c8bf1b6dab8e1864dac26",
  "d01c8bf1b6daf7f7f7b8e1864dac26",
  "c51b7de9a3c9fde0efe6f5d0a1d76a4d9221",
  "c51b7de9a3c9fde0eff7f7f7e6f5d0a1d76a4d9221",
  "c51b7dde77aef1b6dafde0efe6f5d0b8e1867fbc414d9221",
  "c51b7dde77aef1b6dafde0eff7f7f7e6f5d0b8e1867fbc414d9221",
  "8e0152c51b7dde77aef1b6dafde0efe6f5d0b8e1867fbc414d9221276419",
  "8e0152c51b7dde77aef1b6dafde0eff7f7f7e6f5d0b8e1867fbc414d9221276419"
).map(colors_default);
var PiYG_default = ramp_default(scheme3);

// node_modules/d3-scale-chromatic/src/diverging/PuOr.js
var scheme4 = new Array(3).concat(
  "998ec3f7f7f7f1a340",
  "5e3c99b2abd2fdb863e66101",
  "5e3c99b2abd2f7f7f7fdb863e66101",
  "542788998ec3d8daebfee0b6f1a340b35806",
  "542788998ec3d8daebf7f7f7fee0b6f1a340b35806",
  "5427888073acb2abd2d8daebfee0b6fdb863e08214b35806",
  "5427888073acb2abd2d8daebf7f7f7fee0b6fdb863e08214b35806",
  "2d004b5427888073acb2abd2d8daebfee0b6fdb863e08214b358067f3b08",
  "2d004b5427888073acb2abd2d8daebf7f7f7fee0b6fdb863e08214b358067f3b08"
).map(colors_default);
var PuOr_default = ramp_default(scheme4);

// node_modules/d3-scale-chromatic/src/diverging/RdBu.js
var scheme5 = new Array(3).concat(
  "ef8a62f7f7f767a9cf",
  "ca0020f4a58292c5de0571b0",
  "ca0020f4a582f7f7f792c5de0571b0",
  "b2182bef8a62fddbc7d1e5f067a9cf2166ac",
  "b2182bef8a62fddbc7f7f7f7d1e5f067a9cf2166ac",
  "b2182bd6604df4a582fddbc7d1e5f092c5de4393c32166ac",
  "b2182bd6604df4a582fddbc7f7f7f7d1e5f092c5de4393c32166ac",
  "67001fb2182bd6604df4a582fddbc7d1e5f092c5de4393c32166ac053061",
  "67001fb2182bd6604df4a582fddbc7f7f7f7d1e5f092c5de4393c32166ac053061"
).map(colors_default);
var RdBu_default = ramp_default(scheme5);

// node_modules/d3-scale-chromatic/src/diverging/RdGy.js
var scheme6 = new Array(3).concat(
  "ef8a62ffffff999999",
  "ca0020f4a582bababa404040",
  "ca0020f4a582ffffffbababa404040",
  "b2182bef8a62fddbc7e0e0e09999994d4d4d",
  "b2182bef8a62fddbc7ffffffe0e0e09999994d4d4d",
  "b2182bd6604df4a582fddbc7e0e0e0bababa8787874d4d4d",
  "b2182bd6604df4a582fddbc7ffffffe0e0e0bababa8787874d4d4d",
  "67001fb2182bd6604df4a582fddbc7e0e0e0bababa8787874d4d4d1a1a1a",
  "67001fb2182bd6604df4a582fddbc7ffffffe0e0e0bababa8787874d4d4d1a1a1a"
).map(colors_default);
var RdGy_default = ramp_default(scheme6);

// node_modules/d3-scale-chromatic/src/diverging/RdYlBu.js
var scheme7 = new Array(3).concat(
  "fc8d59ffffbf91bfdb",
  "d7191cfdae61abd9e92c7bb6",
  "d7191cfdae61ffffbfabd9e92c7bb6",
  "d73027fc8d59fee090e0f3f891bfdb4575b4",
  "d73027fc8d59fee090ffffbfe0f3f891bfdb4575b4",
  "d73027f46d43fdae61fee090e0f3f8abd9e974add14575b4",
  "d73027f46d43fdae61fee090ffffbfe0f3f8abd9e974add14575b4",
  "a50026d73027f46d43fdae61fee090e0f3f8abd9e974add14575b4313695",
  "a50026d73027f46d43fdae61fee090ffffbfe0f3f8abd9e974add14575b4313695"
).map(colors_default);
var RdYlBu_default = ramp_default(scheme7);

// node_modules/d3-scale-chromatic/src/diverging/RdYlGn.js
var scheme8 = new Array(3).concat(
  "fc8d59ffffbf91cf60",
  "d7191cfdae61a6d96a1a9641",
  "d7191cfdae61ffffbfa6d96a1a9641",
  "d73027fc8d59fee08bd9ef8b91cf601a9850",
  "d73027fc8d59fee08bffffbfd9ef8b91cf601a9850",
  "d73027f46d43fdae61fee08bd9ef8ba6d96a66bd631a9850",
  "d73027f46d43fdae61fee08bffffbfd9ef8ba6d96a66bd631a9850",
  "a50026d73027f46d43fdae61fee08bd9ef8ba6d96a66bd631a9850006837",
  "a50026d73027f46d43fdae61fee08bffffbfd9ef8ba6d96a66bd631a9850006837"
).map(colors_default);
var RdYlGn_default = ramp_default(scheme8);

// node_modules/d3-scale-chromatic/src/diverging/Spectral.js
var scheme9 = new Array(3).concat(
  "fc8d59ffffbf99d594",
  "d7191cfdae61abdda42b83ba",
  "d7191cfdae61ffffbfabdda42b83ba",
  "d53e4ffc8d59fee08be6f59899d5943288bd",
  "d53e4ffc8d59fee08bffffbfe6f59899d5943288bd",
  "d53e4ff46d43fdae61fee08be6f598abdda466c2a53288bd",
  "d53e4ff46d43fdae61fee08bffffbfe6f598abdda466c2a53288bd",
  "9e0142d53e4ff46d43fdae61fee08be6f598abdda466c2a53288bd5e4fa2",
  "9e0142d53e4ff46d43fdae61fee08bffffbfe6f598abdda466c2a53288bd5e4fa2"
).map(colors_default);
var Spectral_default = ramp_default(scheme9);

// node_modules/d3-scale-chromatic/src/sequential-multi/BuGn.js
var scheme10 = new Array(3).concat(
  "e5f5f999d8c92ca25f",
  "edf8fbb2e2e266c2a4238b45",
  "edf8fbb2e2e266c2a42ca25f006d2c",
  "edf8fbccece699d8c966c2a42ca25f006d2c",
  "edf8fbccece699d8c966c2a441ae76238b45005824",
  "f7fcfde5f5f9ccece699d8c966c2a441ae76238b45005824",
  "f7fcfde5f5f9ccece699d8c966c2a441ae76238b45006d2c00441b"
).map(colors_default);
var BuGn_default = ramp_default(scheme10);

// node_modules/d3-scale-chromatic/src/sequential-multi/BuPu.js
var scheme11 = new Array(3).concat(
  "e0ecf49ebcda8856a7",
  "edf8fbb3cde38c96c688419d",
  "edf8fbb3cde38c96c68856a7810f7c",
  "edf8fbbfd3e69ebcda8c96c68856a7810f7c",
  "edf8fbbfd3e69ebcda8c96c68c6bb188419d6e016b",
  "f7fcfde0ecf4bfd3e69ebcda8c96c68c6bb188419d6e016b",
  "f7fcfde0ecf4bfd3e69ebcda8c96c68c6bb188419d810f7c4d004b"
).map(colors_default);
var BuPu_default = ramp_default(scheme11);

// node_modules/d3-scale-chromatic/src/sequential-multi/GnBu.js
var scheme12 = new Array(3).concat(
  "e0f3dba8ddb543a2ca",
  "f0f9e8bae4bc7bccc42b8cbe",
  "f0f9e8bae4bc7bccc443a2ca0868ac",
  "f0f9e8ccebc5a8ddb57bccc443a2ca0868ac",
  "f0f9e8ccebc5a8ddb57bccc44eb3d32b8cbe08589e",
  "f7fcf0e0f3dbccebc5a8ddb57bccc44eb3d32b8cbe08589e",
  "f7fcf0e0f3dbccebc5a8ddb57bccc44eb3d32b8cbe0868ac084081"
).map(colors_default);
var GnBu_default = ramp_default(scheme12);

// node_modules/d3-scale-chromatic/src/sequential-multi/OrRd.js
var scheme13 = new Array(3).concat(
  "fee8c8fdbb84e34a33",
  "fef0d9fdcc8afc8d59d7301f",
  "fef0d9fdcc8afc8d59e34a33b30000",
  "fef0d9fdd49efdbb84fc8d59e34a33b30000",
  "fef0d9fdd49efdbb84fc8d59ef6548d7301f990000",
  "fff7ecfee8c8fdd49efdbb84fc8d59ef6548d7301f990000",
  "fff7ecfee8c8fdd49efdbb84fc8d59ef6548d7301fb300007f0000"
).map(colors_default);
var OrRd_default = ramp_default(scheme13);

// node_modules/d3-scale-chromatic/src/sequential-multi/PuBuGn.js
var scheme14 = new Array(3).concat(
  "ece2f0a6bddb1c9099",
  "f6eff7bdc9e167a9cf02818a",
  "f6eff7bdc9e167a9cf1c9099016c59",
  "f6eff7d0d1e6a6bddb67a9cf1c9099016c59",
  "f6eff7d0d1e6a6bddb67a9cf3690c002818a016450",
  "fff7fbece2f0d0d1e6a6bddb67a9cf3690c002818a016450",
  "fff7fbece2f0d0d1e6a6bddb67a9cf3690c002818a016c59014636"
).map(colors_default);
var PuBuGn_default = ramp_default(scheme14);

// node_modules/d3-scale-chromatic/src/sequential-multi/PuBu.js
var scheme15 = new Array(3).concat(
  "ece7f2a6bddb2b8cbe",
  "f1eef6bdc9e174a9cf0570b0",
  "f1eef6bdc9e174a9cf2b8cbe045a8d",
  "f1eef6d0d1e6a6bddb74a9cf2b8cbe045a8d",
  "f1eef6d0d1e6a6bddb74a9cf3690c00570b0034e7b",
  "fff7fbece7f2d0d1e6a6bddb74a9cf3690c00570b0034e7b",
  "fff7fbece7f2d0d1e6a6bddb74a9cf3690c00570b0045a8d023858"
).map(colors_default);
var PuBu_default = ramp_default(scheme15);

// node_modules/d3-scale-chromatic/src/sequential-multi/PuRd.js
var scheme16 = new Array(3).concat(
  "e7e1efc994c7dd1c77",
  "f1eef6d7b5d8df65b0ce1256",
  "f1eef6d7b5d8df65b0dd1c77980043",
  "f1eef6d4b9dac994c7df65b0dd1c77980043",
  "f1eef6d4b9dac994c7df65b0e7298ace125691003f",
  "f7f4f9e7e1efd4b9dac994c7df65b0e7298ace125691003f",
  "f7f4f9e7e1efd4b9dac994c7df65b0e7298ace125698004367001f"
).map(colors_default);
var PuRd_default = ramp_default(scheme16);

// node_modules/d3-scale-chromatic/src/sequential-multi/RdPu.js
var scheme17 = new Array(3).concat(
  "fde0ddfa9fb5c51b8a",
  "feebe2fbb4b9f768a1ae017e",
  "feebe2fbb4b9f768a1c51b8a7a0177",
  "feebe2fcc5c0fa9fb5f768a1c51b8a7a0177",
  "feebe2fcc5c0fa9fb5f768a1dd3497ae017e7a0177",
  "fff7f3fde0ddfcc5c0fa9fb5f768a1dd3497ae017e7a0177",
  "fff7f3fde0ddfcc5c0fa9fb5f768a1dd3497ae017e7a017749006a"
).map(colors_default);
var RdPu_default = ramp_default(scheme17);

// node_modules/d3-scale-chromatic/src/sequential-multi/YlGnBu.js
var scheme18 = new Array(3).concat(
  "edf8b17fcdbb2c7fb8",
  "ffffcca1dab441b6c4225ea8",
  "ffffcca1dab441b6c42c7fb8253494",
  "ffffccc7e9b47fcdbb41b6c42c7fb8253494",
  "ffffccc7e9b47fcdbb41b6c41d91c0225ea80c2c84",
  "ffffd9edf8b1c7e9b47fcdbb41b6c41d91c0225ea80c2c84",
  "ffffd9edf8b1c7e9b47fcdbb41b6c41d91c0225ea8253494081d58"
).map(colors_default);
var YlGnBu_default = ramp_default(scheme18);

// node_modules/d3-scale-chromatic/src/sequential-multi/YlGn.js
var scheme19 = new Array(3).concat(
  "f7fcb9addd8e31a354",
  "ffffccc2e69978c679238443",
  "ffffccc2e69978c67931a354006837",
  "ffffccd9f0a3addd8e78c67931a354006837",
  "ffffccd9f0a3addd8e78c67941ab5d238443005a32",
  "ffffe5f7fcb9d9f0a3addd8e78c67941ab5d238443005a32",
  "ffffe5f7fcb9d9f0a3addd8e78c67941ab5d238443006837004529"
).map(colors_default);
var YlGn_default = ramp_default(scheme19);

// node_modules/d3-scale-chromatic/src/sequential-multi/YlOrBr.js
var scheme20 = new Array(3).concat(
  "fff7bcfec44fd95f0e",
  "ffffd4fed98efe9929cc4c02",
  "ffffd4fed98efe9929d95f0e993404",
  "ffffd4fee391fec44ffe9929d95f0e993404",
  "ffffd4fee391fec44ffe9929ec7014cc4c028c2d04",
  "ffffe5fff7bcfee391fec44ffe9929ec7014cc4c028c2d04",
  "ffffe5fff7bcfee391fec44ffe9929ec7014cc4c02993404662506"
).map(colors_default);
var YlOrBr_default = ramp_default(scheme20);

// node_modules/d3-scale-chromatic/src/sequential-multi/YlOrRd.js
var scheme21 = new Array(3).concat(
  "ffeda0feb24cf03b20",
  "ffffb2fecc5cfd8d3ce31a1c",
  "ffffb2fecc5cfd8d3cf03b20bd0026",
  "ffffb2fed976feb24cfd8d3cf03b20bd0026",
  "ffffb2fed976feb24cfd8d3cfc4e2ae31a1cb10026",
  "ffffccffeda0fed976feb24cfd8d3cfc4e2ae31a1cb10026",
  "ffffccffeda0fed976feb24cfd8d3cfc4e2ae31a1cbd0026800026"
).map(colors_default);
var YlOrRd_default = ramp_default(scheme21);

// node_modules/d3-scale-chromatic/src/sequential-single/Blues.js
var scheme22 = new Array(3).concat(
  "deebf79ecae13182bd",
  "eff3ffbdd7e76baed62171b5",
  "eff3ffbdd7e76baed63182bd08519c",
  "eff3ffc6dbef9ecae16baed63182bd08519c",
  "eff3ffc6dbef9ecae16baed64292c62171b5084594",
  "f7fbffdeebf7c6dbef9ecae16baed64292c62171b5084594",
  "f7fbffdeebf7c6dbef9ecae16baed64292c62171b508519c08306b"
).map(colors_default);
var Blues_default = ramp_default(scheme22);

// node_modules/d3-scale-chromatic/src/sequential-single/Greens.js
var scheme23 = new Array(3).concat(
  "e5f5e0a1d99b31a354",
  "edf8e9bae4b374c476238b45",
  "edf8e9bae4b374c47631a354006d2c",
  "edf8e9c7e9c0a1d99b74c47631a354006d2c",
  "edf8e9c7e9c0a1d99b74c47641ab5d238b45005a32",
  "f7fcf5e5f5e0c7e9c0a1d99b74c47641ab5d238b45005a32",
  "f7fcf5e5f5e0c7e9c0a1d99b74c47641ab5d238b45006d2c00441b"
).map(colors_default);
var Greens_default = ramp_default(scheme23);

// node_modules/d3-scale-chromatic/src/sequential-single/Greys.js
var scheme24 = new Array(3).concat(
  "f0f0f0bdbdbd636363",
  "f7f7f7cccccc969696525252",
  "f7f7f7cccccc969696636363252525",
  "f7f7f7d9d9d9bdbdbd969696636363252525",
  "f7f7f7d9d9d9bdbdbd969696737373525252252525",
  "fffffff0f0f0d9d9d9bdbdbd969696737373525252252525",
  "fffffff0f0f0d9d9d9bdbdbd969696737373525252252525000000"
).map(colors_default);
var Greys_default = ramp_default(scheme24);

// node_modules/d3-scale-chromatic/src/sequential-single/Purples.js
var scheme25 = new Array(3).concat(
  "efedf5bcbddc756bb1",
  "f2f0f7cbc9e29e9ac86a51a3",
  "f2f0f7cbc9e29e9ac8756bb154278f",
  "f2f0f7dadaebbcbddc9e9ac8756bb154278f",
  "f2f0f7dadaebbcbddc9e9ac8807dba6a51a34a1486",
  "fcfbfdefedf5dadaebbcbddc9e9ac8807dba6a51a34a1486",
  "fcfbfdefedf5dadaebbcbddc9e9ac8807dba6a51a354278f3f007d"
).map(colors_default);
var Purples_default = ramp_default(scheme25);

// node_modules/d3-scale-chromatic/src/sequential-single/Reds.js
var scheme26 = new Array(3).concat(
  "fee0d2fc9272de2d26",
  "fee5d9fcae91fb6a4acb181d",
  "fee5d9fcae91fb6a4ade2d26a50f15",
  "fee5d9fcbba1fc9272fb6a4ade2d26a50f15",
  "fee5d9fcbba1fc9272fb6a4aef3b2ccb181d99000d",
  "fff5f0fee0d2fcbba1fc9272fb6a4aef3b2ccb181d99000d",
  "fff5f0fee0d2fcbba1fc9272fb6a4aef3b2ccb181da50f1567000d"
).map(colors_default);
var Reds_default = ramp_default(scheme26);

// node_modules/d3-scale-chromatic/src/sequential-single/Oranges.js
var scheme27 = new Array(3).concat(
  "fee6cefdae6be6550d",
  "feeddefdbe85fd8d3cd94701",
  "feeddefdbe85fd8d3ce6550da63603",
  "feeddefdd0a2fdae6bfd8d3ce6550da63603",
  "feeddefdd0a2fdae6bfd8d3cf16913d948018c2d04",
  "fff5ebfee6cefdd0a2fdae6bfd8d3cf16913d948018c2d04",
  "fff5ebfee6cefdd0a2fdae6bfd8d3cf16913d94801a636037f2704"
).map(colors_default);
var Oranges_default = ramp_default(scheme27);

// node_modules/d3-scale-chromatic/src/sequential-multi/cividis.js
function cividis_default(t9) {
  t9 = Math.max(0, Math.min(1, t9));
  return "rgb(" + Math.max(0, Math.min(255, Math.round(-4.54 - t9 * (35.34 - t9 * (2381.73 - t9 * (6402.7 - t9 * (7024.72 - t9 * 2710.57))))))) + ", " + Math.max(0, Math.min(255, Math.round(32.49 + t9 * (170.73 + t9 * (52.82 - t9 * (131.46 - t9 * (176.58 - t9 * 67.37))))))) + ", " + Math.max(0, Math.min(255, Math.round(81.24 + t9 * (442.36 - t9 * (2482.43 - t9 * (6167.24 - t9 * (6614.94 - t9 * 2475.67))))))) + ")";
}

// node_modules/d3-scale-chromatic/src/sequential-multi/cubehelix.js
var cubehelix_default2 = cubehelixLong(cubehelix(300, 0.5, 0), cubehelix(-240, 0.5, 1));

// node_modules/d3-scale-chromatic/src/sequential-multi/rainbow.js
var warm = cubehelixLong(cubehelix(-100, 0.75, 0.35), cubehelix(80, 1.5, 0.8));
var cool = cubehelixLong(cubehelix(260, 0.75, 0.35), cubehelix(80, 1.5, 0.8));
var c6 = cubehelix();
function rainbow_default(t9) {
  if (t9 < 0 || t9 > 1) t9 -= Math.floor(t9);
  var ts2 = Math.abs(t9 - 0.5);
  c6.h = 360 * t9 - 100;
  c6.s = 1.5 - 1.5 * ts2;
  c6.l = 0.8 - 0.9 * ts2;
  return c6 + "";
}

// node_modules/d3-scale-chromatic/src/sequential-multi/sinebow.js
var c7 = rgb2();
var pi_1_3 = Math.PI / 3;
var pi_2_3 = Math.PI * 2 / 3;
function sinebow_default(t9) {
  var x4;
  t9 = (0.5 - t9) * Math.PI;
  c7.r = 255 * (x4 = Math.sin(t9)) * x4;
  c7.g = 255 * (x4 = Math.sin(t9 + pi_1_3)) * x4;
  c7.b = 255 * (x4 = Math.sin(t9 + pi_2_3)) * x4;
  return c7 + "";
}

// node_modules/d3-scale-chromatic/src/sequential-multi/turbo.js
function turbo_default(t9) {
  t9 = Math.max(0, Math.min(1, t9));
  return "rgb(" + Math.max(0, Math.min(255, Math.round(34.61 + t9 * (1172.33 - t9 * (10793.56 - t9 * (33300.12 - t9 * (38394.49 - t9 * 14825.05))))))) + ", " + Math.max(0, Math.min(255, Math.round(23.31 + t9 * (557.33 + t9 * (1225.33 - t9 * (3574.96 - t9 * (1073.77 + t9 * 707.56))))))) + ", " + Math.max(0, Math.min(255, Math.round(27.2 + t9 * (3211.1 - t9 * (15327.97 - t9 * (27814 - t9 * (22569.18 - t9 * 6838.66))))))) + ")";
}

// node_modules/d3-scale-chromatic/src/sequential-multi/viridis.js
function ramp(range2) {
  var n9 = range2.length;
  return function(t9) {
    return range2[Math.max(0, Math.min(n9 - 1, Math.floor(t9 * n9)))];
  };
}
var viridis_default = ramp(colors_default(""));
var magma = ramp(colors_default(""));
var inferno = ramp(colors_default(""));
var plasma = ramp(colors_default(""));

// node_modules/@nivo/core/dist/nivo-core.mjs
var import_last = __toESM(require_last(), 1);
var import_isArray = __toESM(require_isArray(), 1);
var import_isFunction = __toESM(require_isFunction(), 1);

// node_modules/@nivo/core/node_modules/d3-time-format/src/locale.js
function localDate2(d4) {
  if (0 <= d4.y && d4.y < 100) {
    var date2 = new Date(-1, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L);
    date2.setFullYear(d4.y);
    return date2;
  }
  return new Date(d4.y, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L);
}
function utcDate2(d4) {
  if (0 <= d4.y && d4.y < 100) {
    var date2 = new Date(Date.UTC(-1, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L));
    date2.setUTCFullYear(d4.y);
    return date2;
  }
  return new Date(Date.UTC(d4.y, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L));
}
function newDate2(y5, m4, d4) {
  return { y: y5, m: m4, d: d4, H: 0, M: 0, S: 0, L: 0 };
}
function formatLocale2(locale6) {
  var locale_dateTime = locale6.dateTime, locale_date = locale6.date, locale_time = locale6.time, locale_periods = locale6.periods, locale_weekdays = locale6.days, locale_shortWeekdays = locale6.shortDays, locale_months = locale6.months, locale_shortMonths = locale6.shortMonths;
  var periodRe = formatRe2(locale_periods), periodLookup = formatLookup2(locale_periods), weekdayRe = formatRe2(locale_weekdays), weekdayLookup = formatLookup2(locale_weekdays), shortWeekdayRe = formatRe2(locale_shortWeekdays), shortWeekdayLookup = formatLookup2(locale_shortWeekdays), monthRe = formatRe2(locale_months), monthLookup = formatLookup2(locale_months), shortMonthRe = formatRe2(locale_shortMonths), shortMonthLookup = formatLookup2(locale_shortMonths);
  var formats = {
    "a": formatShortWeekday,
    "A": formatWeekday,
    "b": formatShortMonth,
    "B": formatMonth,
    "c": null,
    "d": formatDayOfMonth2,
    "e": formatDayOfMonth2,
    "f": formatMicroseconds2,
    "g": formatYearISO2,
    "G": formatFullYearISO2,
    "H": formatHour242,
    "I": formatHour122,
    "j": formatDayOfYear2,
    "L": formatMilliseconds2,
    "m": formatMonthNumber2,
    "M": formatMinutes2,
    "p": formatPeriod,
    "q": formatQuarter,
    "Q": formatUnixTimestamp2,
    "s": formatUnixTimestampSeconds2,
    "S": formatSeconds2,
    "u": formatWeekdayNumberMonday2,
    "U": formatWeekNumberSunday2,
    "V": formatWeekNumberISO2,
    "w": formatWeekdayNumberSunday2,
    "W": formatWeekNumberMonday2,
    "x": null,
    "X": null,
    "y": formatYear2,
    "Y": formatFullYear2,
    "Z": formatZone2,
    "%": formatLiteralPercent2
  };
  var utcFormats = {
    "a": formatUTCShortWeekday,
    "A": formatUTCWeekday,
    "b": formatUTCShortMonth,
    "B": formatUTCMonth,
    "c": null,
    "d": formatUTCDayOfMonth2,
    "e": formatUTCDayOfMonth2,
    "f": formatUTCMicroseconds2,
    "g": formatUTCYearISO2,
    "G": formatUTCFullYearISO2,
    "H": formatUTCHour242,
    "I": formatUTCHour122,
    "j": formatUTCDayOfYear2,
    "L": formatUTCMilliseconds2,
    "m": formatUTCMonthNumber2,
    "M": formatUTCMinutes2,
    "p": formatUTCPeriod,
    "q": formatUTCQuarter,
    "Q": formatUnixTimestamp2,
    "s": formatUnixTimestampSeconds2,
    "S": formatUTCSeconds2,
    "u": formatUTCWeekdayNumberMonday2,
    "U": formatUTCWeekNumberSunday2,
    "V": formatUTCWeekNumberISO2,
    "w": formatUTCWeekdayNumberSunday2,
    "W": formatUTCWeekNumberMonday2,
    "x": null,
    "X": null,
    "y": formatUTCYear2,
    "Y": formatUTCFullYear2,
    "Z": formatUTCZone2,
    "%": formatLiteralPercent2
  };
  var parses = {
    "a": parseShortWeekday,
    "A": parseWeekday,
    "b": parseShortMonth,
    "B": parseMonth,
    "c": parseLocaleDateTime,
    "d": parseDayOfMonth2,
    "e": parseDayOfMonth2,
    "f": parseMicroseconds2,
    "g": parseYear2,
    "G": parseFullYear2,
    "H": parseHour242,
    "I": parseHour242,
    "j": parseDayOfYear2,
    "L": parseMilliseconds2,
    "m": parseMonthNumber2,
    "M": parseMinutes2,
    "p": parsePeriod,
    "q": parseQuarter2,
    "Q": parseUnixTimestamp2,
    "s": parseUnixTimestampSeconds2,
    "S": parseSeconds2,
    "u": parseWeekdayNumberMonday2,
    "U": parseWeekNumberSunday2,
    "V": parseWeekNumberISO2,
    "w": parseWeekdayNumberSunday2,
    "W": parseWeekNumberMonday2,
    "x": parseLocaleDate,
    "X": parseLocaleTime,
    "y": parseYear2,
    "Y": parseFullYear2,
    "Z": parseZone2,
    "%": parseLiteralPercent2
  };
  formats.x = newFormat(locale_date, formats);
  formats.X = newFormat(locale_time, formats);
  formats.c = newFormat(locale_dateTime, formats);
  utcFormats.x = newFormat(locale_date, utcFormats);
  utcFormats.X = newFormat(locale_time, utcFormats);
  utcFormats.c = newFormat(locale_dateTime, utcFormats);
  function newFormat(specifier, formats2) {
    return function(date2) {
      var string = [], i8 = -1, j5 = 0, n9 = specifier.length, c9, pad5, format2;
      if (!(date2 instanceof Date)) date2 = /* @__PURE__ */ new Date(+date2);
      while (++i8 < n9) {
        if (specifier.charCodeAt(i8) === 37) {
          string.push(specifier.slice(j5, i8));
          if ((pad5 = pads2[c9 = specifier.charAt(++i8)]) != null) c9 = specifier.charAt(++i8);
          else pad5 = c9 === "e" ? " " : "0";
          if (format2 = formats2[c9]) c9 = format2(date2, pad5);
          string.push(c9);
          j5 = i8 + 1;
        }
      }
      string.push(specifier.slice(j5, i8));
      return string.join("");
    };
  }
  function newParse(specifier, Z2) {
    return function(string) {
      var d4 = newDate2(1900, void 0, 1), i8 = parseSpecifier(d4, specifier, string += "", 0), week, day2;
      if (i8 != string.length) return null;
      if ("Q" in d4) return new Date(d4.Q);
      if ("s" in d4) return new Date(d4.s * 1e3 + ("L" in d4 ? d4.L : 0));
      if (Z2 && !("Z" in d4)) d4.Z = 0;
      if ("p" in d4) d4.H = d4.H % 12 + d4.p * 12;
      if (d4.m === void 0) d4.m = "q" in d4 ? d4.q : 0;
      if ("V" in d4) {
        if (d4.V < 1 || d4.V > 53) return null;
        if (!("w" in d4)) d4.w = 1;
        if ("Z" in d4) {
          week = utcDate2(newDate2(d4.y, 0, 1)), day2 = week.getUTCDay();
          week = day2 > 4 || day2 === 0 ? utcMonday2.ceil(week) : utcMonday2(week);
          week = utcDay_default.offset(week, (d4.V - 1) * 7);
          d4.y = week.getUTCFullYear();
          d4.m = week.getUTCMonth();
          d4.d = week.getUTCDate() + (d4.w + 6) % 7;
        } else {
          week = localDate2(newDate2(d4.y, 0, 1)), day2 = week.getDay();
          week = day2 > 4 || day2 === 0 ? monday.ceil(week) : monday(week);
          week = day_default.offset(week, (d4.V - 1) * 7);
          d4.y = week.getFullYear();
          d4.m = week.getMonth();
          d4.d = week.getDate() + (d4.w + 6) % 7;
        }
      } else if ("W" in d4 || "U" in d4) {
        if (!("w" in d4)) d4.w = "u" in d4 ? d4.u % 7 : "W" in d4 ? 1 : 0;
        day2 = "Z" in d4 ? utcDate2(newDate2(d4.y, 0, 1)).getUTCDay() : localDate2(newDate2(d4.y, 0, 1)).getDay();
        d4.m = 0;
        d4.d = "W" in d4 ? (d4.w + 6) % 7 + d4.W * 7 - (day2 + 5) % 7 : d4.w + d4.U * 7 - (day2 + 6) % 7;
      }
      if ("Z" in d4) {
        d4.H += d4.Z / 100 | 0;
        d4.M += d4.Z % 100;
        return utcDate2(d4);
      }
      return localDate2(d4);
    };
  }
  function parseSpecifier(d4, specifier, string, j5) {
    var i8 = 0, n9 = specifier.length, m4 = string.length, c9, parse;
    while (i8 < n9) {
      if (j5 >= m4) return -1;
      c9 = specifier.charCodeAt(i8++);
      if (c9 === 37) {
        c9 = specifier.charAt(i8++);
        parse = parses[c9 in pads2 ? specifier.charAt(i8++) : c9];
        if (!parse || (j5 = parse(d4, string, j5)) < 0) return -1;
      } else if (c9 != string.charCodeAt(j5++)) {
        return -1;
      }
    }
    return j5;
  }
  function parsePeriod(d4, string, i8) {
    var n9 = periodRe.exec(string.slice(i8));
    return n9 ? (d4.p = periodLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseShortWeekday(d4, string, i8) {
    var n9 = shortWeekdayRe.exec(string.slice(i8));
    return n9 ? (d4.w = shortWeekdayLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseWeekday(d4, string, i8) {
    var n9 = weekdayRe.exec(string.slice(i8));
    return n9 ? (d4.w = weekdayLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseShortMonth(d4, string, i8) {
    var n9 = shortMonthRe.exec(string.slice(i8));
    return n9 ? (d4.m = shortMonthLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseMonth(d4, string, i8) {
    var n9 = monthRe.exec(string.slice(i8));
    return n9 ? (d4.m = monthLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseLocaleDateTime(d4, string, i8) {
    return parseSpecifier(d4, locale_dateTime, string, i8);
  }
  function parseLocaleDate(d4, string, i8) {
    return parseSpecifier(d4, locale_date, string, i8);
  }
  function parseLocaleTime(d4, string, i8) {
    return parseSpecifier(d4, locale_time, string, i8);
  }
  function formatShortWeekday(d4) {
    return locale_shortWeekdays[d4.getDay()];
  }
  function formatWeekday(d4) {
    return locale_weekdays[d4.getDay()];
  }
  function formatShortMonth(d4) {
    return locale_shortMonths[d4.getMonth()];
  }
  function formatMonth(d4) {
    return locale_months[d4.getMonth()];
  }
  function formatPeriod(d4) {
    return locale_periods[+(d4.getHours() >= 12)];
  }
  function formatQuarter(d4) {
    return 1 + ~~(d4.getMonth() / 3);
  }
  function formatUTCShortWeekday(d4) {
    return locale_shortWeekdays[d4.getUTCDay()];
  }
  function formatUTCWeekday(d4) {
    return locale_weekdays[d4.getUTCDay()];
  }
  function formatUTCShortMonth(d4) {
    return locale_shortMonths[d4.getUTCMonth()];
  }
  function formatUTCMonth(d4) {
    return locale_months[d4.getUTCMonth()];
  }
  function formatUTCPeriod(d4) {
    return locale_periods[+(d4.getUTCHours() >= 12)];
  }
  function formatUTCQuarter(d4) {
    return 1 + ~~(d4.getUTCMonth() / 3);
  }
  return {
    format: function(specifier) {
      var f2 = newFormat(specifier += "", formats);
      f2.toString = function() {
        return specifier;
      };
      return f2;
    },
    parse: function(specifier) {
      var p3 = newParse(specifier += "", false);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    },
    utcFormat: function(specifier) {
      var f2 = newFormat(specifier += "", utcFormats);
      f2.toString = function() {
        return specifier;
      };
      return f2;
    },
    utcParse: function(specifier) {
      var p3 = newParse(specifier += "", true);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    }
  };
}
var pads2 = { "-": "", "_": " ", "0": "0" };
var numberRe2 = /^\s*\d+/;
var percentRe2 = /^%/;
var requoteRe2 = /[\\^$*+?|[\]().{}]/g;
function pad2(value, fill, width) {
  var sign2 = value < 0 ? "-" : "", string = (sign2 ? -value : value) + "", length = string.length;
  return sign2 + (length < width ? new Array(width - length + 1).join(fill) + string : string);
}
function requote2(s5) {
  return s5.replace(requoteRe2, "\\$&");
}
function formatRe2(names) {
  return new RegExp("^(?:" + names.map(requote2).join("|") + ")", "i");
}
function formatLookup2(names) {
  return new Map(names.map((name, i8) => [name.toLowerCase(), i8]));
}
function parseWeekdayNumberSunday2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.w = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekdayNumberMonday2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.u = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberSunday2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.U = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberISO2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.V = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberMonday2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.W = +n9[0], i8 + n9[0].length) : -1;
}
function parseFullYear2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 4));
  return n9 ? (d4.y = +n9[0], i8 + n9[0].length) : -1;
}
function parseYear2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.y = +n9[0] + (+n9[0] > 68 ? 1900 : 2e3), i8 + n9[0].length) : -1;
}
function parseZone2(d4, string, i8) {
  var n9 = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i8, i8 + 6));
  return n9 ? (d4.Z = n9[1] ? 0 : -(n9[2] + (n9[3] || "00")), i8 + n9[0].length) : -1;
}
function parseQuarter2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.q = n9[0] * 3 - 3, i8 + n9[0].length) : -1;
}
function parseMonthNumber2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.m = n9[0] - 1, i8 + n9[0].length) : -1;
}
function parseDayOfMonth2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.d = +n9[0], i8 + n9[0].length) : -1;
}
function parseDayOfYear2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 3));
  return n9 ? (d4.m = 0, d4.d = +n9[0], i8 + n9[0].length) : -1;
}
function parseHour242(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.H = +n9[0], i8 + n9[0].length) : -1;
}
function parseMinutes2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.M = +n9[0], i8 + n9[0].length) : -1;
}
function parseSeconds2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.S = +n9[0], i8 + n9[0].length) : -1;
}
function parseMilliseconds2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 3));
  return n9 ? (d4.L = +n9[0], i8 + n9[0].length) : -1;
}
function parseMicroseconds2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8, i8 + 6));
  return n9 ? (d4.L = Math.floor(n9[0] / 1e3), i8 + n9[0].length) : -1;
}
function parseLiteralPercent2(d4, string, i8) {
  var n9 = percentRe2.exec(string.slice(i8, i8 + 1));
  return n9 ? i8 + n9[0].length : -1;
}
function parseUnixTimestamp2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8));
  return n9 ? (d4.Q = +n9[0], i8 + n9[0].length) : -1;
}
function parseUnixTimestampSeconds2(d4, string, i8) {
  var n9 = numberRe2.exec(string.slice(i8));
  return n9 ? (d4.s = +n9[0], i8 + n9[0].length) : -1;
}
function formatDayOfMonth2(d4, p3) {
  return pad2(d4.getDate(), p3, 2);
}
function formatHour242(d4, p3) {
  return pad2(d4.getHours(), p3, 2);
}
function formatHour122(d4, p3) {
  return pad2(d4.getHours() % 12 || 12, p3, 2);
}
function formatDayOfYear2(d4, p3) {
  return pad2(1 + day_default.count(year_default(d4), d4), p3, 3);
}
function formatMilliseconds2(d4, p3) {
  return pad2(d4.getMilliseconds(), p3, 3);
}
function formatMicroseconds2(d4, p3) {
  return formatMilliseconds2(d4, p3) + "000";
}
function formatMonthNumber2(d4, p3) {
  return pad2(d4.getMonth() + 1, p3, 2);
}
function formatMinutes2(d4, p3) {
  return pad2(d4.getMinutes(), p3, 2);
}
function formatSeconds2(d4, p3) {
  return pad2(d4.getSeconds(), p3, 2);
}
function formatWeekdayNumberMonday2(d4) {
  var day2 = d4.getDay();
  return day2 === 0 ? 7 : day2;
}
function formatWeekNumberSunday2(d4, p3) {
  return pad2(sunday.count(year_default(d4) - 1, d4), p3, 2);
}
function dISO2(d4) {
  var day2 = d4.getDay();
  return day2 >= 4 || day2 === 0 ? thursday(d4) : thursday.ceil(d4);
}
function formatWeekNumberISO2(d4, p3) {
  d4 = dISO2(d4);
  return pad2(thursday.count(year_default(d4), d4) + (year_default(d4).getDay() === 4), p3, 2);
}
function formatWeekdayNumberSunday2(d4) {
  return d4.getDay();
}
function formatWeekNumberMonday2(d4, p3) {
  return pad2(monday.count(year_default(d4) - 1, d4), p3, 2);
}
function formatYear2(d4, p3) {
  return pad2(d4.getFullYear() % 100, p3, 2);
}
function formatYearISO2(d4, p3) {
  d4 = dISO2(d4);
  return pad2(d4.getFullYear() % 100, p3, 2);
}
function formatFullYear2(d4, p3) {
  return pad2(d4.getFullYear() % 1e4, p3, 4);
}
function formatFullYearISO2(d4, p3) {
  var day2 = d4.getDay();
  d4 = day2 >= 4 || day2 === 0 ? thursday(d4) : thursday.ceil(d4);
  return pad2(d4.getFullYear() % 1e4, p3, 4);
}
function formatZone2(d4) {
  var z5 = d4.getTimezoneOffset();
  return (z5 > 0 ? "-" : (z5 *= -1, "+")) + pad2(z5 / 60 | 0, "0", 2) + pad2(z5 % 60, "0", 2);
}
function formatUTCDayOfMonth2(d4, p3) {
  return pad2(d4.getUTCDate(), p3, 2);
}
function formatUTCHour242(d4, p3) {
  return pad2(d4.getUTCHours(), p3, 2);
}
function formatUTCHour122(d4, p3) {
  return pad2(d4.getUTCHours() % 12 || 12, p3, 2);
}
function formatUTCDayOfYear2(d4, p3) {
  return pad2(1 + utcDay_default.count(utcYear_default(d4), d4), p3, 3);
}
function formatUTCMilliseconds2(d4, p3) {
  return pad2(d4.getUTCMilliseconds(), p3, 3);
}
function formatUTCMicroseconds2(d4, p3) {
  return formatUTCMilliseconds2(d4, p3) + "000";
}
function formatUTCMonthNumber2(d4, p3) {
  return pad2(d4.getUTCMonth() + 1, p3, 2);
}
function formatUTCMinutes2(d4, p3) {
  return pad2(d4.getUTCMinutes(), p3, 2);
}
function formatUTCSeconds2(d4, p3) {
  return pad2(d4.getUTCSeconds(), p3, 2);
}
function formatUTCWeekdayNumberMonday2(d4) {
  var dow = d4.getUTCDay();
  return dow === 0 ? 7 : dow;
}
function formatUTCWeekNumberSunday2(d4, p3) {
  return pad2(utcSunday2.count(utcYear_default(d4) - 1, d4), p3, 2);
}
function UTCdISO2(d4) {
  var day2 = d4.getUTCDay();
  return day2 >= 4 || day2 === 0 ? utcThursday2(d4) : utcThursday2.ceil(d4);
}
function formatUTCWeekNumberISO2(d4, p3) {
  d4 = UTCdISO2(d4);
  return pad2(utcThursday2.count(utcYear_default(d4), d4) + (utcYear_default(d4).getUTCDay() === 4), p3, 2);
}
function formatUTCWeekdayNumberSunday2(d4) {
  return d4.getUTCDay();
}
function formatUTCWeekNumberMonday2(d4, p3) {
  return pad2(utcMonday2.count(utcYear_default(d4) - 1, d4), p3, 2);
}
function formatUTCYear2(d4, p3) {
  return pad2(d4.getUTCFullYear() % 100, p3, 2);
}
function formatUTCYearISO2(d4, p3) {
  d4 = UTCdISO2(d4);
  return pad2(d4.getUTCFullYear() % 100, p3, 2);
}
function formatUTCFullYear2(d4, p3) {
  return pad2(d4.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCFullYearISO2(d4, p3) {
  var day2 = d4.getUTCDay();
  d4 = day2 >= 4 || day2 === 0 ? utcThursday2(d4) : utcThursday2.ceil(d4);
  return pad2(d4.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCZone2() {
  return "+0000";
}
function formatLiteralPercent2() {
  return "%";
}
function formatUnixTimestamp2(d4) {
  return +d4;
}
function formatUnixTimestampSeconds2(d4) {
  return Math.floor(+d4 / 1e3);
}

// node_modules/@nivo/core/node_modules/d3-time-format/src/defaultLocale.js
var locale3;
var timeFormat2;
var timeParse2;
var utcFormat2;
var utcParse2;
defaultLocale3({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});
function defaultLocale3(definition) {
  locale3 = formatLocale2(definition);
  timeFormat2 = locale3.format;
  timeParse2 = locale3.parse;
  utcFormat2 = locale3.utcFormat;
  utcParse2 = locale3.utcParse;
  return locale3;
}

// node_modules/@nivo/core/node_modules/d3-time-format/src/isoFormat.js
var isoSpecifier2 = "%Y-%m-%dT%H:%M:%S.%LZ";
function formatIsoNative2(date2) {
  return date2.toISOString();
}
var formatIso2 = Date.prototype.toISOString ? formatIsoNative2 : utcFormat2(isoSpecifier2);

// node_modules/@nivo/core/node_modules/d3-time-format/src/isoParse.js
function parseIsoNative2(string) {
  var date2 = new Date(string);
  return isNaN(date2) ? null : date2;
}
var parseIso2 = +/* @__PURE__ */ new Date("2000-01-01T00:00:00.000Z") ? parseIsoNative2 : utcParse2(isoSpecifier2);

// node_modules/@nivo/core/dist/nivo-core.mjs
var import_get2 = __toESM(require_get(), 1);
var import_isPlainObject = __toESM(require_isPlainObject(), 1);
var import_pick = __toESM(require_pick(), 1);
var import_isEqual = __toESM(require_isEqual(), 1);
var import_set2 = __toESM(require_set(), 1);
var Sr = (0, import_react18.createContext)();
var qr = function(e11) {
  var t9 = e11.children, n9 = e11.animate, o6 = void 0 === n9 || n9, i8 = e11.config, a5 = void 0 === i8 ? "default" : i8, l5 = (0, import_react18.useMemo)((function() {
    var e12 = (0, import_isString.default)(a5) ? config[a5] : a5;
    return { animate: o6, config: e12 };
  }), [o6, a5]);
  return (0, import_jsx_runtime2.jsx)(Sr.Provider, { value: l5, children: t9 });
};
var Dr = function() {
  return (0, import_react18.useContext)(Sr);
};
var Er = function(e11) {
  var r8 = e11.children, t9 = e11.condition, o6 = e11.wrapper;
  return t9 ? (0, import_react18.cloneElement)(o6, {}, r8) : r8;
};
var Ur = { position: "relative" };
var Fr = function(e11) {
  var r8 = e11.children, t9 = e11.theme, n9 = e11.renderWrapper, i8 = void 0 === n9 || n9, a5 = e11.isInteractive, l5 = void 0 === a5 || a5, d4 = e11.animate, u8 = e11.motionConfig, c9 = (0, import_react18.useRef)(null);
  return (0, import_jsx_runtime2.jsx)(z, { theme: t9, children: (0, import_jsx_runtime2.jsx)(qr, { animate: d4, config: u8, children: (0, import_jsx_runtime2.jsx)(W2, { container: c9, children: (0, import_jsx_runtime2.jsxs)(Er, { condition: i8, wrapper: (0, import_jsx_runtime2.jsx)("div", { style: Ur, ref: c9 }), children: [r8, l5 && (0, import_jsx_runtime2.jsx)(M2, {})] }) }) }) });
};
var Kr = function(e11, r8) {
  return e11.width === r8.width && e11.height === r8.height;
};
var Nr = function(e11) {
  var r8 = e11.children, t9 = e11.width, n9 = e11.height, o6 = e11.onResize, i8 = e11.debounceResize, l5 = a({ width: t9, height: n9 }, i8, { equalityFn: Kr })[0];
  return (0, import_react18.useEffect)((function() {
    null == o6 || o6(l5);
  }), [l5, o6]), (0, import_jsx_runtime2.jsx)(import_jsx_runtime2.Fragment, { children: r8(l5) });
};
var $r = function(e11) {
  var r8 = e11.children, t9 = e11.defaultWidth, n9 = e11.defaultHeight, o6 = e11.onResize, i8 = e11.debounceResize, a5 = void 0 === i8 ? 0 : i8;
  return (0, import_jsx_runtime2.jsx)(AutoSizer, { defaultWidth: t9, defaultHeight: n9, children: function(e12) {
    var t10 = e12.width, n10 = e12.height;
    return (0, import_jsx_runtime2.jsx)(Nr, { width: t10, height: n10, onResize: o6, debounceResize: a5, children: r8 });
  } });
};
function Jr(e11, r8) {
  (null == r8 || r8 > e11.length) && (r8 = e11.length);
  for (var t9 = 0, n9 = Array(r8); t9 < r8; t9++) n9[t9] = e11[t9];
  return n9;
}
function Qr(e11, r8) {
  var t9 = "undefined" != typeof Symbol && e11[Symbol.iterator] || e11["@@iterator"];
  if (t9) return (t9 = t9.call(e11)).next.bind(t9);
  if (Array.isArray(e11) || (t9 = (function(e12, r9) {
    if (e12) {
      if ("string" == typeof e12) return Jr(e12, r9);
      var t10 = {}.toString.call(e12).slice(8, -1);
      return "Object" === t10 && e12.constructor && (t10 = e12.constructor.name), "Map" === t10 || "Set" === t10 ? Array.from(e12) : "Arguments" === t10 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t10) ? Jr(e12, r9) : void 0;
    }
  })(e11)) || r8 && e11 && "number" == typeof e11.length) {
    t9 && (e11 = t9);
    var n9 = 0;
    return function() {
      return n9 >= e11.length ? { done: true } : { done: false, value: e11[n9++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function Vr() {
  return Vr = Object.assign ? Object.assign.bind() : function(e11) {
    for (var r8 = 1; r8 < arguments.length; r8++) {
      var t9 = arguments[r8];
      for (var n9 in t9) ({}).hasOwnProperty.call(t9, n9) && (e11[n9] = t9[n9]);
    }
    return e11;
  }, Vr.apply(null, arguments);
}
function Zr(e11, r8) {
  if (null == e11) return {};
  var t9 = {};
  for (var n9 in e11) if ({}.hasOwnProperty.call(e11, n9)) {
    if (-1 !== r8.indexOf(n9)) continue;
    t9[n9] = e11[n9];
  }
  return t9;
}
var rt = ["id", "colors"];
var tt = function(e11) {
  var r8 = e11.id, t9 = e11.colors, n9 = Zr(e11, rt);
  return (0, import_jsx_runtime2.jsx)("linearGradient", Vr({ id: r8, x1: 0, x2: 0, y1: 0, y2: 1 }, n9, { children: t9.map((function(e12) {
    var r9 = e12.offset, t10 = e12.color, n10 = e12.opacity;
    return (0, import_jsx_runtime2.jsx)("stop", { offset: r9 + "%", stopColor: t10, stopOpacity: void 0 !== n10 ? n10 : 1 }, r9);
  })) }));
};
var ot = { linearGradient: tt };
var it = { color: "#000000", background: "#ffffff", size: 4, padding: 4, stagger: false };
var at = (0, import_react18.memo)((function(e11) {
  var r8 = e11.id, t9 = e11.background, n9 = void 0 === t9 ? it.background : t9, o6 = e11.color, i8 = void 0 === o6 ? it.color : o6, a5 = e11.size, l5 = void 0 === a5 ? it.size : a5, d4 = e11.padding, u8 = void 0 === d4 ? it.padding : d4, c9 = e11.stagger, s5 = void 0 === c9 ? it.stagger : c9, f2 = l5 + u8, h2 = l5 / 2, p3 = u8 / 2;
  return true === s5 && (f2 = 2 * l5 + 2 * u8), (0, import_jsx_runtime2.jsxs)("pattern", { id: r8, width: f2, height: f2, patternUnits: "userSpaceOnUse", children: [(0, import_jsx_runtime2.jsx)("rect", { width: f2, height: f2, fill: n9 }), (0, import_jsx_runtime2.jsx)("circle", { cx: p3 + h2, cy: p3 + h2, r: h2, fill: i8 }), s5 && (0, import_jsx_runtime2.jsx)("circle", { cx: 1.5 * u8 + l5 + h2, cy: 1.5 * u8 + l5 + h2, r: h2, fill: i8 })] });
}));
var dt = 2 * Math.PI;
var ut = function(e11) {
  return e11 * Math.PI / 180;
};
var ct = function(e11) {
  return 180 * e11 / Math.PI;
};
var ft = function(e11, r8) {
  return { x: Math.cos(e11) * r8, y: Math.sin(e11) * r8 };
};
var ht = function(e11) {
  var r8 = e11 % 360;
  return r8 < 0 && (r8 += 360), r8;
};
var gt = { svg: { align: { left: "start", center: "middle", right: "end", start: "start", middle: "middle", end: "end" }, baseline: { top: "text-before-edge", center: "central", bottom: "alphabetic" } }, canvas: { align: { left: "left", center: "center", right: "right", start: "left", middle: "center", end: "right" }, baseline: { top: "top", center: "middle", bottom: "bottom" } } };
var mt = { spacing: 5, rotation: 0, background: "#000000", color: "#ffffff", lineWidth: 2 };
var vt = (0, import_react18.memo)((function(e11) {
  var r8 = e11.id, t9 = e11.spacing, n9 = void 0 === t9 ? mt.spacing : t9, o6 = e11.rotation, i8 = void 0 === o6 ? mt.rotation : o6, a5 = e11.background, l5 = void 0 === a5 ? mt.background : a5, d4 = e11.color, u8 = void 0 === d4 ? mt.color : d4, c9 = e11.lineWidth, s5 = void 0 === c9 ? mt.lineWidth : c9, f2 = Math.round(i8) % 360, h2 = Math.abs(n9);
  f2 > 180 ? f2 -= 360 : f2 > 90 ? f2 -= 180 : f2 < -180 ? f2 += 360 : f2 < -90 && (f2 += 180);
  var p3, g2 = h2, b6 = h2;
  return 0 === f2 ? p3 = "\n                M 0 0 L " + g2 + " 0\n                M 0 " + b6 + " L " + g2 + " " + b6 + "\n            " : 90 === f2 ? p3 = "\n                M 0 0 L 0 " + b6 + "\n                M " + g2 + " 0 L " + g2 + " " + b6 + "\n            " : (g2 = Math.abs(h2 / Math.sin(ut(f2))), b6 = h2 / Math.sin(ut(90 - f2)), p3 = f2 > 0 ? "\n                    M 0 " + -b6 + " L " + 2 * g2 + " " + b6 + "\n                    M " + -g2 + " " + -b6 + " L " + g2 + " " + b6 + "\n                    M " + -g2 + " 0 L " + g2 + " " + 2 * b6 + "\n                " : "\n                    M " + -g2 + " " + b6 + " L " + g2 + " " + -b6 + "\n                    M " + -g2 + " " + 2 * b6 + " L " + 2 * g2 + " " + -b6 + "\n                    M 0 " + 2 * b6 + " L " + 2 * g2 + " 0\n                "), (0, import_jsx_runtime2.jsxs)("pattern", { id: r8, width: g2, height: b6, patternUnits: "userSpaceOnUse", children: [(0, import_jsx_runtime2.jsx)("rect", { width: g2, height: b6, fill: l5, stroke: "rgba(255, 0, 0, 0.1)", strokeWidth: 0 }), (0, import_jsx_runtime2.jsx)("path", { d: p3, strokeWidth: s5, stroke: u8, strokeLinecap: "square" })] });
}));
var _t = { color: "#000000", background: "#ffffff", size: 4, padding: 4, stagger: false };
var wt = (0, import_react18.memo)((function(e11) {
  var r8 = e11.id, t9 = e11.color, n9 = void 0 === t9 ? _t.color : t9, o6 = e11.background, i8 = void 0 === o6 ? _t.background : o6, a5 = e11.size, l5 = void 0 === a5 ? _t.size : a5, d4 = e11.padding, u8 = void 0 === d4 ? _t.padding : d4, c9 = e11.stagger, s5 = void 0 === c9 ? _t.stagger : c9, f2 = l5 + u8, h2 = u8 / 2;
  return true === s5 && (f2 = 2 * l5 + 2 * u8), (0, import_jsx_runtime2.jsxs)("pattern", { id: r8, width: f2, height: f2, patternUnits: "userSpaceOnUse", children: [(0, import_jsx_runtime2.jsx)("rect", { width: f2, height: f2, fill: i8 }), (0, import_jsx_runtime2.jsx)("rect", { x: h2, y: h2, width: l5, height: l5, fill: n9 }), s5 && (0, import_jsx_runtime2.jsx)("rect", { x: 1.5 * u8 + l5, y: 1.5 * u8 + l5, width: l5, height: l5, fill: n9 })] });
}));
var xt = { patternDots: at, patternLines: vt, patternSquares: wt };
var Ot = ["type"];
var zt = Vr({}, ot, xt);
var Mt = (0, import_react18.memo)((function(e11) {
  var r8 = e11.defs;
  return !r8 || r8.length < 1 ? null : (0, import_jsx_runtime2.jsx)("defs", { "aria-hidden": true, children: r8.map((function(e12) {
    var r9 = e12.type, t9 = Zr(e12, Ot);
    return zt[r9] ? (0, import_react18.createElement)(zt[r9], Vr({ key: t9.id }, t9)) : null;
  })) });
}));
var Rt = (0, import_react18.forwardRef)((function(e11, r8) {
  var t9 = e11.width, n9 = e11.height, o6 = e11.margin, i8 = e11.defs, a5 = e11.children, l5 = e11.role, d4 = e11.ariaLabel, u8 = e11.ariaLabelledBy, c9 = e11.ariaDescribedBy, s5 = e11.isFocusable, f2 = M();
  return (0, import_jsx_runtime2.jsxs)("svg", { xmlns: "http://www.w3.org/2000/svg", width: t9, height: n9, role: l5, "aria-label": d4, "aria-labelledby": u8, "aria-describedby": c9, focusable: s5, tabIndex: s5 ? 0 : void 0, ref: r8, children: [(0, import_jsx_runtime2.jsx)(Mt, { defs: i8 }), (0, import_jsx_runtime2.jsx)("rect", { width: t9, height: n9, fill: f2.background }), (0, import_jsx_runtime2.jsx)("g", { transform: "translate(" + o6.left + "," + o6.top + ")", children: a5 })] });
}));
var jt = (0, import_react18.memo)((function(e11) {
  var r8 = e11.size, t9 = e11.color, n9 = e11.borderWidth, o6 = e11.borderColor;
  return (0, import_jsx_runtime2.jsx)("circle", { r: r8 / 2, fill: t9, stroke: o6, strokeWidth: n9, style: { pointerEvents: "none" } });
}));
var Ct = (0, import_react18.memo)((function(e11) {
  var r8 = e11.x, t9 = e11.y, n9 = e11.symbol, o6 = void 0 === n9 ? jt : n9, a5 = e11.size, l5 = e11.datum, u8 = e11.color, c9 = e11.borderWidth, s5 = e11.borderColor, f2 = e11.label, h2 = e11.labelTextAnchor, p3 = void 0 === h2 ? "middle" : h2, g2 = e11.labelYOffset, b6 = void 0 === g2 ? -12 : g2, m4 = e11.ariaLabel, _3 = e11.ariaLabelledBy, w5 = e11.ariaDescribedBy, O5 = e11.ariaHidden, R5 = e11.ariaDisabled, j5 = e11.isFocusable, C6 = void 0 !== j5 && j5, B4 = e11.tabIndex, P5 = void 0 === B4 ? 0 : B4, W6 = e11.onFocus, G = e11.onBlur, I2 = e11.testId, A5 = M(), L4 = Dr(), S4 = L4.animate, Y3 = L4.config, q3 = useSpring({ transform: "translate(" + r8 + ", " + t9 + ")", config: Y3, immediate: !S4 }), D5 = (0, import_react18.useCallback)((function(e12) {
    null == W6 || W6(l5, e12);
  }), [W6, l5]), E5 = (0, import_react18.useCallback)((function(e12) {
    null == G || G(l5, e12);
  }), [G, l5]);
  return (0, import_jsx_runtime2.jsxs)(animated.g, { transform: q3.transform, style: { pointerEvents: "none" }, focusable: C6, tabIndex: C6 ? P5 : void 0, "aria-label": m4, "aria-labelledby": _3, "aria-describedby": w5, "aria-disabled": R5, "aria-hidden": O5, onFocus: C6 && W6 ? D5 : void 0, onBlur: C6 && G ? E5 : void 0, "data-testid": I2, children: [(0, import_react18.createElement)(o6, { size: a5, color: u8, datum: l5, borderWidth: c9, borderColor: s5 }), f2 && (0, import_jsx_runtime2.jsx)("text", { textAnchor: p3, y: b6, style: b(A5.dots.text), children: f2 })] });
}));
var Bt = (0, import_react18.memo)((function(e11) {
  var r8 = e11.width, t9 = e11.height, n9 = e11.axis, o6 = e11.scale, i8 = e11.value, a5 = e11.lineStyle, l5 = e11.textStyle, d4 = e11.legend, u8 = e11.legendNode, c9 = e11.legendPosition, s5 = void 0 === c9 ? "top-right" : c9, f2 = e11.legendOffsetX, h2 = void 0 === f2 ? 14 : f2, p3 = e11.legendOffsetY, g2 = void 0 === p3 ? 14 : p3, b6 = e11.legendOrientation, m4 = void 0 === b6 ? "horizontal" : b6, y5 = M(), _3 = 0, w5 = 0, k4 = 0, x4 = 0;
  if ("y" === n9 ? (k4 = o6(i8), w5 = r8) : (_3 = o6(i8), x4 = t9), d4 && !u8) {
    var O5 = (function(e12) {
      var r9 = e12.axis, t10 = e12.width, n10 = e12.height, o7 = e12.position, i9 = e12.offsetX, a6 = e12.offsetY, l6 = e12.orientation, d5 = 0, u9 = 0, c10 = "vertical" === l6 ? -90 : 0, s6 = "start";
      if ("x" === r9) switch (o7) {
        case "top-left":
          d5 = -i9, u9 = a6, s6 = "end";
          break;
        case "top":
          u9 = -a6, s6 = "horizontal" === l6 ? "middle" : "start";
          break;
        case "top-right":
          d5 = i9, u9 = a6, s6 = "horizontal" === l6 ? "start" : "end";
          break;
        case "right":
          d5 = i9, u9 = n10 / 2, s6 = "horizontal" === l6 ? "start" : "middle";
          break;
        case "bottom-right":
          d5 = i9, u9 = n10 - a6, s6 = "start";
          break;
        case "bottom":
          u9 = n10 + a6, s6 = "horizontal" === l6 ? "middle" : "end";
          break;
        case "bottom-left":
          u9 = n10 - a6, d5 = -i9, s6 = "horizontal" === l6 ? "end" : "start";
          break;
        case "left":
          d5 = -i9, u9 = n10 / 2, s6 = "horizontal" === l6 ? "end" : "middle";
      }
      else switch (o7) {
        case "top-left":
          d5 = i9, u9 = -a6, s6 = "start";
          break;
        case "top":
          d5 = t10 / 2, u9 = -a6, s6 = "horizontal" === l6 ? "middle" : "start";
          break;
        case "top-right":
          d5 = t10 - i9, u9 = -a6, s6 = "horizontal" === l6 ? "end" : "start";
          break;
        case "right":
          d5 = t10 + i9, s6 = "horizontal" === l6 ? "start" : "middle";
          break;
        case "bottom-right":
          d5 = t10 - i9, u9 = a6, s6 = "end";
          break;
        case "bottom":
          d5 = t10 / 2, u9 = a6, s6 = "horizontal" === l6 ? "middle" : "end";
          break;
        case "bottom-left":
          d5 = i9, u9 = a6, s6 = "horizontal" === l6 ? "start" : "end";
          break;
        case "left":
          d5 = -i9, s6 = "horizontal" === l6 ? "end" : "middle";
      }
      return { x: d5, y: u9, rotation: c10, textAnchor: s6 };
    })({ axis: n9, width: r8, height: t9, position: s5, offsetX: h2, offsetY: g2, orientation: m4 });
    u8 = (0, import_jsx_runtime2.jsx)("text", { transform: "translate(" + O5.x + ", " + O5.y + ") rotate(" + O5.rotation + ")", textAnchor: O5.textAnchor, dominantBaseline: "central", style: l5, children: d4 });
  }
  return (0, import_jsx_runtime2.jsxs)("g", { transform: "translate(" + _3 + ", " + k4 + ")", children: [(0, import_jsx_runtime2.jsx)("line", { x1: 0, x2: w5, y1: 0, y2: x4, stroke: y5.markers.lineColor, strokeWidth: y5.markers.lineStrokeWidth, style: a5 }), u8] });
}));
var Pt = (0, import_react18.memo)((function(e11) {
  var r8 = e11.markers, t9 = e11.width, n9 = e11.height, o6 = e11.xScale, i8 = e11.yScale;
  return r8 && 0 !== r8.length ? r8.map((function(e12, r9) {
    return (0, import_jsx_runtime2.jsx)(Bt, Vr({}, e12, { width: t9, height: n9, scale: "y" === e12.axis ? i8 : o6 }), r9);
  })) : null;
}));
var It = function(e11) {
  var t9 = Dr(), n9 = t9.animate, i8 = t9.config, l5 = (function(e12) {
    var r8 = (0, import_react18.useRef)();
    return (0, import_react18.useEffect)((function() {
      r8.current = e12;
    }), [e12]), r8.current;
  })(e11), d4 = (0, import_react18.useMemo)((function() {
    return string_default(l5, e11);
  }), [l5, e11]), u8 = useSpring({ from: { value: 0 }, to: { value: 1 }, reset: true, config: i8, immediate: !n9 }).value;
  return to2(u8, d4);
};
var At = (0, import_react18.createContext)(void 0);
var Lt = { basis: basis_default2, basisClosed: basisClosed_default2, basisOpen: basisOpen_default, bundle: bundle_default, cardinal: cardinal_default, cardinalClosed: cardinalClosed_default, cardinalOpen: cardinalOpen_default, catmullRom: catmullRom_default, catmullRomClosed: catmullRomClosed_default, catmullRomOpen: catmullRomOpen_default, linear: linear_default, linearClosed: linearClosed_default, monotoneX, monotoneY, natural: natural_default, step: step_default, stepAfter, stepBefore };
var St = Object.keys(Lt);
var Yt = St.filter((function(e11) {
  return e11.endsWith("Closed");
}));
var qt = (0, import_without.default)(St, "bundle", "basisClosed", "basisOpen", "cardinalClosed", "cardinalOpen", "catmullRomClosed", "catmullRomOpen", "linearClosed");
var Dt = (0, import_without.default)(St, "bundle", "basisClosed", "basisOpen", "cardinalClosed", "cardinalOpen", "catmullRomClosed", "catmullRomOpen", "linearClosed");
var Ut = { ascending: ascending_default, descending: descending_default2, insideOut: insideOut_default, none: none_default2, reverse: reverse_default };
var Ft = Object.keys(Ut);
var Ht = { expand: expand_default, diverging: diverging_default, none: none_default, silhouette: silhouette_default, wiggle: wiggle_default };
var Xt = Object.keys(Ht);
var Jt = { nivo: ["#d76445", "#f47560", "#e8c1a0", "#97e3d5", "#61cdbb", "#00b0a7"], BrBG: (0, import_last.default)(scheme), PRGn: (0, import_last.default)(scheme2), PiYG: (0, import_last.default)(scheme3), PuOr: (0, import_last.default)(scheme4), RdBu: (0, import_last.default)(scheme5), RdGy: (0, import_last.default)(scheme6), RdYlBu: (0, import_last.default)(scheme7), RdYlGn: (0, import_last.default)(scheme8), spectral: (0, import_last.default)(scheme9), blues: (0, import_last.default)(scheme22), greens: (0, import_last.default)(scheme23), greys: (0, import_last.default)(scheme24), oranges: (0, import_last.default)(scheme27), purples: (0, import_last.default)(scheme25), reds: (0, import_last.default)(scheme26), BuGn: (0, import_last.default)(scheme10), BuPu: (0, import_last.default)(scheme11), GnBu: (0, import_last.default)(scheme12), OrRd: (0, import_last.default)(scheme13), PuBuGn: (0, import_last.default)(scheme14), PuBu: (0, import_last.default)(scheme15), PuRd: (0, import_last.default)(scheme16), RdPu: (0, import_last.default)(scheme17), YlGnBu: (0, import_last.default)(scheme18), YlGn: (0, import_last.default)(scheme19), YlOrBr: (0, import_last.default)(scheme20), YlOrRd: (0, import_last.default)(scheme21) };
var Qt = Object.keys(Jt);
var Zt = { nivo: ["#e8c1a0", "#f47560", "#f1e15b", "#e8a838", "#61cdbb", "#97e3d5"], category10: category10_default, accent: Accent_default, dark2: Dark2_default, paired: Paired_default, pastel1: Pastel1_default, pastel2: Pastel2_default, set1: Set1_default, set2: Set2_default, set3: Set3_default, brown_blueGreen: (0, import_last.default)(scheme), purpleRed_green: (0, import_last.default)(scheme2), pink_yellowGreen: (0, import_last.default)(scheme3), purple_orange: (0, import_last.default)(scheme4), red_blue: (0, import_last.default)(scheme5), red_grey: (0, import_last.default)(scheme6), red_yellow_blue: (0, import_last.default)(scheme7), red_yellow_green: (0, import_last.default)(scheme8), spectral: (0, import_last.default)(scheme9), blues: (0, import_last.default)(scheme22), greens: (0, import_last.default)(scheme23), greys: (0, import_last.default)(scheme24), oranges: (0, import_last.default)(scheme27), purples: (0, import_last.default)(scheme25), reds: (0, import_last.default)(scheme26), blue_green: (0, import_last.default)(scheme10), blue_purple: (0, import_last.default)(scheme11), green_blue: (0, import_last.default)(scheme12), orange_red: (0, import_last.default)(scheme13), purple_blue_green: (0, import_last.default)(scheme14), purple_blue: (0, import_last.default)(scheme15), purple_red: (0, import_last.default)(scheme16), red_purple: (0, import_last.default)(scheme17), yellow_green_blue: (0, import_last.default)(scheme18), yellow_green: (0, import_last.default)(scheme19), yellow_orange_brown: (0, import_last.default)(scheme20), yellow_orange_red: (0, import_last.default)(scheme21) };
var dn = ordinal(Set3_default);
var un = { top: 0, right: 0, bottom: 0, left: 0 };
var cn = function(e11, t9, n9) {
  return void 0 === n9 && (n9 = {}), (0, import_react18.useMemo)((function() {
    var r8 = Vr({}, un, n9);
    return { margin: r8, innerWidth: e11 - r8.left - r8.right, innerHeight: t9 - r8.top - r8.bottom, outerWidth: e11, outerHeight: t9 };
  }), [e11, t9, n9]);
};
var sn = function() {
  var e11 = (0, import_react18.useRef)(null), r8 = (0, import_react18.useState)({ left: 0, top: 0, width: 0, height: 0 }), t9 = r8[0], n9 = r8[1], i8 = (0, import_react18.useState)((function() {
    return "undefined" == typeof ResizeObserver ? null : new ResizeObserver((function(e12) {
      var r9 = e12[0];
      return n9(r9.contentRect);
    }));
  }))[0];
  return (0, import_react18.useEffect)((function() {
    return e11.current && null !== i8 && i8.observe(e11.current), function() {
      null !== i8 && i8.disconnect();
    };
  }), [i8]), [e11, t9];
};
var fn = function(e11) {
  return "function" == typeof e11 ? e11 : "string" == typeof e11 ? 0 === e11.indexOf("time:") ? timeFormat2(e11.slice("5")) : format(e11) : function(e12) {
    return "" + e12;
  };
};
var hn = function(e11) {
  return (0, import_react18.useMemo)((function() {
    return fn(e11);
  }), [e11]);
};
var gn = function(e11) {
  return (0, import_isFunction.default)(e11) ? e11 : function(r8) {
    return (0, import_get2.default)(r8, e11);
  };
};
var bn = function(e11) {
  return (0, import_react18.useMemo)((function() {
    return gn(e11);
  }), [e11]);
};
var yn = function(e11, r8, t9, n9) {
  return Math.sqrt(Math.pow(t9 - e11, 2) + Math.pow(n9 - r8, 2));
};
var wn = function(e11, r8, t9, n9, o6, i8) {
  return e11 <= o6 && o6 <= e11 + t9 && r8 <= i8 && i8 <= r8 + n9;
};
var kn = function(e11, r8) {
  var t9, n9 = "touches" in r8 ? r8.touches[0] : r8, o6 = n9.clientX, i8 = n9.clientY, a5 = e11.getBoundingClientRect(), l5 = (t9 = void 0 !== e11.getBBox ? e11.getBBox() : { width: e11.offsetWidth || 0, height: e11.offsetHeight || 0 }).width === a5.width ? 1 : t9.width / a5.width;
  return [(o6 - a5.left) * l5, (i8 - a5.top) * l5];
};
var xn = Object.keys(ot);
var On = Object.keys(xt);
var zn = function(e11, r8, t9) {
  if ("*" === e11) return true;
  if ((0, import_isFunction.default)(e11)) return e11(r8);
  if ((0, import_isPlainObject.default)(e11)) {
    var n9 = t9 ? (0, import_get2.default)(r8, t9) : r8;
    return (0, import_isEqual.default)((0, import_pick.default)(n9, Object.keys(e11)), e11);
  }
  return false;
};
var Mn = function(e11, r8, t9, n9) {
  var o6 = void 0 === n9 ? {} : n9, i8 = o6.dataKey, a5 = o6.colorKey, l5 = void 0 === a5 ? "color" : a5, d4 = o6.targetKey, u8 = void 0 === d4 ? "fill" : d4, c9 = [], s5 = {};
  return e11.length && r8.length && (c9 = [].concat(e11), r8.forEach((function(r9) {
    for (var n10, o7 = function() {
      var t10 = n10.value, o8 = t10.id, a7 = t10.match;
      if (zn(a7, r9, i8)) {
        var d5 = e11.find((function(e12) {
          return e12.id === o8;
        }));
        if (d5) {
          if (On.includes(d5.type)) if ("inherit" === d5.background || "inherit" === d5.color) {
            var f2 = (0, import_get2.default)(r9, l5), h2 = d5.background, p3 = d5.color, g2 = o8;
            "inherit" === d5.background && (g2 = g2 + ".bg." + f2, h2 = f2), "inherit" === d5.color && (g2 = g2 + ".fg." + f2, p3 = f2), (0, import_set2.default)(r9, u8, "url(#" + g2 + ")"), s5[g2] || (c9.push(Vr({}, d5, { id: g2, background: h2, color: p3 })), s5[g2] = 1);
          } else (0, import_set2.default)(r9, u8, "url(#" + o8 + ")");
          else if (xn.includes(d5.type)) {
            if (d5.colors.map((function(e12) {
              return e12.color;
            })).includes("inherit")) {
              var b6 = (0, import_get2.default)(r9, l5), m4 = o8, v3 = Vr({}, d5, { colors: d5.colors.map((function(e12, r10) {
                return "inherit" !== e12.color ? e12 : (m4 = m4 + "." + r10 + "." + b6, Vr({}, e12, { color: "inherit" === e12.color ? b6 : e12.color }));
              })) });
              v3.id = m4, (0, import_set2.default)(r9, u8, "url(#" + m4 + ")"), s5[m4] || (c9.push(v3), s5[m4] = 1);
            } else (0, import_set2.default)(r9, u8, "url(#" + o8 + ")");
          }
        }
        return 1;
      }
    }, a6 = Qr(t9); !(n10 = a6()).done && !o7(); ) ;
  }))), c9;
};
function Rn() {
  for (var e11 = arguments.length, r8 = new Array(e11), t9 = 0; t9 < e11; t9++) r8[t9] = arguments[t9];
  return function(e12) {
    for (var t10 = 0, n9 = r8; t10 < n9.length; t10++) {
      var o6 = n9[t10];
      "function" == typeof o6 ? o6(e12) : null != o6 && (o6.current = e12);
    }
  };
}

// node_modules/@nivo/tooltip/dist/nivo-tooltip.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
function v() {
  return v = Object.assign ? Object.assign.bind() : function(t9) {
    for (var i8 = 1; i8 < arguments.length; i8++) {
      var n9 = arguments[i8];
      for (var o6 in n9) ({}).hasOwnProperty.call(n9, o6) && (t9[o6] = n9[o6]);
    }
    return t9;
  }, v.apply(null, arguments);
}
var x2 = ["basic", "chip", "container", "table", "tableCell", "tableCellValue"];
var m = { pointerEvents: "none", position: "absolute", zIndex: 10, top: 0, left: 0 };
var b2 = function(t9, i8) {
  return "translate(" + t9 + "px, " + i8 + "px)";
};
var g = (0, import_react19.memo)((function(t9) {
  var n9, o6 = t9.position, r8 = t9.anchor, e11 = t9.children, l5 = M(), u8 = Dr(), y5 = u8.animate, f2 = u8.config, g2 = sn(), w5 = g2[0], T4 = g2[1], C6 = (0, import_react19.useRef)(false), E5 = void 0, P5 = false, V3 = T4.width > 0 && T4.height > 0, O5 = Math.round(o6[0]), N3 = Math.round(o6[1]);
  V3 && ("top" === r8 ? (O5 -= T4.width / 2, N3 -= T4.height + 14) : "right" === r8 ? (O5 += 14, N3 -= T4.height / 2) : "bottom" === r8 ? (O5 -= T4.width / 2, N3 += 14) : "left" === r8 ? (O5 -= T4.width + 14, N3 -= T4.height / 2) : "center" === r8 && (O5 -= T4.width / 2, N3 -= T4.height / 2), E5 = { transform: b2(O5, N3) }, C6.current || (P5 = true), C6.current = [O5, N3]);
  var j5 = useSpring({ to: E5, config: f2, immediate: !y5 || P5 }), k4 = l5.tooltip;
  k4.basic, k4.chip, k4.container, k4.table, k4.tableCell, k4.tableCellValue;
  var z5 = (function(t10, i8) {
    if (null == t10) return {};
    var n10 = {};
    for (var o7 in t10) if ({}.hasOwnProperty.call(t10, o7)) {
      if (-1 !== i8.indexOf(o7)) continue;
      n10[o7] = t10[o7];
    }
    return n10;
  })(k4, x2), A5 = v({}, m, z5, { transform: null != (n9 = j5.transform) ? n9 : b2(O5, N3), opacity: j5.transform ? 1 : 0 });
  return (0, import_jsx_runtime3.jsx)(animated.div, { ref: w5, style: A5, children: e11 });
}));
g.displayName = "TooltipWrapper";
var w2 = (0, import_react19.memo)((function(t9) {
  var i8 = t9.size, n9 = void 0 === i8 ? 12 : i8, o6 = t9.color, r8 = t9.style;
  return (0, import_jsx_runtime3.jsx)("span", { style: v({ display: "block", width: n9, height: n9, background: o6 }, void 0 === r8 ? {} : r8) });
}));
var T = (0, import_react19.memo)((function(t9) {
  var i8, n9 = t9.id, o6 = t9.value, r8 = t9.format, e11 = t9.enableChip, l5 = void 0 !== e11 && e11, a5 = t9.color, c9 = t9.renderContent, s5 = M(), h2 = hn(r8);
  if ("function" == typeof c9) i8 = c9();
  else {
    var f2 = o6;
    void 0 !== h2 && void 0 !== f2 && (f2 = h2(f2)), i8 = (0, import_jsx_runtime3.jsxs)("div", { style: s5.tooltip.basic, children: [l5 && (0, import_jsx_runtime3.jsx)(w2, { color: a5, style: s5.tooltip.chip }), void 0 !== f2 ? (0, import_jsx_runtime3.jsxs)("span", { children: [n9, ": ", (0, import_jsx_runtime3.jsx)("strong", { children: "" + f2 })] }) : n9] });
  }
  return (0, import_jsx_runtime3.jsx)("div", { style: s5.tooltip.container, role: "tooltip", children: i8 });
}));
var C3 = { width: "100%", borderCollapse: "collapse" };
var E2 = (0, import_react19.memo)((function(t9) {
  var i8, n9 = t9.title, o6 = t9.rows, r8 = void 0 === o6 ? [] : o6, e11 = t9.renderContent, l5 = M();
  return r8.length ? (i8 = "function" == typeof e11 ? e11() : (0, import_jsx_runtime3.jsxs)("div", { children: [n9 && n9, (0, import_jsx_runtime3.jsx)("table", { style: v({}, C3, l5.tooltip.table), children: (0, import_jsx_runtime3.jsx)("tbody", { children: r8.map((function(t10, i9) {
    return (0, import_jsx_runtime3.jsx)("tr", { children: t10.map((function(t11, i10) {
      return (0, import_jsx_runtime3.jsx)("td", { style: l5.tooltip.tableCell, children: t11 }, i10);
    })) }, i9);
  })) }) })] }), (0, import_jsx_runtime3.jsx)("div", { style: l5.tooltip.container, children: i8 })) : null;
}));
E2.displayName = "TableTooltip";
var P2 = (0, import_react19.memo)((function(t9) {
  var i8 = t9.x0, o6 = t9.x1, r8 = t9.y0, e11 = t9.y1, l5 = M(), h2 = Dr(), u8 = h2.animate, y5 = h2.config, f2 = (0, import_react19.useMemo)((function() {
    return v({}, l5.crosshair.line, { pointerEvents: "none" });
  }), [l5.crosshair.line]), x4 = useSpring({ x1: i8, x2: o6, y1: r8, y2: e11, config: y5, immediate: !u8 });
  return (0, import_jsx_runtime3.jsx)(animated.line, v({}, x4, { fill: "none", style: f2 }));
}));
P2.displayName = "CrosshairLine";
var V = (0, import_react19.memo)((function(t9) {
  var i8, n9, o6 = t9.width, r8 = t9.height, e11 = t9.type, l5 = t9.x, a5 = t9.y;
  return "cross" === e11 ? (i8 = { x0: l5, x1: l5, y0: 0, y1: r8 }, n9 = { x0: 0, x1: o6, y0: a5, y1: a5 }) : "top-left" === e11 ? (i8 = { x0: l5, x1: l5, y0: 0, y1: a5 }, n9 = { x0: 0, x1: l5, y0: a5, y1: a5 }) : "top" === e11 ? i8 = { x0: l5, x1: l5, y0: 0, y1: a5 } : "top-right" === e11 ? (i8 = { x0: l5, x1: l5, y0: 0, y1: a5 }, n9 = { x0: l5, x1: o6, y0: a5, y1: a5 }) : "right" === e11 ? n9 = { x0: l5, x1: o6, y0: a5, y1: a5 } : "bottom-right" === e11 ? (i8 = { x0: l5, x1: l5, y0: a5, y1: r8 }, n9 = { x0: l5, x1: o6, y0: a5, y1: a5 }) : "bottom" === e11 ? i8 = { x0: l5, x1: l5, y0: a5, y1: r8 } : "bottom-left" === e11 ? (i8 = { x0: l5, x1: l5, y0: a5, y1: r8 }, n9 = { x0: 0, x1: l5, y0: a5, y1: a5 }) : "left" === e11 ? n9 = { x0: 0, x1: l5, y0: a5, y1: a5 } : "x" === e11 ? i8 = { x0: l5, x1: l5, y0: 0, y1: r8 } : "y" === e11 && (n9 = { x0: 0, x1: o6, y0: a5, y1: a5 }), (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [i8 && (0, import_jsx_runtime3.jsx)(P2, { x0: i8.x0, x1: i8.x1, y0: i8.y0, y1: i8.y1 }), n9 && (0, import_jsx_runtime3.jsx)(P2, { x0: n9.x0, x1: n9.x1, y0: n9.y0, y1: n9.y1 })] });
}));
V.displayName = "Crosshair";
var O2 = (0, import_react19.createContext)({ showTooltipAt: function() {
}, showTooltipFromEvent: function() {
}, hideTooltip: function() {
} });
var N = { isVisible: false, position: [null, null], content: null, anchor: null };
var j2 = (0, import_react19.createContext)(N);
var k2 = function(t9) {
  var i8 = (0, import_react19.useState)(N), o6 = i8[0], l5 = i8[1], a5 = (0, import_react19.useCallback)((function(t10, i9, n9) {
    var o7 = i9[0], r8 = i9[1];
    void 0 === n9 && (n9 = "top"), l5({ isVisible: true, position: [o7, r8], anchor: n9, content: t10 });
  }), [l5]), c9 = (0, import_react19.useCallback)((function(i9, n9, o7) {
    void 0 === o7 && (o7 = "top");
    var r8 = t9.current.getBoundingClientRect(), e11 = t9.current.offsetWidth, a6 = e11 === r8.width ? 1 : e11 / r8.width, c10 = "touches" in n9 ? n9.touches[0] : n9, s6 = c10.clientX, h2 = c10.clientY, u8 = (s6 - r8.left) * a6, d4 = (h2 - r8.top) * a6;
    "left" !== o7 && "right" !== o7 || (o7 = u8 < r8.width / 2 ? "right" : "left"), l5({ isVisible: true, position: [u8, d4], anchor: o7, content: i9 });
  }), [t9, l5]), s5 = (0, import_react19.useCallback)((function() {
    l5(N);
  }), [l5]);
  return { actions: (0, import_react19.useMemo)((function() {
    return { showTooltipAt: a5, showTooltipFromEvent: c9, hideTooltip: s5 };
  }), [a5, c9, s5]), state: o6 };
};
var z3 = function() {
  var t9 = (0, import_react19.useContext)(O2);
  if (void 0 === t9) throw new Error("useTooltip must be used within a TooltipProvider");
  return t9;
};
var A2 = function() {
  var t9 = (0, import_react19.useContext)(j2);
  if (void 0 === t9) throw new Error("useTooltipState must be used within a TooltipProvider");
  return t9;
};
var F = function(t9) {
  return t9.isVisible;
};
var M2 = function() {
  var t9 = A2();
  return F(t9) ? (0, import_jsx_runtime3.jsx)(g, { position: t9.position, anchor: t9.anchor, children: t9.content }) : null;
};
var W2 = function(t9) {
  var i8 = t9.container, n9 = t9.children, o6 = k2(i8), r8 = o6.actions, e11 = o6.state;
  return (0, import_jsx_runtime3.jsx)(O2.Provider, { value: r8, children: (0, import_jsx_runtime3.jsx)(j2.Provider, { value: e11, children: n9 }) });
};

// node_modules/@nivo/text/dist/nivo-text.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var a4 = function(t9, e11) {
  t9.font = (e11.fontWeight ? e11.fontWeight + " " : "") + e11.fontSize + "px " + e11.fontFamily;
};
var d3 = function(t9, e11, o6, n9, r8) {
  void 0 === n9 && (n9 = 0), void 0 === r8 && (r8 = 0), e11.outlineWidth > 0 && (t9.strokeStyle = e11.outlineColor, t9.lineWidth = 2 * e11.outlineWidth, t9.lineJoin = "round", t9.strokeText(o6, n9, r8)), t9.fillStyle = e11.fill, t9.fillText(o6, n9, r8);
};
function p2() {
  return p2 = Object.assign ? Object.assign.bind() : function(t9) {
    for (var e11 = 1; e11 < arguments.length; e11++) {
      var o6 = arguments[e11];
      for (var n9 in o6) ({}).hasOwnProperty.call(o6, n9) && (t9[n9] = o6[n9]);
    }
    return t9;
  }, p2.apply(null, arguments);
}
function s4(t9, e11) {
  if (null == t9) return {};
  var o6 = {};
  for (var n9 in t9) if ({}.hasOwnProperty.call(t9, n9)) {
    if (-1 !== e11.indexOf(n9)) continue;
    o6[n9] = t9[n9];
  }
  return o6;
}
var h = ["style", "children"];
var m2 = ["outlineWidth", "outlineColor", "outlineOpacity"];
var b3 = function(r8) {
  var i8 = r8.style, l5 = r8.children, c9 = s4(r8, h), f2 = i8.outlineWidth, u8 = i8.outlineColor, a5 = i8.outlineOpacity, d4 = s4(i8, m2);
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [f2 > 0 && (0, import_jsx_runtime4.jsx)(animated.text, p2({}, c9, { style: p2({}, d4, { strokeWidth: 2 * f2, stroke: u8, strokeOpacity: a5, strokeLinejoin: "round" }), children: l5 })), (0, import_jsx_runtime4.jsx)(animated.text, p2({}, c9, { style: d4, children: l5 }))] });
};

// node_modules/@nivo/scales/dist/nivo-scales.mjs
var import_uniq = __toESM(require_uniq(), 1);
var import_uniqBy = __toESM(require_uniqBy(), 1);
var import_sortBy = __toESM(require_sortBy(), 1);
var import_last2 = __toESM(require_last(), 1);
var import_isDate = __toESM(require_isDate(), 1);

// node_modules/@nivo/scales/node_modules/d3-time-format/src/locale.js
function localDate3(d4) {
  if (0 <= d4.y && d4.y < 100) {
    var date2 = new Date(-1, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L);
    date2.setFullYear(d4.y);
    return date2;
  }
  return new Date(d4.y, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L);
}
function utcDate3(d4) {
  if (0 <= d4.y && d4.y < 100) {
    var date2 = new Date(Date.UTC(-1, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L));
    date2.setUTCFullYear(d4.y);
    return date2;
  }
  return new Date(Date.UTC(d4.y, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L));
}
function newDate3(y5, m4, d4) {
  return { y: y5, m: m4, d: d4, H: 0, M: 0, S: 0, L: 0 };
}
function formatLocale3(locale6) {
  var locale_dateTime = locale6.dateTime, locale_date = locale6.date, locale_time = locale6.time, locale_periods = locale6.periods, locale_weekdays = locale6.days, locale_shortWeekdays = locale6.shortDays, locale_months = locale6.months, locale_shortMonths = locale6.shortMonths;
  var periodRe = formatRe3(locale_periods), periodLookup = formatLookup3(locale_periods), weekdayRe = formatRe3(locale_weekdays), weekdayLookup = formatLookup3(locale_weekdays), shortWeekdayRe = formatRe3(locale_shortWeekdays), shortWeekdayLookup = formatLookup3(locale_shortWeekdays), monthRe = formatRe3(locale_months), monthLookup = formatLookup3(locale_months), shortMonthRe = formatRe3(locale_shortMonths), shortMonthLookup = formatLookup3(locale_shortMonths);
  var formats = {
    "a": formatShortWeekday,
    "A": formatWeekday,
    "b": formatShortMonth,
    "B": formatMonth,
    "c": null,
    "d": formatDayOfMonth3,
    "e": formatDayOfMonth3,
    "f": formatMicroseconds3,
    "g": formatYearISO3,
    "G": formatFullYearISO3,
    "H": formatHour243,
    "I": formatHour123,
    "j": formatDayOfYear3,
    "L": formatMilliseconds3,
    "m": formatMonthNumber3,
    "M": formatMinutes3,
    "p": formatPeriod,
    "q": formatQuarter,
    "Q": formatUnixTimestamp3,
    "s": formatUnixTimestampSeconds3,
    "S": formatSeconds3,
    "u": formatWeekdayNumberMonday3,
    "U": formatWeekNumberSunday3,
    "V": formatWeekNumberISO3,
    "w": formatWeekdayNumberSunday3,
    "W": formatWeekNumberMonday3,
    "x": null,
    "X": null,
    "y": formatYear3,
    "Y": formatFullYear3,
    "Z": formatZone3,
    "%": formatLiteralPercent3
  };
  var utcFormats = {
    "a": formatUTCShortWeekday,
    "A": formatUTCWeekday,
    "b": formatUTCShortMonth,
    "B": formatUTCMonth,
    "c": null,
    "d": formatUTCDayOfMonth3,
    "e": formatUTCDayOfMonth3,
    "f": formatUTCMicroseconds3,
    "g": formatUTCYearISO3,
    "G": formatUTCFullYearISO3,
    "H": formatUTCHour243,
    "I": formatUTCHour123,
    "j": formatUTCDayOfYear3,
    "L": formatUTCMilliseconds3,
    "m": formatUTCMonthNumber3,
    "M": formatUTCMinutes3,
    "p": formatUTCPeriod,
    "q": formatUTCQuarter,
    "Q": formatUnixTimestamp3,
    "s": formatUnixTimestampSeconds3,
    "S": formatUTCSeconds3,
    "u": formatUTCWeekdayNumberMonday3,
    "U": formatUTCWeekNumberSunday3,
    "V": formatUTCWeekNumberISO3,
    "w": formatUTCWeekdayNumberSunday3,
    "W": formatUTCWeekNumberMonday3,
    "x": null,
    "X": null,
    "y": formatUTCYear3,
    "Y": formatUTCFullYear3,
    "Z": formatUTCZone3,
    "%": formatLiteralPercent3
  };
  var parses = {
    "a": parseShortWeekday,
    "A": parseWeekday,
    "b": parseShortMonth,
    "B": parseMonth,
    "c": parseLocaleDateTime,
    "d": parseDayOfMonth3,
    "e": parseDayOfMonth3,
    "f": parseMicroseconds3,
    "g": parseYear3,
    "G": parseFullYear3,
    "H": parseHour243,
    "I": parseHour243,
    "j": parseDayOfYear3,
    "L": parseMilliseconds3,
    "m": parseMonthNumber3,
    "M": parseMinutes3,
    "p": parsePeriod,
    "q": parseQuarter3,
    "Q": parseUnixTimestamp3,
    "s": parseUnixTimestampSeconds3,
    "S": parseSeconds3,
    "u": parseWeekdayNumberMonday3,
    "U": parseWeekNumberSunday3,
    "V": parseWeekNumberISO3,
    "w": parseWeekdayNumberSunday3,
    "W": parseWeekNumberMonday3,
    "x": parseLocaleDate,
    "X": parseLocaleTime,
    "y": parseYear3,
    "Y": parseFullYear3,
    "Z": parseZone3,
    "%": parseLiteralPercent3
  };
  formats.x = newFormat(locale_date, formats);
  formats.X = newFormat(locale_time, formats);
  formats.c = newFormat(locale_dateTime, formats);
  utcFormats.x = newFormat(locale_date, utcFormats);
  utcFormats.X = newFormat(locale_time, utcFormats);
  utcFormats.c = newFormat(locale_dateTime, utcFormats);
  function newFormat(specifier, formats2) {
    return function(date2) {
      var string = [], i8 = -1, j5 = 0, n9 = specifier.length, c9, pad5, format2;
      if (!(date2 instanceof Date)) date2 = /* @__PURE__ */ new Date(+date2);
      while (++i8 < n9) {
        if (specifier.charCodeAt(i8) === 37) {
          string.push(specifier.slice(j5, i8));
          if ((pad5 = pads3[c9 = specifier.charAt(++i8)]) != null) c9 = specifier.charAt(++i8);
          else pad5 = c9 === "e" ? " " : "0";
          if (format2 = formats2[c9]) c9 = format2(date2, pad5);
          string.push(c9);
          j5 = i8 + 1;
        }
      }
      string.push(specifier.slice(j5, i8));
      return string.join("");
    };
  }
  function newParse(specifier, Z2) {
    return function(string) {
      var d4 = newDate3(1900, void 0, 1), i8 = parseSpecifier(d4, specifier, string += "", 0), week, day2;
      if (i8 != string.length) return null;
      if ("Q" in d4) return new Date(d4.Q);
      if ("s" in d4) return new Date(d4.s * 1e3 + ("L" in d4 ? d4.L : 0));
      if (Z2 && !("Z" in d4)) d4.Z = 0;
      if ("p" in d4) d4.H = d4.H % 12 + d4.p * 12;
      if (d4.m === void 0) d4.m = "q" in d4 ? d4.q : 0;
      if ("V" in d4) {
        if (d4.V < 1 || d4.V > 53) return null;
        if (!("w" in d4)) d4.w = 1;
        if ("Z" in d4) {
          week = utcDate3(newDate3(d4.y, 0, 1)), day2 = week.getUTCDay();
          week = day2 > 4 || day2 === 0 ? utcMonday2.ceil(week) : utcMonday2(week);
          week = utcDay_default.offset(week, (d4.V - 1) * 7);
          d4.y = week.getUTCFullYear();
          d4.m = week.getUTCMonth();
          d4.d = week.getUTCDate() + (d4.w + 6) % 7;
        } else {
          week = localDate3(newDate3(d4.y, 0, 1)), day2 = week.getDay();
          week = day2 > 4 || day2 === 0 ? monday.ceil(week) : monday(week);
          week = day_default.offset(week, (d4.V - 1) * 7);
          d4.y = week.getFullYear();
          d4.m = week.getMonth();
          d4.d = week.getDate() + (d4.w + 6) % 7;
        }
      } else if ("W" in d4 || "U" in d4) {
        if (!("w" in d4)) d4.w = "u" in d4 ? d4.u % 7 : "W" in d4 ? 1 : 0;
        day2 = "Z" in d4 ? utcDate3(newDate3(d4.y, 0, 1)).getUTCDay() : localDate3(newDate3(d4.y, 0, 1)).getDay();
        d4.m = 0;
        d4.d = "W" in d4 ? (d4.w + 6) % 7 + d4.W * 7 - (day2 + 5) % 7 : d4.w + d4.U * 7 - (day2 + 6) % 7;
      }
      if ("Z" in d4) {
        d4.H += d4.Z / 100 | 0;
        d4.M += d4.Z % 100;
        return utcDate3(d4);
      }
      return localDate3(d4);
    };
  }
  function parseSpecifier(d4, specifier, string, j5) {
    var i8 = 0, n9 = specifier.length, m4 = string.length, c9, parse;
    while (i8 < n9) {
      if (j5 >= m4) return -1;
      c9 = specifier.charCodeAt(i8++);
      if (c9 === 37) {
        c9 = specifier.charAt(i8++);
        parse = parses[c9 in pads3 ? specifier.charAt(i8++) : c9];
        if (!parse || (j5 = parse(d4, string, j5)) < 0) return -1;
      } else if (c9 != string.charCodeAt(j5++)) {
        return -1;
      }
    }
    return j5;
  }
  function parsePeriod(d4, string, i8) {
    var n9 = periodRe.exec(string.slice(i8));
    return n9 ? (d4.p = periodLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseShortWeekday(d4, string, i8) {
    var n9 = shortWeekdayRe.exec(string.slice(i8));
    return n9 ? (d4.w = shortWeekdayLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseWeekday(d4, string, i8) {
    var n9 = weekdayRe.exec(string.slice(i8));
    return n9 ? (d4.w = weekdayLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseShortMonth(d4, string, i8) {
    var n9 = shortMonthRe.exec(string.slice(i8));
    return n9 ? (d4.m = shortMonthLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseMonth(d4, string, i8) {
    var n9 = monthRe.exec(string.slice(i8));
    return n9 ? (d4.m = monthLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseLocaleDateTime(d4, string, i8) {
    return parseSpecifier(d4, locale_dateTime, string, i8);
  }
  function parseLocaleDate(d4, string, i8) {
    return parseSpecifier(d4, locale_date, string, i8);
  }
  function parseLocaleTime(d4, string, i8) {
    return parseSpecifier(d4, locale_time, string, i8);
  }
  function formatShortWeekday(d4) {
    return locale_shortWeekdays[d4.getDay()];
  }
  function formatWeekday(d4) {
    return locale_weekdays[d4.getDay()];
  }
  function formatShortMonth(d4) {
    return locale_shortMonths[d4.getMonth()];
  }
  function formatMonth(d4) {
    return locale_months[d4.getMonth()];
  }
  function formatPeriod(d4) {
    return locale_periods[+(d4.getHours() >= 12)];
  }
  function formatQuarter(d4) {
    return 1 + ~~(d4.getMonth() / 3);
  }
  function formatUTCShortWeekday(d4) {
    return locale_shortWeekdays[d4.getUTCDay()];
  }
  function formatUTCWeekday(d4) {
    return locale_weekdays[d4.getUTCDay()];
  }
  function formatUTCShortMonth(d4) {
    return locale_shortMonths[d4.getUTCMonth()];
  }
  function formatUTCMonth(d4) {
    return locale_months[d4.getUTCMonth()];
  }
  function formatUTCPeriod(d4) {
    return locale_periods[+(d4.getUTCHours() >= 12)];
  }
  function formatUTCQuarter(d4) {
    return 1 + ~~(d4.getUTCMonth() / 3);
  }
  return {
    format: function(specifier) {
      var f2 = newFormat(specifier += "", formats);
      f2.toString = function() {
        return specifier;
      };
      return f2;
    },
    parse: function(specifier) {
      var p3 = newParse(specifier += "", false);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    },
    utcFormat: function(specifier) {
      var f2 = newFormat(specifier += "", utcFormats);
      f2.toString = function() {
        return specifier;
      };
      return f2;
    },
    utcParse: function(specifier) {
      var p3 = newParse(specifier += "", true);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    }
  };
}
var pads3 = { "-": "", "_": " ", "0": "0" };
var numberRe3 = /^\s*\d+/;
var percentRe3 = /^%/;
var requoteRe3 = /[\\^$*+?|[\]().{}]/g;
function pad3(value, fill, width) {
  var sign2 = value < 0 ? "-" : "", string = (sign2 ? -value : value) + "", length = string.length;
  return sign2 + (length < width ? new Array(width - length + 1).join(fill) + string : string);
}
function requote3(s5) {
  return s5.replace(requoteRe3, "\\$&");
}
function formatRe3(names) {
  return new RegExp("^(?:" + names.map(requote3).join("|") + ")", "i");
}
function formatLookup3(names) {
  return new Map(names.map((name, i8) => [name.toLowerCase(), i8]));
}
function parseWeekdayNumberSunday3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.w = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekdayNumberMonday3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.u = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberSunday3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.U = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberISO3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.V = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberMonday3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.W = +n9[0], i8 + n9[0].length) : -1;
}
function parseFullYear3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 4));
  return n9 ? (d4.y = +n9[0], i8 + n9[0].length) : -1;
}
function parseYear3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.y = +n9[0] + (+n9[0] > 68 ? 1900 : 2e3), i8 + n9[0].length) : -1;
}
function parseZone3(d4, string, i8) {
  var n9 = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i8, i8 + 6));
  return n9 ? (d4.Z = n9[1] ? 0 : -(n9[2] + (n9[3] || "00")), i8 + n9[0].length) : -1;
}
function parseQuarter3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.q = n9[0] * 3 - 3, i8 + n9[0].length) : -1;
}
function parseMonthNumber3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.m = n9[0] - 1, i8 + n9[0].length) : -1;
}
function parseDayOfMonth3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.d = +n9[0], i8 + n9[0].length) : -1;
}
function parseDayOfYear3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 3));
  return n9 ? (d4.m = 0, d4.d = +n9[0], i8 + n9[0].length) : -1;
}
function parseHour243(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.H = +n9[0], i8 + n9[0].length) : -1;
}
function parseMinutes3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.M = +n9[0], i8 + n9[0].length) : -1;
}
function parseSeconds3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.S = +n9[0], i8 + n9[0].length) : -1;
}
function parseMilliseconds3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 3));
  return n9 ? (d4.L = +n9[0], i8 + n9[0].length) : -1;
}
function parseMicroseconds3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8, i8 + 6));
  return n9 ? (d4.L = Math.floor(n9[0] / 1e3), i8 + n9[0].length) : -1;
}
function parseLiteralPercent3(d4, string, i8) {
  var n9 = percentRe3.exec(string.slice(i8, i8 + 1));
  return n9 ? i8 + n9[0].length : -1;
}
function parseUnixTimestamp3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8));
  return n9 ? (d4.Q = +n9[0], i8 + n9[0].length) : -1;
}
function parseUnixTimestampSeconds3(d4, string, i8) {
  var n9 = numberRe3.exec(string.slice(i8));
  return n9 ? (d4.s = +n9[0], i8 + n9[0].length) : -1;
}
function formatDayOfMonth3(d4, p3) {
  return pad3(d4.getDate(), p3, 2);
}
function formatHour243(d4, p3) {
  return pad3(d4.getHours(), p3, 2);
}
function formatHour123(d4, p3) {
  return pad3(d4.getHours() % 12 || 12, p3, 2);
}
function formatDayOfYear3(d4, p3) {
  return pad3(1 + day_default.count(year_default(d4), d4), p3, 3);
}
function formatMilliseconds3(d4, p3) {
  return pad3(d4.getMilliseconds(), p3, 3);
}
function formatMicroseconds3(d4, p3) {
  return formatMilliseconds3(d4, p3) + "000";
}
function formatMonthNumber3(d4, p3) {
  return pad3(d4.getMonth() + 1, p3, 2);
}
function formatMinutes3(d4, p3) {
  return pad3(d4.getMinutes(), p3, 2);
}
function formatSeconds3(d4, p3) {
  return pad3(d4.getSeconds(), p3, 2);
}
function formatWeekdayNumberMonday3(d4) {
  var day2 = d4.getDay();
  return day2 === 0 ? 7 : day2;
}
function formatWeekNumberSunday3(d4, p3) {
  return pad3(sunday.count(year_default(d4) - 1, d4), p3, 2);
}
function dISO3(d4) {
  var day2 = d4.getDay();
  return day2 >= 4 || day2 === 0 ? thursday(d4) : thursday.ceil(d4);
}
function formatWeekNumberISO3(d4, p3) {
  d4 = dISO3(d4);
  return pad3(thursday.count(year_default(d4), d4) + (year_default(d4).getDay() === 4), p3, 2);
}
function formatWeekdayNumberSunday3(d4) {
  return d4.getDay();
}
function formatWeekNumberMonday3(d4, p3) {
  return pad3(monday.count(year_default(d4) - 1, d4), p3, 2);
}
function formatYear3(d4, p3) {
  return pad3(d4.getFullYear() % 100, p3, 2);
}
function formatYearISO3(d4, p3) {
  d4 = dISO3(d4);
  return pad3(d4.getFullYear() % 100, p3, 2);
}
function formatFullYear3(d4, p3) {
  return pad3(d4.getFullYear() % 1e4, p3, 4);
}
function formatFullYearISO3(d4, p3) {
  var day2 = d4.getDay();
  d4 = day2 >= 4 || day2 === 0 ? thursday(d4) : thursday.ceil(d4);
  return pad3(d4.getFullYear() % 1e4, p3, 4);
}
function formatZone3(d4) {
  var z5 = d4.getTimezoneOffset();
  return (z5 > 0 ? "-" : (z5 *= -1, "+")) + pad3(z5 / 60 | 0, "0", 2) + pad3(z5 % 60, "0", 2);
}
function formatUTCDayOfMonth3(d4, p3) {
  return pad3(d4.getUTCDate(), p3, 2);
}
function formatUTCHour243(d4, p3) {
  return pad3(d4.getUTCHours(), p3, 2);
}
function formatUTCHour123(d4, p3) {
  return pad3(d4.getUTCHours() % 12 || 12, p3, 2);
}
function formatUTCDayOfYear3(d4, p3) {
  return pad3(1 + utcDay_default.count(utcYear_default(d4), d4), p3, 3);
}
function formatUTCMilliseconds3(d4, p3) {
  return pad3(d4.getUTCMilliseconds(), p3, 3);
}
function formatUTCMicroseconds3(d4, p3) {
  return formatUTCMilliseconds3(d4, p3) + "000";
}
function formatUTCMonthNumber3(d4, p3) {
  return pad3(d4.getUTCMonth() + 1, p3, 2);
}
function formatUTCMinutes3(d4, p3) {
  return pad3(d4.getUTCMinutes(), p3, 2);
}
function formatUTCSeconds3(d4, p3) {
  return pad3(d4.getUTCSeconds(), p3, 2);
}
function formatUTCWeekdayNumberMonday3(d4) {
  var dow = d4.getUTCDay();
  return dow === 0 ? 7 : dow;
}
function formatUTCWeekNumberSunday3(d4, p3) {
  return pad3(utcSunday2.count(utcYear_default(d4) - 1, d4), p3, 2);
}
function UTCdISO3(d4) {
  var day2 = d4.getUTCDay();
  return day2 >= 4 || day2 === 0 ? utcThursday2(d4) : utcThursday2.ceil(d4);
}
function formatUTCWeekNumberISO3(d4, p3) {
  d4 = UTCdISO3(d4);
  return pad3(utcThursday2.count(utcYear_default(d4), d4) + (utcYear_default(d4).getUTCDay() === 4), p3, 2);
}
function formatUTCWeekdayNumberSunday3(d4) {
  return d4.getUTCDay();
}
function formatUTCWeekNumberMonday3(d4, p3) {
  return pad3(utcMonday2.count(utcYear_default(d4) - 1, d4), p3, 2);
}
function formatUTCYear3(d4, p3) {
  return pad3(d4.getUTCFullYear() % 100, p3, 2);
}
function formatUTCYearISO3(d4, p3) {
  d4 = UTCdISO3(d4);
  return pad3(d4.getUTCFullYear() % 100, p3, 2);
}
function formatUTCFullYear3(d4, p3) {
  return pad3(d4.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCFullYearISO3(d4, p3) {
  var day2 = d4.getUTCDay();
  d4 = day2 >= 4 || day2 === 0 ? utcThursday2(d4) : utcThursday2.ceil(d4);
  return pad3(d4.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCZone3() {
  return "+0000";
}
function formatLiteralPercent3() {
  return "%";
}
function formatUnixTimestamp3(d4) {
  return +d4;
}
function formatUnixTimestampSeconds3(d4) {
  return Math.floor(+d4 / 1e3);
}

// node_modules/@nivo/scales/node_modules/d3-time-format/src/defaultLocale.js
var locale4;
var timeFormat3;
var timeParse3;
var utcFormat3;
var utcParse3;
defaultLocale4({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});
function defaultLocale4(definition) {
  locale4 = formatLocale3(definition);
  timeFormat3 = locale4.format;
  timeParse3 = locale4.parse;
  utcFormat3 = locale4.utcFormat;
  utcParse3 = locale4.utcParse;
  return locale4;
}

// node_modules/@nivo/scales/node_modules/d3-time-format/src/isoFormat.js
var isoSpecifier3 = "%Y-%m-%dT%H:%M:%S.%LZ";
function formatIsoNative3(date2) {
  return date2.toISOString();
}
var formatIso3 = Date.prototype.toISOString ? formatIsoNative3 : utcFormat3(isoSpecifier3);

// node_modules/@nivo/scales/node_modules/d3-time-format/src/isoParse.js
function parseIsoNative3(string) {
  var date2 = new Date(string);
  return isNaN(date2) ? null : date2;
}
var parseIso3 = +/* @__PURE__ */ new Date("2000-01-01T00:00:00.000Z") ? parseIsoNative3 : utcParse3(isoSpecifier3);

// node_modules/@nivo/scales/dist/nivo-scales.mjs
function J() {
  return J = Object.assign ? Object.assign.bind() : function(n9) {
    for (var t9 = 1; t9 < arguments.length; t9++) {
      var e11 = arguments[t9];
      for (var r8 in e11) ({}).hasOwnProperty.call(e11, r8) && (n9[r8] = e11[r8]);
    }
    return n9;
  }, J.apply(null, arguments);
}
var L2 = [function(n9) {
  return n9.setMilliseconds(0);
}, function(n9) {
  return n9.setSeconds(0);
}, function(n9) {
  return n9.setMinutes(0);
}, function(n9) {
  return n9.setHours(0);
}, function(n9) {
  return n9.setDate(1);
}, function(n9) {
  return n9.setMonth(0);
}];
var Q = { millisecond: [], second: L2.slice(0, 1), minute: L2.slice(0, 2), hour: L2.slice(0, 3), day: L2.slice(0, 4), month: L2.slice(0, 5), year: L2.slice(0, 6) };
var W3 = function(n9) {
  return function(t9) {
    return Q[n9].forEach((function(n10) {
      n10(t9);
    })), t9;
  };
};
var X = function(n9) {
  var t9 = n9.format, e11 = void 0 === t9 ? "native" : t9, r8 = n9.precision, i8 = void 0 === r8 ? "millisecond" : r8, u8 = n9.useUTC, c9 = void 0 === u8 || u8, s5 = W3(i8);
  return function(n10) {
    if (void 0 === n10) return n10;
    if ("native" === e11 || n10 instanceof Date) return s5(n10);
    var t10 = c9 ? utcParse3(e11) : timeParse3(e11);
    return s5(t10(n10));
  };
};
var Y = { type: "linear", min: 0, max: "auto", stacked: false, reverse: false, clamp: false, nice: true, round: false };
var Z = function(n9, t9, e11, r8) {
  var i8, a5, o6, c9, s5 = n9.min, d4 = void 0 === s5 ? Y.min : s5, m4 = n9.max, f2 = void 0 === m4 ? Y.max : m4, l5 = n9.stacked, y5 = void 0 === l5 ? Y.stacked : l5, h2 = n9.reverse, g2 = void 0 === h2 ? Y.reverse : h2, x4 = n9.clamp, k4 = void 0 === x4 ? Y.clamp : x4, T4 = n9.nice, b6 = void 0 === T4 ? Y.nice : T4, M4 = n9.round, w5 = void 0 === M4 ? Y.round : M4;
  "auto" === d4 ? i8 = true === y5 ? null != (a5 = t9.minStacked) ? a5 : 0 : t9.min : i8 = d4;
  "auto" === f2 ? o6 = true === y5 ? null != (c9 = t9.maxStacked) ? c9 : 0 : t9.max : o6 = f2;
  var E5 = linear2().range("x" === r8 ? [0, e11] : [e11, 0]).interpolate(w5 ? round_default : number_default).domain(g2 ? [o6, i8] : [i8, o6]).clamp(k4);
  return true === b6 ? E5.nice() : "number" == typeof b6 && E5.nice(b6), _2(E5, y5);
};
var _2 = function(n9, t9) {
  void 0 === t9 && (t9 = false);
  var e11 = n9;
  return e11.type = "linear", e11.stacked = t9, e11;
};
var nn = function(n9, t9, e11) {
  var r8 = point5().range([0, e11]).domain(t9.all);
  return r8.type = "point", r8;
};
var tn = function(n9) {
  var t9 = n9;
  return t9.type = "point", t9;
};
var en = { type: "band", round: false };
var rn = function(n9, t9, e11, r8) {
  var i8 = n9.round, a5 = void 0 === i8 ? en.round : i8, o6 = band().range("x" === r8 ? [0, e11] : [e11, 0]).domain(t9.all).round(a5);
  return an(o6);
};
var an = function(n9) {
  var t9 = n9;
  return t9.type = "band", t9;
};
var on = { type: "time", format: "native", precision: "millisecond", min: "auto", max: "auto", useUTC: true, nice: false };
var un2 = function(n9, t9, e11) {
  var r8, i8, a5 = n9.format, o6 = void 0 === a5 ? on.format : a5, u8 = n9.precision, c9 = void 0 === u8 ? on.precision : u8, s5 = n9.min, f2 = void 0 === s5 ? on.min : s5, l5 = n9.max, v3 = void 0 === l5 ? on.max : l5, p3 = n9.useUTC, y5 = void 0 === p3 ? on.useUTC : p3, h2 = n9.nice, g2 = void 0 === h2 ? on.nice : h2, x4 = X({ format: o6, precision: c9, useUTC: y5 });
  r8 = "auto" === f2 ? x4(t9.min) : "native" !== o6 ? x4(f2) : f2, i8 = "auto" === v3 ? x4(t9.max) : "native" !== o6 ? x4(v3) : v3;
  var k4 = y5 ? utcTime() : time();
  k4.range([0, e11]), r8 && i8 && k4.domain([r8, i8]), true === g2 ? k4.nice() : "object" != typeof g2 && "number" != typeof g2 || k4.nice(g2);
  var T4 = k4;
  return T4.type = "time", T4.useUTC = y5, T4;
};
var cn2 = { type: "log", base: 10, min: "auto", max: "auto", round: false, reverse: false, nice: true };
var sn2 = function(n9, t9, e11, r8) {
  var i8, a5 = n9.base, o6 = void 0 === a5 ? cn2.base : a5, u8 = n9.min, c9 = void 0 === u8 ? cn2.min : u8, s5 = n9.max, d4 = void 0 === s5 ? cn2.max : s5, m4 = n9.round, l5 = void 0 === m4 ? cn2.round : m4, v3 = n9.reverse, p3 = void 0 === v3 ? cn2.reverse : v3, y5 = n9.nice, h2 = void 0 === y5 ? cn2.nice : y5;
  if (t9.all.some((function(n10) {
    return 0 === n10;
  }))) throw new Error("a log scale domain must not include or cross zero");
  var g2, x4, k4 = false;
  if (t9.all.filter((function(n10) {
    return null != n10;
  })).forEach((function(n10) {
    k4 || (void 0 === i8 ? i8 = Math.sign(n10) : Math.sign(n10) !== i8 && (k4 = true));
  })), k4) throw new Error("a log scale domain must be strictly-positive or strictly-negative");
  g2 = "auto" === c9 ? t9.min : c9, x4 = "auto" === d4 ? t9.max : d4;
  var T4 = log().base(o6), b6 = "x" === r8 ? [0, e11] : [e11, 0];
  true === l5 ? T4.rangeRound(b6) : T4.range(b6), true === p3 ? T4.domain([x4, g2]) : T4.domain([g2, x4]), true === h2 ? T4.nice() : "number" == typeof h2 && T4.nice(h2);
  var M4 = T4;
  return M4.type = "log", M4;
};
var dn2 = { type: "symlog", constant: 1, min: "auto", max: "auto", round: false, reverse: false, nice: true };
var mn = function(n9, t9, e11, r8) {
  var i8, a5, o6 = n9.constant, u8 = void 0 === o6 ? dn2.constant : o6, c9 = n9.min, s5 = void 0 === c9 ? dn2.min : c9, d4 = n9.max, m4 = void 0 === d4 ? dn2.max : d4, f2 = n9.round, v3 = void 0 === f2 ? dn2.round : f2, p3 = n9.reverse, y5 = void 0 === p3 ? dn2.reverse : p3, h2 = n9.nice, g2 = void 0 === h2 ? dn2.nice : h2;
  i8 = "auto" === s5 ? t9.min : s5, a5 = "auto" === m4 ? t9.max : m4;
  var x4 = symlog().constant(u8), k4 = "x" === r8 ? [0, e11] : [e11, 0];
  true === v3 ? x4.rangeRound(k4) : x4.range(k4), true === y5 ? x4.domain([a5, i8]) : x4.domain([i8, a5]), true === g2 ? x4.nice() : "number" == typeof g2 && x4.nice(g2);
  var T4 = x4;
  return T4.type = "symlog", T4;
};
var fn2 = function(n9) {
  return "x" === n9 ? "y" : "x";
};
var ln = function(n9, t9) {
  return n9 === t9;
};
var vn = function(n9, t9) {
  return n9.getTime() === t9.getTime();
};
function pn(n9, t9, e11, r8) {
  switch (n9.type) {
    case "linear":
      return Z(n9, t9, e11, r8);
    case "point":
      return nn(0, t9, e11);
    case "band":
      return rn(n9, t9, e11, r8);
    case "time":
      return un2(n9, t9, e11);
    case "log":
      return sn2(n9, t9, e11, r8);
    case "symlog":
      return mn(n9, t9, e11, r8);
    default:
      throw new Error("invalid scale spec");
  }
}
var yn2 = function(n9, t9, e11) {
  var r8;
  if ("stacked" in e11 && e11.stacked) {
    var i8 = n9.data["x" === t9 ? "xStacked" : "yStacked"];
    return null == i8 ? null : e11(i8);
  }
  return null != (r8 = e11(n9.data[t9])) ? r8 : null;
};
var hn2 = function(n9, t9, e11, r8, i8) {
  var a5 = n9.map((function(n10) {
    return (function(n11) {
      return J({}, n11, { data: n11.data.map((function(n12) {
        return { data: J({}, n12) };
      })) });
    })(n10);
  })), o6 = gn2(a5, t9, e11);
  "stacked" in t9 && true === t9.stacked && Tn(o6, a5), "stacked" in e11 && true === e11.stacked && bn2(o6, a5);
  var u8 = pn(t9, o6.x, r8, "x"), c9 = pn(e11, o6.y, i8, "y"), s5 = a5.map((function(n10) {
    return J({}, n10, { data: n10.data.map((function(n11) {
      return J({}, n11, { position: { x: yn2(n11, "x", u8), y: yn2(n11, "y", c9) } });
    })) });
  }));
  return J({}, o6, { series: s5, xScale: u8, yScale: c9 });
};
var gn2 = function(n9, t9, e11) {
  return { x: xn2(n9, "x", t9), y: xn2(n9, "y", e11) };
};
var xn2 = function(i8, a5, o6, u8) {
  var c9 = void 0 === u8 ? {} : u8, s5 = c9.getValue, d4 = void 0 === s5 ? function(n9) {
    return n9.data[a5];
  } : s5, m4 = c9.setValue, f2 = void 0 === m4 ? function(n9, t9) {
    n9.data[a5] = t9;
  } : m4;
  if ("linear" === o6.type) i8.forEach((function(n9) {
    n9.data.forEach((function(n10) {
      var t9 = d4(n10);
      t9 && f2(n10, parseFloat(String(t9)));
    }));
  }));
  else if ("time" === o6.type && "native" !== o6.format) {
    var l5 = X(o6);
    i8.forEach((function(n9) {
      n9.data.forEach((function(n10) {
        var t9 = d4(n10);
        t9 && f2(n10, l5(t9));
      }));
    }));
  }
  var v3 = [];
  switch (i8.forEach((function(n9) {
    n9.data.forEach((function(n10) {
      v3.push(d4(n10));
    }));
  })), o6.type) {
    case "linear":
      var p3 = (0, import_sortBy.default)((0, import_uniq.default)(v3).filter((function(n9) {
        return null !== n9;
      })), (function(n9) {
        return n9;
      }));
      return { all: p3, min: Math.min.apply(Math, p3), max: Math.max.apply(Math, p3) };
    case "time":
      var y5 = (0, import_uniqBy.default)(v3, (function(n9) {
        return n9.getTime();
      })).slice(0).sort((function(n9, t9) {
        return t9.getTime() - n9.getTime();
      })).reverse();
      return { all: y5, min: y5[0], max: (0, import_last2.default)(y5) };
    default:
      var h2 = (0, import_uniq.default)(v3);
      return { all: h2, min: h2[0], max: (0, import_last2.default)(h2) };
  }
};
var kn2 = function(n9, t9, e11) {
  var a5 = fn2(n9), o6 = [];
  t9[a5].all.forEach((function(t10) {
    var u8 = (0, import_isDate.default)(t10) ? vn : ln, c9 = [];
    e11.forEach((function(e12) {
      var i8 = e12.data.find((function(n10) {
        return u8(n10.data[a5], t10);
      })), s5 = null, d4 = null;
      if (void 0 !== i8) {
        if (null !== (s5 = i8.data[n9])) {
          var m4 = (0, import_last2.default)(c9);
          void 0 === m4 ? d4 = s5 : null !== m4 && (d4 = m4 + s5);
        }
        i8.data["x" === n9 ? "xStacked" : "yStacked"] = d4;
      }
      c9.push(d4), null !== d4 && o6.push(d4);
    }));
  })), t9[n9].minStacked = Math.min.apply(Math, o6), t9[n9].maxStacked = Math.max.apply(Math, o6);
};
var Tn = function(n9, t9) {
  return kn2("x", n9, t9);
};
var bn2 = function(n9, t9) {
  return kn2("y", n9, t9);
};
var Mn2 = function(n9) {
  var t9 = n9.bandwidth();
  if (0 === t9) return n9;
  var e11 = t9 / 2;
  return n9.round() && (e11 = Math.round(e11)), function(t10) {
    var r8;
    return (null != (r8 = n9(t10)) ? r8 : 0) + e11;
  };
};
var wn2 = { millisecond: [millisecond_default, millisecond_default], second: [second_default, second_default], minute: [minute_default, utcMinute_default], hour: [hour_default, utcHour_default], day: [newInterval((function(n9) {
  return n9.setHours(0, 0, 0, 0);
}), (function(n9, t9) {
  return n9.setDate(n9.getDate() + t9);
}), (function(n9, t9) {
  return (t9.getTime() - n9.getTime()) / 864e5;
}), (function(n9) {
  return Math.floor(n9.getTime() / 864e5);
})), newInterval((function(n9) {
  return n9.setUTCHours(0, 0, 0, 0);
}), (function(n9, t9) {
  return n9.setUTCDate(n9.getUTCDate() + t9);
}), (function(n9, t9) {
  return (t9.getTime() - n9.getTime()) / 864e5;
}), (function(n9) {
  return Math.floor(n9.getTime() / 864e5);
}))], week: [sunday, utcSunday2], sunday: [sunday, utcSunday2], monday: [monday, utcMonday2], tuesday: [tuesday, utcTuesday2], wednesday: [wednesday, utcWednesday2], thursday: [thursday, utcThursday2], friday: [friday, utcFriday2], saturday: [saturday, utcSaturday2], month: [month_default, utcMonth_default], year: [year_default, utcYear_default] };
var En = Object.keys(wn2);
var Sn = new RegExp("^every\\s*(\\d+)?\\s*(" + En.join("|") + ")s?$", "i");
var Cn = function(n9, t9) {
  if (Array.isArray(t9)) return t9;
  if ("string" == typeof t9 && "useUTC" in n9) {
    var e11 = t9.match(Sn);
    if (e11) {
      var r8 = e11[1], i8 = e11[2], a5 = wn2[i8][n9.useUTC ? 1 : 0];
      if ("day" === i8) {
        var o6, u8, c9 = n9.domain(), s5 = c9[0], d4 = c9[1], m4 = new Date(d4);
        return m4.setDate(m4.getDate() + 1), null != (o6 = null == (u8 = a5.every(Number(null != r8 ? r8 : 1))) ? void 0 : u8.range(s5, m4)) ? o6 : [];
      }
      if (void 0 === r8) return n9.ticks(a5);
      var f2 = a5.every(Number(r8));
      if (f2) return n9.ticks(f2);
    }
    throw new Error("Invalid tickValues: " + t9);
  }
  if ("ticks" in n9) {
    if (void 0 === t9) return n9.ticks();
    if ("number" == typeof (l5 = t9) && isFinite(l5) && Math.floor(l5) === l5) return n9.ticks(t9);
  }
  var l5;
  return n9.domain();
};

// node_modules/@nivo/axes/dist/nivo-axes.mjs
var t7 = __toESM(require_react(), 1);
var import_react20 = __toESM(require_react(), 1);

// node_modules/@nivo/axes/node_modules/d3-time-format/src/locale.js
function localDate4(d4) {
  if (0 <= d4.y && d4.y < 100) {
    var date2 = new Date(-1, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L);
    date2.setFullYear(d4.y);
    return date2;
  }
  return new Date(d4.y, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L);
}
function utcDate4(d4) {
  if (0 <= d4.y && d4.y < 100) {
    var date2 = new Date(Date.UTC(-1, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L));
    date2.setUTCFullYear(d4.y);
    return date2;
  }
  return new Date(Date.UTC(d4.y, d4.m, d4.d, d4.H, d4.M, d4.S, d4.L));
}
function newDate4(y5, m4, d4) {
  return { y: y5, m: m4, d: d4, H: 0, M: 0, S: 0, L: 0 };
}
function formatLocale4(locale6) {
  var locale_dateTime = locale6.dateTime, locale_date = locale6.date, locale_time = locale6.time, locale_periods = locale6.periods, locale_weekdays = locale6.days, locale_shortWeekdays = locale6.shortDays, locale_months = locale6.months, locale_shortMonths = locale6.shortMonths;
  var periodRe = formatRe4(locale_periods), periodLookup = formatLookup4(locale_periods), weekdayRe = formatRe4(locale_weekdays), weekdayLookup = formatLookup4(locale_weekdays), shortWeekdayRe = formatRe4(locale_shortWeekdays), shortWeekdayLookup = formatLookup4(locale_shortWeekdays), monthRe = formatRe4(locale_months), monthLookup = formatLookup4(locale_months), shortMonthRe = formatRe4(locale_shortMonths), shortMonthLookup = formatLookup4(locale_shortMonths);
  var formats = {
    "a": formatShortWeekday,
    "A": formatWeekday,
    "b": formatShortMonth,
    "B": formatMonth,
    "c": null,
    "d": formatDayOfMonth4,
    "e": formatDayOfMonth4,
    "f": formatMicroseconds4,
    "g": formatYearISO4,
    "G": formatFullYearISO4,
    "H": formatHour244,
    "I": formatHour124,
    "j": formatDayOfYear4,
    "L": formatMilliseconds4,
    "m": formatMonthNumber4,
    "M": formatMinutes4,
    "p": formatPeriod,
    "q": formatQuarter,
    "Q": formatUnixTimestamp4,
    "s": formatUnixTimestampSeconds4,
    "S": formatSeconds4,
    "u": formatWeekdayNumberMonday4,
    "U": formatWeekNumberSunday4,
    "V": formatWeekNumberISO4,
    "w": formatWeekdayNumberSunday4,
    "W": formatWeekNumberMonday4,
    "x": null,
    "X": null,
    "y": formatYear4,
    "Y": formatFullYear4,
    "Z": formatZone4,
    "%": formatLiteralPercent4
  };
  var utcFormats = {
    "a": formatUTCShortWeekday,
    "A": formatUTCWeekday,
    "b": formatUTCShortMonth,
    "B": formatUTCMonth,
    "c": null,
    "d": formatUTCDayOfMonth4,
    "e": formatUTCDayOfMonth4,
    "f": formatUTCMicroseconds4,
    "g": formatUTCYearISO4,
    "G": formatUTCFullYearISO4,
    "H": formatUTCHour244,
    "I": formatUTCHour124,
    "j": formatUTCDayOfYear4,
    "L": formatUTCMilliseconds4,
    "m": formatUTCMonthNumber4,
    "M": formatUTCMinutes4,
    "p": formatUTCPeriod,
    "q": formatUTCQuarter,
    "Q": formatUnixTimestamp4,
    "s": formatUnixTimestampSeconds4,
    "S": formatUTCSeconds4,
    "u": formatUTCWeekdayNumberMonday4,
    "U": formatUTCWeekNumberSunday4,
    "V": formatUTCWeekNumberISO4,
    "w": formatUTCWeekdayNumberSunday4,
    "W": formatUTCWeekNumberMonday4,
    "x": null,
    "X": null,
    "y": formatUTCYear4,
    "Y": formatUTCFullYear4,
    "Z": formatUTCZone4,
    "%": formatLiteralPercent4
  };
  var parses = {
    "a": parseShortWeekday,
    "A": parseWeekday,
    "b": parseShortMonth,
    "B": parseMonth,
    "c": parseLocaleDateTime,
    "d": parseDayOfMonth4,
    "e": parseDayOfMonth4,
    "f": parseMicroseconds4,
    "g": parseYear4,
    "G": parseFullYear4,
    "H": parseHour244,
    "I": parseHour244,
    "j": parseDayOfYear4,
    "L": parseMilliseconds4,
    "m": parseMonthNumber4,
    "M": parseMinutes4,
    "p": parsePeriod,
    "q": parseQuarter4,
    "Q": parseUnixTimestamp4,
    "s": parseUnixTimestampSeconds4,
    "S": parseSeconds4,
    "u": parseWeekdayNumberMonday4,
    "U": parseWeekNumberSunday4,
    "V": parseWeekNumberISO4,
    "w": parseWeekdayNumberSunday4,
    "W": parseWeekNumberMonday4,
    "x": parseLocaleDate,
    "X": parseLocaleTime,
    "y": parseYear4,
    "Y": parseFullYear4,
    "Z": parseZone4,
    "%": parseLiteralPercent4
  };
  formats.x = newFormat(locale_date, formats);
  formats.X = newFormat(locale_time, formats);
  formats.c = newFormat(locale_dateTime, formats);
  utcFormats.x = newFormat(locale_date, utcFormats);
  utcFormats.X = newFormat(locale_time, utcFormats);
  utcFormats.c = newFormat(locale_dateTime, utcFormats);
  function newFormat(specifier, formats2) {
    return function(date2) {
      var string = [], i8 = -1, j5 = 0, n9 = specifier.length, c9, pad5, format2;
      if (!(date2 instanceof Date)) date2 = /* @__PURE__ */ new Date(+date2);
      while (++i8 < n9) {
        if (specifier.charCodeAt(i8) === 37) {
          string.push(specifier.slice(j5, i8));
          if ((pad5 = pads4[c9 = specifier.charAt(++i8)]) != null) c9 = specifier.charAt(++i8);
          else pad5 = c9 === "e" ? " " : "0";
          if (format2 = formats2[c9]) c9 = format2(date2, pad5);
          string.push(c9);
          j5 = i8 + 1;
        }
      }
      string.push(specifier.slice(j5, i8));
      return string.join("");
    };
  }
  function newParse(specifier, Z2) {
    return function(string) {
      var d4 = newDate4(1900, void 0, 1), i8 = parseSpecifier(d4, specifier, string += "", 0), week, day2;
      if (i8 != string.length) return null;
      if ("Q" in d4) return new Date(d4.Q);
      if ("s" in d4) return new Date(d4.s * 1e3 + ("L" in d4 ? d4.L : 0));
      if (Z2 && !("Z" in d4)) d4.Z = 0;
      if ("p" in d4) d4.H = d4.H % 12 + d4.p * 12;
      if (d4.m === void 0) d4.m = "q" in d4 ? d4.q : 0;
      if ("V" in d4) {
        if (d4.V < 1 || d4.V > 53) return null;
        if (!("w" in d4)) d4.w = 1;
        if ("Z" in d4) {
          week = utcDate4(newDate4(d4.y, 0, 1)), day2 = week.getUTCDay();
          week = day2 > 4 || day2 === 0 ? utcMonday2.ceil(week) : utcMonday2(week);
          week = utcDay_default.offset(week, (d4.V - 1) * 7);
          d4.y = week.getUTCFullYear();
          d4.m = week.getUTCMonth();
          d4.d = week.getUTCDate() + (d4.w + 6) % 7;
        } else {
          week = localDate4(newDate4(d4.y, 0, 1)), day2 = week.getDay();
          week = day2 > 4 || day2 === 0 ? monday.ceil(week) : monday(week);
          week = day_default.offset(week, (d4.V - 1) * 7);
          d4.y = week.getFullYear();
          d4.m = week.getMonth();
          d4.d = week.getDate() + (d4.w + 6) % 7;
        }
      } else if ("W" in d4 || "U" in d4) {
        if (!("w" in d4)) d4.w = "u" in d4 ? d4.u % 7 : "W" in d4 ? 1 : 0;
        day2 = "Z" in d4 ? utcDate4(newDate4(d4.y, 0, 1)).getUTCDay() : localDate4(newDate4(d4.y, 0, 1)).getDay();
        d4.m = 0;
        d4.d = "W" in d4 ? (d4.w + 6) % 7 + d4.W * 7 - (day2 + 5) % 7 : d4.w + d4.U * 7 - (day2 + 6) % 7;
      }
      if ("Z" in d4) {
        d4.H += d4.Z / 100 | 0;
        d4.M += d4.Z % 100;
        return utcDate4(d4);
      }
      return localDate4(d4);
    };
  }
  function parseSpecifier(d4, specifier, string, j5) {
    var i8 = 0, n9 = specifier.length, m4 = string.length, c9, parse;
    while (i8 < n9) {
      if (j5 >= m4) return -1;
      c9 = specifier.charCodeAt(i8++);
      if (c9 === 37) {
        c9 = specifier.charAt(i8++);
        parse = parses[c9 in pads4 ? specifier.charAt(i8++) : c9];
        if (!parse || (j5 = parse(d4, string, j5)) < 0) return -1;
      } else if (c9 != string.charCodeAt(j5++)) {
        return -1;
      }
    }
    return j5;
  }
  function parsePeriod(d4, string, i8) {
    var n9 = periodRe.exec(string.slice(i8));
    return n9 ? (d4.p = periodLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseShortWeekday(d4, string, i8) {
    var n9 = shortWeekdayRe.exec(string.slice(i8));
    return n9 ? (d4.w = shortWeekdayLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseWeekday(d4, string, i8) {
    var n9 = weekdayRe.exec(string.slice(i8));
    return n9 ? (d4.w = weekdayLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseShortMonth(d4, string, i8) {
    var n9 = shortMonthRe.exec(string.slice(i8));
    return n9 ? (d4.m = shortMonthLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseMonth(d4, string, i8) {
    var n9 = monthRe.exec(string.slice(i8));
    return n9 ? (d4.m = monthLookup.get(n9[0].toLowerCase()), i8 + n9[0].length) : -1;
  }
  function parseLocaleDateTime(d4, string, i8) {
    return parseSpecifier(d4, locale_dateTime, string, i8);
  }
  function parseLocaleDate(d4, string, i8) {
    return parseSpecifier(d4, locale_date, string, i8);
  }
  function parseLocaleTime(d4, string, i8) {
    return parseSpecifier(d4, locale_time, string, i8);
  }
  function formatShortWeekday(d4) {
    return locale_shortWeekdays[d4.getDay()];
  }
  function formatWeekday(d4) {
    return locale_weekdays[d4.getDay()];
  }
  function formatShortMonth(d4) {
    return locale_shortMonths[d4.getMonth()];
  }
  function formatMonth(d4) {
    return locale_months[d4.getMonth()];
  }
  function formatPeriod(d4) {
    return locale_periods[+(d4.getHours() >= 12)];
  }
  function formatQuarter(d4) {
    return 1 + ~~(d4.getMonth() / 3);
  }
  function formatUTCShortWeekday(d4) {
    return locale_shortWeekdays[d4.getUTCDay()];
  }
  function formatUTCWeekday(d4) {
    return locale_weekdays[d4.getUTCDay()];
  }
  function formatUTCShortMonth(d4) {
    return locale_shortMonths[d4.getUTCMonth()];
  }
  function formatUTCMonth(d4) {
    return locale_months[d4.getUTCMonth()];
  }
  function formatUTCPeriod(d4) {
    return locale_periods[+(d4.getUTCHours() >= 12)];
  }
  function formatUTCQuarter(d4) {
    return 1 + ~~(d4.getUTCMonth() / 3);
  }
  return {
    format: function(specifier) {
      var f2 = newFormat(specifier += "", formats);
      f2.toString = function() {
        return specifier;
      };
      return f2;
    },
    parse: function(specifier) {
      var p3 = newParse(specifier += "", false);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    },
    utcFormat: function(specifier) {
      var f2 = newFormat(specifier += "", utcFormats);
      f2.toString = function() {
        return specifier;
      };
      return f2;
    },
    utcParse: function(specifier) {
      var p3 = newParse(specifier += "", true);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    }
  };
}
var pads4 = { "-": "", "_": " ", "0": "0" };
var numberRe4 = /^\s*\d+/;
var percentRe4 = /^%/;
var requoteRe4 = /[\\^$*+?|[\]().{}]/g;
function pad4(value, fill, width) {
  var sign2 = value < 0 ? "-" : "", string = (sign2 ? -value : value) + "", length = string.length;
  return sign2 + (length < width ? new Array(width - length + 1).join(fill) + string : string);
}
function requote4(s5) {
  return s5.replace(requoteRe4, "\\$&");
}
function formatRe4(names) {
  return new RegExp("^(?:" + names.map(requote4).join("|") + ")", "i");
}
function formatLookup4(names) {
  return new Map(names.map((name, i8) => [name.toLowerCase(), i8]));
}
function parseWeekdayNumberSunday4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.w = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekdayNumberMonday4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.u = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberSunday4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.U = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberISO4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.V = +n9[0], i8 + n9[0].length) : -1;
}
function parseWeekNumberMonday4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.W = +n9[0], i8 + n9[0].length) : -1;
}
function parseFullYear4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 4));
  return n9 ? (d4.y = +n9[0], i8 + n9[0].length) : -1;
}
function parseYear4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.y = +n9[0] + (+n9[0] > 68 ? 1900 : 2e3), i8 + n9[0].length) : -1;
}
function parseZone4(d4, string, i8) {
  var n9 = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i8, i8 + 6));
  return n9 ? (d4.Z = n9[1] ? 0 : -(n9[2] + (n9[3] || "00")), i8 + n9[0].length) : -1;
}
function parseQuarter4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 1));
  return n9 ? (d4.q = n9[0] * 3 - 3, i8 + n9[0].length) : -1;
}
function parseMonthNumber4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.m = n9[0] - 1, i8 + n9[0].length) : -1;
}
function parseDayOfMonth4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.d = +n9[0], i8 + n9[0].length) : -1;
}
function parseDayOfYear4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 3));
  return n9 ? (d4.m = 0, d4.d = +n9[0], i8 + n9[0].length) : -1;
}
function parseHour244(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.H = +n9[0], i8 + n9[0].length) : -1;
}
function parseMinutes4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.M = +n9[0], i8 + n9[0].length) : -1;
}
function parseSeconds4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 2));
  return n9 ? (d4.S = +n9[0], i8 + n9[0].length) : -1;
}
function parseMilliseconds4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 3));
  return n9 ? (d4.L = +n9[0], i8 + n9[0].length) : -1;
}
function parseMicroseconds4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8, i8 + 6));
  return n9 ? (d4.L = Math.floor(n9[0] / 1e3), i8 + n9[0].length) : -1;
}
function parseLiteralPercent4(d4, string, i8) {
  var n9 = percentRe4.exec(string.slice(i8, i8 + 1));
  return n9 ? i8 + n9[0].length : -1;
}
function parseUnixTimestamp4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8));
  return n9 ? (d4.Q = +n9[0], i8 + n9[0].length) : -1;
}
function parseUnixTimestampSeconds4(d4, string, i8) {
  var n9 = numberRe4.exec(string.slice(i8));
  return n9 ? (d4.s = +n9[0], i8 + n9[0].length) : -1;
}
function formatDayOfMonth4(d4, p3) {
  return pad4(d4.getDate(), p3, 2);
}
function formatHour244(d4, p3) {
  return pad4(d4.getHours(), p3, 2);
}
function formatHour124(d4, p3) {
  return pad4(d4.getHours() % 12 || 12, p3, 2);
}
function formatDayOfYear4(d4, p3) {
  return pad4(1 + day_default.count(year_default(d4), d4), p3, 3);
}
function formatMilliseconds4(d4, p3) {
  return pad4(d4.getMilliseconds(), p3, 3);
}
function formatMicroseconds4(d4, p3) {
  return formatMilliseconds4(d4, p3) + "000";
}
function formatMonthNumber4(d4, p3) {
  return pad4(d4.getMonth() + 1, p3, 2);
}
function formatMinutes4(d4, p3) {
  return pad4(d4.getMinutes(), p3, 2);
}
function formatSeconds4(d4, p3) {
  return pad4(d4.getSeconds(), p3, 2);
}
function formatWeekdayNumberMonday4(d4) {
  var day2 = d4.getDay();
  return day2 === 0 ? 7 : day2;
}
function formatWeekNumberSunday4(d4, p3) {
  return pad4(sunday.count(year_default(d4) - 1, d4), p3, 2);
}
function dISO4(d4) {
  var day2 = d4.getDay();
  return day2 >= 4 || day2 === 0 ? thursday(d4) : thursday.ceil(d4);
}
function formatWeekNumberISO4(d4, p3) {
  d4 = dISO4(d4);
  return pad4(thursday.count(year_default(d4), d4) + (year_default(d4).getDay() === 4), p3, 2);
}
function formatWeekdayNumberSunday4(d4) {
  return d4.getDay();
}
function formatWeekNumberMonday4(d4, p3) {
  return pad4(monday.count(year_default(d4) - 1, d4), p3, 2);
}
function formatYear4(d4, p3) {
  return pad4(d4.getFullYear() % 100, p3, 2);
}
function formatYearISO4(d4, p3) {
  d4 = dISO4(d4);
  return pad4(d4.getFullYear() % 100, p3, 2);
}
function formatFullYear4(d4, p3) {
  return pad4(d4.getFullYear() % 1e4, p3, 4);
}
function formatFullYearISO4(d4, p3) {
  var day2 = d4.getDay();
  d4 = day2 >= 4 || day2 === 0 ? thursday(d4) : thursday.ceil(d4);
  return pad4(d4.getFullYear() % 1e4, p3, 4);
}
function formatZone4(d4) {
  var z5 = d4.getTimezoneOffset();
  return (z5 > 0 ? "-" : (z5 *= -1, "+")) + pad4(z5 / 60 | 0, "0", 2) + pad4(z5 % 60, "0", 2);
}
function formatUTCDayOfMonth4(d4, p3) {
  return pad4(d4.getUTCDate(), p3, 2);
}
function formatUTCHour244(d4, p3) {
  return pad4(d4.getUTCHours(), p3, 2);
}
function formatUTCHour124(d4, p3) {
  return pad4(d4.getUTCHours() % 12 || 12, p3, 2);
}
function formatUTCDayOfYear4(d4, p3) {
  return pad4(1 + utcDay_default.count(utcYear_default(d4), d4), p3, 3);
}
function formatUTCMilliseconds4(d4, p3) {
  return pad4(d4.getUTCMilliseconds(), p3, 3);
}
function formatUTCMicroseconds4(d4, p3) {
  return formatUTCMilliseconds4(d4, p3) + "000";
}
function formatUTCMonthNumber4(d4, p3) {
  return pad4(d4.getUTCMonth() + 1, p3, 2);
}
function formatUTCMinutes4(d4, p3) {
  return pad4(d4.getUTCMinutes(), p3, 2);
}
function formatUTCSeconds4(d4, p3) {
  return pad4(d4.getUTCSeconds(), p3, 2);
}
function formatUTCWeekdayNumberMonday4(d4) {
  var dow = d4.getUTCDay();
  return dow === 0 ? 7 : dow;
}
function formatUTCWeekNumberSunday4(d4, p3) {
  return pad4(utcSunday2.count(utcYear_default(d4) - 1, d4), p3, 2);
}
function UTCdISO4(d4) {
  var day2 = d4.getUTCDay();
  return day2 >= 4 || day2 === 0 ? utcThursday2(d4) : utcThursday2.ceil(d4);
}
function formatUTCWeekNumberISO4(d4, p3) {
  d4 = UTCdISO4(d4);
  return pad4(utcThursday2.count(utcYear_default(d4), d4) + (utcYear_default(d4).getUTCDay() === 4), p3, 2);
}
function formatUTCWeekdayNumberSunday4(d4) {
  return d4.getUTCDay();
}
function formatUTCWeekNumberMonday4(d4, p3) {
  return pad4(utcMonday2.count(utcYear_default(d4) - 1, d4), p3, 2);
}
function formatUTCYear4(d4, p3) {
  return pad4(d4.getUTCFullYear() % 100, p3, 2);
}
function formatUTCYearISO4(d4, p3) {
  d4 = UTCdISO4(d4);
  return pad4(d4.getUTCFullYear() % 100, p3, 2);
}
function formatUTCFullYear4(d4, p3) {
  return pad4(d4.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCFullYearISO4(d4, p3) {
  var day2 = d4.getUTCDay();
  d4 = day2 >= 4 || day2 === 0 ? utcThursday2(d4) : utcThursday2.ceil(d4);
  return pad4(d4.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCZone4() {
  return "+0000";
}
function formatLiteralPercent4() {
  return "%";
}
function formatUnixTimestamp4(d4) {
  return +d4;
}
function formatUnixTimestampSeconds4(d4) {
  return Math.floor(+d4 / 1e3);
}

// node_modules/@nivo/axes/node_modules/d3-time-format/src/defaultLocale.js
var locale5;
var timeFormat4;
var timeParse4;
var utcFormat4;
var utcParse4;
defaultLocale5({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});
function defaultLocale5(definition) {
  locale5 = formatLocale4(definition);
  timeFormat4 = locale5.format;
  timeParse4 = locale5.parse;
  utcFormat4 = locale5.utcFormat;
  utcParse4 = locale5.utcParse;
  return locale5;
}

// node_modules/@nivo/axes/node_modules/d3-time-format/src/isoFormat.js
var isoSpecifier4 = "%Y-%m-%dT%H:%M:%S.%LZ";
function formatIsoNative4(date2) {
  return date2.toISOString();
}
var formatIso4 = Date.prototype.toISOString ? formatIsoNative4 : utcFormat4(isoSpecifier4);

// node_modules/@nivo/axes/node_modules/d3-time-format/src/isoParse.js
function parseIsoNative4(string) {
  var date2 = new Date(string);
  return isNaN(date2) ? null : date2;
}
var parseIso4 = +/* @__PURE__ */ new Date("2000-01-01T00:00:00.000Z") ? parseIsoNative4 : utcParse4(isoSpecifier4);

// node_modules/@nivo/axes/dist/nivo-axes.mjs
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
function A3() {
  return A3 = Object.assign ? Object.assign.bind() : function(t9) {
    for (var e11 = 1; e11 < arguments.length; e11++) {
      var i8 = arguments[e11];
      for (var n9 in i8) ({}).hasOwnProperty.call(i8, n9) && (t9[n9] = i8[n9]);
    }
    return t9;
  }, A3.apply(null, arguments);
}
var T2 = function(t9) {
  var e11, i8 = t9.axis, n9 = t9.scale, r8 = t9.ticksPosition, o6 = t9.tickValues, l5 = t9.tickSize, c9 = t9.tickPadding, s5 = t9.tickRotation, f2 = t9.truncateTickAt, d4 = t9.engine, u8 = void 0 === d4 ? "svg" : d4, x4 = Cn(n9, o6), m4 = gt[u8], y5 = "bandwidth" in n9 ? Mn2(n9) : n9, g2 = { lineX: 0, lineY: 0 }, v3 = { textX: 0, textY: 0 }, k4 = "object" == typeof document && "rtl" === document.dir, b6 = m4.align.center, P5 = m4.baseline.center;
  "x" === i8 ? (e11 = function(t10) {
    var e12;
    return { x: null != (e12 = y5(t10)) ? e12 : 0, y: 0 };
  }, g2.lineY = l5 * ("after" === r8 ? 1 : -1), v3.textY = (l5 + c9) * ("after" === r8 ? 1 : -1), P5 = "after" === r8 ? m4.baseline.top : m4.baseline.bottom, 0 === s5 ? b6 = m4.align.center : "after" === r8 && s5 < 0 || "before" === r8 && s5 > 0 ? (b6 = m4.align[k4 ? "left" : "right"], P5 = m4.baseline.center) : ("after" === r8 && s5 > 0 || "before" === r8 && s5 < 0) && (b6 = m4.align[k4 ? "right" : "left"], P5 = m4.baseline.center)) : (e11 = function(t10) {
    var e12;
    return { x: 0, y: null != (e12 = y5(t10)) ? e12 : 0 };
  }, g2.lineX = l5 * ("after" === r8 ? 1 : -1), v3.textX = (l5 + c9) * ("after" === r8 ? 1 : -1), b6 = "after" === r8 ? m4.align.left : m4.align.right);
  return { ticks: x4.map((function(t10) {
    var i9 = "string" == typeof t10 ? (function(t11) {
      var e12 = String(t11).length;
      return f2 && f2 > 0 && e12 > f2 ? "" + String(t11).slice(0, f2).concat("...") : "" + t11;
    })(t10) : t10;
    return A3({ key: t10 instanceof Date ? "" + t10.valueOf() : "" + t10, value: i9 }, e11(t10), g2, v3);
  })), textAlign: b6, textBaseline: P5 };
};
var w3 = function(t9, e11) {
  if (void 0 === t9 || "function" == typeof t9) return t9;
  if ("time" === e11.type) {
    var i8 = timeFormat4(t9);
    return function(t10) {
      return i8(t10 instanceof Date ? t10 : new Date(t10));
    };
  }
  return format(t9);
};
var O3 = function(t9) {
  var e11, i8 = t9.width, n9 = t9.height, r8 = t9.scale, a5 = t9.axis, o6 = t9.values, l5 = (e11 = o6, Array.isArray(e11) ? o6 : void 0) || Cn(r8, o6), c9 = "bandwidth" in r8 ? Mn2(r8) : r8, s5 = "x" === a5 ? l5.map((function(t10) {
    var e12, i9;
    return { key: t10 instanceof Date ? "" + t10.valueOf() : "" + t10, x1: null != (e12 = c9(t10)) ? e12 : 0, x2: null != (i9 = c9(t10)) ? i9 : 0, y1: 0, y2: n9 };
  })) : l5.map((function(t10) {
    var e12, n10;
    return { key: t10 instanceof Date ? "" + t10.valueOf() : "" + t10, x1: 0, x2: i8, y1: null != (e12 = c9(t10)) ? e12 : 0, y2: null != (n10 = c9(t10)) ? n10 : 0 };
  }));
  return s5;
};
var X2 = (0, import_react20.memo)((function(t9) {
  var e11, n9 = t9.value, r8 = t9.format, a5 = t9.lineX, o6 = t9.lineY, l5 = t9.onClick, c9 = t9.textBaseline, s5 = t9.textAnchor, f2 = t9.theme, u8 = t9.animatedProps, x4 = null != (e11 = null == r8 ? void 0 : r8(n9)) ? e11 : n9, y5 = (0, import_react20.useMemo)((function() {
    var t10 = { opacity: u8.opacity };
    return l5 ? { style: A3({}, t10, { cursor: "pointer" }), onClick: function(t11) {
      return l5(t11, x4);
    } } : { style: t10 };
  }), [u8.opacity, l5, x4]);
  return (0, import_jsx_runtime5.jsxs)(animated.g, A3({ transform: u8.transform }, y5, { children: [(0, import_jsx_runtime5.jsx)("line", { x1: 0, x2: a5, y1: 0, y2: o6, style: f2.line }), (0, import_jsx_runtime5.jsx)(b3, { dominantBaseline: c9, textAnchor: s5, transform: u8.textTransform, style: f2.text, children: "" + x4 })] }));
}));
var Y2 = { tickSize: 5, tickPadding: 5, tickRotation: 0, legendPosition: "middle", legendOffset: 0 };
var B2 = function(e11) {
  var r8 = e11.axis, a5 = e11.scale, l5 = e11.x, f2 = void 0 === l5 ? 0 : l5, u8 = e11.y, x4 = void 0 === u8 ? 0 : u8, v3 = e11.length, k4 = e11.ticksPosition, h2 = e11.tickValues, p3 = e11.tickSize, O5 = void 0 === p3 ? Y2.tickSize : p3, B4 = e11.tickPadding, z5 = void 0 === B4 ? Y2.tickPadding : B4, R5 = e11.tickRotation, V3 = void 0 === R5 ? Y2.tickRotation : R5, C6 = e11.format, D5 = e11.renderTick, j5 = void 0 === D5 ? X2 : D5, E5 = e11.truncateTickAt, W6 = e11.legend, q3 = e11.legendPosition, H2 = void 0 === q3 ? Y2.legendPosition : q3, I2 = e11.legendOffset, F3 = void 0 === I2 ? Y2.legendOffset : I2, G = e11.style, J2 = e11.onClick, K2 = e11.ariaHidden, L4 = M(), M4 = w(L4.axis, G), N3 = (0, import_react20.useMemo)((function() {
    return w3(C6, a5);
  }), [C6, a5]), Q2 = T2({ axis: r8, scale: a5, ticksPosition: k4, tickValues: h2, tickSize: O5, tickPadding: z5, tickRotation: V3, truncateTickAt: E5 }), U2 = Q2.ticks, Z2 = Q2.textAlign, $ = Q2.textBaseline, _3 = null;
  if (void 0 !== W6) {
    var tt2, et = 0, it2 = 0, nt = 0;
    "y" === r8 ? (nt = -90, et = F3, "start" === H2 ? (tt2 = "start", it2 = v3) : "middle" === H2 ? (tt2 = "middle", it2 = v3 / 2) : "end" === H2 && (tt2 = "end")) : (it2 = F3, "start" === H2 ? tt2 = "start" : "middle" === H2 ? (tt2 = "middle", et = v3 / 2) : "end" === H2 && (tt2 = "end", et = v3)), _3 = (0, import_jsx_runtime5.jsx)(import_jsx_runtime5.Fragment, { children: (0, import_jsx_runtime5.jsx)(b3, { transform: "translate(" + et + ", " + it2 + ") rotate(" + nt + ")", textAnchor: tt2, style: A3({}, M4.legend.text, { dominantBaseline: "central" }), children: W6 }) });
  }
  var rt2 = Dr(), at2 = rt2.animate, ot2 = rt2.config, lt = useSpring({ transform: "translate(" + f2 + "," + x4 + ")", lineX2: "x" === r8 ? v3 : 0, lineY2: "x" === r8 ? 0 : v3, config: ot2, immediate: !at2 }), ct2 = (0, import_react20.useCallback)((function(t9) {
    return { opacity: 1, transform: "translate(" + t9.x + "," + t9.y + ")", textTransform: "translate(" + t9.textX + "," + t9.textY + ") rotate(" + V3 + ")" };
  }), [V3]), st = (0, import_react20.useCallback)((function(t9) {
    return { opacity: 0, transform: "translate(" + t9.x + "," + t9.y + ")", textTransform: "translate(" + t9.textX + "," + t9.textY + ") rotate(" + V3 + ")" };
  }), [V3]), ft2 = useTransition(U2, { keys: function(t9) {
    return t9.key;
  }, initial: ct2, from: st, enter: ct2, update: ct2, leave: { opacity: 0 }, config: ot2, immediate: !at2 });
  return (0, import_jsx_runtime5.jsxs)(animated.g, { transform: lt.transform, "aria-hidden": K2, children: [ft2((function(e12, i8, n9, r9) {
    return t7.createElement(j5, A3({ tickIndex: r9, format: N3, rotate: V3, textBaseline: $, textAnchor: Z2, truncateTickAt: E5, animatedProps: e12, theme: M4.ticks }, i8, J2 ? { onClick: J2 } : {}));
  })), (0, import_jsx_runtime5.jsx)(animated.line, { style: M4.domain.line, x1: 0, x2: lt.lineX2, y1: 0, y2: lt.lineY2 }), _3] });
};
var z4 = (0, import_react20.memo)(B2);
var R3 = ["top", "right", "bottom", "left"];
var V2 = (0, import_react20.memo)((function(t9) {
  var e11 = t9.xScale, i8 = t9.yScale, n9 = t9.width, r8 = t9.height, a5 = { top: t9.top, right: t9.right, bottom: t9.bottom, left: t9.left };
  return (0, import_jsx_runtime5.jsx)(import_jsx_runtime5.Fragment, { children: R3.map((function(t10) {
    var o6 = a5[t10];
    if (!o6) return null;
    var l5 = "top" === t10 || "bottom" === t10;
    return (0, import_jsx_runtime5.jsx)(z4, A3({}, o6, { axis: l5 ? "x" : "y", x: "right" === t10 ? n9 : 0, y: "bottom" === t10 ? r8 : 0, scale: l5 ? e11 : i8, length: l5 ? n9 : r8, ticksPosition: "top" === t10 || "left" === t10 ? "before" : "after", truncateTickAt: o6.truncateTickAt }), t10);
  })) });
}));
var C4 = (0, import_react20.memo)((function(t9) {
  var e11 = t9.animatedProps, i8 = M();
  return (0, import_jsx_runtime5.jsx)(animated.line, A3({}, e11, i8.grid.line));
}));
var D2 = (0, import_react20.memo)((function(t9) {
  var e11 = t9.lines, i8 = Dr(), n9 = i8.animate, a5 = i8.config, l5 = useTransition(e11, { keys: function(t10) {
    return t10.key;
  }, initial: function(t10) {
    return { opacity: 1, x1: t10.x1, x2: t10.x2, y1: t10.y1, y2: t10.y2 };
  }, from: function(t10) {
    return { opacity: 0, x1: t10.x1, x2: t10.x2, y1: t10.y1, y2: t10.y2 };
  }, enter: function(t10) {
    return { opacity: 1, x1: t10.x1, x2: t10.x2, y1: t10.y1, y2: t10.y2 };
  }, update: function(t10) {
    return { opacity: 1, x1: t10.x1, x2: t10.x2, y1: t10.y1, y2: t10.y2 };
  }, leave: { opacity: 0 }, config: a5, immediate: !n9 });
  return (0, import_jsx_runtime5.jsx)("g", { children: l5((function(t10, e12) {
    return (0, import_react20.createElement)(C4, A3({}, e12, { key: e12.key, animatedProps: t10 }));
  })) });
}));
var j3 = (0, import_react20.memo)((function(t9) {
  var e11 = t9.width, n9 = t9.height, r8 = t9.xScale, a5 = t9.yScale, o6 = t9.xValues, l5 = t9.yValues, c9 = (0, import_react20.useMemo)((function() {
    return !!r8 && O3({ width: e11, height: n9, scale: r8, axis: "x", values: o6 });
  }), [r8, o6, e11, n9]), s5 = (0, import_react20.useMemo)((function() {
    return !!a5 && O3({ width: e11, height: n9, scale: a5, axis: "y", values: l5 });
  }), [n9, e11, a5, l5]);
  return (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [c9 && (0, import_jsx_runtime5.jsx)(D2, { lines: c9 }), s5 && (0, import_jsx_runtime5.jsx)(D2, { lines: s5 })] });
}));
var E3 = function(t9, e11) {
  var i8, n9, r8 = e11.axis, a5 = e11.scale, o6 = e11.x, c9 = void 0 === o6 ? 0 : o6, s5 = e11.y, d4 = void 0 === s5 ? 0 : s5, m4 = e11.length, y5 = e11.ticksPosition, g2 = e11.tickValues, v3 = e11.tickSize, k4 = void 0 === v3 ? Y2.tickSize : v3, h2 = e11.tickPadding, p3 = void 0 === h2 ? Y2.tickPadding : h2, b6 = e11.tickRotation, P5 = void 0 === b6 ? Y2.tickRotation : b6, S4 = e11.format, A5 = e11.legend, w5 = e11.legendPosition, O5 = void 0 === w5 ? Y2.legendPosition : w5, X3 = e11.legendOffset, B4 = void 0 === X3 ? Y2.legendOffset : X3, z5 = e11.theme, R5 = e11.style, V3 = T2({ axis: r8, scale: a5, ticksPosition: y5, tickValues: g2, tickSize: k4, tickPadding: p3, tickRotation: P5, engine: "canvas" }), C6 = V3.ticks, D5 = V3.textAlign, j5 = V3.textBaseline;
  t9.save(), t9.translate(c9, d4);
  var E5 = R(z5.axis, R5);
  t9.textAlign = D5, t9.textBaseline = j5, a4(t9, E5.ticks.text);
  var W6 = null != (i8 = E5.domain.line.strokeWidth) ? i8 : 0;
  "string" != typeof W6 && W6 > 0 && (t9.lineWidth = W6, t9.lineCap = "square", E5.domain.line.stroke && (t9.strokeStyle = E5.domain.line.stroke), t9.beginPath(), t9.moveTo(0, 0), t9.lineTo("x" === r8 ? m4 : 0, "x" === r8 ? 0 : m4), t9.stroke());
  var q3 = "function" == typeof S4 ? S4 : function(t10) {
    return "" + t10;
  }, H2 = null != (n9 = E5.ticks.line.strokeWidth) ? n9 : 0, I2 = "string" != typeof H2 && H2 > 0;
  if (C6.forEach((function(e12) {
    I2 && (t9.lineWidth = H2, t9.lineCap = "square", E5.ticks.line.stroke && (t9.strokeStyle = E5.ticks.line.stroke), t9.beginPath(), t9.moveTo(e12.x, e12.y), t9.lineTo(e12.x + e12.lineX, e12.y + e12.lineY), t9.stroke());
    var i9 = q3(e12.value);
    t9.save(), t9.translate(e12.x + e12.textX, e12.y + e12.textY), t9.rotate(ut(P5)), d3(t9, E5.ticks.text, "" + i9), t9.fillText("" + i9, 0, 0), t9.restore();
  })), void 0 !== A5) {
    var F3 = 0, G = 0, J2 = 0, K2 = "center";
    "y" === r8 ? (J2 = -90, F3 = B4, "start" === O5 ? (K2 = "start", G = m4) : "middle" === O5 ? (K2 = "center", G = m4 / 2) : "end" === O5 && (K2 = "end")) : (G = B4, "start" === O5 ? K2 = "start" : "middle" === O5 ? (K2 = "center", F3 = m4 / 2) : "end" === O5 && (K2 = "end", F3 = m4)), t9.translate(F3, G), t9.rotate(ut(J2)), a4(t9, E5.legend.text), E5.legend.text.fill && (t9.fillStyle = E5.legend.text.fill), t9.textAlign = K2, t9.textBaseline = "middle", d3(t9, E5.legend.text, A5);
  }
  t9.restore();
};
var W4 = function(t9, e11) {
  var i8 = e11.xScale, n9 = e11.yScale, r8 = e11.width, a5 = e11.height, o6 = e11.top, l5 = e11.right, c9 = e11.bottom, s5 = e11.left, f2 = e11.theme, d4 = { top: o6, right: l5, bottom: c9, left: s5 };
  R3.forEach((function(e12) {
    var o7 = d4[e12];
    if (!o7) return null;
    var l6 = "top" === e12 || "bottom" === e12, c10 = "top" === e12 || "left" === e12 ? "before" : "after", s6 = l6 ? i8 : n9, u8 = w3(o7.format, s6);
    E3(t9, A3({}, o7, { axis: l6 ? "x" : "y", x: "right" === e12 ? r8 : 0, y: "bottom" === e12 ? a5 : 0, scale: s6, format: u8, length: l6 ? r8 : a5, ticksPosition: c10, theme: f2 }));
  }));
};
var q = function(t9, e11) {
  var i8 = e11.width, n9 = e11.height, r8 = e11.scale, a5 = e11.axis, o6 = e11.values;
  O3({ width: i8, height: n9, scale: r8, axis: a5, values: o6 }).forEach((function(e12) {
    t9.beginPath(), t9.moveTo(e12.x1, e12.y1), t9.lineTo(e12.x2, e12.y2), t9.stroke();
  }));
};

// node_modules/@nivo/colors/dist/nivo-colors.mjs
var import_react21 = __toESM(require_react(), 1);
var import_get3 = __toESM(require_get(), 1);
var import_isPlainObject2 = __toESM(require_isPlainObject(), 1);
function Se(e11, r8) {
  (null == r8 || r8 > e11.length) && (r8 = e11.length);
  for (var n9 = 0, t9 = Array(r8); n9 < r8; n9++) t9[n9] = e11[n9];
  return t9;
}
function qe(e11, r8) {
  var n9 = "undefined" != typeof Symbol && e11[Symbol.iterator] || e11["@@iterator"];
  if (n9) return (n9 = n9.call(e11)).next.bind(n9);
  if (Array.isArray(e11) || (n9 = (function(e12, r9) {
    if (e12) {
      if ("string" == typeof e12) return Se(e12, r9);
      var n10 = {}.toString.call(e12).slice(8, -1);
      return "Object" === n10 && e12.constructor && (n10 = e12.constructor.name), "Map" === n10 || "Set" === n10 ? Array.from(e12) : "Arguments" === n10 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n10) ? Se(e12, r9) : void 0;
    }
  })(e11)) || r8 && e11 && "number" == typeof e11.length) {
    n9 && (e11 = n9);
    var t9 = 0;
    return function() {
      return t9 >= e11.length ? { done: true } : { done: false, value: e11[t9++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function Ce() {
  return Ce = Object.assign ? Object.assign.bind() : function(e11) {
    for (var r8 = 1; r8 < arguments.length; r8++) {
      var n9 = arguments[r8];
      for (var t9 in n9) ({}).hasOwnProperty.call(n9, t9) && (e11[t9] = n9[t9]);
    }
    return e11;
  }, Ce.apply(null, arguments);
}
var Ge = { nivo: ["#e8c1a0", "#f47560", "#f1e15b", "#e8a838", "#61cdbb", "#97e3d5"], category10: category10_default, accent: Accent_default, dark2: Dark2_default, paired: Paired_default, pastel1: Pastel1_default, pastel2: Pastel2_default, set1: Set1_default, set2: Set2_default, set3: Set3_default, tableau10: Tableau10_default };
var Ve = Object.keys(Ge);
var Pe = { brown_blueGreen: scheme, purpleRed_green: scheme2, pink_yellowGreen: scheme3, purple_orange: scheme4, red_blue: scheme5, red_grey: scheme6, red_yellow_blue: scheme7, red_yellow_green: scheme8, spectral: scheme9 };
var Re = Object.keys(Pe);
var Ue = { brown_blueGreen: BrBG_default, purpleRed_green: PRGn_default, pink_yellowGreen: PiYG_default, purple_orange: PuOr_default, red_blue: RdBu_default, red_grey: RdGy_default, red_yellow_blue: RdYlBu_default, red_yellow_green: RdYlGn_default, spectral: Spectral_default };
var De = { blues: scheme22, greens: scheme23, greys: scheme24, oranges: scheme27, purples: scheme25, reds: scheme26, blue_green: scheme10, blue_purple: scheme11, green_blue: scheme12, orange_red: scheme13, purple_blue_green: scheme14, purple_blue: scheme15, purple_red: scheme16, red_purple: scheme17, yellow_green_blue: scheme18, yellow_green: scheme19, yellow_orange_brown: scheme20, yellow_orange_red: scheme21 };
var Me = Object.keys(De);
var Te = { blues: Blues_default, greens: Greens_default, greys: Greys_default, oranges: Oranges_default, purples: Purples_default, reds: Reds_default, turbo: turbo_default, viridis: viridis_default, inferno, magma, plasma, cividis: cividis_default, warm, cool, cubehelixDefault: cubehelix_default2, blue_green: BuGn_default, blue_purple: BuPu_default, green_blue: GnBu_default, orange_red: OrRd_default, purple_blue_green: PuBuGn_default, purple_blue: PuBu_default, purple_red: PuRd_default, red_purple: RdPu_default, yellow_green_blue: YlGnBu_default, yellow_green: YlGn_default, yellow_orange_brown: YlOrBr_default, yellow_orange_red: YlOrRd_default };
var $e = Ce({}, Ge, Pe, De);
var Be = Object.keys($e);
var Fe = function(e11) {
  return Ve.includes(e11);
};
var He = function(e11) {
  return Re.includes(e11);
};
var Je = function(e11) {
  return Me.includes(e11);
};
var Ke = { rainbow: rainbow_default, sinebow: sinebow_default };
var Le = Ce({}, Ue, Te, Ke);
var Ne = Object.keys(Le);
var Qe = function(e11) {
  return void 0 !== e11.theme;
};
var We = function(e11) {
  return void 0 !== e11.from;
};
var Xe = function(e11, r8) {
  if ("function" == typeof e11) return e11;
  if ((0, import_isPlainObject2.default)(e11)) {
    if (Qe(e11)) {
      if (void 0 === r8) throw new Error("Unable to use color from theme as no theme was provided");
      var n9 = (0, import_get3.default)(r8, e11.theme);
      if (void 0 === n9) throw new Error("Color from theme is undefined at path: '" + e11.theme + "'");
      return function() {
        return n9;
      };
    }
    if (We(e11)) {
      var t9 = function(r9) {
        return (0, import_get3.default)(r9, e11.from);
      };
      if (Array.isArray(e11.modifiers)) {
        for (var o6, i8 = [], u8 = function() {
          var e12 = o6.value, r9 = e12[0], n10 = e12[1];
          if ("brighter" === r9) i8.push((function(e13) {
            return e13.brighter(n10);
          }));
          else if ("darker" === r9) i8.push((function(e13) {
            return e13.darker(n10);
          }));
          else {
            if ("opacity" !== r9) throw new Error("Invalid color modifier: '" + r9 + "', must be one of: 'brighter', 'darker', 'opacity'");
            i8.push((function(e13) {
              return e13.opacity = n10, e13;
            }));
          }
        }, l5 = qe(e11.modifiers); !(o6 = l5()).done; ) u8();
        return 0 === i8.length ? t9 : function(e12) {
          return i8.reduce((function(e13, r9) {
            return r9(e13);
          }), rgb2(t9(e12))).toString();
        };
      }
      return t9;
    }
    throw new Error("Invalid color spec, you should either specify 'theme' or 'from' when using a config object");
  }
  return function() {
    return e11;
  };
};
var Ye = function(e11, r8) {
  return (0, import_react21.useMemo)((function() {
    return Xe(e11, r8);
  }), [e11, r8]);
};
var gr = function(e11, r8) {
  if ("function" == typeof e11) return e11;
  var n9 = "function" == typeof r8 ? r8 : function(e12) {
    return (0, import_get3.default)(e12, r8);
  };
  if (Array.isArray(e11)) {
    var t9 = ordinal(e11), o6 = function(e12) {
      return t9(n9(e12));
    };
    return o6.scale = t9, o6;
  }
  if ((0, import_isPlainObject2.default)(e11)) {
    if ((function(e12) {
      return void 0 !== e12.datum;
    })(e11)) return function(r9) {
      return (0, import_get3.default)(r9, e11.datum);
    };
    if ((function(e12) {
      return void 0 !== e12.scheme;
    })(e11)) {
      if (Fe(e11.scheme)) {
        var i8 = ordinal($e[e11.scheme]), u8 = function(e12) {
          return i8(n9(e12));
        };
        return u8.scale = i8, u8;
      }
      if (He(e11.scheme)) {
        if (void 0 !== e11.size && (e11.size < 3 || e11.size > 11)) throw new Error("Invalid size '" + e11.size + "' for diverging color scheme '" + e11.scheme + "', must be between 3~11");
        var l5 = ordinal($e[e11.scheme][e11.size || 11]), a5 = function(e12) {
          return l5(n9(e12));
        };
        return a5.scale = l5, a5;
      }
      if (Je(e11.scheme)) {
        if (void 0 !== e11.size && (e11.size < 3 || e11.size > 9)) throw new Error("Invalid size '" + e11.size + "' for sequential color scheme '" + e11.scheme + "', must be between 3~9");
        var c9 = ordinal($e[e11.scheme][e11.size || 9]), s5 = function(e12) {
          return c9(n9(e12));
        };
        return s5.scale = c9, s5;
      }
    }
    throw new Error("Invalid colors, when using an object, you should either pass a 'datum' or a 'scheme' property");
  }
  return function() {
    return e11;
  };
};
var hr = function(e11, r8) {
  return (0, import_react21.useMemo)((function() {
    return gr(e11, r8);
  }), [e11, r8]);
};

// node_modules/@nivo/voronoi/dist/nivo-voronoi.mjs
var import_react22 = __toESM(require_react(), 1);

// node_modules/robust-predicates/esm/util.js
var epsilon4 = 11102230246251565e-32;
var splitter = 134217729;
var resulterrbound = (3 + 8 * epsilon4) * epsilon4;
function sum3(elen, e11, flen, f2, h2) {
  let Q2, Qnew, hh, bvirt;
  let enow = e11[0];
  let fnow = f2[0];
  let eindex = 0;
  let findex = 0;
  if (fnow > enow === fnow > -enow) {
    Q2 = enow;
    enow = e11[++eindex];
  } else {
    Q2 = fnow;
    fnow = f2[++findex];
  }
  let hindex = 0;
  if (eindex < elen && findex < flen) {
    if (fnow > enow === fnow > -enow) {
      Qnew = enow + Q2;
      hh = Q2 - (Qnew - enow);
      enow = e11[++eindex];
    } else {
      Qnew = fnow + Q2;
      hh = Q2 - (Qnew - fnow);
      fnow = f2[++findex];
    }
    Q2 = Qnew;
    if (hh !== 0) {
      h2[hindex++] = hh;
    }
    while (eindex < elen && findex < flen) {
      if (fnow > enow === fnow > -enow) {
        Qnew = Q2 + enow;
        bvirt = Qnew - Q2;
        hh = Q2 - (Qnew - bvirt) + (enow - bvirt);
        enow = e11[++eindex];
      } else {
        Qnew = Q2 + fnow;
        bvirt = Qnew - Q2;
        hh = Q2 - (Qnew - bvirt) + (fnow - bvirt);
        fnow = f2[++findex];
      }
      Q2 = Qnew;
      if (hh !== 0) {
        h2[hindex++] = hh;
      }
    }
  }
  while (eindex < elen) {
    Qnew = Q2 + enow;
    bvirt = Qnew - Q2;
    hh = Q2 - (Qnew - bvirt) + (enow - bvirt);
    enow = e11[++eindex];
    Q2 = Qnew;
    if (hh !== 0) {
      h2[hindex++] = hh;
    }
  }
  while (findex < flen) {
    Qnew = Q2 + fnow;
    bvirt = Qnew - Q2;
    hh = Q2 - (Qnew - bvirt) + (fnow - bvirt);
    fnow = f2[++findex];
    Q2 = Qnew;
    if (hh !== 0) {
      h2[hindex++] = hh;
    }
  }
  if (Q2 !== 0 || hindex === 0) {
    h2[hindex++] = Q2;
  }
  return hindex;
}
function estimate(elen, e11) {
  let Q2 = e11[0];
  for (let i8 = 1; i8 < elen; i8++) Q2 += e11[i8];
  return Q2;
}
function vec(n9) {
  return new Float64Array(n9);
}

// node_modules/robust-predicates/esm/orient2d.js
var ccwerrboundA = (3 + 16 * epsilon4) * epsilon4;
var ccwerrboundB = (2 + 12 * epsilon4) * epsilon4;
var ccwerrboundC = (9 + 64 * epsilon4) * epsilon4 * epsilon4;
var B3 = vec(4);
var C1 = vec(8);
var C22 = vec(12);
var D3 = vec(16);
var u4 = vec(4);
function orient2dadapt(ax, ay, bx, by, cx, cy, detsum) {
  let acxtail, acytail, bcxtail, bcytail;
  let bvirt, c9, ahi, alo, bhi, blo, _i, _j, _0, s1, s0, t14, t04, u32;
  const acx = ax - cx;
  const bcx = bx - cx;
  const acy = ay - cy;
  const bcy = by - cy;
  s1 = acx * bcy;
  c9 = splitter * acx;
  ahi = c9 - (c9 - acx);
  alo = acx - ahi;
  c9 = splitter * bcy;
  bhi = c9 - (c9 - bcy);
  blo = bcy - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t14 = acy * bcx;
  c9 = splitter * acy;
  ahi = c9 - (c9 - acy);
  alo = acy - ahi;
  c9 = splitter * bcx;
  bhi = c9 - (c9 - bcx);
  blo = bcx - bhi;
  t04 = alo * blo - (t14 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t04;
  bvirt = s0 - _i;
  B3[0] = s0 - (_i + bvirt) + (bvirt - t04);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t14;
  bvirt = _0 - _i;
  B3[1] = _0 - (_i + bvirt) + (bvirt - t14);
  u32 = _j + _i;
  bvirt = u32 - _j;
  B3[2] = _j - (u32 - bvirt) + (_i - bvirt);
  B3[3] = u32;
  let det = estimate(4, B3);
  let errbound = ccwerrboundB * detsum;
  if (det >= errbound || -det >= errbound) {
    return det;
  }
  bvirt = ax - acx;
  acxtail = ax - (acx + bvirt) + (bvirt - cx);
  bvirt = bx - bcx;
  bcxtail = bx - (bcx + bvirt) + (bvirt - cx);
  bvirt = ay - acy;
  acytail = ay - (acy + bvirt) + (bvirt - cy);
  bvirt = by - bcy;
  bcytail = by - (bcy + bvirt) + (bvirt - cy);
  if (acxtail === 0 && acytail === 0 && bcxtail === 0 && bcytail === 0) {
    return det;
  }
  errbound = ccwerrboundC * detsum + resulterrbound * Math.abs(det);
  det += acx * bcytail + bcy * acxtail - (acy * bcxtail + bcx * acytail);
  if (det >= errbound || -det >= errbound) return det;
  s1 = acxtail * bcy;
  c9 = splitter * acxtail;
  ahi = c9 - (c9 - acxtail);
  alo = acxtail - ahi;
  c9 = splitter * bcy;
  bhi = c9 - (c9 - bcy);
  blo = bcy - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t14 = acytail * bcx;
  c9 = splitter * acytail;
  ahi = c9 - (c9 - acytail);
  alo = acytail - ahi;
  c9 = splitter * bcx;
  bhi = c9 - (c9 - bcx);
  blo = bcx - bhi;
  t04 = alo * blo - (t14 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t04;
  bvirt = s0 - _i;
  u4[0] = s0 - (_i + bvirt) + (bvirt - t04);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t14;
  bvirt = _0 - _i;
  u4[1] = _0 - (_i + bvirt) + (bvirt - t14);
  u32 = _j + _i;
  bvirt = u32 - _j;
  u4[2] = _j - (u32 - bvirt) + (_i - bvirt);
  u4[3] = u32;
  const C1len = sum3(4, B3, 4, u4, C1);
  s1 = acx * bcytail;
  c9 = splitter * acx;
  ahi = c9 - (c9 - acx);
  alo = acx - ahi;
  c9 = splitter * bcytail;
  bhi = c9 - (c9 - bcytail);
  blo = bcytail - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t14 = acy * bcxtail;
  c9 = splitter * acy;
  ahi = c9 - (c9 - acy);
  alo = acy - ahi;
  c9 = splitter * bcxtail;
  bhi = c9 - (c9 - bcxtail);
  blo = bcxtail - bhi;
  t04 = alo * blo - (t14 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t04;
  bvirt = s0 - _i;
  u4[0] = s0 - (_i + bvirt) + (bvirt - t04);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t14;
  bvirt = _0 - _i;
  u4[1] = _0 - (_i + bvirt) + (bvirt - t14);
  u32 = _j + _i;
  bvirt = u32 - _j;
  u4[2] = _j - (u32 - bvirt) + (_i - bvirt);
  u4[3] = u32;
  const C2len = sum3(C1len, C1, 4, u4, C22);
  s1 = acxtail * bcytail;
  c9 = splitter * acxtail;
  ahi = c9 - (c9 - acxtail);
  alo = acxtail - ahi;
  c9 = splitter * bcytail;
  bhi = c9 - (c9 - bcytail);
  blo = bcytail - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t14 = acytail * bcxtail;
  c9 = splitter * acytail;
  ahi = c9 - (c9 - acytail);
  alo = acytail - ahi;
  c9 = splitter * bcxtail;
  bhi = c9 - (c9 - bcxtail);
  blo = bcxtail - bhi;
  t04 = alo * blo - (t14 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t04;
  bvirt = s0 - _i;
  u4[0] = s0 - (_i + bvirt) + (bvirt - t04);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t14;
  bvirt = _0 - _i;
  u4[1] = _0 - (_i + bvirt) + (bvirt - t14);
  u32 = _j + _i;
  bvirt = u32 - _j;
  u4[2] = _j - (u32 - bvirt) + (_i - bvirt);
  u4[3] = u32;
  const Dlen = sum3(C2len, C22, 4, u4, D3);
  return D3[Dlen - 1];
}
function orient2d(ax, ay, bx, by, cx, cy) {
  const detleft = (ay - cy) * (bx - cx);
  const detright = (ax - cx) * (by - cy);
  const det = detleft - detright;
  const detsum = Math.abs(detleft + detright);
  if (Math.abs(det) >= ccwerrboundA * detsum) return det;
  return -orient2dadapt(ax, ay, bx, by, cx, cy, detsum);
}

// node_modules/robust-predicates/esm/orient3d.js
var o3derrboundA = (7 + 56 * epsilon4) * epsilon4;
var o3derrboundB = (3 + 28 * epsilon4) * epsilon4;
var o3derrboundC = (26 + 288 * epsilon4) * epsilon4 * epsilon4;
var bc = vec(4);
var ca = vec(4);
var ab = vec(4);
var at_b = vec(4);
var at_c = vec(4);
var bt_c = vec(4);
var bt_a = vec(4);
var ct_a = vec(4);
var ct_b = vec(4);
var bct = vec(8);
var cat = vec(8);
var abt = vec(8);
var u5 = vec(4);
var _8 = vec(8);
var _8b = vec(8);
var _16 = vec(8);
var _12 = vec(12);
var fin = vec(192);
var fin2 = vec(192);

// node_modules/robust-predicates/esm/incircle.js
var iccerrboundA = (10 + 96 * epsilon4) * epsilon4;
var iccerrboundB = (4 + 48 * epsilon4) * epsilon4;
var iccerrboundC = (44 + 576 * epsilon4) * epsilon4 * epsilon4;
var bc2 = vec(4);
var ca2 = vec(4);
var ab2 = vec(4);
var aa = vec(4);
var bb = vec(4);
var cc = vec(4);
var u6 = vec(4);
var v2 = vec(4);
var axtbc = vec(8);
var aytbc = vec(8);
var bxtca = vec(8);
var bytca = vec(8);
var cxtab = vec(8);
var cytab = vec(8);
var abt2 = vec(8);
var bct2 = vec(8);
var cat2 = vec(8);
var abtt = vec(4);
var bctt = vec(4);
var catt = vec(4);
var _82 = vec(8);
var _162 = vec(16);
var _16b = vec(16);
var _16c = vec(16);
var _32 = vec(32);
var _32b = vec(32);
var _48 = vec(48);
var _64 = vec(64);
var fin3 = vec(1152);
var fin22 = vec(1152);

// node_modules/robust-predicates/esm/insphere.js
var isperrboundA = (16 + 224 * epsilon4) * epsilon4;
var isperrboundB = (5 + 72 * epsilon4) * epsilon4;
var isperrboundC = (71 + 1408 * epsilon4) * epsilon4 * epsilon4;
var ab3 = vec(4);
var bc3 = vec(4);
var cd = vec(4);
var de = vec(4);
var ea = vec(4);
var ac = vec(4);
var bd = vec(4);
var ce = vec(4);
var da = vec(4);
var eb = vec(4);
var abc = vec(24);
var bcd = vec(24);
var cde = vec(24);
var dea = vec(24);
var eab = vec(24);
var abd = vec(24);
var bce = vec(24);
var cda = vec(24);
var deb = vec(24);
var eac = vec(24);
var adet = vec(1152);
var bdet = vec(1152);
var cdet = vec(1152);
var ddet = vec(1152);
var edet = vec(1152);
var abdet = vec(2304);
var cddet = vec(2304);
var cdedet = vec(3456);
var deter = vec(5760);
var _83 = vec(8);
var _8b2 = vec(8);
var _8c = vec(8);
var _163 = vec(16);
var _24 = vec(24);
var _482 = vec(48);
var _48b = vec(48);
var _96 = vec(96);
var _192 = vec(192);
var _384x = vec(384);
var _384y = vec(384);
var _384z = vec(384);
var _768 = vec(768);
var xdet = vec(96);
var ydet = vec(96);
var zdet = vec(96);
var fin4 = vec(1152);

// node_modules/delaunator/index.js
var EPSILON = Math.pow(2, -52);
var EDGE_STACK = new Uint32Array(512);
var Delaunator = class _Delaunator {
  static from(points, getX = defaultGetX, getY = defaultGetY) {
    const n9 = points.length;
    const coords = new Float64Array(n9 * 2);
    for (let i8 = 0; i8 < n9; i8++) {
      const p3 = points[i8];
      coords[2 * i8] = getX(p3);
      coords[2 * i8 + 1] = getY(p3);
    }
    return new _Delaunator(coords);
  }
  constructor(coords) {
    const n9 = coords.length >> 1;
    if (n9 > 0 && typeof coords[0] !== "number") throw new Error("Expected coords to contain numbers.");
    this.coords = coords;
    const maxTriangles = Math.max(2 * n9 - 5, 0);
    this._triangles = new Uint32Array(maxTriangles * 3);
    this._halfedges = new Int32Array(maxTriangles * 3);
    this._hashSize = Math.ceil(Math.sqrt(n9));
    this._hullPrev = new Uint32Array(n9);
    this._hullNext = new Uint32Array(n9);
    this._hullTri = new Uint32Array(n9);
    this._hullHash = new Int32Array(this._hashSize);
    this._ids = new Uint32Array(n9);
    this._dists = new Float64Array(n9);
    this.update();
  }
  update() {
    const { coords, _hullPrev: hullPrev, _hullNext: hullNext, _hullTri: hullTri, _hullHash: hullHash } = this;
    const n9 = coords.length >> 1;
    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;
    for (let i8 = 0; i8 < n9; i8++) {
      const x4 = coords[2 * i8];
      const y5 = coords[2 * i8 + 1];
      if (x4 < minX) minX = x4;
      if (y5 < minY) minY = y5;
      if (x4 > maxX) maxX = x4;
      if (y5 > maxY) maxY = y5;
      this._ids[i8] = i8;
    }
    const cx = (minX + maxX) / 2;
    const cy = (minY + maxY) / 2;
    let i0, i1, i22;
    for (let i8 = 0, minDist = Infinity; i8 < n9; i8++) {
      const d4 = dist(cx, cy, coords[2 * i8], coords[2 * i8 + 1]);
      if (d4 < minDist) {
        i0 = i8;
        minDist = d4;
      }
    }
    const i0x = coords[2 * i0];
    const i0y = coords[2 * i0 + 1];
    for (let i8 = 0, minDist = Infinity; i8 < n9; i8++) {
      if (i8 === i0) continue;
      const d4 = dist(i0x, i0y, coords[2 * i8], coords[2 * i8 + 1]);
      if (d4 < minDist && d4 > 0) {
        i1 = i8;
        minDist = d4;
      }
    }
    let i1x = coords[2 * i1];
    let i1y = coords[2 * i1 + 1];
    let minRadius = Infinity;
    for (let i8 = 0; i8 < n9; i8++) {
      if (i8 === i0 || i8 === i1) continue;
      const r8 = circumradius(i0x, i0y, i1x, i1y, coords[2 * i8], coords[2 * i8 + 1]);
      if (r8 < minRadius) {
        i22 = i8;
        minRadius = r8;
      }
    }
    let i2x = coords[2 * i22];
    let i2y = coords[2 * i22 + 1];
    if (minRadius === Infinity) {
      for (let i8 = 0; i8 < n9; i8++) {
        this._dists[i8] = coords[2 * i8] - coords[0] || coords[2 * i8 + 1] - coords[1];
      }
      quicksort(this._ids, this._dists, 0, n9 - 1);
      const hull = new Uint32Array(n9);
      let j5 = 0;
      for (let i8 = 0, d0 = -Infinity; i8 < n9; i8++) {
        const id = this._ids[i8];
        const d4 = this._dists[id];
        if (d4 > d0) {
          hull[j5++] = id;
          d0 = d4;
        }
      }
      this.hull = hull.subarray(0, j5);
      this.triangles = new Uint32Array(0);
      this.halfedges = new Uint32Array(0);
      return;
    }
    if (orient2d(i0x, i0y, i1x, i1y, i2x, i2y) < 0) {
      const i8 = i1;
      const x4 = i1x;
      const y5 = i1y;
      i1 = i22;
      i1x = i2x;
      i1y = i2y;
      i22 = i8;
      i2x = x4;
      i2y = y5;
    }
    const center = circumcenter(i0x, i0y, i1x, i1y, i2x, i2y);
    this._cx = center.x;
    this._cy = center.y;
    for (let i8 = 0; i8 < n9; i8++) {
      this._dists[i8] = dist(coords[2 * i8], coords[2 * i8 + 1], center.x, center.y);
    }
    quicksort(this._ids, this._dists, 0, n9 - 1);
    this._hullStart = i0;
    let hullSize = 3;
    hullNext[i0] = hullPrev[i22] = i1;
    hullNext[i1] = hullPrev[i0] = i22;
    hullNext[i22] = hullPrev[i1] = i0;
    hullTri[i0] = 0;
    hullTri[i1] = 1;
    hullTri[i22] = 2;
    hullHash.fill(-1);
    hullHash[this._hashKey(i0x, i0y)] = i0;
    hullHash[this._hashKey(i1x, i1y)] = i1;
    hullHash[this._hashKey(i2x, i2y)] = i22;
    this.trianglesLen = 0;
    this._addTriangle(i0, i1, i22, -1, -1, -1);
    for (let k4 = 0, xp, yp; k4 < this._ids.length; k4++) {
      const i8 = this._ids[k4];
      const x4 = coords[2 * i8];
      const y5 = coords[2 * i8 + 1];
      if (k4 > 0 && Math.abs(x4 - xp) <= EPSILON && Math.abs(y5 - yp) <= EPSILON) continue;
      xp = x4;
      yp = y5;
      if (i8 === i0 || i8 === i1 || i8 === i22) continue;
      let start2 = 0;
      for (let j5 = 0, key = this._hashKey(x4, y5); j5 < this._hashSize; j5++) {
        start2 = hullHash[(key + j5) % this._hashSize];
        if (start2 !== -1 && start2 !== hullNext[start2]) break;
      }
      start2 = hullPrev[start2];
      let e11 = start2, q3;
      while (q3 = hullNext[e11], orient2d(x4, y5, coords[2 * e11], coords[2 * e11 + 1], coords[2 * q3], coords[2 * q3 + 1]) >= 0) {
        e11 = q3;
        if (e11 === start2) {
          e11 = -1;
          break;
        }
      }
      if (e11 === -1) continue;
      let t9 = this._addTriangle(e11, i8, hullNext[e11], -1, -1, hullTri[e11]);
      hullTri[i8] = this._legalize(t9 + 2);
      hullTri[e11] = t9;
      hullSize++;
      let n10 = hullNext[e11];
      while (q3 = hullNext[n10], orient2d(x4, y5, coords[2 * n10], coords[2 * n10 + 1], coords[2 * q3], coords[2 * q3 + 1]) < 0) {
        t9 = this._addTriangle(n10, i8, q3, hullTri[i8], -1, hullTri[n10]);
        hullTri[i8] = this._legalize(t9 + 2);
        hullNext[n10] = n10;
        hullSize--;
        n10 = q3;
      }
      if (e11 === start2) {
        while (q3 = hullPrev[e11], orient2d(x4, y5, coords[2 * q3], coords[2 * q3 + 1], coords[2 * e11], coords[2 * e11 + 1]) < 0) {
          t9 = this._addTriangle(q3, i8, e11, -1, hullTri[e11], hullTri[q3]);
          this._legalize(t9 + 2);
          hullTri[q3] = t9;
          hullNext[e11] = e11;
          hullSize--;
          e11 = q3;
        }
      }
      this._hullStart = hullPrev[i8] = e11;
      hullNext[e11] = hullPrev[n10] = i8;
      hullNext[i8] = n10;
      hullHash[this._hashKey(x4, y5)] = i8;
      hullHash[this._hashKey(coords[2 * e11], coords[2 * e11 + 1])] = e11;
    }
    this.hull = new Uint32Array(hullSize);
    for (let i8 = 0, e11 = this._hullStart; i8 < hullSize; i8++) {
      this.hull[i8] = e11;
      e11 = hullNext[e11];
    }
    this.triangles = this._triangles.subarray(0, this.trianglesLen);
    this.halfedges = this._halfedges.subarray(0, this.trianglesLen);
  }
  _hashKey(x4, y5) {
    return Math.floor(pseudoAngle(x4 - this._cx, y5 - this._cy) * this._hashSize) % this._hashSize;
  }
  _legalize(a5) {
    const { _triangles: triangles, _halfedges: halfedges, coords } = this;
    let i8 = 0;
    let ar = 0;
    while (true) {
      const b6 = halfedges[a5];
      const a0 = a5 - a5 % 3;
      ar = a0 + (a5 + 2) % 3;
      if (b6 === -1) {
        if (i8 === 0) break;
        a5 = EDGE_STACK[--i8];
        continue;
      }
      const b0 = b6 - b6 % 3;
      const al = a0 + (a5 + 1) % 3;
      const bl = b0 + (b6 + 2) % 3;
      const p0 = triangles[ar];
      const pr = triangles[a5];
      const pl = triangles[al];
      const p1 = triangles[bl];
      const illegal = inCircle(
        coords[2 * p0],
        coords[2 * p0 + 1],
        coords[2 * pr],
        coords[2 * pr + 1],
        coords[2 * pl],
        coords[2 * pl + 1],
        coords[2 * p1],
        coords[2 * p1 + 1]
      );
      if (illegal) {
        triangles[a5] = p1;
        triangles[b6] = p0;
        const hbl = halfedges[bl];
        if (hbl === -1) {
          let e11 = this._hullStart;
          do {
            if (this._hullTri[e11] === bl) {
              this._hullTri[e11] = a5;
              break;
            }
            e11 = this._hullPrev[e11];
          } while (e11 !== this._hullStart);
        }
        this._link(a5, hbl);
        this._link(b6, halfedges[ar]);
        this._link(ar, bl);
        const br = b0 + (b6 + 1) % 3;
        if (i8 < EDGE_STACK.length) {
          EDGE_STACK[i8++] = br;
        }
      } else {
        if (i8 === 0) break;
        a5 = EDGE_STACK[--i8];
      }
    }
    return ar;
  }
  _link(a5, b6) {
    this._halfedges[a5] = b6;
    if (b6 !== -1) this._halfedges[b6] = a5;
  }
  // add a new triangle given vertex indices and adjacent half-edge ids
  _addTriangle(i0, i1, i22, a5, b6, c9) {
    const t9 = this.trianglesLen;
    this._triangles[t9] = i0;
    this._triangles[t9 + 1] = i1;
    this._triangles[t9 + 2] = i22;
    this._link(t9, a5);
    this._link(t9 + 1, b6);
    this._link(t9 + 2, c9);
    this.trianglesLen += 3;
    return t9;
  }
};
function pseudoAngle(dx, dy) {
  const p3 = dx / (Math.abs(dx) + Math.abs(dy));
  return (dy > 0 ? 3 - p3 : 1 + p3) / 4;
}
function dist(ax, ay, bx, by) {
  const dx = ax - bx;
  const dy = ay - by;
  return dx * dx + dy * dy;
}
function inCircle(ax, ay, bx, by, cx, cy, px, py) {
  const dx = ax - px;
  const dy = ay - py;
  const ex = bx - px;
  const ey = by - py;
  const fx = cx - px;
  const fy = cy - py;
  const ap = dx * dx + dy * dy;
  const bp = ex * ex + ey * ey;
  const cp = fx * fx + fy * fy;
  return dx * (ey * cp - bp * fy) - dy * (ex * cp - bp * fx) + ap * (ex * fy - ey * fx) < 0;
}
function circumradius(ax, ay, bx, by, cx, cy) {
  const dx = bx - ax;
  const dy = by - ay;
  const ex = cx - ax;
  const ey = cy - ay;
  const bl = dx * dx + dy * dy;
  const cl = ex * ex + ey * ey;
  const d4 = 0.5 / (dx * ey - dy * ex);
  const x4 = (ey * bl - dy * cl) * d4;
  const y5 = (dx * cl - ex * bl) * d4;
  return x4 * x4 + y5 * y5;
}
function circumcenter(ax, ay, bx, by, cx, cy) {
  const dx = bx - ax;
  const dy = by - ay;
  const ex = cx - ax;
  const ey = cy - ay;
  const bl = dx * dx + dy * dy;
  const cl = ex * ex + ey * ey;
  const d4 = 0.5 / (dx * ey - dy * ex);
  const x4 = ax + (ey * bl - dy * cl) * d4;
  const y5 = ay + (dx * cl - ex * bl) * d4;
  return { x: x4, y: y5 };
}
function quicksort(ids, dists, left, right) {
  if (right - left <= 20) {
    for (let i8 = left + 1; i8 <= right; i8++) {
      const temp = ids[i8];
      const tempDist = dists[temp];
      let j5 = i8 - 1;
      while (j5 >= left && dists[ids[j5]] > tempDist) ids[j5 + 1] = ids[j5--];
      ids[j5 + 1] = temp;
    }
  } else {
    const median2 = left + right >> 1;
    let i8 = left + 1;
    let j5 = right;
    swap(ids, median2, i8);
    if (dists[ids[left]] > dists[ids[right]]) swap(ids, left, right);
    if (dists[ids[i8]] > dists[ids[right]]) swap(ids, i8, right);
    if (dists[ids[left]] > dists[ids[i8]]) swap(ids, left, i8);
    const temp = ids[i8];
    const tempDist = dists[temp];
    while (true) {
      do
        i8++;
      while (dists[ids[i8]] < tempDist);
      do
        j5--;
      while (dists[ids[j5]] > tempDist);
      if (j5 < i8) break;
      swap(ids, i8, j5);
    }
    ids[left + 1] = ids[j5];
    ids[j5] = temp;
    if (right - i8 + 1 >= j5 - left) {
      quicksort(ids, dists, i8, right);
      quicksort(ids, dists, left, j5 - 1);
    } else {
      quicksort(ids, dists, left, j5 - 1);
      quicksort(ids, dists, i8, right);
    }
  }
}
function swap(arr, i8, j5) {
  const tmp = arr[i8];
  arr[i8] = arr[j5];
  arr[j5] = tmp;
}
function defaultGetX(p3) {
  return p3[0];
}
function defaultGetY(p3) {
  return p3[1];
}

// node_modules/d3-delaunay/src/path.js
var epsilon5 = 1e-6;
var Path2 = class {
  constructor() {
    this._x0 = this._y0 = // start of current subpath
    this._x1 = this._y1 = null;
    this._ = "";
  }
  moveTo(x4, y5) {
    this._ += `M${this._x0 = this._x1 = +x4},${this._y0 = this._y1 = +y5}`;
  }
  closePath() {
    if (this._x1 !== null) {
      this._x1 = this._x0, this._y1 = this._y0;
      this._ += "Z";
    }
  }
  lineTo(x4, y5) {
    this._ += `L${this._x1 = +x4},${this._y1 = +y5}`;
  }
  arc(x4, y5, r8) {
    x4 = +x4, y5 = +y5, r8 = +r8;
    const x0 = x4 + r8;
    const y0 = y5;
    if (r8 < 0) throw new Error("negative radius");
    if (this._x1 === null) this._ += `M${x0},${y0}`;
    else if (Math.abs(this._x1 - x0) > epsilon5 || Math.abs(this._y1 - y0) > epsilon5) this._ += "L" + x0 + "," + y0;
    if (!r8) return;
    this._ += `A${r8},${r8},0,1,1,${x4 - r8},${y5}A${r8},${r8},0,1,1,${this._x1 = x0},${this._y1 = y0}`;
  }
  rect(x4, y5, w5, h2) {
    this._ += `M${this._x0 = this._x1 = +x4},${this._y0 = this._y1 = +y5}h${+w5}v${+h2}h${-w5}Z`;
  }
  value() {
    return this._ || null;
  }
};

// node_modules/d3-delaunay/src/polygon.js
var Polygon = class {
  constructor() {
    this._ = [];
  }
  moveTo(x4, y5) {
    this._.push([x4, y5]);
  }
  closePath() {
    this._.push(this._[0].slice());
  }
  lineTo(x4, y5) {
    this._.push([x4, y5]);
  }
  value() {
    return this._.length ? this._ : null;
  }
};

// node_modules/d3-delaunay/src/voronoi.js
var Voronoi = class {
  constructor(delaunay, [xmin, ymin, xmax, ymax] = [0, 0, 960, 500]) {
    if (!((xmax = +xmax) >= (xmin = +xmin)) || !((ymax = +ymax) >= (ymin = +ymin))) throw new Error("invalid bounds");
    this.delaunay = delaunay;
    this._circumcenters = new Float64Array(delaunay.points.length * 2);
    this.vectors = new Float64Array(delaunay.points.length * 2);
    this.xmax = xmax, this.xmin = xmin;
    this.ymax = ymax, this.ymin = ymin;
    this._init();
  }
  update() {
    this.delaunay.update();
    this._init();
    return this;
  }
  _init() {
    const { delaunay: { points, hull, triangles }, vectors } = this;
    let bx, by;
    const circumcenters = this.circumcenters = this._circumcenters.subarray(0, triangles.length / 3 * 2);
    for (let i8 = 0, j5 = 0, n9 = triangles.length, x4, y5; i8 < n9; i8 += 3, j5 += 2) {
      const t14 = triangles[i8] * 2;
      const t23 = triangles[i8 + 1] * 2;
      const t32 = triangles[i8 + 2] * 2;
      const x12 = points[t14];
      const y12 = points[t14 + 1];
      const x22 = points[t23];
      const y22 = points[t23 + 1];
      const x32 = points[t32];
      const y32 = points[t32 + 1];
      const dx = x22 - x12;
      const dy = y22 - y12;
      const ex = x32 - x12;
      const ey = y32 - y12;
      const ab4 = (dx * ey - dy * ex) * 2;
      if (Math.abs(ab4) < 1e-9) {
        if (bx === void 0) {
          bx = by = 0;
          for (const i9 of hull) bx += points[i9 * 2], by += points[i9 * 2 + 1];
          bx /= hull.length, by /= hull.length;
        }
        const a5 = 1e9 * Math.sign((bx - x12) * ey - (by - y12) * ex);
        x4 = (x12 + x32) / 2 - a5 * ey;
        y5 = (y12 + y32) / 2 + a5 * ex;
      } else {
        const d4 = 1 / ab4;
        const bl = dx * dx + dy * dy;
        const cl = ex * ex + ey * ey;
        x4 = x12 + (ey * bl - dy * cl) * d4;
        y5 = y12 + (dx * cl - ex * bl) * d4;
      }
      circumcenters[j5] = x4;
      circumcenters[j5 + 1] = y5;
    }
    let h2 = hull[hull.length - 1];
    let p0, p1 = h2 * 4;
    let x0, x1 = points[2 * h2];
    let y0, y1 = points[2 * h2 + 1];
    vectors.fill(0);
    for (let i8 = 0; i8 < hull.length; ++i8) {
      h2 = hull[i8];
      p0 = p1, x0 = x1, y0 = y1;
      p1 = h2 * 4, x1 = points[2 * h2], y1 = points[2 * h2 + 1];
      vectors[p0 + 2] = vectors[p1] = y0 - y1;
      vectors[p0 + 3] = vectors[p1 + 1] = x1 - x0;
    }
  }
  render(context) {
    const buffer = context == null ? context = new Path2() : void 0;
    const { delaunay: { halfedges, inedges, hull }, circumcenters, vectors } = this;
    if (hull.length <= 1) return null;
    for (let i8 = 0, n9 = halfedges.length; i8 < n9; ++i8) {
      const j5 = halfedges[i8];
      if (j5 < i8) continue;
      const ti = Math.floor(i8 / 3) * 2;
      const tj = Math.floor(j5 / 3) * 2;
      const xi = circumcenters[ti];
      const yi = circumcenters[ti + 1];
      const xj = circumcenters[tj];
      const yj = circumcenters[tj + 1];
      this._renderSegment(xi, yi, xj, yj, context);
    }
    let h0, h1 = hull[hull.length - 1];
    for (let i8 = 0; i8 < hull.length; ++i8) {
      h0 = h1, h1 = hull[i8];
      const t9 = Math.floor(inedges[h1] / 3) * 2;
      const x4 = circumcenters[t9];
      const y5 = circumcenters[t9 + 1];
      const v3 = h0 * 4;
      const p3 = this._project(x4, y5, vectors[v3 + 2], vectors[v3 + 3]);
      if (p3) this._renderSegment(x4, y5, p3[0], p3[1], context);
    }
    return buffer && buffer.value();
  }
  renderBounds(context) {
    const buffer = context == null ? context = new Path2() : void 0;
    context.rect(this.xmin, this.ymin, this.xmax - this.xmin, this.ymax - this.ymin);
    return buffer && buffer.value();
  }
  renderCell(i8, context) {
    const buffer = context == null ? context = new Path2() : void 0;
    const points = this._clip(i8);
    if (points === null || !points.length) return;
    context.moveTo(points[0], points[1]);
    let n9 = points.length;
    while (points[0] === points[n9 - 2] && points[1] === points[n9 - 1] && n9 > 1) n9 -= 2;
    for (let i9 = 2; i9 < n9; i9 += 2) {
      if (points[i9] !== points[i9 - 2] || points[i9 + 1] !== points[i9 - 1])
        context.lineTo(points[i9], points[i9 + 1]);
    }
    context.closePath();
    return buffer && buffer.value();
  }
  *cellPolygons() {
    const { delaunay: { points } } = this;
    for (let i8 = 0, n9 = points.length / 2; i8 < n9; ++i8) {
      const cell = this.cellPolygon(i8);
      if (cell) cell.index = i8, yield cell;
    }
  }
  cellPolygon(i8) {
    const polygon = new Polygon();
    this.renderCell(i8, polygon);
    return polygon.value();
  }
  _renderSegment(x0, y0, x1, y1, context) {
    let S4;
    const c0 = this._regioncode(x0, y0);
    const c12 = this._regioncode(x1, y1);
    if (c0 === 0 && c12 === 0) {
      context.moveTo(x0, y0);
      context.lineTo(x1, y1);
    } else if (S4 = this._clipSegment(x0, y0, x1, y1, c0, c12)) {
      context.moveTo(S4[0], S4[1]);
      context.lineTo(S4[2], S4[3]);
    }
  }
  contains(i8, x4, y5) {
    if ((x4 = +x4, x4 !== x4) || (y5 = +y5, y5 !== y5)) return false;
    return this.delaunay._step(i8, x4, y5) === i8;
  }
  *neighbors(i8) {
    const ci = this._clip(i8);
    if (ci) for (const j5 of this.delaunay.neighbors(i8)) {
      const cj = this._clip(j5);
      if (cj) loop: for (let ai = 0, li = ci.length; ai < li; ai += 2) {
        for (let aj = 0, lj = cj.length; aj < lj; aj += 2) {
          if (ci[ai] === cj[aj] && ci[ai + 1] === cj[aj + 1] && ci[(ai + 2) % li] === cj[(aj + lj - 2) % lj] && ci[(ai + 3) % li] === cj[(aj + lj - 1) % lj]) {
            yield j5;
            break loop;
          }
        }
      }
    }
  }
  _cell(i8) {
    const { circumcenters, delaunay: { inedges, halfedges, triangles } } = this;
    const e0 = inedges[i8];
    if (e0 === -1) return null;
    const points = [];
    let e11 = e0;
    do {
      const t9 = Math.floor(e11 / 3);
      points.push(circumcenters[t9 * 2], circumcenters[t9 * 2 + 1]);
      e11 = e11 % 3 === 2 ? e11 - 2 : e11 + 1;
      if (triangles[e11] !== i8) break;
      e11 = halfedges[e11];
    } while (e11 !== e0 && e11 !== -1);
    return points;
  }
  _clip(i8) {
    if (i8 === 0 && this.delaunay.hull.length === 1) {
      return [this.xmax, this.ymin, this.xmax, this.ymax, this.xmin, this.ymax, this.xmin, this.ymin];
    }
    const points = this._cell(i8);
    if (points === null) return null;
    const { vectors: V3 } = this;
    const v3 = i8 * 4;
    return this._simplify(V3[v3] || V3[v3 + 1] ? this._clipInfinite(i8, points, V3[v3], V3[v3 + 1], V3[v3 + 2], V3[v3 + 3]) : this._clipFinite(i8, points));
  }
  _clipFinite(i8, points) {
    const n9 = points.length;
    let P5 = null;
    let x0, y0, x1 = points[n9 - 2], y1 = points[n9 - 1];
    let c0, c12 = this._regioncode(x1, y1);
    let e0, e1 = 0;
    for (let j5 = 0; j5 < n9; j5 += 2) {
      x0 = x1, y0 = y1, x1 = points[j5], y1 = points[j5 + 1];
      c0 = c12, c12 = this._regioncode(x1, y1);
      if (c0 === 0 && c12 === 0) {
        e0 = e1, e1 = 0;
        if (P5) P5.push(x1, y1);
        else P5 = [x1, y1];
      } else {
        let S4, sx0, sy0, sx1, sy1;
        if (c0 === 0) {
          if ((S4 = this._clipSegment(x0, y0, x1, y1, c0, c12)) === null) continue;
          [sx0, sy0, sx1, sy1] = S4;
        } else {
          if ((S4 = this._clipSegment(x1, y1, x0, y0, c12, c0)) === null) continue;
          [sx1, sy1, sx0, sy0] = S4;
          e0 = e1, e1 = this._edgecode(sx0, sy0);
          if (e0 && e1) this._edge(i8, e0, e1, P5, P5.length);
          if (P5) P5.push(sx0, sy0);
          else P5 = [sx0, sy0];
        }
        e0 = e1, e1 = this._edgecode(sx1, sy1);
        if (e0 && e1) this._edge(i8, e0, e1, P5, P5.length);
        if (P5) P5.push(sx1, sy1);
        else P5 = [sx1, sy1];
      }
    }
    if (P5) {
      e0 = e1, e1 = this._edgecode(P5[0], P5[1]);
      if (e0 && e1) this._edge(i8, e0, e1, P5, P5.length);
    } else if (this.contains(i8, (this.xmin + this.xmax) / 2, (this.ymin + this.ymax) / 2)) {
      return [this.xmax, this.ymin, this.xmax, this.ymax, this.xmin, this.ymax, this.xmin, this.ymin];
    }
    return P5;
  }
  _clipSegment(x0, y0, x1, y1, c0, c12) {
    const flip = c0 < c12;
    if (flip) [x0, y0, x1, y1, c0, c12] = [x1, y1, x0, y0, c12, c0];
    while (true) {
      if (c0 === 0 && c12 === 0) return flip ? [x1, y1, x0, y0] : [x0, y0, x1, y1];
      if (c0 & c12) return null;
      let x4, y5, c9 = c0 || c12;
      if (c9 & 8) x4 = x0 + (x1 - x0) * (this.ymax - y0) / (y1 - y0), y5 = this.ymax;
      else if (c9 & 4) x4 = x0 + (x1 - x0) * (this.ymin - y0) / (y1 - y0), y5 = this.ymin;
      else if (c9 & 2) y5 = y0 + (y1 - y0) * (this.xmax - x0) / (x1 - x0), x4 = this.xmax;
      else y5 = y0 + (y1 - y0) * (this.xmin - x0) / (x1 - x0), x4 = this.xmin;
      if (c0) x0 = x4, y0 = y5, c0 = this._regioncode(x0, y0);
      else x1 = x4, y1 = y5, c12 = this._regioncode(x1, y1);
    }
  }
  _clipInfinite(i8, points, vx0, vy0, vxn, vyn) {
    let P5 = Array.from(points), p3;
    if (p3 = this._project(P5[0], P5[1], vx0, vy0)) P5.unshift(p3[0], p3[1]);
    if (p3 = this._project(P5[P5.length - 2], P5[P5.length - 1], vxn, vyn)) P5.push(p3[0], p3[1]);
    if (P5 = this._clipFinite(i8, P5)) {
      for (let j5 = 0, n9 = P5.length, c0, c12 = this._edgecode(P5[n9 - 2], P5[n9 - 1]); j5 < n9; j5 += 2) {
        c0 = c12, c12 = this._edgecode(P5[j5], P5[j5 + 1]);
        if (c0 && c12) j5 = this._edge(i8, c0, c12, P5, j5), n9 = P5.length;
      }
    } else if (this.contains(i8, (this.xmin + this.xmax) / 2, (this.ymin + this.ymax) / 2)) {
      P5 = [this.xmin, this.ymin, this.xmax, this.ymin, this.xmax, this.ymax, this.xmin, this.ymax];
    }
    return P5;
  }
  _edge(i8, e0, e1, P5, j5) {
    while (e0 !== e1) {
      let x4, y5;
      switch (e0) {
        case 5:
          e0 = 4;
          continue;
        // top-left
        case 4:
          e0 = 6, x4 = this.xmax, y5 = this.ymin;
          break;
        // top
        case 6:
          e0 = 2;
          continue;
        // top-right
        case 2:
          e0 = 10, x4 = this.xmax, y5 = this.ymax;
          break;
        // right
        case 10:
          e0 = 8;
          continue;
        // bottom-right
        case 8:
          e0 = 9, x4 = this.xmin, y5 = this.ymax;
          break;
        // bottom
        case 9:
          e0 = 1;
          continue;
        // bottom-left
        case 1:
          e0 = 5, x4 = this.xmin, y5 = this.ymin;
          break;
      }
      if ((P5[j5] !== x4 || P5[j5 + 1] !== y5) && this.contains(i8, x4, y5)) {
        P5.splice(j5, 0, x4, y5), j5 += 2;
      }
    }
    return j5;
  }
  _project(x0, y0, vx, vy) {
    let t9 = Infinity, c9, x4, y5;
    if (vy < 0) {
      if (y0 <= this.ymin) return null;
      if ((c9 = (this.ymin - y0) / vy) < t9) y5 = this.ymin, x4 = x0 + (t9 = c9) * vx;
    } else if (vy > 0) {
      if (y0 >= this.ymax) return null;
      if ((c9 = (this.ymax - y0) / vy) < t9) y5 = this.ymax, x4 = x0 + (t9 = c9) * vx;
    }
    if (vx > 0) {
      if (x0 >= this.xmax) return null;
      if ((c9 = (this.xmax - x0) / vx) < t9) x4 = this.xmax, y5 = y0 + (t9 = c9) * vy;
    } else if (vx < 0) {
      if (x0 <= this.xmin) return null;
      if ((c9 = (this.xmin - x0) / vx) < t9) x4 = this.xmin, y5 = y0 + (t9 = c9) * vy;
    }
    return [x4, y5];
  }
  _edgecode(x4, y5) {
    return (x4 === this.xmin ? 1 : x4 === this.xmax ? 2 : 0) | (y5 === this.ymin ? 4 : y5 === this.ymax ? 8 : 0);
  }
  _regioncode(x4, y5) {
    return (x4 < this.xmin ? 1 : x4 > this.xmax ? 2 : 0) | (y5 < this.ymin ? 4 : y5 > this.ymax ? 8 : 0);
  }
  _simplify(P5) {
    if (P5 && P5.length > 4) {
      for (let i8 = 0; i8 < P5.length; i8 += 2) {
        const j5 = (i8 + 2) % P5.length, k4 = (i8 + 4) % P5.length;
        if (P5[i8] === P5[j5] && P5[j5] === P5[k4] || P5[i8 + 1] === P5[j5 + 1] && P5[j5 + 1] === P5[k4 + 1]) {
          P5.splice(j5, 2), i8 -= 2;
        }
      }
      if (!P5.length) P5 = null;
    }
    return P5;
  }
};

// node_modules/d3-delaunay/src/delaunay.js
var tau3 = 2 * Math.PI;
var pow2 = Math.pow;
function pointX(p3) {
  return p3[0];
}
function pointY(p3) {
  return p3[1];
}
function collinear(d4) {
  const { triangles, coords } = d4;
  for (let i8 = 0; i8 < triangles.length; i8 += 3) {
    const a5 = 2 * triangles[i8], b6 = 2 * triangles[i8 + 1], c9 = 2 * triangles[i8 + 2], cross2 = (coords[c9] - coords[a5]) * (coords[b6 + 1] - coords[a5 + 1]) - (coords[b6] - coords[a5]) * (coords[c9 + 1] - coords[a5 + 1]);
    if (cross2 > 1e-10) return false;
  }
  return true;
}
function jitter(x4, y5, r8) {
  return [x4 + Math.sin(x4 + y5) * r8, y5 + Math.cos(x4 - y5) * r8];
}
var Delaunay = class _Delaunay {
  static from(points, fx = pointX, fy = pointY, that) {
    return new _Delaunay("length" in points ? flatArray(points, fx, fy, that) : Float64Array.from(flatIterable(points, fx, fy, that)));
  }
  constructor(points) {
    this._delaunator = new Delaunator(points);
    this.inedges = new Int32Array(points.length / 2);
    this._hullIndex = new Int32Array(points.length / 2);
    this.points = this._delaunator.coords;
    this._init();
  }
  update() {
    this._delaunator.update();
    this._init();
    return this;
  }
  _init() {
    const d4 = this._delaunator, points = this.points;
    if (d4.hull && d4.hull.length > 2 && collinear(d4)) {
      this.collinear = Int32Array.from({ length: points.length / 2 }, (_3, i8) => i8).sort((i8, j5) => points[2 * i8] - points[2 * j5] || points[2 * i8 + 1] - points[2 * j5 + 1]);
      const e11 = this.collinear[0], f2 = this.collinear[this.collinear.length - 1], bounds = [points[2 * e11], points[2 * e11 + 1], points[2 * f2], points[2 * f2 + 1]], r8 = 1e-8 * Math.hypot(bounds[3] - bounds[1], bounds[2] - bounds[0]);
      for (let i8 = 0, n9 = points.length / 2; i8 < n9; ++i8) {
        const p3 = jitter(points[2 * i8], points[2 * i8 + 1], r8);
        points[2 * i8] = p3[0];
        points[2 * i8 + 1] = p3[1];
      }
      this._delaunator = new Delaunator(points);
    } else {
      delete this.collinear;
    }
    const halfedges = this.halfedges = this._delaunator.halfedges;
    const hull = this.hull = this._delaunator.hull;
    const triangles = this.triangles = this._delaunator.triangles;
    const inedges = this.inedges.fill(-1);
    const hullIndex = this._hullIndex.fill(-1);
    for (let e11 = 0, n9 = halfedges.length; e11 < n9; ++e11) {
      const p3 = triangles[e11 % 3 === 2 ? e11 - 2 : e11 + 1];
      if (halfedges[e11] === -1 || inedges[p3] === -1) inedges[p3] = e11;
    }
    for (let i8 = 0, n9 = hull.length; i8 < n9; ++i8) {
      hullIndex[hull[i8]] = i8;
    }
    if (hull.length <= 2 && hull.length > 0) {
      this.triangles = new Int32Array(3).fill(-1);
      this.halfedges = new Int32Array(3).fill(-1);
      this.triangles[0] = hull[0];
      inedges[hull[0]] = 1;
      if (hull.length === 2) {
        inedges[hull[1]] = 0;
        this.triangles[1] = hull[1];
        this.triangles[2] = hull[1];
      }
    }
  }
  voronoi(bounds) {
    return new Voronoi(this, bounds);
  }
  *neighbors(i8) {
    const { inedges, hull, _hullIndex, halfedges, triangles, collinear: collinear2 } = this;
    if (collinear2) {
      const l5 = collinear2.indexOf(i8);
      if (l5 > 0) yield collinear2[l5 - 1];
      if (l5 < collinear2.length - 1) yield collinear2[l5 + 1];
      return;
    }
    const e0 = inedges[i8];
    if (e0 === -1) return;
    let e11 = e0, p0 = -1;
    do {
      yield p0 = triangles[e11];
      e11 = e11 % 3 === 2 ? e11 - 2 : e11 + 1;
      if (triangles[e11] !== i8) return;
      e11 = halfedges[e11];
      if (e11 === -1) {
        const p3 = hull[(_hullIndex[i8] + 1) % hull.length];
        if (p3 !== p0) yield p3;
        return;
      }
    } while (e11 !== e0);
  }
  find(x4, y5, i8 = 0) {
    if ((x4 = +x4, x4 !== x4) || (y5 = +y5, y5 !== y5)) return -1;
    const i0 = i8;
    let c9;
    while ((c9 = this._step(i8, x4, y5)) >= 0 && c9 !== i8 && c9 !== i0) i8 = c9;
    return c9;
  }
  _step(i8, x4, y5) {
    const { inedges, hull, _hullIndex, halfedges, triangles, points } = this;
    if (inedges[i8] === -1 || !points.length) return (i8 + 1) % (points.length >> 1);
    let c9 = i8;
    let dc = pow2(x4 - points[i8 * 2], 2) + pow2(y5 - points[i8 * 2 + 1], 2);
    const e0 = inedges[i8];
    let e11 = e0;
    do {
      let t9 = triangles[e11];
      const dt2 = pow2(x4 - points[t9 * 2], 2) + pow2(y5 - points[t9 * 2 + 1], 2);
      if (dt2 < dc) dc = dt2, c9 = t9;
      e11 = e11 % 3 === 2 ? e11 - 2 : e11 + 1;
      if (triangles[e11] !== i8) break;
      e11 = halfedges[e11];
      if (e11 === -1) {
        e11 = hull[(_hullIndex[i8] + 1) % hull.length];
        if (e11 !== t9) {
          if (pow2(x4 - points[e11 * 2], 2) + pow2(y5 - points[e11 * 2 + 1], 2) < dc) return e11;
        }
        break;
      }
    } while (e11 !== e0);
    return c9;
  }
  render(context) {
    const buffer = context == null ? context = new Path2() : void 0;
    const { points, halfedges, triangles } = this;
    for (let i8 = 0, n9 = halfedges.length; i8 < n9; ++i8) {
      const j5 = halfedges[i8];
      if (j5 < i8) continue;
      const ti = triangles[i8] * 2;
      const tj = triangles[j5] * 2;
      context.moveTo(points[ti], points[ti + 1]);
      context.lineTo(points[tj], points[tj + 1]);
    }
    this.renderHull(context);
    return buffer && buffer.value();
  }
  renderPoints(context, r8) {
    if (r8 === void 0 && (!context || typeof context.moveTo !== "function")) r8 = context, context = null;
    r8 = r8 == void 0 ? 2 : +r8;
    const buffer = context == null ? context = new Path2() : void 0;
    const { points } = this;
    for (let i8 = 0, n9 = points.length; i8 < n9; i8 += 2) {
      const x4 = points[i8], y5 = points[i8 + 1];
      context.moveTo(x4 + r8, y5);
      context.arc(x4, y5, r8, 0, tau3);
    }
    return buffer && buffer.value();
  }
  renderHull(context) {
    const buffer = context == null ? context = new Path2() : void 0;
    const { hull, points } = this;
    const h2 = hull[0] * 2, n9 = hull.length;
    context.moveTo(points[h2], points[h2 + 1]);
    for (let i8 = 1; i8 < n9; ++i8) {
      const h3 = 2 * hull[i8];
      context.lineTo(points[h3], points[h3 + 1]);
    }
    context.closePath();
    return buffer && buffer.value();
  }
  hullPolygon() {
    const polygon = new Polygon();
    this.renderHull(polygon);
    return polygon.value();
  }
  renderTriangle(i8, context) {
    const buffer = context == null ? context = new Path2() : void 0;
    const { points, triangles } = this;
    const t04 = triangles[i8 *= 3] * 2;
    const t14 = triangles[i8 + 1] * 2;
    const t23 = triangles[i8 + 2] * 2;
    context.moveTo(points[t04], points[t04 + 1]);
    context.lineTo(points[t14], points[t14 + 1]);
    context.lineTo(points[t23], points[t23 + 1]);
    context.closePath();
    return buffer && buffer.value();
  }
  *trianglePolygons() {
    const { triangles } = this;
    for (let i8 = 0, n9 = triangles.length / 3; i8 < n9; ++i8) {
      yield this.trianglePolygon(i8);
    }
  }
  trianglePolygon(i8) {
    const polygon = new Polygon();
    this.renderTriangle(i8, polygon);
    return polygon.value();
  }
};
function flatArray(points, fx, fy, that) {
  const n9 = points.length;
  const array2 = new Float64Array(n9 * 2);
  for (let i8 = 0; i8 < n9; ++i8) {
    const p3 = points[i8];
    array2[i8 * 2] = fx.call(that, p3, i8, points);
    array2[i8 * 2 + 1] = fy.call(that, p3, i8, points);
  }
  return array2;
}
function* flatIterable(points, fx, fy, that) {
  let i8 = 0;
  for (const p3 of points) {
    yield fx.call(that, p3, i8, points);
    yield fy.call(that, p3, i8, points);
    ++i8;
  }
}

// node_modules/@nivo/voronoi/dist/nivo-voronoi.mjs
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
function C5() {
  return C5 = Object.assign ? Object.assign.bind() : function(n9) {
    for (var o6 = 1; o6 < arguments.length; o6++) {
      var e11 = arguments[o6];
      for (var i8 in e11) ({}).hasOwnProperty.call(e11, i8) && (n9[i8] = e11[i8]);
    }
    return n9;
  }, C5.apply(null, arguments);
}
function y4(n9, o6) {
  if (null == n9) return {};
  var e11 = {};
  for (var i8 in n9) if ({}.hasOwnProperty.call(n9, i8)) {
    if (-1 !== o6.indexOf(i8)) continue;
    e11[i8] = n9[i8];
  }
  return e11;
}
var w4 = { xDomain: [0, 1], yDomain: [0, 1], layers: ["links", "cells", "points", "bounds"], enableLinks: false, linkLineWidth: 1, linkLineColor: "#bbbbbb", enableCells: true, cellLineWidth: 2, cellLineColor: "#000000", enablePoints: true, pointSize: 4, pointColor: "#666666", role: "img" };
var D4 = function(n9) {
  return [n9.x, n9.y];
};
var L3 = un;
var T3 = "cursor";
var P4 = "top";
var R4 = function(n9) {
  var o6 = n9.points, e11 = n9.getNodePosition, i8 = void 0 === e11 ? D4 : e11, t9 = n9.margin, l5 = void 0 === t9 ? L3 : t9;
  return o6.map((function(n10) {
    var o7 = i8(n10), e12 = o7[0], t10 = o7[1];
    return [e12 + l5.left, t10 + l5.top];
  }));
};
var W5 = function(n9) {
  var o6 = n9.points, e11 = n9.width, i8 = n9.height, t9 = n9.margin, l5 = void 0 === t9 ? L3 : t9, r8 = n9.debug, u8 = Delaunay.from(o6), a5 = r8 ? u8.voronoi([0, 0, l5.left + e11 + l5.right, l5.top + i8 + l5.bottom]) : void 0;
  return { points: o6, delaunay: u8, voronoi: a5 };
};
var E4 = function(o6) {
  var e11 = o6.points, i8 = o6.getNodePosition, t9 = void 0 === i8 ? D4 : i8, l5 = o6.width, r8 = o6.height, u8 = o6.margin, a5 = void 0 === u8 ? L3 : u8, d4 = o6.debug;
  return (0, import_react22.useMemo)((function() {
    return W5({ points: R4({ points: e11, margin: a5, getNodePosition: t9 }), width: l5, height: r8, margin: a5, debug: d4 });
  }), [t9, e11, l5, r8, a5, d4]);
};
var x3 = function(o6) {
  var e11 = o6.data, i8 = o6.width, t9 = o6.height, l5 = o6.xDomain, r8 = o6.yDomain, u8 = (0, import_react22.useMemo)((function() {
    return linear2().domain(l5).range([0, i8]);
  }), [l5, i8]), a5 = (0, import_react22.useMemo)((function() {
    return linear2().domain(r8).range([0, t9]);
  }), [r8, t9]), d4 = (0, import_react22.useMemo)((function() {
    return e11.map((function(n9) {
      return { x: u8(n9.x), y: a5(n9.y), data: n9 };
    }));
  }), [e11, u8, a5]);
  return (0, import_react22.useMemo)((function() {
    var n9 = Delaunay.from(d4.map((function(n10) {
      return [n10.x, n10.y];
    }))), o7 = n9.voronoi([0, 0, i8, t9]);
    return { points: d4, delaunay: n9, voronoi: o7 };
  }), [d4, i8, t9]);
};
var S3 = function(o6) {
  var e11 = o6.points, i8 = o6.delaunay, t9 = o6.voronoi;
  return (0, import_react22.useMemo)((function() {
    return { points: e11, delaunay: i8, voronoi: t9 };
  }), [e11, i8, t9]);
};
var U = function(l5) {
  var r8 = l5.elementRef, u8 = l5.nodes, a5 = l5.getNodePosition, h2 = void 0 === a5 ? D4 : a5, c9 = l5.delaunay, v3 = l5.setCurrent, f2 = l5.margin, p3 = void 0 === f2 ? L3 : f2, g2 = l5.detectionRadius, m4 = void 0 === g2 ? 1 / 0 : g2, b6 = l5.isInteractive, k4 = void 0 === b6 || b6, C6 = l5.onMouseEnter, y5 = l5.onMouseMove, w5 = l5.onMouseLeave, R5 = l5.onMouseDown, W6 = l5.onMouseUp, E5 = l5.onClick, x4 = l5.onDoubleClick, S4 = l5.onTouchStart, U2 = l5.onTouchMove, z5 = l5.onTouchEnd, A5 = l5.enableTouchCrosshair, N3 = void 0 !== A5 && A5, H2 = l5.tooltip, O5 = l5.tooltipPosition, I2 = void 0 === O5 ? T3 : O5, j5 = l5.tooltipAnchor, F3 = void 0 === j5 ? P4 : j5, B4 = (0, import_react22.useState)(null), q3 = B4[0], G = B4[1], J2 = (0, import_react22.useRef)(null);
  (0, import_react22.useEffect)((function() {
    J2.current = q3;
  }), [J2, q3]);
  var K2 = (0, import_react22.useCallback)((function(n9) {
    if (!r8.current || 0 === u8.length) return null;
    var o6 = kn(r8.current, n9), e11 = o6[0], i8 = o6[1], t9 = c9.find(e11, i8), l6 = void 0 !== t9 ? u8[t9] : null;
    if (l6 && m4 !== 1 / 0) {
      var a6 = h2(l6), v4 = a6[0], f3 = a6[1];
      yn(e11, i8, v4 + p3.left, f3 + p3.top) > m4 && (t9 = null, l6 = null);
    }
    return null === t9 || null === l6 ? null : [t9, l6];
  }), [r8, c9, u8, h2, p3, m4]), Q2 = z3(), V3 = Q2.showTooltipAt, X3 = Q2.showTooltipFromEvent, Y3 = Q2.hideTooltip, Z2 = (0, import_react22.useMemo)((function() {
    if (H2) return "cursor" === I2 ? function(n9, o6) {
      X3(H2(n9), o6, F3);
    } : function(n9) {
      var o6 = h2(n9), e11 = o6[0], i8 = o6[1];
      V3(H2(n9), [e11 + p3.left, i8 + p3.top], F3);
    };
  }), [V3, X3, H2, I2, F3, h2, p3]), $ = (0, import_react22.useCallback)((function(n9) {
    var o6 = K2(n9);
    if (G(o6), null == v3 || v3(o6 ? o6[1] : null), o6) {
      var e11 = o6[1];
      null == Z2 || Z2(e11, n9), null == C6 || C6(o6[1], n9);
    }
  }), [K2, G, v3, Z2, C6]), _3 = (0, import_react22.useCallback)((function(n9) {
    var o6 = K2(n9);
    if (G(o6), o6) {
      var e11 = o6[0], i8 = o6[1];
      if (null == v3 || v3(i8), null == Z2 || Z2(i8, n9), J2.current) {
        var t9 = J2.current, l6 = t9[0], r9 = t9[1];
        e11 !== l6 ? null == w5 || w5(r9, n9) : null == y5 || y5(i8, n9);
      } else null == C6 || C6(i8, n9);
    } else null == v3 || v3(null), null == Y3 || Y3(), J2.current && (null == w5 || w5(J2.current[1], n9));
  }), [K2, G, v3, J2, C6, y5, w5, Z2, Y3]), nn2 = (0, import_react22.useCallback)((function(n9) {
    G(null), null == v3 || v3(null), Y3(), w5 && J2.current && w5(J2.current[1], n9);
  }), [G, v3, J2, Y3, w5]), on2 = (0, import_react22.useCallback)((function(n9) {
    var o6 = K2(n9);
    G(o6), o6 && (null == R5 || R5(o6[1], n9));
  }), [K2, G, R5]), en2 = (0, import_react22.useCallback)((function(n9) {
    var o6 = K2(n9);
    G(o6), o6 && (null == W6 || W6(o6[1], n9));
  }), [K2, G, W6]), tn2 = (0, import_react22.useCallback)((function(n9) {
    var o6 = K2(n9);
    G(o6), o6 && (null == E5 || E5(o6[1], n9));
  }), [K2, G, E5]), ln2 = (0, import_react22.useCallback)((function(n9) {
    var o6 = K2(n9);
    G(o6), o6 && (null == x4 || x4(o6[1], n9));
  }), [K2, G, x4]), rn2 = (0, import_react22.useCallback)((function(n9) {
    var o6 = K2(n9);
    N3 && (G(o6), null == v3 || v3(o6 ? o6[1] : null)), o6 && (null == S4 || S4(o6[1], n9));
  }), [K2, G, v3, N3, S4]), un3 = (0, import_react22.useCallback)((function(n9) {
    var o6 = K2(n9);
    N3 && (G(o6), null == v3 || v3(o6 ? o6[1] : null)), o6 && (null == U2 || U2(o6[1], n9));
  }), [K2, G, v3, N3, U2]), an2 = (0, import_react22.useCallback)((function(n9) {
    N3 && (G(null), null == v3 || v3(null)), z5 && J2.current && z5(J2.current[1], n9);
  }), [N3, G, v3, z5, J2]);
  return { current: q3, handleMouseEnter: k4 ? $ : void 0, handleMouseMove: k4 ? _3 : void 0, handleMouseLeave: k4 ? nn2 : void 0, handleMouseDown: k4 ? on2 : void 0, handleMouseUp: k4 ? en2 : void 0, handleClick: k4 ? tn2 : void 0, handleDoubleClick: k4 ? ln2 : void 0, handleTouchStart: k4 ? rn2 : void 0, handleTouchMove: k4 ? un3 : void 0, handleTouchEnd: k4 ? an2 : void 0 };
};
var A4 = ["theme"];
var N2 = function(n9) {
  var o6 = n9.data, e11 = n9.width, i8 = n9.height, t9 = n9.margin, l5 = n9.layers, a5 = void 0 === l5 ? w4.layers : l5, d4 = n9.xDomain, s5 = void 0 === d4 ? w4.xDomain : d4, h2 = n9.yDomain, f2 = void 0 === h2 ? w4.yDomain : h2, p3 = n9.enableLinks, g2 = void 0 === p3 ? w4.enableLinks : p3, M4 = n9.linkLineWidth, b6 = void 0 === M4 ? w4.linkLineWidth : M4, k4 = n9.linkLineColor, C6 = void 0 === k4 ? w4.linkLineColor : k4, y5 = n9.enableCells, D5 = void 0 === y5 ? w4.enableCells : y5, L4 = n9.cellLineWidth, T4 = void 0 === L4 ? w4.cellLineWidth : L4, P5 = n9.cellLineColor, R5 = void 0 === P5 ? w4.cellLineColor : P5, W6 = n9.enablePoints, E5 = void 0 === W6 ? w4.enableCells : W6, U2 = n9.pointSize, z5 = void 0 === U2 ? w4.pointSize : U2, A5 = n9.pointColor, N3 = void 0 === A5 ? w4.pointColor : A5, H2 = n9.role, O5 = void 0 === H2 ? w4.role : H2, I2 = n9.forwardedRef, j5 = cn(e11, i8, t9), F3 = j5.outerWidth, B4 = j5.outerHeight, q3 = j5.margin, G = j5.innerWidth, J2 = j5.innerHeight, K2 = x3({ data: o6, width: G, height: J2, xDomain: s5, yDomain: f2 }), Q2 = K2.points, V3 = K2.delaunay, X3 = K2.voronoi, Y3 = { links: null, cells: null, points: null, bounds: null };
  g2 && a5.includes("links") && (Y3.links = (0, import_jsx_runtime6.jsx)("path", { stroke: C6, strokeWidth: b6, fill: "none", d: V3.render() }, "links")), D5 && a5.includes("cells") && (Y3.cells = (0, import_jsx_runtime6.jsx)("path", { d: X3.render(), fill: "none", stroke: R5, strokeWidth: T4 }, "cells")), E5 && a5.includes("points") && (Y3.points = (0, import_jsx_runtime6.jsx)("path", { stroke: "none", fill: N3, d: V3.renderPoints(void 0, z5 / 2) }, "points")), a5.includes("bounds") && (Y3.bounds = (0, import_jsx_runtime6.jsx)("path", { fill: "none", stroke: R5, strokeWidth: T4, d: X3.renderBounds() }, "bounds"));
  var Z2 = S3({ points: Q2, delaunay: V3, voronoi: X3 });
  return (0, import_jsx_runtime6.jsx)(Rt, { width: F3, height: B4, margin: q3, role: O5, ref: I2, children: a5.map((function(n10, o7) {
    return void 0 !== Y3[n10] ? Y3[n10] : "function" == typeof n10 ? (0, import_jsx_runtime6.jsx)(import_react22.Fragment, { children: (0, import_react22.createElement)(n10, Z2) }, o7) : null;
  })) });
};
var H = (0, import_react22.forwardRef)((function(n9, o6) {
  var e11 = n9.theme, i8 = y4(n9, A4);
  return (0, import_jsx_runtime6.jsx)(Fr, { isInteractive: false, animate: false, theme: e11, children: (0, import_jsx_runtime6.jsx)(N2, C5({}, i8, { forwardedRef: o6 })) });
}));
var O4 = ["defaultWidth", "defaultHeight", "onResize", "debounceResize"];
var I = (0, import_react22.forwardRef)((function(n9, o6) {
  var e11 = n9.defaultWidth, i8 = n9.defaultHeight, t9 = n9.onResize, l5 = n9.debounceResize, r8 = y4(n9, O4);
  return (0, import_jsx_runtime6.jsx)($r, { defaultWidth: e11, defaultHeight: i8, onResize: t9, debounceResize: l5, children: function(n10) {
    var e12 = n10.width, i9 = n10.height;
    return (0, import_jsx_runtime6.jsx)(H, C5({}, r8, { width: e12, height: i9, ref: o6 }));
  } });
}));
var j4 = function(o6) {
  var i8 = o6.nodes, t9 = o6.width, l5 = o6.height, r8 = o6.margin, u8 = void 0 === r8 ? L3 : r8, a5 = o6.getNodePosition, d4 = o6.setCurrent, s5 = o6.onMouseEnter, h2 = o6.onMouseMove, c9 = o6.onMouseLeave, v3 = o6.onMouseDown, f2 = o6.onMouseUp, p3 = o6.onClick, g2 = o6.onDoubleClick, M4 = o6.onTouchStart, C6 = o6.onTouchMove, y5 = o6.onTouchEnd, w5 = o6.enableTouchCrosshair, D5 = void 0 !== w5 && w5, R5 = o6.detectionRadius, W6 = void 0 === R5 ? 1 / 0 : R5, x4 = o6.tooltip, S4 = o6.tooltipPosition, z5 = void 0 === S4 ? T3 : S4, A5 = o6.tooltipAnchor, N3 = void 0 === A5 ? P4 : A5, H2 = o6.debug, O5 = (0, import_react22.useRef)(null), I2 = E4({ points: i8, getNodePosition: a5, width: t9, height: l5, margin: u8, debug: H2 }), j5 = I2.delaunay, F3 = I2.voronoi, B4 = U({ elementRef: O5, nodes: i8, delaunay: j5, margin: u8, detectionRadius: W6, setCurrent: d4, onMouseEnter: s5, onMouseMove: h2, onMouseLeave: c9, onMouseDown: v3, onMouseUp: f2, onClick: p3, onDoubleClick: g2, onTouchStart: M4, onTouchMove: C6, onTouchEnd: y5, enableTouchCrosshair: D5, tooltip: x4, tooltipPosition: z5, tooltipAnchor: N3 }), q3 = B4.current, G = B4.handleMouseEnter, J2 = B4.handleMouseMove, K2 = B4.handleMouseLeave, Q2 = B4.handleMouseDown, V3 = B4.handleMouseUp, X3 = B4.handleClick, Y3 = B4.handleDoubleClick, Z2 = B4.handleTouchStart, $ = B4.handleTouchMove, _3 = B4.handleTouchEnd, nn2 = (0, import_react22.useMemo)((function() {
    if (H2 && F3) return F3.render();
  }), [H2, F3]);
  return (0, import_jsx_runtime6.jsxs)("g", { ref: O5, transform: "translate(" + -u8.left + "," + -u8.top + ")", children: [H2 && F3 && (0, import_jsx_runtime6.jsxs)(import_jsx_runtime6.Fragment, { children: [(0, import_jsx_runtime6.jsx)("path", { d: nn2, stroke: "red", strokeWidth: 1, opacity: 0.75 }), W6 < 1 / 0 && (0, import_jsx_runtime6.jsx)("path", { stroke: "red", strokeWidth: 0.35, fill: "none", d: j5.renderPoints(void 0, W6) }), q3 && (0, import_jsx_runtime6.jsx)("path", { fill: "pink", opacity: 0.35, d: F3.renderCell(q3[0]) })] }), (0, import_jsx_runtime6.jsx)("rect", { "data-ref": "mesh-interceptor", width: u8.left + t9 + u8.right, height: u8.top + l5 + u8.bottom, fill: "red", opacity: 0, style: { cursor: "auto" }, onMouseEnter: G, onMouseMove: J2, onMouseLeave: K2, onMouseDown: Q2, onMouseUp: V3, onTouchStart: Z2, onTouchMove: $, onTouchEnd: _3, onClick: X3, onDoubleClick: Y3 })] });
};
var F2 = function(n9, o6) {
  n9.save(), n9.globalAlpha = 0.75, n9.beginPath(), o6.render(n9), n9.strokeStyle = "red", n9.lineWidth = 1, n9.stroke(), n9.restore();
};
var q2 = function(n9, o6, e11) {
  n9.save(), n9.globalAlpha = 0.35, n9.beginPath(), o6.renderCell(e11, n9), n9.fillStyle = "pink", n9.fill(), n9.restore();
};

export {
  useSprings,
  useSpring,
  useTransition,
  to2 as to,
  animated,
  require_Symbol,
  require_baseGetTag,
  require_isObject,
  require_Stack,
  require_cloneBuffer,
  require_cloneArrayBuffer,
  require_cloneTypedArray,
  require_copyArray,
  require_getPrototype,
  require_initCloneObject,
  require_isObjectLike,
  require_isArray,
  require_isBuffer,
  require_isPlainObject,
  require_baseUnary,
  require_nodeUtil,
  require_assignValue,
  require_copyObject,
  require_keysIn,
  require_arrayMap,
  require_castPath,
  require_toKey,
  require_baseGet,
  require_get,
  M,
  T,
  z3 as z,
  require_isString,
  point5 as point,
  linear2 as linear,
  require_last,
  require_arrayPush,
  require_flatRest,
  require_baseGetAllKeys,
  require_arrayFilter,
  require_stubArray,
  require_getSymbols,
  require_keys,
  require_getAllKeys,
  require_getTag,
  Dr,
  Fr,
  $r,
  ut,
  ct,
  ft,
  ht,
  Rt,
  Pt,
  It,
  cn,
  hn,
  bn,
  wn,
  kn,
  Mn,
  Rn,
  a4 as a,
  d3 as d,
  b3 as b,
  require_baseIteratee,
  require_baseEach,
  _2 as _,
  tn,
  hn2,
  V2 as V,
  j3 as j,
  W4 as W,
  q,
  Ye,
  hr,
  E4 as E,
  j4 as j2,
  F2 as F,
  q2
};
//# sourceMappingURL=chunk-CHN5IPYD.js.map
